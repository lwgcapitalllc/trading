# Drop folder for TradingView Pine chart-data exports used by
# ../tools/compare_fvg.py. These CSVs are large, local-only test data —
# never commit them. This file keeps the folder present in the repo.
*.csv
