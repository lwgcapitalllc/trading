# CLAUDE.md — Command Center

**Purpose:** Local operations platform for LWG Capital — a React frontend + FastAPI backend that monitors the live bots and runs/evaluates NinjaTrader + MT5 backtests. It also surfaces the Smart Money pipeline, which is built and **flagged OFF** in the UI since 2026-08-04.
**Scope:** This covers the command-center app (backend + frontend). Sub-directory CLAUDE.md files cover backend and frontend internals. It does NOT cover `algos/` or `smart-money/` source — those are read-only outputs to this app.
**Status:** Live — all modules shipped (Bots, Backtests lab, Sweeps, Optimizations, Stress Tests, MT5 runner, Python runner, portfolio stacks). Smart Money is shipped and hidden behind a feature flag.
**Last reviewed:** 2026-08-10 (latest) — 🔴 **A STACK'S LEG TOGGLE SHOWED THE LEG'S SHARE OF THE PORTFOLIO AND READ AS WHAT IT WOULD HAVE MADE ALONE — $47,758,999 AGAINST A TRUE $21,064.** Aaron switched one strategy off, then ran the other standalone over the same window, and the two disagreed by 2,266x. ✅ **MEASURED, and both figures are right: 99 trades either way, identical entry and stop prices, +17.8674R either way** — in a shared account a leg sizes off a balance every strategy grew, so its last trade risks **$16,925,791** instead of **$3,102** for the identical +0.014R. 🔴 **The honest answer had been computed on every shared run since the feature was built and thrown away**: `run_stack` replays each leg SOLO as a control and `_persist` kept two scalars from it. The full control book is stored and served now, a leg toggle swaps it in, and a subset nobody replayed **refuses** rather than composing one. ⚠ **The per-leg row leads with R**, the one figure a change of position size cannot move. ✅ 4 backend + 4 browser checks, all proven by mutation; backfilled and driven end to end in a real browser. Full detail: `backend/CLAUDE.md` → *The SOLO CONTROL is a BOOK* and `frontend/CLAUDE.md` → *A leg toggle swaps in the SOLO CONTROL*. Earlier the same day: 🔴 **FOUR FIXES OFF ONE SCREENSHOT, AND TWO OF THEM WERE DEFAULTS NOBODY HAD CHOSEN.** (1) **A NEW STACK IS ALWAYS A SHARED ACCOUNT — the mode picker is gone** (*"I would never ever ever wanna do a screen… that's what a stack IS — we're sharing the same resource. I wanna know how two strategies affect each other and where some trades are dropped because others have taken up all the capacity"*). Offering a screen as an equal choice made the one mode he wants a coin flip, and `?? 'screen'` is what a caller got by saying nothing. ⚠ **NOT a removal** — three stored stacks are screens, they still render with their chip, and a RERUN carries its own mode forward; deleting the mode would rewrite what those rows mean. (2) **The shared-account panel is three lines** (*"does this section need to be so verbose? Like I'm reading a storybook"*), with every RULE the old paragraphs stated kept on an ⓘ and both browser checks HOVERING rather than reading the chip. (3) 🔴 **Regimes default OFF on every equity curve, and there were THREE definitions of that preference with only ONE persisting anything** — a stored one on the run page defaulting ON, plus a bare `useState(true)` on the stack page and another in the tuning workbench, so switching it off on two of three surfaces did not survive a navigation. ⚠ **The default is the POLARITY of the stored check, never a second key.** (4) 🔴 **The stack's leg palette filtered by exact string inequality against the portfolio's green, and `#00ff7f` vs `#00ff82` passes that test** — so the second leg of every two-strategy stack drew in the portfolio's own colour. Explicit list now. ✅ 13 stack checks green, new ones proven by MUTATION; tuning suite green; typechecks and builds. ⚠ Three `backtests.spec.ts` failures are PRE-EXISTING at HEAD. Full detail: `frontend/CLAUDE.md`. Earlier the same day: 🟢 **A STACK AND A SINGLE BACKTEST ARE ONE PAGE NOW, AND THE STRATEGY TOGGLES LIVE IN THE VERDICT CARD.** Aaron: *"it should look exactly like a single run backtest page… I don't want stack stuff here and a regular backtest details page to look two different. Unless there's some cumulative thing that I need to watch"*, and *"the strategies on your stack could be toggled from within the verdict section… the KPIs and everything should change as you toggle them."* 🔴 **`StackDetail` already reused `PerformancePanel`, so the NUMBERS were computed identically and the page still read as a different feature — everything AROUND them differed**: a full-width strategy ribbon where a run has a Verdict card, three cards where a run has four, a bare `<h2>` where a run has a collapse control, and the leg chips in a section two scrolls further down. It is now Verdict / Made / Risked / Trusted behind the same `PerfCollapseToggle`, reading the same `usePerfCollapsed` key, with each leg a clickable `PanelRow` inside the Verdict card. ⚠ **`PanelRow` gained `onClick`/`lead`/`muted` rather than the card forking its own row markup** — a second private copy of that anatomy is how a fourth card drifts out of line with the three beside it, which is the argument that moved `panelCardCls`/`CardHead`/`CardHero` to module scope when the verdict first became a card. ⚠ **The Verdict card passes `collapsed: false` ALWAYS**: the panel's collapse means *hero numbers only*, and the leg rows are a CONTROL, not a supporting metric — hiding them would put the toggles behind a preference the reader last set on a different page. ⚠ **A leg that cannot be toggled is still LISTED, dimmed, with the reason on its ⓘ** — *not finished* and *not in this stack* are different answers. 🔴 **The shared-account panel was three big figures nobody could read** (*"one account, the screen promised, peak open risk — I don't know the significance of these things"*) over a seven-column table that is **five columns of em-dashes on every run measured so far**. One card now, with each fact carrying the sentence that makes it mean something, and the table behind a disclosure that OPENS ITSELF when there is contention. ⚠ **The screen-vs-shared figures are deliberately no longer two headline numbers side by side** — the gap is one balance compounding both legs rather than refusals, and two bare figures with nothing between them invite exactly the misreading `docs/SHARED_RISK_STACK.md` already got wrong. ✅ 3 new browser checks (13 green in the file), **each proven by MUTATION** — a fail-watch is vacuous for a card that did not exist. 🔴 **One was VACUOUS on its first run and PASSED against its own mutation**: `locator('table')).toHaveCount(0)` asserted straight after `goto` is satisfied while the PANEL is absent too, so it was green against a build rendering the table unconditionally. **Fourth instance of that trap recorded in `frontend/CLAUDE.md`**, and it was caught only because the mutation was actually run rather than reasoned about. ⚠ **Three `backtests.spec.ts` failures were confirmed PRE-EXISTING at HEAD and are not from this pass.** Full detail: `frontend/CLAUDE.md` → *A stack renders a RUN's panel*. Earlier: 2026-08-09 — 🟢 **BOTS CAN BE ADDED TO AND REMOVED FROM AN ACCOUNT FROM THE BROWSER, AND EVERY BOT ROW NOW SAYS WHICH VERSION IT IS RUNNING.** Aaron's ask, and removal is the half that needed a new idea: it lands on the **BENCH** — `account: null`, a bot registered, configured and deliberately trading nothing — because a stored membership list would be a second answer that can disagree with what the bots actually do. `mpc_bleg_demo` ships on the bench so there is something real to add. ⚠ **A move writes four fields and adopts the target account's RISK CAP**, or every bot already on that account refuses to start at its next restart; a RUNNING bot is refused outright. ✅ **`VersionPill` is one component on the Monitor tab, the Accounts tab and the Configure banner** (*"Everything should be aligned. All the versions. Create a pill that looks clean across all"*) sharing ONE cache entry per bot, so three surfaces cannot disagree — it reports the DEPLOYED version and never `v0` when the question cannot be answered. ✅ **The raw Magic column is gone** (*"I don't know what the column magic even means"*), replaced by a banner that names the two bots when they actually share an order tag; State is the same green Running pill the Monitor tab draws, from the same component. 🔴 **THREE COLOUR CLASSES ON THAT TAB WERE NOT THEME TOKENS AT ALL** — `bg-negative-muted` / `text-negative` / `bg-positive-muted`, names in no theme — so the cap chip and the disagreement banner had been rendering with no colour since the day they shipped. **A misspelled token does not fail a build; it silently draws nothing**, which is what "starting to deviate from the style guide" looked like from the screen. Full detail: `backend/CLAUDE.md` and `frontend/CLAUDE.md`. Earlier the same day: 🔴 **THE CANDLE LAYER'S "DRAWDOWN ZONE" WAS A REGION IN TIME, AND THE ZONE IS A REGION IN PRICE — ONE MISTAKE, FOUR SEPARATE COMPLAINTS.** Aaron reported six trades off the chart over two rounds, and the defects point in OPPOSITE directions, which is why no single bound could fix it. ✅ **MEASURED on run `e51d95f212e3`, 166 anchors:** 255 of 1084 marks sat entirely on the FAVOURABLE side of the entry (a confirm tail measured off the end of the drawdown instead of off the turn — his 2026-07-15 long's only mark sat **$25 above the entry, two bars into a rally**, and named the chip — plus a span opening at the ENTRY BAR, so a trade running into profit first painted the whole excursion, **one turn 112 bars after entry**); a stopped-out LOSER was marked **after it closed**, because its adverse extreme IS its final bar (*"I don't care what the candles after the trade. It has to be within the trade."*); and the contiguous-excursion fix then MISSED price coming BACK to the entry an hour later, while the chart's own red box plainly covered it (*"we came back up to entry… and you didn't highlight any of them"*). ✅ **One price test and one clamp now: a bar is drawn while ANY part of it trades on the adverse side of the entry, between the entry bar and the EXIT bar.** `_drawdown_end` is DELETED — no walk can express *and again when price comes back* without swallowing the profit in between. **1090 → 853 marks, ZERO past any exit, 31 outside the band and all within two bars of the extreme.** 🔴 **A fourth defect fell out of the audit: a trade that OPENS AND CLOSES INSIDE ONE BAR read as having no end at all** and got the 3-bar fallback — 4 marks on 3 trades, one drawn after a trade that lasted 4 minutes. ⚠ **FOUR of the six candles he named are NOT this layer's doing, measured clause by clause**: three miss `open >= close[1]` by **$0.05 / $0.06 / $0.01** and one fails the **117-bar uptrend gate** — all four are IN the zone and would paint the instant the engine saw them, and his own TradingView chart does not draw them either. **Never add a tolerance there.** ✅ 6 new tests, 4 watched RED; 870 backend tests pass, 1 pre-existing failure. ⚠ **Existing runs need Rebuild chart.** **The standing lesson: he kept saying "in the drawdown", which is the red box on his screen — and every version of this that reasoned in BARS was self-consistent, easy to test, and answering a question nobody asked.** Full detail: `backend/CLAUDE.md` → *Candlestick reversals*. Earlier: 2026-08-08 — 🟢 **THE PRICE CHART PAINTS THE CANDLE THAT TURNED PRICE, AND THE FIRST VERSION LOOKED RANDOM BECAUSE OF WHERE IT WAS ALLOWED TO PAINT.** Aaron's ask: highlight the reversal candle at his trades and at the setups he missed, *"so I could do analysis and say, can I add candle patterns as an additional confluence to get better entries or avoid bad trades"*. The canonical `engines/candlesticks/` engine is replayed server-side into one navy candle per setup. 🔴 **The anchor set is the whole feature, and it was wrong: BLOCKED setups were anchors, 324 of them against 159 trades and 35 three-of-three misses — two thirds of every mark — and since the Blocked layer defaults OFF they painted navy candles where the reader could see no setup at all.** Reported off the screen in exactly those words. ✅ **518 anchors / 424 marks → 194 / 153, measured.** 🔴 **The second defect was a `Bearish Engulfing` marking a LONG that WON**: ranking started at nearest-the-extreme, so whichever pattern sat closest won regardless of which way it pointed. Direction is now a three-tier PREFERENCE — aligned, neutral, opposing — with nearness the tie-break WITHIN a tier. ⚠ **The opposing tier is KEPT and is half the point** (*"if not, it will show me why I was wrong"*), so this is a preference and never a filter: **89 aligned / 49 neutral / 15 against / 41 with no pattern at the turn**, measured over the 194 anchors. ⚠ **The NEUTRAL tier does most of the work, and the reason is in the source Pine** — ten of its fifteen rules gate on a trend lookback, so in an uptrend the bullish rules cannot fire at all, which is exactly why the reference mark was bearish. ⚠ **A WINNER is searched over its whole HOLD**, because *"the ultimate reversal point before the trade went into my favour"* is the deepest adverse price of the hold — MEASURED at a median 2 bars past entry but **p90 27 and worst 112**, so the obvious fixed window finds the real turn on barely half of them; a ±2-bar tolerance stops a long hold reporting a distant pattern as its turn. ✅ **Aaron's third question — a 3/3 miss with no mark — was MEASURED rather than reasoned about: all nine unmarked 3/3 misses have ZERO patterns anywhere in their window.** No pattern at the turn is a real answer. ✅ **The pattern NAME is a Chart setting now, default OFF** — the tags carry no cross-overlay de-collision, unlike the batched `LABEL` template, so they write across neighbouring candles; the colour is the marker. 30 backend tests + 4 browser checks, **all proven by MUTATION** (a fail-watch is vacuous for a new module). 🔴 **Two existing browser checks broke and broke CORRECTLY — they read the opening viewport, which held a mark only while the run carried 424 of them, and an empty viewport is pixel-identical to a layer that never draws.** 810 backend tests green, 1 pre-existing failure. **The standing lesson is about scope rather than correctness: every individual mark this layer drew was the right candle by its own rule, and the feature still read as broken, because the rule was allowed to answer a question in places the reader could not see one being asked.** Before trusting a layer that annotates, check that everything it annotates is VISIBLE on the same screen. Earlier: 2026-08-07 — 🔴 **THE DRILL-DOWN ENDPOINT'S `available` FLAG WAS `bool(candles)` WHILE ITS DOCSTRING CLAIMED IT MEANT THE FEED COULD NOT BE REACHED**, so the price chart could only hedge — *"no data here (feed offline, or none this far back?)"* — and the distinction had already been destroyed one layer down, where `_build_candles` caught every exception and returned `[]`. `available` now means the feed could be ASKED and `feed_error` names the failure; proven live, M1 in 2010 returns `HistoryFloorError: XAUUSD has no real 1-minute history before 2018-09-14 on VantageMarkets-Demo`, which the chart prints verbatim. Same pass: **switching display timeframes no longer throws the reader forward** (measured 42 days out after a jump, now 0.4) and a drill-down shows a loading badge over the plot on its ~4.5s fetch. 4 browser checks, all proven by mutation. Earlier: 🔴 **THE PRICE CHART'S DRILL-DOWN FETCHED THE NEWEST BARS INSTEAD OF THE ONES BEING READ, AND THE OHLC READOUT SAID `Volume: n/a` FOR A NUMBER THE PIPELINE HELD.** Both reported by Aaron off the screen. Pressing M5 while reading 2020-08-05 moved the chart to 2026-08-06 (measured), because `ChartPanel.runFetch` anchored on the run's LAST bar; M30/H1 looked fine only because they are resamples of bars already in memory. The window follows the viewport now and pages like any other history. Separately, `chart_spec` was DELETING the volume column once its server-side layers had read it, so klinecharts' tooltip rendered its `volume` row as `n/a` — the strip cost +16.0 bytes/candle to undo (23.32 → 25.77 MB on a 155,807-candle spec) and the readout now reads a real figure. **H4 and D1** were missing from the timeframe ladder entirely and are now offered. ⚠ **A third fix was written, measured and REVERTED** — a suspected 90-second freeze on a timeframe switch after a jump is the test harness, not the page (an in-page timer logged 2,407 samples over 120 s, and it reproduces at HEAD). Detail: `frontend/src/components/ChartPanel/CLAUDE.md` and `backend/CLAUDE.md`. Earlier: 🔴 **STOP CANCELLED WHATEVER JOB A SHARED FILE HAPPENED TO NAME, AND THE RUNS LIST COULD THROW A RESULT AWAY ON ONE UNCONFIRMED CLICK.** Aaron asked for an in-depth audit of the Backtests list and the Backtest detail page, then for the fixes — **27 findings, nine real defects, three destructive, and not one of them produced an error.** 🔴 **`stop_backtest_run` read its job id out of `lab_progress.json`** — ONE file shared by every runner — while `job_id == run_id` for every backtest this app starts, so the lookup could only ever be wrong; **measured on the live file it held a stale `"j2"`**, so a Stop cancelled another platform's job or nothing at all, swallowed the failure, marked THIS run cancelled, released its platform lock **and left the real job running.** 🔴 **Cancelling did not stop the poller either**, so when the agent finished, `_handle_complete` wrote `complete` back over `failed_cancelled` — a run you had stopped came back to life with results. The DB row is the single lock source and is now the single place a cancellation is read: marked BEFORE the runner is told, and re-checked AFTER the results fetch, which is where a Stop actually lands. 🔴 **A rerun deleted two of a run's artefacts and left the rest**, so the page served the PREVIOUS attempt: `chart_spec.json` is cached, so the Price tab drew the old candles and trades; the blocked/missed files are written only when non-empty, so the old refusals stayed on the chart; a rerun over a new window kept the old regime calendar; a stale `engine_timeline.json` kept `sized` true. ⚠ **The converse is the transferable half: an optional artefact's ABSENCE is what removes its chart layer, so `if blocked:` was not a write guard — it was a way of publishing last time's data.** 🔴 **A ten-minute WALL CLOCK killed healthy heartbeating jobs and reported "No heartbeat for 0s"**, blaming the agent for something the lab did; latent only because the longest run in this lab is 275s. 🔴 **On the list, the row Rerun fired on the click** — 13px, inside a row whose own click navigates — while **there was no per-row DELETE at all**, so the one warning naming what a delete cascades through was unreachable and the bulk path, which deletes the most, showed the generic message. 🔴 **The news filter's two rules did not compose**: one `if / else if` did the counting and the removing, so a trade that was both a holiday and a news window **stayed in the result when you ticked News with Holidays off**, silently exempt from the rule you had just switched on. 🔴 **A run with no evaluated ruleset could never show a drawdown percentage, and the page asked you to "set an account balance" while rendering no control that could** — the run's own opening balance was on its equity curve the whole time, and it is the more correct denominator for a self-sizing run anyway. 🔴 **The detail page and the Runs list used two definitions of one number** — the backend prepends the opening balance to the drawdown series and the page did not; ✅ **MEASURED on the shape that decides it, a run down 40% from $10k that never regains its start reads 25% without the anchor and 40% with it.** ✅ **Efficiency MEASURED rather than asserted: the runs list was N+1 (one sqlite connection per row) — 12 connections / 23.72 ms → 1 / 2.28 ms with byte-identical output; the run page stopped pulling the whole lab for one badge, 20.6 KB → 0.002 KB; the python runner's `_JOBS` was never evicted (a 115-trade run's payload is 4.14 MB, retained for the process's life) and now sheds the PAYLOAD while keeping the status, because deleting the entry turns *finished an hour ago* into *failed*; and two `/log` polls during a run became one.** ⚠ **Two audit findings were WRONG and are recorded as wrong** — the per-GET regime breakdown and the re-price's four curve walks were flagged as costs and measure **20.1 ms and 19.3 ms for the whole endpoint**; *a cost guessed at is the same error as a number guessed at.* 🔴 **A TENTH defect was found by DRIVING the fixes rather than by reading them: `GET /backtests/running-job` named `nt8` and `mt5` and never passed `python`.** `lab_db.get_running_job()` computed the bucket, `RunningJobStatus` DECLARED it, and the model's `running=False` default filled the hole in silence — **so a python backtest reported its own platform FREE for its entire run.** The LOCK was never affected (`has_running_job` is a separate query; a second run was refused `409`, measured), so nothing could double-run; what broke is every control gated on that answer — the list's Rerun, the page's Retry and Rerun, the Run modal, Optimize — all enabled through a running job and able only to raise an error toast. The response is derived from `_SCOPE_RUNNER_SQL` now, so a new scope cannot be added to the lock and forgotten here. ⚠ **It is the `entry_ms`/`favorable` trap from the opposite direction, and the more dangerous direction: not a field the model failed to DECLARE, but a declared field the constructor failed to ASSIGN — and a Pydantic default is indistinguishable from a measurement.** ✅ **Both destructive paths were then PRESSED against real running work rather than reasoned about: Stop cancelled a full-history replay in 287 ms, released the lock, and the run was still `failed_cancelled` 120 s later with `[failed_cancelled] cancelled` in the job's own log; a rerun drove a run directory 6 artefacts → 0 → 5 rebuilt, the cached `chart_spec.json` included. All four probe runs were deleted afterwards, leaving no orphaned directories.** 🔴 **Closed out the same day, and BOTH of the two loose ends turned out to be misdiagnosed by the note that recorded them.** (1) **One reload could leave this app DEAD with port 8000 still bound, and it had been written down as an unexplained one-off blamed on `--reload` killing a replay thread.** It has nothing to do with backtests: **`touch main.py` on an idle server reproduces it**, /health going from `200` in 19 ms to a hard timeout with the same worker PID alive at 0% CPU. On a reload the reloader joins the worker and the worker's shutdown waits for open connections to close — **and the Vite dev server holds a keep-alive POOL to that port, measured at 19 established sockets, which will never close on their own.** ✅ **Fixed with `--timeout-graceful-shutdown 10` in `start.sh` and proven by reproducing the same sequence twice: `200` in 1.9 ms.** 10s clears the slowest measured request here (a cold ChartSpec build, 7.6s), so it only ever bounds a wait on sockets doing nothing. (2) **A red Calendar check was blamed on a clock-dependent fixture and was really a mock defeating the test's clock** — `mockCalendar` served `server_now_ms: Date.now()` from the NODE process on the real clock, and `useServerClock` trusts the server, so `page.clock.install` did nothing all file. ⚠ **The lesson both share: a plausible written-down guess is worse than no note, because it sends the next reader at the innocent half of the system.** Both cracked in minutes by refusing to reason and just running the thing. ✅ **25 backend tests (723 green) and 11 browser checks; 19 of the 25 and 10 of the 11 were WATCHED RED against HEAD**, the rest labelled in their own docstrings as pinning the half of a rule that was already right. 🔴 **One browser check was VACUOUS on its first attempt and PASSED against the broken page** — it asserted a page-wide Retry button while the page HEADER carries its own, so the locator matched a control that was never in question; it is scoped by `data-testid` now and its non-vacuity was proven by MUTATION. **The standing lesson is a new one and it is about identity: `lab_progress.json` exists to drive one progress bar, and the moment something READ an identifier back out of that shared mutable file, an unrelated platform's job became the target of this run's Stop — while the run id was in hand the whole time. Never recover an identity from a channel that was built to carry a status.** The sharper second lesson is on the tests: it is not enough to have watched the suite go red, you have to have watched THIS test go red for THIS reason. Earlier the same day: 🔴 **THE STRATEGIES PAGE WAS AUDITED, AND THE ANNOYING TOAST WAS THE VISIBLE EDGE OF A PAGE THAT COULD NOT TELL "THE VPS SAYS NO" FROM "NOBODY ASKED THE VPS".** Aaron asked for a full audit — Strategies tab, Deployed tab, Scan — and reported one symptom: *"if the NT8 agent is down I get this annoying error about remote end closed connection without response, every couple of seconds."* ⚠ **A toast is an EVENT; a dependency being down is a STATE.** Both strategy-file queries poll, `api.get` toasts on every non-ok response, and seven mutation hooks carried an `onError` that toasted the same failure a second time — **~6 popups a minute for as long as the page was open, plus a burst on every window focus.** `request()` takes a `silent` option now, both polling hooks use it with `retry: false`, and the failure is RENDERED as a banner naming which agent is down. 🔴 **The deeper defect is what the storm was hiding: a dead NT8 agent 502'd `sync-status`, so EVERY row lost its sync object — including MT5 and Python rows — and `sync === undefined` fell through every pill and every guard to the default action, so a strategy that needed deploying offered a Run button.** An absent answer took the shape of a healthy one. Both file endpoints return an envelope carrying `nt8_error`/`mt5_error` now, and the split is the transferable part: **`needs_deploy`/`needs_compile` SURVIVE an unreachable agent** (they are the local source hash against this app's own deploy record, answerable with the box switched off) while `file_exists_on_vps`/`in_sync`/`is_compiled` go `None`. **Ask of each field whether the thing that answers it was reachable — not whether the request succeeded.** 🔴 **`nt8_running` and `nt8_sa_visible` initialised to `False`, so the sidebar reported "NinjaTrader not running" for a NinjaTrader nobody had asked about** — and it was exactly wrong: NT8 was open on the VPS the whole time. Three-state now, the `mt5_link` rule for the sixth time. 🔴 **`file_exists_on_vps` had been returned by the backend and rendered by NOTHING, so a deployment whose file was deleted off the box read green "In sync"**; ⚠ **the first fix reintroduced the exact contradiction it existed to remove**, drawing the new red chip BESIDE the hash-derived pill so one row said *In sync* and *Missing on VPS* at once — **the browser check caught it, reading the diff had not.** 🔴 **The version chip named the DEPLOYED version as what was running on a strategy not yet compiled, while both NT8 and MT5 execute the COMPILED artefact.** Also: the compile modal had no way out while a job ran (no header X, no Escape, and the footer Close renders only on completion — a hung poll spun for ever), `POST /strategies/{id}/deploy` 500'd on a python strategy (a package DIRECTORY hitting `read_bytes()`), a deploy job was readable from any strategy's URL, `_deploy_jobs` grew for ever, a python package that failed to IMPORT was indistinguishable from one that is not a lab strategy (silent stale schema + a "Needs scan" pill that never cleared), and Reconcile was gated on `scan.data?.orphans` — mutation state — so a deleted source file was invisible until somebody pressed Scan. 🔴 **THE INCIDENT UNDERNEATH IT ALL: THE NT8 AGENT HAD BEEN DEAD FOR TWO DAYS AND NOTHING IN THIS APP COULD RECOVER IT.** The process was **alive with its socket dead** — PID 13396 up since 2026-08-04 02:59 UTC, nothing bound to 8765 — and Windows refuses to launch a second instance of a task whose first is still running (`0x800710E0`), **so both the supervisor's fire and the sidebar's Start button were guaranteed no-ops**, logging `nt8-fired-but-still-down` once a minute while reporting an action each time. The supervisor kills the corpse and re-fires now, with the two-clause `wmic` match (`name='python.exe'` **and** the agent's own script) — verified live: one PID matched while the trading bot, the MT5 agent and both Telegram processes were untouched. ⚠ **A task that CANNOT start is not a task that failed, and "nothing to kill" is a different diagnosis** — logged as one, because that means the task genuinely never started and a second fire would not fix it either. ✅ **16 new backend tests (693 green), 11 new browser checks, frontend typechecks and builds.** ⚠ **A clean fail-watch against HEAD was IMPOSSIBLE here and is recorded as not done** — the endpoints changed shape in the same pass, so the old page fails against the new backend for unrelated reasons; non-vacuity came from MUTATION instead, removing each fix and confirming the test went red. 🔴 **That found a test of mine that passed with the guard it named DELETED**, because its mock made the assertion true either way. **The standing lesson is the sharper version of this repo's own three-state rule: it is not enough for an API to distinguish "no" from "cannot ask" — the distinction has to survive the transport.** A 502 collapses every field in the response into one word, and a page that renders a missing payload as a default renders the most reassuring answer available at the exact moment nothing is known. Earlier: 2026-08-05 — 🔴 **THE STRESS TEST FEATURE WAS AUDITED END TO END, AND THE `stress_tests` TABLE HELD ONE ROW — WRITTEN THREE DAYS BEFORE THE ACCURACY PASS THAT REWROTE THE ENGINE UNDERNEATH IT.** Aaron asked for a full audit of the page and its in-depth functionality, then for the fixes. **That single query is the frame for everything: nothing here had ever been exercised against the code it actually runs on**, and re-grading the stored row through the live backend moved it from a confident **D** to **ungraded**, with both probabilities NULL and sensitivity 0.858 → 0.205. 🔴 **The load-bearing defect is the repo's newest refrain arriving in a third place: a walk-forward or sensitivity child was launched WITHOUT the baseline's `cost_layers` / `broker_profile` / `sizing_mode`** — so stress-testing a run that charged spread and swap measured every child on a FREE BOOK, and sensitivity attributed the cost gap to the parameter it had shifted. The Tuning workbench had the identical defect the same day, in a different page. ✅ **PROVEN ON REAL DATA, not by reading the diff: every child of a charged 161-trade baseline now carries `["spread","swap"] / vantage_demo / consistent`.** 🔴 **On the page, the drawdown was rendered in DOLLARS against a dollar limit while the grade beside it had been decided in PERCENT** — the engine picks its basis per run and the page read neither `dd_basis` nor the percent columns — **so a red "over limit" could sit next to an A**. 🔴 **`degradation` is a MAGNITUDE and the radar drew `-degradation`, so a parameter shift that IMPROVED the result rendered as a long red bar**; 🔴 **a shift whose backtest FAILED was drawn as a flat zero bar**, i.e. *tested, no effect* — the most reassuring answer available for a measurement that never happened; 🔴 **`prob_pass_eval ?? 0` printed "0%"** for a ruleset with nothing to breach. 🔴 **`is_trades`/`oos_trades` were written by the engine and undeclared on the API model**, so Pydantic stripped them and nothing could show that every walk-forward window on the stored test closed **6** out-of-sample trades against the engine's own floor of 20 — **the `entry_ms` / `exit_ms` / `favorable` trap for the fourth time.** 🔴 **Cancel did not stop the work and delete left the files**: `reports/lab` held **191 directories against 84 live runs**, and a real delete now takes 216 → 192 (the test's own dir plus every child's). 🔴 **`asyncio.create_task` was called with no strong reference**, so a long-awaiting stress test was collectable mid-flight and would leave its row `running` for ever. ⚠ **One fix is about honesty rather than a wrong number: a phase that RAN AND CRASHED left a NULL summary, which grading reads as "not run" — explicitly unpenalised — so a crashed walk-forward could hand out an A with the caveat "walk-forward not run".** `phases_requested` is written when the row is INSERTED (the only point that cannot be missed) and `phase_failures` names what died. ⚠ **Sensitivity now skips a param that is UNREACHABLE in this run** (behind a `show_if` switch it has off) — 3 of 17 on the measured baseline, i.e. 12 pointless backtests — and reports its coverage rather than leaving the reader to assume it tested everything. ✅ **27 new backend tests (650 green) and 11 new browser checks, ALL 11 watched to fail against the page at HEAD.** ⚠ **Two of those checks were VACUOUS on the first attempt and passed against the broken page** — `page.locator('svg').first()` is the sidebar logo, and a chart label appears three times over — **which is the reminder that a green new test proves nothing until you have seen it red.** **The standing lesson is the Optimizations one at one remove, and it is worse: that page had never been run at all; this one had been run ONCE, and then the engine beneath it was replaced. A feature exercised before its own rewrite is not an exercised feature — it is a feature whose last real evidence describes code that no longer exists.** Earlier the same day: 🟢 **TWO SCHEDULED JOBS LEFT THE BOTS PAGE BECAUSE THEY NEVER DID ANYTHING, AND TWO THAT DO WERE NEVER LISTED.** Aaron asked what `SYS_PNLTRACKER` and `SYS_REPORTER` were for and whether he still needed them; the answer was that **neither could have done anything if switched on** — both carried an empty bot registry inherited from the four bots deleted 2026-06-22 (`algos/CLAUDE.md`). This page had been rendering them as **DISABLED** with a deliberate comment saying a disabled task is not a failed one, which was true and beside the point: **a job that is switched off and a job that does nothing look identical from here, and only one of them is a feature.** Both deleted, and `SYS_DEADMAN` + `SYS_LOGBACKUP` — real, enabled, and never in `_SCHEDULED_JOBS` — take their place, so the three jobs listed are now the three that run. ⚠ **`_SYS_TASK_BY_JOB` is DERIVED from `_SYS_DISPLAY_NAMES`**, because the loop restated the name→task map and a job listed under a name that map missed resolved to a permanent `UNKNOWN` — a job the page cannot see, rendered as one it never asked about. 🔴 **Seven fields came off `BotStatus` with them, and leaving them would have been the real defect**: `daily_pnl`, `daily_pnl_pct`, `weekly_pnl`, `weekly_pnl_pct`, `peak_balance`, `trades_today` and the three cap fields (`daily_goal_pct` / `daily_cap_pct` / `weekly_cap_pct`) had **no writer left**, so the Monitor tab's four stat tiles and three cap chips were drawing an em-dash or nothing — which reads as *quiet*, not as *nothing measures this*. **This repo's own rule, and the third time it has been applied on this page: a fabricated value and a measured one must never look alike** (`mt5_link`, `grid_sensitivity_score`, now this). `balance`, `total_pnl_pct` and `uptime` survive because `algos/live/runner.py` writes them, and they are in the row above. ⚠ **The caps were the dangerous half: they rendered as PROTECTION.** They were the P&L tracker's Telegram ALERT levels — nothing in them could refuse a trade — so the page showed a daily cap the bot would have traded straight through. `_BOT_THRESHOLDS`, `_load_thresholds_json` and `_get_thresholds` are gone, along with `algos/shared/thresholds.json`. **A real cap belongs in the bot's own loop.** ✅ 650 backend tests green, frontend typechecks and builds. **The standing lesson is the Optimizations one from a different direction: there, a feature nobody had RUN was 24 defects waiting; here, a feature nobody had run was wearing the word DISABLED, which is what kept anyone from checking.** Before switching a dormant job on, read what it would do against today's registries — not what its name says it does. Earlier the same day: 🔴 **THE NEWS CALENDAR WAS AUDITED END TO END AND THE BEAT/MISS COLOURS HAD BEEN WRITTEN FOR THE WRONG PROVIDER.** Aaron asked for an audit of the calendar feature, then for the fixes; the same fixes were applied to the Overview's preview. **`calendar_service._LOWER_IS_BETTER` decides which way a released `actual` is coloured, and it had been written against Forex Factory's naming while the tab reads TRADINGVIEW.** ✅ **MEASURED against the live feed rather than reasoned about — 811 distinct real titles over ~275 days: SIX of its eleven keys matched ZERO of them** (`initial claims`, `continuing claims`, `crude oil inventories`, `gasoline inventories`, `natural gas storage`, `producer prices` — TradingView calls those *Initial Jobless Claims*, *EIA Crude Oil Stocks Change* and *PPI*). ⚠ **A dead key costs nothing visible, and that is precisely what makes it dangerous: the damage is what it leaves UNCOVERED.** The worst case was **`Core PCE Price Index MoM` — HIGH impact, USD, the Fed's own preferred inflation gauge** — which matched nothing and fell through to higher-is-better, so **a cool PCE printed RED "miss" rows under a cool CPI printing GREEN "beat"**: two inflation prints, one table, opposite colours. ✅ **PROVEN THROUGH THE RUNNING BACKEND on real released data, not by unit test alone: `Core PCE Price Index MoM` at 0.1% against a 0.2% forecast now reads `beat` where it read `miss`, across 18 real PCE rows**, with `Initial Jobless Claims` 229 vs 219 → `miss` and `EIA Crude Oil Stocks Change` −8.262 vs −4.6 → `beat` (both previously uncovered). **The fix that matters is the GUARD, not the added keys: the suite now fails the build on any key that matches nothing in a committed corpus of real titles.** A list of substrings is a CLAIM about one provider's vocabulary — this repo's most-repeated defect in its quietest form, because a wrong polarity renders a confident colour rather than an error. 🔴 **On the page, nine more defects and not one of them rendered an error either.** **The week was memoized on `weekOffset`, which does not change at midnight, so a tab left open across Sunday→Monday asked for LAST week for ever — the identical defect the Overview fixed on 2026-08-05, whose own comment asserted that the Calendar page recomputed and was right. It did not.** **Paging a week rendered the PREVIOUS week's rows under the new week's header with an all-zero day strip**, because `placeholderData` holds the old payload and the page only checked `isLoading` (false while placeholder data exists) — **held data is only honest while the KEY is unchanged; when the key changes it is not stale, it is the answer to a different question.** **A failed background poll DELETED a good week** (now a dated banner over the retained rows, the same rule the bot snapshot follows). `?day=abc` rendered as an empty week; a category the loaded week has none of rendered as a blank dropdown over an empty list; the "now" line drew on every week; and **duplicate React keys, real rather than theoretical — the live feed carries two `CAD Budget Balance` rows at one timestamp.** ⚠ **Efficiency: the cost was the CLOCK, not the data** — the 1s server-clock tick rebuilt ~200 rows a second, each constructing an `Intl` formatter, so `EventRow` is memoised and the formatters are module-level. Also: `dayIndexOf` / `fmtDay` / `fmtWeekRange` are shared, because **the Overview WRITES the day index the Calendar page READS**; the fetch cache is bounded and locked; and every upstream failure is classified at one seam (a `JSONDecodeError` **is** a `ValueError`, so a feed outage surfaced as a **400 "your request was malformed"**). ✅ **88 new backend tests (618 green) and 11 new browser checks, 10 of them WATCHED TO FAIL against the page at HEAD** — the 11th passed there and was kept deliberately, because it pins the half of the error rule that was always right. ⚠ **That spec needs NO BACKEND, only the dev server** — the calendar reads one endpoint, so intercepting it whole makes the suite runnable without the SSH tunnel or the live MT5 box; **prefer that shape for any new suite the page allows it for.** **The standing lesson is the repo's own arriving through a provider boundary: a list of magic strings matched against someone else's vocabulary is a claim nobody is checking, and it fails SILENTLY — not by matching the wrong thing, but by matching nothing at all and leaving the default to answer.** Earlier: 2026-08-04 — **SMART MONEY IS HIDDEN BEHIND A FEATURE FLAG, NOT DELETED.** Aaron is auditing this app down to the functionality he actually uses, and the Smart Money scanner is not on the list for a while. `frontend/src/lib/features.ts` holds the switch and it is read in three places, because **a flag has to hide an AREA rather than a component**: the nav row, the routes and the Overview cards move together — hiding only the nav leaves a page reachable by typing its URL, and hiding only the route leaves a row that goes nowhere. ⚠ **Hidden means NOT FETCHED** — `useRunProgress` polls every 30s forever, so an unrendered card would go on costing two requests a minute for something nobody can see; both hooks took an `enabled` param and the Overview now issues **0** `/smart-money` requests, measured in a real browser. ⚠ **Nothing was deleted and nothing on the BACKEND changed**: the pages, hooks, types, `routers/smart_money.py` and the `smart-money/` pipeline are untouched and the endpoints are still served, so this is a UI decision and one boolean reverses it. **Rendering it is what found the one real defect** — an unmatched route drew NOTHING, a blank main area beside a working sidebar, so a stale `/smart-money` bookmark read as the app breaking; there is a `path="*"` redirect to Overview now. Frontend typechecks and builds; verified in a real browser at 1670×940. Full rules: `frontend/CLAUDE.md` → *Feature flags*. Earlier the same day: **THE BOTS PAGE'S CONFIGURE TAB SCALES NOW: A BOT SELECTOR, NOT A STACK.** Aaron's ask. It rendered a full screen per bot with no selector, so with several bots the Promote button you want sits between two identical ones you do not — a misclick surface on the one page that deploys code onto a live account. It is now a **left rail + detail panel** plus a **fleet version strip**, closing 2 of G11's 3 bullets (Monitor's global-vs-per-bot controls are the third, still open). ✅ **The guard is a property, not a layout: only the SELECTED bot's controls exist in the DOM** — measured in a real browser against a mocked 4-bot fleet, **1 promote button rather than 4**. ⚠ **The fleet strip shares the per-bot card's cache entry and its single `versionFlags` derivation**, so *which bots are behind the repo / have a restart pending* cannot disagree with the card it sends you to — and it costs no extra fetch, because the flat stack was already reading every bot's version. ⚠ **An unreadable deployment record is counted as `unreadable`, never as clean** — the same *no data ≠ cannot ask* rule as the `No MT5 link` chip. **The standing lesson is small and unflattering: `frontend/CLAUDE.md` already documented the 22px padded-scroller sticky trap, the new sticky header hit it anyway, and a SCREENSHOT found it in one look where reading the diff had not.** Earlier the same day: ⚠ **THE PER-BOT START BUTTON COULD CREATE A DUPLICATE BOT, AND THE FIX LIVES IN `algos/`.** `POST /bots/{name}/start` runs `startup_coordinator.py --bot <key>` on the VPS, and that path launched the bot unconditionally — so pressing **Start** on a bot that was already running gave you TWO processes on one account and one magic number, each sizing a full position off the same setup. Nothing on this page could show it: the snapshot reports whether the bot is running, not how many copies. **Measured on the live box 2026-08-04** (via `SYS_STARTUP`, the same coordinator), and fixed there — the coordinator now skips a running bot on both launch paths and `runner.py` refuses to be a second copy. ⚠ **Nothing changed in this app, and that is the point worth recording: a button whose safety depends on the remote script being idempotent has no way to be safe on its own.** The Bots page cannot tell how many copies are running, so "is Start safe to press twice" was never a question it could answer — the guard has to live where the process is created. Same shape as the health dots: this page renders a claim that some other code has to make true. Same pass, also in `algos/`: **restarting a bot no longer kills the Telegram bot.** The coordinator used to force-restart Telegram on every run, so every Start/Restart pressed here dropped the alert channel for a few seconds and produced a 🟢 *Telegram Bot Restarted* message a minute later — which read as a crash for weeks. Earlier the same day: 🔴 **AN EM-DASH IN THE BOTS PAGE'S BALANCE COLUMN WAS THE ONLY THING IN THE SYSTEM REPORTING A BOT THAT HAD BEEN BLIND FOR 50 MINUTES.** MetaTrader auto-updated itself on the VPS at 02:57:53 UTC and restarted two seconds later, taking the running bot's terminal connection with it. The bot stayed alive and kept stamping its heartbeat, so the watchdog saw a healthy bot, `wmic` still listed the process, and this page said **RUNNING** — while it received no bars at all across an open session. `BotStatus.mt5_link` now carries the answer and Monitor draws a **`No MT5 link`** chip. ⚠ **The chip sits BESIDE the Running pill rather than replacing it**, because both are true at once and they are different questions: the process is ALIVE (so a restart is the fix, and the watchdog was right not to fire) and it is BLIND (so it is trading nothing). ⚠ **`Optional[bool]`, read `=== false` and never falsy** — `None` means the bot has not stamped a link state, which is not the claim "disconnected"; the same rule `mt5_connected` follows on the health strip, and painting a healthy bot as disconnected is the identical mistake in reverse. ⚠ **The balance cell says `no link` in `warn` rather than an em-dash**, so the two causes of a missing number cannot look alike again. Same pass, two stale rows corrected: this file's Bots row claimed **"no bots registered yet"** while `mpc_sos_fade_demo` had been live since 2026-07-31, and now names the **Deployed version** card (hash / commit / date / all 47 params as deployed, with a Promote button that previews by really staging and importing the code) plus its four warning states. ⚠ **Configure renders one full section PER BOT with no selector** — correct for one bot, a misclick surface for several, logged as **G11** in `docs/LIVE_TRADING_PIPELINE.md` to fix before bot #2. **The standing lesson is deliberately not this repo's label-vs-code refrain: every layer beneath that cell behaved defensibly on its own** — an empty bar frame is a fine thing for a data call to return, a null balance a fine thing to write when you have none — **and the defect was that "no data" and "cannot ask" were the SAME VALUE at every hop**, so by the time it reached the browser the distinction did not exist to render. When a cell can be empty for two reasons, the API has to say which. Earlier: **Last reviewed:** 2026-08-03 — 🔴 **THE COSTS PILL REPORTED THE FEES WHILE THE PAGE REPORTED THE BALANCE IMPACT, AND NOTHING RECONCILED THEM.** On run `75ccc776d10c` the Net hero falls **$18,200,741** when spread + swap go on, while the pill said `Charging $332,371` — a 55x disagreement between two correct numbers, which reads from the screen as a broken feature and was reported as one. **They measure different things:** $332,371 is the fees actually paid (0.9% of gross winnings) and $18.2M is what the final balance lost, **98.2% of which is compounding rather than cost**. ✅ **Demonstrated by re-walking the same trades and same fees at different risk sizes — 64.5% of the result at 10% risk per trade, 11.0% at 1%** — so the dollar gap is leverage, not friction. The arithmetic was never in question: a real charged replay gives 123.90R / $10,090,716 against the re-price's 123.86R / $10,058,027, inside the documented ~0.3% swap approximation. ⚠ **The footer's caption sat on the one figure it did not describe** (`$332,371 after compounding`). Fixed: the pill headline is now **R** — one number, and the only unit that cannot contradict the page or the rows above it — and the popover names **Charged −12.08R · Fees charged $332,371 · Net $28,248,768 → $10,048,027** with the ratio spelled out. `useCostFilter` now returns `netBefore`/`netAfter`/`balanceImpact` summed off the SAME rows the Net hero sums, so pill and card cannot drift; the `Costs charged` KPI row became **`Fees charged`**. Frontend typechecks and builds. **The standing lesson is the repo's own with the emphasis moved: the previous instances were labels claiming what no code did — here every number was right and every label defensible alone, yet the page still misled, because two true figures of different KINDS were presented as one and the reader was left to subtract.** Earlier the same day: **THE PRICE TAB IS INSTANT: THE RUN PAGE BUILDS IT IN THE BACKGROUND ON ARRIVAL.** Aaron's ask — land on a backtest, and by the time you click Price it is already there. **MEASURED on run `432aff31f374` (32,978 M15 candles): 2,453 ms from click to a painted chart → 167 ms**, verified in a real browser. ✅ **The result worth carrying is the breakdown, because the two obvious costs were the small ones.** The wait was (1) a ~3.5 MB `ChartSpec` fetch — 0.39 s off the run dir's cache, **7.6 s on a cold build** — plus the browser's parse, (2) the lazy klinecharts chunk, and (3) klinecharts laying 33k candles and their overlays out ON MOUNT. Prefetching (1) and preloading (2) together bought only 2,453 → 1,964 ms. **The rest could not be prefetched at all, because a BUILD cost is only payable by mounting the thing** — so `ChartTabPanel` gained `keepMounted` and the Price tab now stays mounted, hidden, while another tab shows. ⚠ **Hidden means `visibility: hidden` + `position: absolute`, and `display: none` is the trap that would look like it worked**: a display-none container measures 0 wide, so klinecharts sizes its canvas to nothing and has to resize on reveal — the exact swap this removes. `visibility` keeps the real width (canvas 1033 px either side of the reveal) and contributes no height. ⚠ **The warm mount is deferred to an idle beat (floor 1.2 s)** — klinecharts blocks the main thread while laying out, and the equity curve is what the reader is waiting for on arrival. ⚠ **The cost is real and is the trade that was asked for: a run page now always fetches the spec and builds the chart, even if the Price tab is never opened.** `StackDetail` and `StressTestDetail` pass no `keepMounted` and are unchanged. Frontend typechecks and builds; the two build chunks are byte-identical in shape to before, so the lazy klinecharts split is intact. Earlier the same day: **THE PRICE CHART DRAWS ORDER BLOCKS.** Aaron's brother asked to see them on the backtest chart, so the canonical `engines/order_blocks/` engine is replayed server-side (`backend/services/ob_overlays.py`) under the SAME anchor rule the fair-value gaps use — a block is drawn only if it was LIVE on the bar of a trade ENTRY, a blocked setup or a missed setup. **MEASURED on run `432aff31f374`: 2,567 blocks created over the window, 579 drawn**, beside the gap layer's 661, so the two together sit at a readable ~1,240 boxes rather than ~3,200. One toggle in **Analysis**, default OFF, and **deliberately NOT in Deep debug** (Aaron's call). ✅ **The interesting result is what it COST: no new overlay template, no new render effect, no new panel concept — one string in `ANALYSIS_GROUPS`, a colour, and a backend module that is the FVG one with a different engine.** That is the payoff on a decision made when the gap layer landed, and it is the shape to reach for next time: the generic `box` group plus a named roster of which groups belong in which menu did all the work. ⚠ **The one real difference is the BOX GEOMETRY, and it is what would have looked plausible if it were wrong**: an order block box is a fixed **30-bar stub** from its anchor candle, stretched to the live bar only while price is back within one block-height, where a gap box tracks the live bar. Two consequences that read as bugs and are not: a block's box legitimately ends long BEFORE the block dies, and it can end AFTER the bar it died on (the stub runs past the live bar into empty space). ⚠ **No settings fork to warn about here, unlike the gaps** — the strategy files dropped order blocks entirely in 2026-07, so `mpc_assistant.pine` is the only source. The flip side is worth stating: **a drawn block never explains an entry, because `mpc_sos_fade` reads none** — it is context the reader brings, not a rule the bot applied. ⚠ **The emitter is unit-tested (18) and the box-vs-Pine-array cross-check the gap layer has could NOT be run** — all three OB exports on disk predate the 2026-07-31 re-port (six slots, no `cfg_ob_*`) and `compare_ob.py` refuses them outright; the ENGINE's parity is proven separately on real 15m/5m exports, and the missing half is NAMED in the test file rather than quietly skipped. Verified in a real browser on run `432aff31f374`, both layers on together. 388 backend tests green; frontend typechecks and builds. **The standing lesson is a new one and it is about generic code, not about labels: the generic `BOX` template's `label` path had never once been used, and it shipped carrying klinecharts' solid-blue-chip default — so the first `OB` tag rendered as a blue pill.** The HLINE label path carried the identical bug, still dormant. A generic mechanism nobody has exercised is not a working mechanism, it is an untested one wearing a general-purpose name; both now share one `FLAT_TEXT` style so the trap cannot come back a third time. Earlier: **COSTS CAN NOW BE SWITCHED ON FROM THE RUN PAGE, NOT ONLY BEFORE THE RUN.** A Costs pill on BacktestDetail's Performance header charges spread / commission / swap onto a run that already happened and reshapes the real KPIs and the Equity chart, with no re-run. **The day before, this repo's own documentation argued it was impossible** — a cost changes what the trades would have been, so a page toggle would flip a number while the trade list under it stayed put. The premise is right; the conclusion does not follow. **Every re-priceable cost is, in R, INDEPENDENT of position size** — a trade risking `dist * qty` and charged `spread * qty` loses `spread / dist` of R whatever the size — so the R is knowable even though a charged run compounds into different sizes, and the dollars follow from re-walking the balance. ✅ **Proven against real replays rather than argued**: `backtest/tests/test_reprice.py` replays `mpc_sos_fade` free and charged, discards the charged replay, rebuilds it from the free run's stored curve and demands equality; on the live 161-trade lab run `75ccc776d10c` the endpoint reproduces a real charged replay to **37¢ on $16.3M**. ⚠ **The real split is not "before vs after the run", it is whether a cost changes WHICH TRADES EXIST.** `bid_ask_fills` moved the reference run 161 → 159 with four setups that never existed on the free path, and `slippage` depends on which exits were market orders — no arithmetic over a stored trade list can produce either, so both are **REFUSED AND NAMED** in `needs_rerun` rather than silently dropped, which would show a spread-only number under a bid/ask label. ⚠ **Two accuracies are captioned, never hidden**: swap is ~0.3% (the replay charges it on the first BAR after a rollover, so any rollover in a stretch with no bars is superseded — Friday and Saturday always, holidays unpredictably) and a run predating the new stored per-trade `r`/`risk_usd` is ~0.02%. ⚠ **Mirroring the replay's swap source line-for-line would have been the WRONG answer** — it skips Saturday only, and charging Friday books 8 nights a week instead of 7, measured at 0.74R too expensive over 161 trades. ⚠ **Costs compose BEFORE the news filter** (a cost is a property of a trade, so it is charged before anything decides which trades count) and rebuild through the SAME `buildFilteredRun` the news filter uses, so the two controls cannot drift into different answers for one KPI. ⚠ **Both are refused under a firm's sizing**, where the curve is path-dependent and the size-independence that justifies the whole thing evaporates. 407 strategy/backtest + 367 backend tests green; `compare_strategy.py` exit 0; frontend typechecks and builds. **The standing lesson is a new one and it is aimed at this repo's own habit: "that cannot be derived" is a CLAIM, and it deserves the same evidence as any other.** The last four entries here are all about a page asserting something no code checked — this one was a page asserting something could not be done, which had never been measured either. Earlier: 2026-08-02 — 🔴 **THE LAB WAS NOT CHARGING THE TWO COSTS IT ALREADY KNEW — THE SPREAD AND THE OVERNIGHT SWAP.** Aaron's framing is the right one: *"you know what the spread is based upon your broker… the only thing we don't know is slippage."* Bar mode charged neither, so every python backtest here has been frictionless in a way that goes past the 2026-08-01 commission fix. Both are chargeable now, from a **broker profile** rather than a typed number, behind **independent layers that are ALL OFF by default** — Aaron's explicit call: the baseline run stays free so it stays comparable to the TradingView Strategy Tester, and each cost is switched on deliberately. Contract: `services/python_runner.py::COST_LAYERS`, stored per run as `cost_layers` + `broker_profile`. **Swap needed almost no code** — the charge path has run on every bar in bar mode since A2 and was dead only because the lab passed `swap=None`. 🔴 **The spread figure this repo had recorded was the WRONG BROKER'S:** `$0.33` is PU Prime's (688k ticks), while Vantage — the broker every backtest here replays — **measures $0.22** (median over **1,494,459** cached ticks), so quoting 0.33 overstated every backtest cost by 50%. It was MEASURED before being hardcoded, and **`GET /backtests/broker-profiles` SERVES the profiles to the Run modal** so no number is ever retyped into a form. **MEASURED on 155,431 M15 bars (2020 → 2026-07-31) at the shipped defaults: free 161 trades / 135.94R / $28.26M · +spread 130.27R / $16.27M · +spread+swap 123.90R / $10.09M · bid/ask fills 159 trades / 141.93R / $29.48M.** ⚠ **A small charge is not a small effect: 12.04R of cost turns $28.3M into $10.1M — 64% of the balance for 9% of the R** — because at fixed % risk a dollar not earned early never compounds; read a cost against the R, never against the net dollars. ⚠ **The bid/ask row is HIGHER than the free baseline and that is the finding, not a bug** — a flat spread charge is the MARKET-ORDER intuition and nothing here is a market order: every entry and exit names a PRICE, so the spread changes WHEN an order fills rather than what it fills at, and the burden lands almost entirely on SHORTS (sell the bid in, BUY THE ASK out). Six marginal entries stopped filling and, with one position slot, four different setups took their place — the queue effect Run 12 already measured, arriving from a new direction. **So the flat charge is a deliberately conservative UPPER BOUND, and `bid_ask_fills` is the question of what the spread actually does** — it is also the newest path here, so treat its result as a lab finding until it has been read on a chart. ⚠ **`cost_layers = NULL` is not `[]`** — NULL means a row written before layers existed and keeps the OLD contract, `[]` means charge nothing; collapsing them would silently re-price all 80 stored runs on their next retry, and the two must RENDER differently. ⚠ **Slippage keeps its own switch and its own typed number**, deliberately apart from the measured costs: it is the one thing history cannot measure. ⚠ **Costs are chosen BEFORE a run and the detail page only REPORTS them** — a cost changes what the trades would have been, so a toggle on the run page would flip a number while the trade list under it stayed put (unlike the News & Holiday filter, which only ever removes trades the run already made). ⚠ **Sweeps and stacks do not write the columns yet**, so they stay on the legacy frictionless branch — correct today, wire them before anyone expects a priced stack. ✅ **Proven rather than argued: the free path reproduces the documented 161 / +135.94R baseline to the cent, `compare_strategy.py` is exit 0, and 386 strategy + 356 backend tests are green.** **The standing lesson is the repo's own from the other side:** the previous four instances were a page CLAIMING something the code did not do — here the code quietly did LESS than the page implied, for months, and there was no wrong label on screen to notice. A cost never charged leaves no false claim behind, so nothing can catch it except asking, of every number a system could price, whether it is actually pricing it. Earlier the same day: **every trade on the price chart can now draw the FIB LEG it was priced off** (Analysis → **Fibs**), so a plotted trade says which retracement levels it went into instead of leaving you to redraw the fib by hand. **The design decision that matters is where the numbers come from:** the STRATEGY records the ladder on the bar it PLACES the order — the same `fiboP*` values that chose the entry edge, the stop anchor and both targets — and every hop downstream copies it, so the chart cannot land on a price the bot never used. A fib rebuilt in the backend or the browser from anchors and a direction would be a second claim about one leg, which is this repo's most-repeated failure (Run-modal costs, Optimize-modal params, the SSH dot, the lab-vs-Pine parameter names). The chart derives only the two things a price ladder cannot state on its own — `entryRatio`, where the fill landed on that leg, and `deepestRatio`, how far the retracement actually ran afterwards — and both are pure geometry off two levels the ladder already carries, so they need no anchor, no direction and no branch for a short. ⚠ **It is snapshotted at PLACEMENT and read back from the ORDER at the fill**, because a fib keeps extending while a limit rests: re-reading it at the fill would report a leg the order was never priced against, and the stop and targets beside it are frozen at placement. ⚠ **No backfill, and only a RERUN produces it** (it would mean replaying the strategy), so the toggle correctly does not appear on older runs. **"Reload charts" cannot supply it and neither can a `?refresh=true` spec rebuild** — the ladder is written into the run's own equity-curve JSON by `backtest/output.py` at replay time, so a run that never recorded one has nothing for the spec to copy; measured on all three newest lab runs, 0 `fib` keys. ✅ Proven cosmetic by measurement rather than argued: `compare_strategy.py` **exit 0 at warmups 100/500/1000/2000**, `compare_bleg.py` exit 0, and — because no `fib_export.pine` CSV exists to run `compare_fib.py` against — the fibonacci engine was A/B'd at HEAD vs the working tree over **47,263 real bars with 0 field differences**. Validated forward too: 17 of 17 trades on a 2024 replay carry a ladder, and their entry ratios independently reproduce the documented entry model. 349 backend tests green (12 new); frontend typechecks and builds. Earlier the same day: **the price chart got a Deep debug toggle at the top of the price chart's Analysis menu** — one row that switches the context behind whatever trades are on screen (Fibs, External Structure, Fair Value Gaps), instead of three switches across two dropdowns set and unset constantly. **The lesson is how many shapes it took in one day:** a segmented `Winners | Losers` pill, then a four-way radio, then one additive on/off row. The first two OWNED THE OUTCOME FILTER — asking "winners, losers or both" in a second place that could disagree with the menu below, and forcing the unanswerable "what does OFF restore?" (the first build had no way out at all). **Additive has neither problem: it never decides which trades are drawn, so the filter has one home and off means off.** On/off is DERIVED from the layers, so unticking one by hand unticks the row. Verified in-browser on run `211384ddbea4`. Detail in `frontend/src/components/ChartPanel/CLAUDE.md` → *Deep debug*. Earlier: 🔴 **the price chart's layers stopped at the shipped candles, so scrolling back far enough emptied the chart while every toggle still read ON.** `chart_spec` ships the newest ~17 months of a long run and the panel pages the rest in on scroll-left, but everything drawn on those bars except the TRADES — structure overlays, fair value gaps, blocked and missed setups — was only ever built over the shipped window. Past that boundary each layer drew nothing and said nothing, which is indistinguishable from the panel forgetting the reader's settings, and is how it was reported. A page now carries its own analysis (`GET /runs/{id}/candles?analysis=true` → `services/chart_spec._page_analysis`) and the panel merges it, deduped by identity, so a layer reaches back exactly as far as the bars do. ⚠ **The engines are streaming, so a page is replayed with 2,000 bars of WARM-UP in front of it** — a cold replay would open every page with no swings and no live gaps, a seam that reads as the same failure one page further back — and a page's internal structure is demoted to Historic, because only the shipped window holds the leg the run actually ended in. ⚠ **The frontend half is the transferable one: a roster DERIVED from data must be RECONCILED, never re-seeded** — `groupsOn` was rebuilt from the overlay groups on every change, which was safe only while that list never changed, and would have switched the reader's layers off mid-scroll the moment a page could rebuild it. Cost ~+2s and ~+230 KB per page; verified in-browser nine months before the shipped window on run `211384ddbea4`, where that region was previously bare candles. 337 backend tests green (7 new); frontend typechecks and builds. **The standing lesson is a NEW one, not the label-on-a-screen one: a per-window computation sitting behind a view the reader can EXTEND is a silent hole — whatever the view can reach, the data has to reach too.** Earlier: 🔴 **two health indicators were reporting something other than what they were named, and the auto-start only ever ran once.** The "SSH" dot ran `ssh forexvps "echo ok"` — a fresh connection with nothing to do with the port forwards — so after a laptop sleep it sat **green** beside two red agent dots and sent you looking at the VPS instead of at the dead tunnel on this laptop; the "MT5 Agent" dot read the Flask agent's `/health`, which answers `ok` whether or not the terminal is running or logged in, so an MT5_Lab that had dropped its broker connection showed green and every python run needing uncached bars failed at fetch time. Both now measure what they say (`ssh_tunnel` = the forwards are bound, plus a new `vps_reachable` carrying the old question; `mt5_connected`/`mt5_server`/`mt5_account` off the agent's `/status`), and both dots gained a yellow state. Alongside them, `main.py`'s one-shot auto-start thread — 8s after boot, never again, which is why the MT5 agent had to be started by hand after every sleep — became **`services/agent_supervisor.py`**, a 60s loop running the SAME pass every time, so "it works on launch" and "it recovers from sleep" cannot diverge. ⚠ **The guard is the feature, not the loop:** no action while the scope it would disturb has a job running, with a **python run counted as MT5 traffic** (the local runner pulls bars through 8766), and one deliberate asymmetry — an unbound port is rebuilt even under a running job (nothing can connect, so the job is already failing and this is its only way back) while a merely stale tunnel is not (an agent driving a heavy backtest legitimately stops answering `/health`). Every `schtasks /run` is **re-probed**, because it reports SUCCESS for a task Windows refuses to launch. ⚠ **The supervisor is disabled under pytest** (`CC_DISABLE_SUPERVISOR=1` in `conftest.py`) — every endpoint test fires the startup hook, so without it a plain `pytest tests/` would rebuild the tunnel and fire two scheduled tasks on the live VPS, the same class of hazard as `test_integration.py`. **Validated against the live system, not only mocks** — the tunnel was killed and rebuilt for real, and the run caught a genuine incident: an NT8 agent that died on a backtest submission while its run row stayed `running`, which is the case the supervisor deliberately will NOT repair (dead and too-loaded-to-answer are indistinguishable from here) and now names in the log instead. Also landed: `services/readiness.py`, a boot-time report of the dependencies that fail SILENTLY (an un-backfilled news calendar makes the News filter inert; missing credentials make Telegram a no-op), served at `GET /system/readiness`; and `start.sh` now reinstalls `node_modules` when `package.json` is **newer**, not only when the folder is missing — a dependency added by someone else's commit never installed after a `git pull`. 330 backend tests green (31 new); frontend typechecks and builds. **The standing lesson is the repo's own, for the third time: a label on a screen is a CLAIM about code somewhere else.** The Run modal collected costs the runner never read; the Optimize modal displayed params the grid builder never read; the SSH dot named a tunnel it never probed. Earlier: 2026-08-01 — **the News & Holiday filter defaults to OFF on both rules**, so a run page opens on the run exactly as traded and every number on it is the backtest's own; turning a rule on is a deliberate what-if. Holidays had defaulted ON and news followed the strategy's `avoid_news`, which made the default differ BETWEEN STRATEGIES. Same day: the *Made* hero became the **dollar figure** with `from $10,000` beneath it (the multiple was the hero while the starting balance appeared nowhere), and `FitMoney` **stopped abbreviating** — `$14.4M` was a measurement bug, not a style choice. Earlier: 🔴 **the lab collected commission and slippage on every Python run and charged neither.** Both fields reached the run row and the page; `services/python_runner.py` read neither, so every Python backtest was frictionless while displaying a cost profile it had not applied — and the 2.25/1 it was displaying came from a FUTURES prop-firm ruleset, meaningless for spot gold. Wired through `_cost_profile` on the single-run AND sweep paths; **defaults are 0/0 everywhere now** (a cost you did not state must never be charged), and that change exposed why the futures figure reached a forex run at all: `RunBacktestModal` resolved its primary ruleset from the **futures list only**, so a forex selection was null and the form shipped its initial state instead of the ruleset's 0/0. At 0/0 nothing is charged and every historical result stays reproducible. Detail: `backend/CLAUDE.md` → *The Python runner's costs*. Same day: **three numbers that were TRUE and were being misread now have the companions they needed.** Recomputing run `f866873aa862`'s every stored KPI from the raw trades matched to the cent — the arithmetic was never the problem. What was: max drawdown listed in **dollars only** on the Runs page ($1.73M beside $14.4M of profit reads as ~12%, against the peak it is **55.9%**), a **win rate** that counts a breakeven scratch as a win (45 of 111 "winners"; honest split 40% won / 27% scratched / 33% lost), and a **profit concentration** measured over quarters — a time question — where the reader hears the trade question (34.5% by quarter on a run where 5 of 165 trades made 47% of the gross). Three new stored metrics, backfilled onto all 79 completed runs, plus two new Performance rows and a percent-led Max DD column. **The lesson: a number reproducing is not a number being understood — ask what a reader will conclude from it.** Earlier the same day: **the price chart got a Step navigator.** `◀ Loss 12/60 ▶` beside Go to date (or ← / → with the pointer over the panel) jumps to the previous / next marker and centres it, paging older history in through the same machinery the date pill drives — so reading a run's losers back to back is two keys instead of a scroll hunt across years of bars. **It walks whatever the Analysis dropdown is SHOWING and has no filters of its own:** untick Winners and ◀ walks the losers, turn Trades off with Blocked on and it walks the refusals, leave both on and it interleaves them by time. That is the decision worth keeping — a second set of filters is a second place for the navigator and the chart to disagree. Detail in `frontend/src/components/ChartPanel/CLAUDE.md` → *Step*. Earlier: 2026-07-31 — **the frontend had two private copies of Sharpe and neither zero-filled flat days, so both were inflated ~3x.** The backend has always counted every weekday in the span with flat days as zero (`services/metrics.py:zero_filled_daily_values`, whose docstring warns about exactly this); `computeFallbacks` and `StackDetail.composeCombined` scored only the days that CLOSED a trade and then annualized by √252 — 142 active days across 1,447 weekdays read **2.96 against a true 0.91**, and a portfolio stack read **13.06**. It surfaced as a news-filter delta of +2.07 from removing 3 of 142 trades: the filtered side fell back to the frontend formula while the unfiltered side used the stored backend one, so the "delta" was two different formulas rather than a change. There is now one frontend definition (`dailySharpe()`), and it reproduces the stored backend value to 15 significant figures — that equality is the regression test. The same sparse-series mistake produced three earlier bugs on this panel; `frontend/CLAUDE.md` → *Metrics that were saying the wrong thing* lists them together. Same day: **the verdict became the FIRST card on the Performance row.** It had been a full-width ribbon above three question cards, which cost a whole row (44px + gap) in both states; as a card sharing the row the section is **180px collapsed / 305px expanded** at 1670, from 234 / 345. The move only worked because the content changed shape with it — the rules were inline pills laid out by wrap, which are fine at full width and five lines at a quarter of it, and `items-stretch` means a tall fourth card drags the other three with it. As rows a rule is 24px whatever it says, and each rule's explanation moved from a `title` attribute nobody discovers to the ⓘ every other row uses. The `verdict unfiltered` badge became a `Graded on → all 142` row, which states the number instead of a label to decode. Stacks and unscored optimizer combos keep the bar on purpose. Detail in `frontend/CLAUDE.md` → *The verdict is a card, not a bar*. Earlier the same day: **the Performance panel's numbers were audited against the raw trades, and three were wrong.** (1) `worst_losing_streak` counts **trades**, not days — `backtest/output.py` walks the trade list, so "4 days" described a run of luck that never happened (2 consecutive losing calendar days did). (2) Time underwater counted `daily_pnl` ROWS, which omit flat days, so "67% of days" was 67% of ACTIVE days — 71% weighted by the calendar. (3) **Profit concentration was weighted in DOLLARS, which on a compounding account measures the compounding, not the clustering it exists to detect**: the last quarter of an 85x run holds nearly all the dollars however evenly the edge is spread, so run `d2ab68f9e884` printed 89% and the page's only warning colour ("edge clustered — overfit risk") against an honest 40%. Fixed on both sides with one rule — normalize by the equity a trade was taken with WHEN the curve carries a real account base, keep dollars when it does not (a unit-size NT8 curve) — plus a `profit_concentration_basis` column and an `init_db` re-stamp of all 78 stored runs. Also fixed: `fmtDate` parsed `'YYYY-MM-DD'` as UTC midnight and rendered a day early, in five separate copies. The panel's rows became **label + ⓘ + number** (every explanation on the tooltip, which deleted both the re-explaining suffixes and the ragged value column) and the whole panel now **collapses to its three heroes**, default ON, so the equity curve shares the fold. Detail in `frontend/CLAUDE.md` → *A row is a label, a ⓘ and a number* and `backend/CLAUDE.md` → *Profit concentration*. Earlier the same day: **the Evaluation + Performance panel was rebuilt, and rebuilding it surfaced a live verdict bug.** The 6+6 KPI grid beside a fixed-height evaluation card became `PerformancePanel`: a verdict ribbon over three question cards (**Made / Risked / Trusted**). That one change removes all three standing complaints — cropped values, uneven cards and an empty evaluation box on `unconstrained` — because each was a consequence of the layout rather than of its sizing. Two rules landed with it: **colour marks the exception, not the sign** (a wall of green ranks nothing; Sharpe 0.91 is positive AND weak, so soft numbers say so in words), and the new `DrawdownMeter` **may never invent its references** — no limit tick unless the ruleset states a peak-% limit, no simulated tail unless a `dd_basis === 'percent'` stress test measured one, otherwise it says the tail is *unknown, not zero*. The bug: **`unconstrained` returned PASS on every run, including one that lost 95% of the account** — `_evaluate_personal`'s two checks are both guarded on limits that row deliberately never sets, so `failures` was empty by construction. It returns `INFO` now, and because verdicts are stored, `init_db` carries a migration for the history. Detail in `backend/CLAUDE.md` → *Nothing checked is not a pass* and `frontend/CLAUDE.md` → *The Performance panel is three questions*. Earlier: 2026-07-30 — **the drawdown defect turned up a THIRD time, now in the KPI grid.** `BacktestDetail`'s Max Drawdown and Calmar both divided by the ruleset's static `account_size`, so on a compounding run they reported a percentage of an account that had ceased to exist: the shipped `mpc_sos_fade` run printed **Max DD 1096.7%** and a red **Calmar 0.11** where the honest figures are **54.9%** and **2.25** — two of the six core cards arguing a good strategy was bad. Both now divide by the running PEAK (`frontend/CLAUDE.md` → *Drawdown is peak-relative*), and the card learned that the deepest DOLLAR drawdown is a different episode from the worst PERCENTAGE one. Standing lesson: a percent of a growing account needs a growing denominator — check every place one is computed. Same day: the price chart's **scroll-left paging now shows itself** (the blank strip you scroll into is shaded from the oldest loaded bar back and labelled `Loading earlier bars…`), and **the stress-test engine's accuracy pass.** A D grade traced to the engine, not the strategy: Monte Carlo was shuffling dollar P&L on a compounding run (trade size drifts 17.7x, so it simulated a strategy that never existed), drawdown was compared in dollars against a fixed limit while the account grew, sensitivity scored on net P&L so a position-SIZE knob dominated the score, walk-forward averaged Sharpes off 6-trade windows, and a ruleset stating no drawdown limit could not grade above **D**. All five fixed generically; a `None` grade is now a first-class outcome and `personal_forex_risk` (55%) was seeded as the forex bar. Detail + the ruin walk-back in `backend/CLAUDE.md` → *How stress tests work* / *Robustness grading*. Earlier: 2026-07-29 — the price chart's fib levels became configurable (add/remove/retune/recolour/hide a level, per drawing or as the tool's persisted default, extensions included); the News & Holiday filter works end to end (calendar backfilled 2021→2026, `entry_ms` reaching the frontend, one Equity chart that follows the filter); 2026-07-28 — the price chart got a Go-to-date jump (type a date, it pages the history in and lands there); 2026-07-27 — missed setups (how close the ones that died came) shipped end to end alongside blocked setups; the price chart now opens on the timeframe the run traded

Local operations platform for LWG Capital. Two-process app: React frontend (`:5173`) → FastAPI backend (`:8000`). The backend is the only process that touches the filesystem or the VPS — the frontend never does.

Sub-directory CLAUDE.md files are auto-loaded when editing files in those directories:
- `backend/CLAUDE.md` — Python conventions, router rules, SQLite patterns, VPS interaction
- `frontend/CLAUDE.md` — hook patterns, component rules, theme tokens, routing

---

## Repo structure

```
command-center/
├── backend/
│   ├── main.py            FastAPI entry point; registers all routers
│   ├── config.py          loads config.json → typed module constants
│   ├── config.json        machine-specific paths only (no credentials)
│   ├── models.py          all Pydantic models in one file
│   ├── routers/           one file per domain
│   ├── services/          business logic, DB access, VPS client
│   ├── data/lab.db        SQLite — strategies, rulesets, runs, evaluations
│   └── reports/lab/       run output files (equity curves, logs)
├── frontend/
│   └── src/
│       ├── api/client.ts  only place fetch() lives
│       ├── types/index.ts mirrors all Pydantic models
│       ├── hooks/         one file per domain
│       ├── components/    shared UI primitives
│       └── pages/         one file (or folder) per route
└── start.sh               starts both processes
```

---

## How to run

```bash
cd command-center
./start.sh
# Frontend: http://localhost:5173
# Backend API docs: http://localhost:8000/docs
```

`start.sh` creates the Python venv and runs `npm install` on first launch.

**SSH tunnel** — `start.sh` opens a persistent `ssh -N forexvps` background process on launch. This keeps two LocalForwards alive: `8765` (NT8 nt8_agent_tunnel) and `8766` (MT5 mt5_agent_tunnel). Without the tunnel, both runner_dispatch and mt5_agent_client calls fail even though SSH itself appears healthy. The tunnel is killed automatically on Ctrl-C. **Important:** the `-L` flags must use `127.0.0.1` (not `localhost`) as the remote target — the VPS resolves `localhost` to `::1` (IPv6) but Flask agents bind only `127.0.0.1` (IPv4). Both `start.sh` and `_restart_tunnel()` in `system.py` use `127.0.0.1` explicitly.

**Agent supervisor** (`backend/services/agent_supervisor.py`, 2026-08-02) — one daemon loop, every 60s, doing the SAME thing on every pass, so a cold start and a wake-from-sleep are literally the same code path. It replaced a ONE-SHOT thread that ran 8s after boot and never again — which is why the MT5 agent had to be started by hand after every laptop sleep. It probes the tunnel by **port binding** and the agents by **HTTP `/health`**, because `ssh -L` binds the local port itself: a bound port with both agents silent is a stale tunnel forwarding into nothing, an unbound port is a dead one, and the pair is what tells them apart. Rebuilds the tunnel, fires `NT8Agent` / `MT5AgentRDP`, and **re-probes after every fire** — `schtasks /run` reports SUCCESS for a task Windows refuses to launch, so the fire is not evidence. ⚠ **The guard is the point, not the loop:** every action is skipped when the scope it would disturb has a job running, and a **python run counts as MT5 traffic** (the local runner pulls its bars through 8766). One deliberate asymmetry — an **unbound** port is rebuilt even under a running job (nothing can connect, so that job is already failing and this is its only route back), while a merely **stale** one is not (an agent driving a heavy backtest stops answering `/health` while working fine — the NT8 agent does exactly this under pywinauto). ⚠ **It is OFF under pytest** (`CC_DISABLE_SUPERVISOR=1`, set in `tests/conftest.py`): every endpoint test builds a `TestClient`, which fires the startup hook, so without the guard a plain `pytest tests/` on a laptop with a dead tunnel would rebuild it and fire two scheduled tasks on the live VPS. ⚠ **It will not rescue an agent whose death left a job marked `running`** — "dead" and "too loaded to answer" are indistinguishable from here and the wrong guess kills a live run — so it names the deadlock in the log instead (`nt8-DOWN-with-a-job-running`); clear the lock (Stop, or restart the backend) and the next pass fixes it. Observed live on 2026-08-02, which is why the message exists.

**Readiness report** (`backend/services/readiness.py`) — the dependencies whose failure mode is SILENCE, checked once at boot and served at `GET /system/readiness`. An un-backfilled news calendar makes the News & Holiday filter **inert** (it tags nothing, which looks identical to a broken filter), and missing `algos/credentials.json` makes every Telegram send a no-op. It REPORTS and never acts — neither is fixable from here, and neither is worth refusing to boot over.

**Backtesting prerequisites** — before submitting a run, the SSH tunnel and NT8 agent must be up. See Sidebar health indicators below.

---

## Key design decisions

**Config translation layer** — Smart Money pipeline stores fractional values (`win_rate: 0.75`), UI shows percentages. `_pipeline_cfg_to_api()` and `_api_cfg_to_pipeline()` in `routers/smart_money.py` handle conversion. The API contract is the stable interface.

**Batched VPS snapshot** — `GET /bots/snapshot` makes two SSH calls and returns one `BotSnapshot`. Frontend polls at 60s. Never SSH per-bot.

**No auto-commit** — `PUT /smart-money/config` writes the file only. The user decides when to commit via `GET /smart-money/config/git-status`.

**There is no bot risk-cap deploy, and there is no risk cap.** `PATCH /bots/{name}/caps` wrote `algos/shared/thresholds.json`, committed, pushed, pulled and restarted the bot — it was deleted 2026-08-04 for having no consumer, and on **2026-08-05 its whole subject went with it**: `thresholds.json`, `bot_state.BOT_THRESHOLDS` and the P&L tracker that read them are all gone (see `algos/CLAUDE.md`). Those numbers were daily-goal / daily-cap / weekly-cap **alert** levels for a job that had carried an empty bot registry since June — so the Bots page was rendering a cap that nothing enforced. **An alert is not a limit.** The live bot's risk lever is `strategy_params.exec_risk_pct` in its instance config, edited through `PATCH /bots/{name}/runtime` (which does not restart it); a real account-level cap has to live in `algos/live/runner.py`, where it can refuse a trade.

**Lab experiment model** — user always specifies which rulesets to evaluate against. The system never auto-evaluates against all rulesets. `evaluate_rulesets` is always set explicitly.

---

## What is built and live

| Module | Status | What it does |
|---|---|---|
| App shell | ✅ Live | Sidebar, topbar, routing across all pages |
| Overview | ✅ Live | Stat row plus Bots and Research summary cards, and the calendar preview. The two Smart Money stat cards and its module card are behind `FEATURES.smartMoney` and hidden; **both grids drop a column with them**, because two cards left in a four-column row reads as data that failed to load. 🔴 **Audited 2026-08-05 — 11 defects, and NOT ONE of them showed an error**: every one rendered a confident, healthy-looking answer, which on the page you open to ask *is anything wrong* is the entire failure. A bot RUNNING with a dead MT5 link read `all bots live` in green; two DISABLED scheduled jobs wore the gold "waiting for next trigger" pill; a missing balance was summed as $0; a dead VPS drew its error over undated stale rows; and the calendar window was memoized on mount, so a dashboard left open past Sunday midnight asked for last week for ever. All fixed and **verified in a real browser (25 checks) against mocked blind / empty / dead-VPS / dead-feed fleets**, with the midnight rollover proved under a faked clock. Full detail: `frontend/CLAUDE.md` → *The Overview was audited 2026-08-05* |
| Smart Money | 🟡 Built, flagged OFF | Full pipeline UI: scan, terminal, rankings, profiles, config, cache — all of it still works. Removed from the nav, the Overview and the router on 2026-08-04 (Aaron's audit: not needed for a while), by `frontend/src/lib/features.ts`. **Nothing was deleted** — the pages, hooks, types, `routers/smart_money.py` and the `smart-money/` pipeline are untouched, so one boolean brings it back. ⚠ The backend endpoints are still SERVED; the flag is a UI decision, not an API one |
| Bots | ✅ Live | Monitor / Configure / Users. **One bot registered — `mpc_sos_fade_demo`, dry run** (this row said "no bots registered yet" until 2026-08-04; it has been live since 2026-07-31). Configure shows the **DEPLOYED VERSION** read off the VPS — hash, commit, date, all 47 params as deployed — with a **Promote** button that previews (stages and really imports the code) before it deploys, and warns on the four states that make a version claim false: not frozen, snapshot edited in place, promote not yet restarted into, settings changed since deploy. **Configure is a bot SELECTOR (left rail) + a detail panel for the one selected bot, with a fleet-wide version strip above it (2026-08-04, closes 2 of G11's 3 bullets).** It was a flat per-bot stack, which is correct for one bot and a misclick surface for several. ⚠ **The property that matters is not layout: only the SELECTED bot's controls exist in the DOM** — measured in a real browser against a mocked 4-bot fleet, **1 promote button, not 4** — so a Promote for a bot you did not pick is not there to be hit. Selection lives in `?bot=`; both confirm dialogs NAME the bot; a `live` account is tinted amber. The **fleet strip** answers *which bots are behind the repo / have a restart pending / are not frozen / have settings drift* in one row, and it shares the per-bot card's cache entry and its single `versionFlags` derivation — two readings of one deployment claim is two answers that can disagree. An unreadable record counts as `unreadable`, never as clean. **Monitor's fleet controls left the per-row visual language in the same pass, closing G11 outright**: a danger-bordered `Fleet controls` card with an `ALL N BOTS` chip, the COUNT on every button (`Stop all 4`), `This bot` as the row column header, and each fleet dialog LISTING the bots by name with live accounts flagged — the old copy named the mechanism and never the subjects. 🔴 **That pass found a real defect: the fleet buttons were gated on the FILTERED list** while the endpoints have never heard of the filter, so `Start all`'s "stop everything first" guard could be defeated by choosing the demo/live tab. Both now count over all bots, and a filter that hides one says so. **`mt5_link` (2026-08-04)** shows a `No MT5 link` chip beside the Running pill when the bot's process has lost its terminal: a blank balance was previously the only symptom of a bot that had been blind for 50 minutes. Also: risk-cap deploy and Telegram user management |
| News Calendar | ✅ Live | tab (`/calendar`) — live Forex-Factory-style economic calendar off the free TradingView feed (`engines/news/` `TradingViewSource`, read-only, not the shared cache). Day strip, server-clock "now" line + countdown, actual/forecast/previous w/ beat-miss colour, currency/impact/category filters. Separate path from the backtest news/holiday filter. 🔴 **Audited 2026-08-05 — 10 defects, and not one of them rendered an error.** The beat/miss polarity list had been written against **Forex Factory's** naming while the tab reads **TradingView**, so six of its eleven keys matched zero of 811 real titles and **`Core PCE Price Index MoM` (HIGH, USD) was coloured the OPPOSITE way to CPI on the same screen** — proven fixed through the running backend on 18 real PCE prints. On the page: the week was frozen at mount (stale past midnight), paging showed the previous week under the new week's header, a failed poll deleted a good week, `?day=abc` read as an empty week, and duplicate React keys that occur in real feed data. **The guard is the fix worth carrying: a polarity key matching nothing now fails the build.** Full detail: `backend/CLAUDE.md` → *The calendar's polarity list was written for the wrong provider* and `frontend/CLAUDE.md` → *The Calendar page was audited 2026-08-05* |
| Rulesets | ✅ Live | Own top-level page: firm-grouped tables, contract scaling column, editable personal rules (server-side lock on prop rows) |
| Backtests lab | ✅ Live | Runs and Sweeps tabs; BacktestDetail with collapsible params side panel |
| Optimizations | ✅ Live | Own top-level page (`/optimizations`); native NT8/MT5/Python optimizer; ranked results; "Tune winner" |
| Tuning workbench | ✅ Live | `/backtests/runs/:id/tune` — edit a winner's params, run iterations, leaderboard + deltas + regime-aware equity overlay |
| Per-platform job lock | ✅ Live | One job per platform (NT8/MT5/Python), platforms independent; DB is the single lock source (`has_running_job`) |
| Worthiness badges | ✅ Live | Tier 1/2/3 worthiness badge auto-assigned on every completed run |
| Stress Tests | ✅ Live | Monte Carlo, walk-forward, sensitivity, A–F grade with Telegram notification. MC picks its own shuffle series (dollars, or per-trade returns compounded when a run's trade size drifts) and reports drawdown in the matching unit; the grade is `None`, not D, when the ruleset states no drawdown limit. 🔴 **Audited end to end 2026-08-05, and the frame was one query: the `stress_tests` table held ONE row, written three days BEFORE the accuracy pass that rewrote the engine** — so nothing here had been exercised against the code it actually runs on, and re-grading that row live moved it from a confident **D** to **ungraded**. The load-bearing fix is that **a walk-forward or sensitivity child was launched WITHOUT the baseline's `cost_layers` / `broker_profile` / `sizing_mode`**, so stress-testing a charged run measured every child on a free book and sensitivity reported the cost gap as the parameter's fragility — the identical defect the Tuning workbench had the same day, in a second page, and **the third launcher in this app found carrying the window but not the physics.** Proven on real data: all 50+ children of a charged run now carry `["spread","swap"] / vantage_demo / consistent`. On the page, the drawdown was shown in DOLLARS under a grade decided in PERCENT, a sensitivity shift that IMPROVED the result was drawn as a red loss bar, a failed shift was drawn as a flat zero ("tested, no effect"), and the equity fan drew a drawdown limit on a cumulative-P&L axis. **Cancel and delete both work now** — delete removes the test's own directory and every child's (216 → 192 dirs, measured), where before it deleted the rows and left the files; the pre-existing backlog of **109 orphaned directories (7.9 MB)** was cleared in the same pass, so `reports/lab` is 82 dirs with a clean dirs-vs-rows diff. **Sensitivity also runs its shifts ACROSS THE CORES now** — it replayed 60 full-history backtests one at a time on a 12-core box (69s each, measured) while the optimizer had been using every core all along; **46 shifts went 53 min → 14 min (3.8x), and the same shift through the pool and down the single-run path gives 161 trades / PF 3.268 / $12,184,685.53, identical to the cent.** ⚠ **The tempting fix would have looked like it worked and bought nothing** — those backtests run on threads and an engine replay is pure-Python bar-by-bar, i.e. GIL-bound; it needs processes. ⚠ **Python runner only** (NT8/MT5 each drive one terminal), which is also this feature's stated focus — the native walk-forward path, MT5/NT8 and the pool's peak memory are unit-tested only and are NOT to be read as measured. Full detail: `backend/CLAUDE.md` → *Stress tests — the 2026-08-05 audit* and `frontend/CLAUDE.md` → *The Stress Tests page* |
| Portfolio stacks | ✅ Live | Stacks tab on Backtests + `StackDetail`. Layer 2+ Python strategies over one shared instrument/timeframe/costs/window and read the COMBINED portfolio: the page renders like a single backtest (same Performance panel, Equity/Price/Breakdown charts, full price chart) against a client-side union of the enabled legs' trades on one account. **It is the SAME page as a run since 2026-08-10** (Aaron: *"I don't want stack stuff here and a regular backtest details page to look two different"*) — one collapsible Performance section of four cards, Verdict / Made / Risked / Trusted, off `PerformancePanel`'s `verdict` slot and the same `usePerfCollapsed` key a run reads. **The strategy legs are the Verdict card's ROWS and each row is its own toggle**, so the control deciding what every KPI counts sits in the row with the KPIs; the old ribbon-plus-chip-strip put it two scrolls away. Full detail: `frontend/CLAUDE.md` → *A stack renders a RUN's panel*. **Smart reuse** — a leg whose exact settings already have a completed standalone run is reused instead of re-run (STRICT match); ownership ≠ membership, so a reused run stays in Runs and survives the stack's deletion. Cost defaults are 0/0 to match the Pine strategies. Per-strategy toggles drive every number and chart; the price chart names the strategy in each trade's outcome chip and has its own Strategies dropdown to isolate a leg. A stack can be started from EITHER the Stacks tab or the Strategies page (tick 2+ python rows → Stack N strategies), both opening the same config modal. **The stack opens on the legs' SHARED balance, not the sum** (2026-07-25 fix — two $10k legs were starting the portfolio at $20k and halving every balance-relative KPI). 🔴 **A NEW STACK IS ALWAYS A SHARED ACCOUNT SINCE 2026-08-10 — the mode picker is GONE** (Aaron: *"I would never ever ever wanna do a screen… that's what a stack IS — we're sharing the same resource"*). ⚠ **Not a removal:** the backend still understands both, three stored stacks are screens, `StackDetail` renders them with their `Screen · upper bound` chip, and a RERUN carries its own mode forward through `initial.mode` — what is gone is the way to make a NEW one, because deleting the mode would rewrite what those stored rows mean. **Screen** (the old default) adds up N standalone runs — each leg sized as if it owned the whole account, so no leg can ever block another and it OVERSTATES the result; the chip says *upper bound* out loud. **Shared account** replays the legs together on ONE balance with ONE risk budget they compete for, via `backtest/portfolio/run_stack` — `services/portfolio_runner.py` CALLS it and owns no account model of its own. It reuses nothing (a finished standalone run was measured un-contended), always takes the python lock (`1 + legs` full replays: the shared book plus one SOLO CONTROL each), and serves `GET /stacks/{id}/contention`. **Its panel is the ONE section a run has no counterpart for, which is exactly why it survives as its own section — and it was cut to THREE LINES on 2026-08-10** (Aaron: *"one account, the screen promised, peak open risk — I don't know the significance of these things"*, then *"does this section need to be so verbose? Like I'm reading a storybook"*): peak risk + meter, a refused/not-refused chip, the cap/concurrency caption, and `together $X · apart $Y · +$Z` sharing a line with the disclosure. ⚠ **Every RULE the old paragraphs stated is kept on an ⓘ, and both browser checks HOVER rather than reading the chip** — an empty contention log must still read as a MEASUREMENT, and the together-vs-apart gap must still name COMPOUNDING, since read as risk it is alarming and `docs/SHARED_RISK_STACK.md` got the SIGN wrong from exactly that misreading. The seven-column per-strategy table is behind a disclosure that **opens itself when there is contention** — five of its columns are em-dashes on every run measured so far. ⚠ **An empty contention log is a MEASUREMENT, not a missing one** — open risk is measured to each trade's CURRENT stop, so a stop moved to breakeven releases its room before the other leg asks. 🔴 **And `docs/SHARED_RISK_STACK.md`'s own verification criterion was WRONG: it said a shared run must close LOWER than the screen, and the measured run closes $36,806 against $26,870 with the gate working and nothing refused** — a screen gives each leg a private balance while a shared account COMPOUNDS both onto one. **Compare R, never net dollars.** Full detail: `backend/CLAUDE.md` → *Shared-account stacks* |
| Regime tagging (M4) | ✅ Live | Every trading day in a run's window classified once into `regime_timeline.json` (regime is a property of the market on a date, not of a run); daily PnL tagged from that same map; regime overlays and filters |
| Blocked setups | ✅ Live (Python) | **The trades that never happened.** A signal the strategy had READY that one of its OWN rules refused places no order, so it is in no trade list, no equity curve and no broker report — this is the only place it is countable, and without it there is no way to judge whether a blocking rule protects the account or costs it. Port of `mpc_strategy.pine`'s pink TRADE BLOCKED tag: the strategy records each refusal (reporting-only, parity-safe) → `backtest/output.py` → `blocked_setups.json` in the run dir → the price chart's **Analysis → Blocked** layer (default OFF), where each draws a dashed line at the exact would-be entry price with every refusing rule on hover, filterable by reason. Python runner only — NT8/MT5 cannot report them, and the layer correctly does not appear for them. Full path: `backend/CLAUDE.md` → *Blocked setups* |
| Missed setups | ✅ Live (Python) | **How close the setups that DIED came.** The companion of Blocked, one step earlier in a setup's life: a block is a trade the strategy had fully READY and a rule refused; a miss met some of the strategy's confluences and then died without ever becoming a trade. Port of `mpc_strategy.pine`'s orange 2-of-3 callout: same path as the blocks (strategy → `backtest/output.py` → `missed_setups.json` → the price chart's **Analysis → Missed** layer, default OFF), with the SCORE on the tag (`2/3` / `3/3`) and hover showing what it had vs the one thing it didn't. The routine reasons start unticked, driven by a `missNoise` list the backend DERIVES from the strategy's own near-miss flag — so the layer opens on the misses worth studying without the chart learning what any of them mean. Full path: `backend/CLAUDE.md` → *Missed setups* |
| Fibs on the chart | ✅ Live (Python) | **The fib LEG each trade was actually priced off.** Aaron's brother asked to see, on every plotted trade, the fib run on the points that trade used — which retracement levels it went into. The STRATEGY records the ladder when it places the order (`mpc_sos_fade` `TradeFib` → `backtest/output.py` → `services/chart_spec._trade_fib`), so each level arrives as an explicit `(ratio, price)` pair and **nothing downstream does any fib maths** — a fib rebuilt from anchors would be a second claim about one leg, and this repo has met that failure four times. The chart derives only the two readings a price ladder cannot state: `entryRatio` (where the fill landed on the leg — on the A+ bot it reproduces the entry model, reading exactly 0.618/0.702/0.786 where `_fib_snap` snapped and between rungs on a gap-edge entry) and `deepestRatio` (how far the retracement ran afterwards, **not clamped at 1.0** — a stop-out really did trade past the origin). A sub-toggle of **Analysis → Trades**, default OFF, listed only when trades carry one. ⚠ **No backfill** — it would mean replaying the strategy, so older runs correctly show no switch; NT8/MT5 and `mpc_bleg` record none by construction. Full path: `backend/CLAUDE.md` → *Trade fibs* |
| Fair value gaps on the chart | ✅ Live (Python) | **The gaps that were live when something happened.** The canonical `engines/fair_value_gaps/` engine is replayed server-side (`services/fvg_overlays.py`) and a gap is drawn ONLY if it was in the live list on the bar of a trade ENTRY, a blocked setup or a missed setup — all of them when several overlap. Everything else is dropped, because a 33k-bar run leaves thousands of gaps and a chart carrying all of them answers a question nobody asked. One toggle in the price chart's **Analysis** dropdown, default OFF. ⚠ The gaps are **`mpc_assistant.pine`'s** (cap 8, no close-check, the timeframe-split 0.0/0.04 floor, EQ-exempt cap) — deliberately NOT the stricter set `mpc_sos_fade` pins, so a drawn gap is one the INDICATOR shows and not always one the bot's entry rule counted. Full path + the fork: `backend/CLAUDE.md` → *Fair value gaps* |
| Order blocks on the chart | ✅ Live (Python) | **The supply/demand zones that were live when something happened.** Aaron's brother asked to see order blocks on the backtest chart. The canonical `engines/order_blocks/` engine is replayed server-side (`services/ob_overlays.py`) under the SAME anchor rule as the gaps — a block is drawn only if it was live on the bar of a trade ENTRY, a blocked setup or a missed setup. Measured on run `432aff31f374`: **2,567 blocks created over the window, 579 drawn**, beside the gap layer's 661, so the two together sit at a readable ~1,240 boxes instead of ~3,200. One toggle in the price chart's **Analysis** dropdown, default OFF, and **deliberately NOT in Deep debug** (Aaron's call). It cost the panel **no new template, no new effect and no new concept** — it is a plain `box` group and a second entry in `ANALYSIS_GROUPS`, which is what that list was built for. ⚠ **The BOX is a fixed 30-bar STUB from the anchor candle, not a box tracking the live bar** — the one place this differs from the gaps, and the reason a block's box can legitimately end long before the block dies, or after the bar it died on. ⚠ **No settings fork to warn about here**, unlike the gaps: the strategy files dropped order blocks entirely in 2026-07, so `mpc_assistant.pine` is the only source — which also means **a drawn block never explains an entry**, because the bot reads none. ⚠ **The emitter is unit-tested but there is no box-vs-Pine-array cross-check**, because all three OB exports on disk predate the 2026-07-31 re-port and `compare_ob.py` refuses them; the ENGINE's parity is proven separately on real exports. Full path: `backend/CLAUDE.md` → *Order blocks* |
| Candlestick reversals on the chart | ✅ Live (Python) | **One candle repainted navy per setup: the pattern candle that turned price.** Aaron's ask (2026-08-08) — he wants to read whether candlestick patterns line up with his reversals before deciding whether to add them as a confluence. The canonical `engines/candlesticks/` engine is replayed server-side (`services/candle_overlays.py`) at **`CHART_PRESET`** (his brother's TradingView inputs, not the engine defaults), and a candle is painted only at an ANCHOR. ⚠ **The anchor set is TRADES, win or lose, and 3/3 MISSES — nothing else** (`chart_spec.reversal_anchors`). 🔴 **Blocked setups were anchors for one afternoon and it made the layer look random**: 324 of them against 159 trades, painting navy candles under a Blocked layer that defaults OFF, so marks appeared where the reader could see no setup — **518 anchors / 424 marks → 194 / 153, measured**. ⚠ **A setup's zone is the DRAWDOWN, and it is a region in PRICE, bounded in time only by the trade itself** — a bar is drawn while ANY part of it trades on the adverse side of the entry, anywhere between the entry bar and the EXIT bar, plus two bars at the extreme so a three-bar pattern can complete. 🔴 **Reasoning about it in BARS instead produced four separate complaints**: 23% of marks landed where the trade was winning (a confirm tail measured off the end of the drawdown, and a span that opened at the entry bar so a trade running into profit first painted the whole excursion), a stopped-out LOSER was marked after it closed (its adverse extreme IS its final bar), and the contiguous-excursion fix then MISSED price coming back to the entry later in the hold. **1090 → 853 marks, zero past any exit, 31 outside the band and all at the extreme.** ⚠ **A candle the reader calls a bearish engulfing may simply not be one**: `candle_sticks.pine` needs `open >= close[1]` and a 117-bar uptrend, and on a gapless feed that is decided at the cent — six reported candles miss by $0.01–$0.06 or fail the trend gate, and his TradingView chart does not draw them either. **Never add a tolerance to close that gap** — SWEPT 2026-08-09 against his own export flags and it is strictly worse: engulfing miss/invent goes **243 / 190 at epsilon 0** to 143 / 664 at two cents, because on a gapless feed the open sits within a cent or two of the prior close as the ordinary case, so slack floods the rule. 🔴 **The marks and the indicator also run on DIFFERENT TAPES and nothing here can fix it**: the chart draws the run's MT5 cache bars, the indicator runs on TradingView's, they sit 6 cents apart in the median close with larger intra-bar variation, and **15.9% of every mark disagrees** (600 missed / 515 extra over 20,053 bars). **A constant offset would flip nothing — every clause compares two prices — so it is the variation that bites.** Accepted, no code change; the chart must draw the bars the strategy traded. ⚠ **Direction is a PREFERENCE, never a filter** — aligned, then neutral, then opposing — because *"it will show me why I was wrong"*; ranking on nearness alone is what put a Bearish Engulfing on a long that won. **89 aligned / 49 neutral / 15 against / 41 with no pattern at the turn**, measured. ⚠ **The pattern NAME is a Chart setting, default OFF** — the tags carry no cross-overlay de-collision, so they write over neighbouring candles. ⚠ **The layer is withdrawn off the base timeframe**, row and drawing both: an M15 hammer is not an H1 hammer. Full path: `backend/CLAUDE.md` → *Candlestick reversals* |
| News & Holiday filter | ✅ Live (NT8 + Python) | Post-run view layer on BacktestDetail — a pill on the **Performance** header that removes trades in a high-impact news window and/or on a bank holiday, and **reshapes the page's REAL 12 KPIs plus the Equity chart** rather than showing a second copy of them (the duplicate tiles and the filter's own section were both removed 2026-07-30). Two rules, both switchable, **both default OFF** (2026-08-01) — the page opens on the run exactly as traded and a rule is a deliberate what-if; each card's caption becomes its delta vs unfiltered. Composes `engines/news/`. What deliberately does NOT follow it — per-firm sized runs, the firm verdict, `platform_sharpe` — is in `frontend/CLAUDE.md` → *The News & Holiday filter*. **Inert until the calendar months are backfilled** — the cache is git-ignored, so backfill per machine. Forex/MT5 pending (TODO #3 — non-UTC broker clock) |
| Strategy deployment | ✅ Live | Upload, delete, compile, and one-click Deploy NT8/MT5 strategy files from the UI. 🔴 **Audited 2026-08-06 — the reported symptom was a toast every couple of seconds with the NT8 agent down, and the real defect was that a dead agent 502'd `sync-status`, so every row lost its status and a strategy needing deploy offered a Run button.** Both file endpoints return an envelope naming which agent failed; `needs_deploy`/`needs_compile` survive an unreachable box (local hash vs this app's own deploy record) while `file_exists_on_vps`/`in_sync`/`is_compiled` go `None`. Also: **Missing on VPS** is finally rendered (the backend had always returned it), the version chip names the **compiled** version rather than the deployed one, the compile modal can be closed while a job runs, and Reconcile reads `is_orphan` off the row instead of the last scan's result. Full detail: `backend/CLAUDE.md` → *The Strategies page — the 2026-08-06 audit* and `frontend/CLAUDE.md` → *The Strategies page — audited 2026-08-06* |
| MT5 runner | ✅ Live | MT5 agent on VPS drives Strategy Tester; backtests, optimizer, walk-forward, badges |
| Python runner | ✅ Live | `services/python_runner.py` runs `strategies/python/` packages LOCALLY via the top-level `backtest/` package (no VPS, no compile). Backtests + native optimizer (A4 `backtest/optimizer.py` sweep across cores). Third independent lock scope (`python`), end-to-end: `get_running_job()` returns a `python` bucket and the frontend resolves scope/market/labels through `lib/runner.ts` (**2026-07-16** — replaced the `runner === 'mt5' ? … : NT8` branching that made python jobs wear the NT8 badge and check the NT8 lock). Price charts come from the same `backtest/` bar cache the run replayed |
| Costs on a finished run | ✅ Live (Python) | **A Costs pill on BacktestDetail's Performance header that charges spread / commission / swap onto a run that already happened**, reshaping the real KPIs and the Equity chart with no re-run. ⚠ **The TRADE COUNT does not move and that is correct** — these costs change what a trade was worth, never whether it happened; only `bid_ask_fills` moves the trade list, and it is refused here. Verified on run `432aff31f374`: 73 trades either way, while net $573,812 → $485,984, win rate 65.8% → 60.3% (four marginal winners flip to losers), profit factor 4.04 → 3.83, and **worst drawdown gets WORSE, 57.2% → 60.1%** — a cost deepens every losing stretch as well as shaving the top off, so profit and risk move in opposite directions and both are right. The objection to this was that a cost changes what the trades would have been, so it cannot be arithmetic — **and the objection is wrong for a reason worth keeping: each re-priceable cost is, in R, INDEPENDENT of position size** (a spread over a stop distance), so the R is knowable even though a charged run compounds into different sizes. Proven against real replays (`backtest/reprice.py`); on the live 161-trade run `75ccc776d10c` it reproduces a real charged replay to **37¢ on $16.3M**. ⚠ **`bid_ask_fills` and `slippage` are REFUSED and named** — the first changes which setups fill (161 → 159), the second depends on which exits were market orders; the pill says they need a re-run rather than quietly showing the rest. ⚠ **Swap is ~0.3% and old runs ~0.02%, both captioned** — never presented as exact. ⚠ **Costs compose BEFORE the news filter** (a cost is a property of a trade), and both are refused under a firm's sizing, where the curve is path-dependent. 🔴 **AUDITED END TO END 2026-08-03 (Aaron: this feature has to be perfect) — the arithmetic was sound and FOUR defects sat in the seams around it.** Proven per layer against real charged replays over 155,453 bars: **spread `0.0000R`, commission `0.0000R`, swap `−0.0376R`** (one trade held 2022-12-28 → 2023-01-03, the documented New Year supersede), and **all 81 stored python runs re-price through the live endpoint without error.** The four: (1) **`-Math.abs(cost_usd)` booked every swap CREDIT as a charge** — a short's gold swap really pays, and 39 of 161 trades are net credits — so `Fees charged` read **$415,990 vs the pill's true $332,371 (25% high; 103% high on swap alone)**, two numbers under one label six inches apart; (2) **a layer the RUN already charged could be charged AGAIN** (the endpoint never read `cost_layers`) — latent while every stored run is NULL/`[]`, live the first time anyone launches a priced run, now refused per-layer and rendered ticked-and-LOCKED; (3) **the pill stayed live under a firm's SIZED numbers**, reading `Charging 12.08R` over a page that correctly ignored it — it takes the news pill's `blocked` prop now; (4) **a server REFUSAL rendered as "Charging nothing"** with the reader's boxes still ticked, turning `reprice.py`'s deliberate refuse-don't-guess discipline into a silent lie. Also: **swap — the BIGGEST layer at 6.41R of 12.08R — had no real-replay test at all** (the suite covered only the two exact ones), the broker whose spread was charged was never named on screen, and the "~0.3%" caption never said 0.3% *of what*. ⚠ **The transferable part is WHERE these were: not one was in the cost maths.** Every one sat in a seam — a sign convention crossing a boundary, a guard applied to one control and not its neighbour, an error path nobody rendered. The arithmetic had tests and was right; the seams had none and were wrong four times. Verified in a real browser. Earlier the same day: 🔴 **The pill named only TWO of its three numbers and therefore read as a lying page** — headline `Charging $332,371` (the FEES) beside a Net hero falling **$18,200,741** (the BALANCE IMPACT), with the footer captioning the fees "after compounding", which is the one description that does not fit them. Both figures were right; the page just never showed the subtraction, so the honest reading from the screen was that the feature was broken — and it was reported that way. The headline is now **R** (the only unit that cannot contradict the page or the rows above it) and the popover names all three. Full rules: `frontend/CLAUDE.md` → *Costs are switchable in TWO places* |
| Trading costs | ✅ Live (Python) | **The costs the lab KNOWS, charged in layers you tick.** Aaron's framing — *"you know what the spread is based upon your broker… the only thing we don't know is slippage"* — and bar mode was charging neither the spread nor the overnight swap. The Run modal now carries five independent switches (spread · swap · commission · slippage · model bid/ask fills), **ALL OFF by default** so a bare run stays frictionless and therefore comparable to the TradingView Strategy Tester, stored per run as `cost_layers` + `broker_profile`. **The measured facts are SERVED, never typed** — `GET /backtests/broker-profiles` hands the modal the same `backtest.fills.PROFILES` the runner bills from. ⚠ **Spread and bid/ask fills are alternatives, not layers** (both bill one spread twice), and they deliberately disagree: a flat charge assumes market orders, while every order this strategy places names a PRICE, so the spread lands as fill TIMING and almost entirely on SHORTS. ⚠ **Slippage keeps its own switch and its own typed number** — it is the one cost history cannot measure. ⚠ **`cost_layers = NULL` is a pre-switch run, not a free one.** ⚠ **Sweeps and stacks do not write the columns yet**, so they stay frictionless. Full path: `backend/CLAUDE.md` → *Layered costs* and `frontend/CLAUDE.md` → *Costs are chosen BEFORE the run* |
| History floors | ✅ Live | Backtest windows are refused (400) before the broker's real history for that timeframe. MT5 silently substitutes COARSER bars when it has none, which would produce a plausible but fictional run. The floor is MEASURED off the live terminal (bar-density probe, cached per broker) via the canonical `backtest/data/history.py` — swap brokers and it re-measures rather than inheriting. Enforced at run/retry/sweep/optimization/stack and in `BarSource.load`; the date picker's minimum reads `GET /backtests/history-limit`. Python runner only (NT8/MT5 use their own terminals' history) |
| Settings | ✅ Live | Strategy detail UX, descriptions, best-grade column, runner badges, market filter |
| Sidebar health | ✅ Live | Four live dots: API, SSH tunnel (3-state), NT8 agent (3-state), MT5 agent (3-state — agent **and** terminal) |
| Agent supervisor | ✅ Live | 60s loop keeping the tunnel and both agents up, guarded on the per-platform job lock. Replaces the one-shot startup thread; same code path on a cold start and after a laptop sleep. 🔴 **It could not recover a WEDGED agent until 2026-08-06, and that left the NT8 agent dead for two days**: an agent process can be alive with its socket dead, and Windows then refuses to launch a second instance of its task (`0x800710E0`), so every fire — the loop's and the sidebar Start button's — was a guaranteed no-op logged once a minute as an action. It kills the corpse and re-fires once now, with the **two-clause `wmic` match** (`name='python.exe'` **and** the agent's own script), only after the busy-scope guard has cleared. ⚠ **"Nothing to kill" is a different diagnosis and is logged as one** — the task genuinely never started, which a second fire would not fix either |
| Deep debug toggle | ✅ Live | One row at the top of the price chart's **Analysis** menu, on or off. It switches the CONTEXT behind whatever trades are on screen — **Fibs, External Structure, Fair Value Gaps** — which is three switches across two dropdowns otherwise, set and unset constantly while reading a run one trade at a time. **It is purely ADDITIVE and never touches WHICH trades are drawn:** Winners / Losers / Blocked / Missed stay where the reader put them, so "winners, losers or both" has one home and Step re-scopes off those same rows. On/off is DERIVED from the layers, so unticking one by hand unticks the row — it can never claim a view the chart isn't showing. Hidden on a run with nothing to deepen (NT8/MT5, or a Python run predating the fib field). Full rules: `frontend/src/components/ChartPanel/CLAUDE.md` → *Deep debug* |
| Affirmation ribbon | ✅ Live | **Six affirmations rotating in the top bar, one every 20 seconds** (Aaron's wording, `components/TopBar.tsx` → `AFFIRMATIONS`). The Refresh button moved to the sidebar footer to free the width. **The motion is all in the ARRIVAL and then it holds dead still** — the brief is that these register subconsciously, and a looping shimmer or pulsing glow stops being seen within minutes while competing with the numbers underneath, so words fade up 75ms apart (a line assembling at the pace of a voice) and nothing moves for the rest of its turn. ⚠ **It cannot use `-webkit-background-clip: text` even though the wordmark beside it does** — the clip stops working on an element that also carries a `transform`, and this one moves on every change; the failure is a solid gradient BAR with the letters invisible inside it. ⚠ **The entrance needs a timer beside its rAF**: `requestAnimationFrame` does not fire in a background tab while the cycle's own timers keep running, so the ribbon would park invisible until the tab was looked at again. Full rules: `frontend/CLAUDE.md` → *The affirmation ribbon, and why it holds still* |

---

## Sidebar health indicators

Four dots in the left sidebar, sourced from `GET /system/health` (30 s TTL cache).

| Indicator | What it checks | Green | Yellow | Red |
|---|---|---|---|---|
| **API** | Local FastAPI on `:8000` | Backend healthy | — | Unreachable — restart backend |
| **SSH** | The two LocalForwards (8765/8766) are **bound** | Tunnel up | Tunnel down, VPS reachable — the supervisor is rebuilding it | VPS unreachable — check ForexVPS or `~/.ssh/config` |
| **NT8** | Agent HTTP + NT8 running + Strategy Analyzer open | All three up | Agent up, NT8 not running or SA closed | Agent down — click to start (`NT8Agent` schtask) |
| **MT5 Agent** | `mt5_agent.py` HTTP on `:8766` **+ the terminal's own `/status`** | Agent up and MT5_Lab connected | Agent up, terminal NOT connected to the broker (open it via RDP) | Down — click to start (`MT5AgentRDP` schtask) |

NT8 and NinjaTrader were merged into one dot. Red = agent down (clickable); yellow = agent up but NT8 not running or Strategy Analyzer not open (needs RDP intervention).

⚠ **`nt8_running` and `nt8_sa_visible` are `Optional[bool]` and every check is `=== false`, never falsy (2026-08-06).** Both come off the NT8 agent's own `/nt-health`, so with the agent down the honest answer is `None` — initialising them to `False` claimed NinjaTrader was not running, which is a measurement nothing took, and it was exactly wrong on 2026-08-06: the agent was wedged and NT8 was open on the VPS. Same three-state contract as `mt5_connected`. ⚠ **The Start button on the red NT8 dot fires the `NT8Agent` schtask, which Windows REFUSES while a wedged process still holds the task** — clicking it against a wedged agent does nothing and always did; recovery is the supervisor's kill-then-refire, within a minute.

🔴 **The SSH dot did not check the tunnel until 2026-08-02, and the MT5 dot did not check the terminal.** `_check_ssh` ran `ssh forexvps "echo ok"` — a BRAND NEW connection with nothing to do with the port forwards — so after a laptop sleep the dot sat **green** beside two red agent dots, sending you to the VPS when the problem was on this laptop. `ssh_tunnel` now measures the forwards and a new `vps_reachable` carries the old question, which is what separates a dead tunnel from a dead network. Separately, `/health` on the MT5 agent answers `ok` whether or not MT5 is running or logged in, so an MT5_Lab that had dropped its broker connection showed green and every python run needing uncached bars failed at fetch time; the dot now reads `mt5_connected` off the agent's `/status`, with the account and server on the tooltip. **`mt5_connected` is `Optional[bool]` and `None` means the agent could not be asked — never render an unanswered question as a disconnected terminal.** The standing lesson is the repo's own: a label on a screen is a CLAIM about code somewhere else, and both of these were claims nothing was checking.

**Stuck progress lock** — if a run dies mid-flight (backend restart, network drop), `data/lab_progress.json` can be left with `status: running`, blocking new runs with a 409. Fix: hit the Stop button, or restart the backend (startup hook resets stale locks automatically). **Stuck stress tests** are handled separately: `reset_stale_stress_tests()` in `main.py` startup marks any `running%` stress tests and their child runs as failed on every boot. ⚠ **A stale lock now also freezes the supervisor's repair of that platform** — it refuses to restart an agent whose scope holds a running job, so an agent that died mid-submission stays down until the lock clears. The log names it (`nt8-DOWN-with-a-job-running`) rather than retrying silently.

---

## Never do

- **Reach for `test_integration.py` only when a LIVE check was actually asked for** — it submits real VPS backtests and deliberately kills a running agent. ✅ **It can no longer be run by accident (2026-08-05): `pytest.ini` carries `-m "not integration"`, so a bare `pytest tests/` DESELECTS it** (measured: 432 collected, 5 deselected), and the live suite runs only when someone asks — `pytest tests/test_integration.py -m integration`, whose command-line `-m` replaces the default. ⚠ **The old rule here was "always remember `--ignore=tests/test_integration.py`", and that is exactly the shape of rule this repo should not be writing** — the module's own docstring had said to select it explicitly since the day it was written, nothing enforced it, and the safety of a live trading box rested on a person remembering a flag. **An interlock you have to remember is not an interlock.** 🔴 **Its Case 6 ran `taskkill /f /im python.exe` on the VPS until the same day**, which kills every python process there — both backtest agents, the Telegram bot, and the LIVE TRADING BOT. There was no live bot when the test was written and there has been one since 2026-07-31; that blanket kill is what left the bot dead for three days in July. It is now a `wmic` match scoped on **both** `name='python.exe'` and the agent's own script name, the same two-clause rule `routers/bots.py::_kill_bot` documents. Recovery if an old checkout still does it: `ssh forexvps "schtasks /run /tn MT5AgentRDP"` (and `NT8Agent`), then REBUILD the SSH tunnel — the old `ssh -N -L` survives holding the ports while forwarding to a dead agent.
- **Let a unit test reach the live VPS.** ✅ **It can no longer, on either channel (2026-08-05):** `tests/conftest.py::_no_live_vps` is autouse and refuses both the agent HTTP calls and any `subprocess` that shells out to the box — an `ssh`/`scp`/`sftp` program, or any argv carrying the SSH alias, which is what catches `restart_tunnel`'s `pkill -f "ssh -N.*forexvps"` **killing the developer's own tunnel from inside a test**. A test that needs one stubs the named function (`bots._ssh`, `sup.vps_reachable`) and its patch wins. ⚠ **It raises a `BaseException`, deliberately** — every probe on this path catches `Exception` and reports the failure as "the box is down", so an `AssertionError` guard is swallowed by the exact code it polices; **measured: with one stub removed, the catchable version left 11 of 12 tests passing while every one opened a real ssh connection.** ⚠ Do not "tidy" `LiveVpsCall` into an `Exception` — a test pins that it is not one, because that edit disarms the whole suite silently
- Touch `algos/` or `smart-money/` source code — read their output files only
- Commit secrets (`.env`, credentials, tokens)
- Add a frontend route without a corresponding `NavItem` in `Sidebar.tsx` — and the reverse: never hide a nav row while leaving its route mounted. A page with no way to reach it is still reachable by URL, which is not "removed". A feature flag (`frontend/src/lib/features.ts`) must switch BOTH, in one commit
- Put a Telegram token (or any credential) in a source file. Since 2026-07-30 both sides resolve them at runtime — env var, else the git-ignored `algos/credentials.json` — through `services/notify.py` here and `algos/shared/credentials.py` there. `routers/bots.py` delegates to `services/notify.py` and must never grow its own sender again: a private copy of the token in that router is exactly how the old one ended up committed in six places. The former "keep the two constants in sync" rule is retired — there are no constants left
- Send a Telegram message from this app without a `kind`, or with `notify.TRADE`. Since 2026-08-05 a message declares what it IS and the kind picks the chat: trades in one room, everything about the machinery in another. **This backend can never legitimately send a TRADE** — only the bot on the VPS knows a fill happened — so every send here is `notify.HEALTH`, and `tests/test_notification_routing.py` refuses both mistakes by test rather than by memory
- SSH synchronously from a request handler that could take > 2s — background it
