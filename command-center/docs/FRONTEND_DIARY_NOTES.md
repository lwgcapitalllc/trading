# Frontend Diary Notes

**Status:** ARCHIVE - relocated history, deliberately. Implementation-level history and
war-story detail moved OUT of `command-center/frontend/CLAUDE.md` on 2026-08-12 so that file could stay standing rules
and current status. **Nothing here was deleted from the record** - same pattern as
`command-center/docs/BACKEND_BUILD_NOTES.md`.

**Why it moved.** `command-center/frontend/CLAUDE.md` had grown to 346,246 bytes, most of it dated narrative,
and every byte loaded into context whenever anyone worked in this area. Worse than the size:
the diary sat in 3 paragraph(s), the largest **47,237 bytes on a
SINGLE LINE** - not readable by a person at all. A rule nobody can find is not a rule.

**What is here.** The entries below, verbatim and unedited, newest first. Any RULE they taught
stays behind in `command-center/frontend/CLAUDE.md`; this is the evidence, not the instruction.

---

**Last reviewed:** 2026-08-12 (latest) — 🔴 **THE ACCOUNTS RAIL SAID `2 BOTS`, WHICH IS A FACT ABOUT THE CONFIG AND READS AS A FACT ABOUT THE ACCOUNT BEING LIVE.** Aaron: *"make more obvious that the account has a bot running against it or not."* Two bots assigned and stopped rendered **identically** to two bots trading, so the rail — the one place every account is on screen together — could not answer *which of these is running right now*, and getting the answer meant selecting each account in turn and reading the State column. ✅ The rail row carries a steady green dot + `1 of 2 trading` and the detail pane a `Trading · 1 of 2` chip, FIRST in the readiness row, because everything else there (password, terminal, cap) is about whether the account COULD trade and this one says whether anything IS. ✅ **Driven against the live fleet: the ECN account reads `trading`, the bench row `1 bot · idle`, no console errors.** ⚠ **THREE states, and the third is the whole reason `liveOf` is a function**: `known` counts the bots the VPS snapshot actually came back for, so `running: 0` with `known: 0` means **nobody asked** — `/bots/accounts` never touches the VPS, so this tab renders in full while the box is unreachable, and calling that silence `idle` would report the fleet as stopped at exactly the moment nothing can be checked. It reads `Running state unknown`. ⚠ **An idle account says `Nothing running` rather than drawing no marker** — an absent green dot is indistinguishable from an account nobody could ask about, which is the same collapse the plain count was guilty of. ⚠ **ONE derivation, read by the rail and the chip**; two hand-written readings is how a green dot in the list ends up beside *nothing running* on the pane it opens. ⚠ **The dot is STEADY, never `animate-pulse`** — a bot runs for weeks, and permanent motion in a list is an alert on day one and background by day two. ⚠ **The default SELECTION was deliberately left on `busiest` by bot count**: keying it on the snapshot would move the reader's selection ~4s after load, when the VPS answers. ✅ 3 new browser checks (41 in the file), **each proven by MUTATION**, each turning its own named check red and leaving the others green; typecheck clean. Detail: *Whether anything is TRADING an account*. Earlier the same day: 🔴 **THE ACCOUNTS TAB LOOKED UNFINISHED BECAUSE TWO PANES SIZED THEMSELVES TO THEIR OWN CONTENT AND A THIRD TAB'S LOADING SKELETON RENDERED OVER BOTH.** Aaron, off the rebuilt tab: *"make the site navigation where all the accounts are the height of the page. Okay? Also, make the details on the right the same height of the page. I don't like how everything is, like, kind of not aligned right now."* ✅ **Both panes now take the space left below their own top and each scrolls inside itself** — measured to within 2px of each other and of the bottom of the viewport, by a browser check. ⚠ **The height is MEASURED, never a `calc()` off assumed chrome** (`usePaneHeight`): `100vh - 148px` is right for the top bar, the padding and the tab row and wrong the moment a banner appears above it, which really happens on a VPS-failure poll — and a hardcoded height then runs the pane's bottom edge off the screen. 🔴 **THE MONITOR TAB'S LOADING SKELETON WAS UNGATED, so opening Accounts drew ~400px of fake Monitor cards above it for the four seconds the VPS snapshot takes, then snapped away and moved everything up.** Neither Accounts nor Users reads that snapshot to render — Accounts joins it only for the State column, which honestly says `—` — so both tabs were blocked by a fetch neither needs. **Watched RED against HEAD**, and the observable is where the card SITS, because the skeleton has no testid and pushing the pane down is the whole damage. 🔴 **THE DRAG GESTURE HAD NOTHING ON SCREEN ADVERTISING IT** (*"drag a bot from the panel — what? that drag feature doesn't even work, I don't even know what that does"*): a `draggable` attribute is invisible, and the only mention of it was a sentence under the rail, which reads as an instruction for a feature nobody can find. Every movable row carries a **grip marker** whose hover says where to drop it; the sentence is gone. ⚠ **The grip is a MARKER, not a handle** — the whole row is still the drag source, so grabbing anywhere works. ✅ **The counts moved onto the TAB CHIPS** (*"accounts on the left navigation as account four — just put that count inside the accounts tab where I could see it, and users should have a count for how many users we have"*), each from ONE exported definition, and an unanswered query renders NO chip rather than a `0`. ✅ **A bot row links to Configure**, which is the answer to *what is the difference between Accounts and Configure*: this tab decides WHICH account a bot trades, that one decides HOW it trades there. ✅ **The detail pane is header / scrolling body / pinned footer** — identity stays put while the bot list scrolls, and the risk cap is a footer rather than something you scroll to find. 4 new browser checks (38 in the file), **each proven by MUTATION**; typechecks, builds, 48 checks green across both Bots suites, driven at 1670×940 with no console errors. 🔴 **One of those mutations did NOT bite and the comment was corrected rather than the mutation re-chosen**: swapping `items-stretch` for `items-start` leaves the height check green, because the shell carries an explicit height and each pane is `h-full` — it is the MEASURED height the check actually bites on (216px apart without it). **It is not enough to know a test is non-vacuous; you have to know which change makes it red.** Earlier the same day: 🔴 **THE ACCOUNTS TAB WAS A STACK OF CARDS AND IT DID NOT SCALE PAST THREE ACCOUNTS.** Aaron, off the screen: *“the more accounts I add, it's just gonna keep scrolling up and down… what is the broker? Then what's the account number? And then the account type? Those things don't stand out to me at all… I can't tell easily what bot is trading on what account… I don't see an easy way to add bots or remove bots from accounts… I don't even know how to remove the bot on the account ending 2905. Do I have to stop it first? I don't know.”* It is a **RAIL + DETAIL** now, the same shape `ConfigureTab` already uses: the rail is the only thing that grows and the detail pane is fixed. ⚠ **The three facts that IDENTIFY an account were set in the same grey `text-micro` as the three that describe it** — broker, then number, then tier, now in that order and in that visual weight, with the number `font-mono tabular-nums` because it is read digit-by-digit against a broker statement. ✅ **A bot row carries its STRATEGY and a `Move to…` menu**, so *what is trading where* and *put it somewhere else* are both answerable without dragging — and the running-bot refusal is now stated on THREE controls in the same words (drag, Move, Remove) rather than only on the one somebody happened to find. 🔴 **Two defects were visible only on a real render and neither was in the diff**: **Add account** sat 10px below the fold of a 940px viewport with five accounts registered — so the one control the tab exists for would be the first thing to disappear as the fleet grew — and the page opened on the numerically-lowest login, a RETIRED Standard demo with no bots, making its first answer to *what is running* an account with nothing on it. The button is above the list with the list scrolling under it, and the default lands on the account that is actually trading. ⚠ **A native `<select>` sizes itself to its WIDEST OPTION**, and an option here is a whole account identity — so the closed Move control rendered ~320px wide to hold a string only ever shown in the open popup; it takes an explicit width. ✅ 4 new browser checks (34 in the file), **five mutations run, each turning its named check red**; typechecks, builds, driven at 1670×940 with no console errors. 🔴 **One existing check was FLAKY and the flake was in the test, not the page** — `page.evaluate` runs the moment the document exists, so dispatching a drag at a row React had not rendered threw a null-reference inside the harness, which reads exactly like the drop handler being missing; it passed alone and failed in a full run, purely on how loaded the box was. Both ends are waited for now. Earlier the same day: 🟢 **YOU CAN ADD A BROKER ACCOUNT FROM THE BOTS PAGE, AND THAT MISSING CONTROL IS WHY THE ECN MOVE WAS A MANUAL AFTERNOON.** The Accounts tab could only show accounts a bot was ALREADY on — the grouping is derived from the instance configs, which is right, and which left the first bot onto a new account with nothing to be moved to. It renders a card per REGISTERED account now, with an **Add account** form. 🔴 **The form puts the SYMBOL SUFFIX front and centre because that is the field the move forgot**: PU Prime quotes gold as `XAUUSD.s` on Standard and `XAUUSD.p` on ECN, and a bot pointed at the wrong one connects, warms up and receives no bars — indistinguishable from a quiet market. ⚠ **It is a CHECKBOX plus a field, because `null` is a real value** — unticked means *nobody recorded it* (leave the symbol alone) where empty means *this broker quotes bare symbols* (really strip it), and the empty string is the one that silently rewrites a live instrument. ⚠ **`has_password` is three-state and `null` renders as `Password unknown`, never `No password`** — the `mt5_link` rule again, on a pre-condition rather than a reading. ⚠ **No risk cap on the form and there must not be one** — that is stored per instance and set on the card above. ⚠ **The move's `notes` are raised as WARNING toasts**, not folded into the success line: they describe a failure that is silent on the box. ✅ 8 new browser checks (27 in the file), **five mutations run, each turning its named check red**; typechecks and builds. ⚠ **`mock()` now routes the registry endpoint and MUST** — it asks the VPS about a stored password, so an unmocked one reaches the live box from a unit check. ✅ **A bot's row is DRAGGABLE onto another account's card** — the same mutation Add bot fires, never a second write path, and **deliberately with no staged “pending moves, then Deploy” step**: that would be a stored intention able to disagree with what the bots are configured to do. Earlier: 2026-08-11 — 🟢 **A B-LEG RUN'S PRICE CHART OFFERS FIBS NOW, AND THE PANEL DID NOT CHANGE AT ALL.** Aaron: *"when I do a b leg backtest and I go to the price chart, I don't have any analysis… for fibs it's missing."* The row was absent because `tradeFibCount` was **0** — the B-LEG bot recorded no ladder, so `tr.fib?.levels?.length` was falsy on all 99 trades and the layer correctly hid itself. **The fix is entirely in the strategy package** (`strategies/python/mpc_bleg/CLAUDE.md`); this folder's interest is that a layer whose data arrives can light up with no frontend change, which is the payoff on `TRADE_FIB` having been built strategy-agnostic. ⚠ **Blocked and Missed stay absent on a B-LEG run and that is CORRECT, not a second half of this bug** — their codes describe why an **A+** setup was refused and A+ never places an order in that fork, so a marker there would report the near-misses of a trade that was never on the table. ⚠ **Everything else was already drawing and was CHECKED rather than assumed** — 595 fair value gaps, 543 order blocks, 3,915 external-structure overlays, 1,259 liquidity levels, 407 candlestick reversals and the session VWAP, read off the served spec. ✅ **`tests/bleg-fibs.spec.ts` — 2 checks, and the first is a GENUINE fail-watch rather than the vacuous kind this folder keeps recording**: the Fibs row and its template both already existed and worked on A+ runs, so pointing the spec at a B-LEG run made before the change turns it red on the row being absent, for the right reason. The second is non-vacuous by construction (same pixels off and on). 🔴 **The second check FAILED on its first run and it was the empty-viewport trap, not a bug** — this bot takes ~2 trades a month, so the opening viewport usually holds no trade at all, and **a pixel count on a viewport with nothing in it is identical to a layer that does not draw**. It jumps to a date with a trade now. **Third instance of that trap in this folder** and the Candlestick Reversals suite already records it, which is the argument for reading a sibling spec's warnings before writing a new pixel check. ⚠ **The count assertion is the real number (99), never *some*** — a B leg cannot be priced without a ladder, so anything under 99 means trades are silently recording nothing. ⚠ **EXISTING B-LEG RUNS NEED A RERUN**, not *Rebuild chart*: the ladder is written into the equity curve at REPLAY time, so no chart rebuild can supply it. Typecheck clean. Earlier: 2026-08-10 (latest) — 🔴 **A STACK PAGE LEFT OPEN ON A FAILED STACK REFETCHED 226 KB EVERY THREE SECONDS, FOR EVER, WITH NOTHING ON SCREEN MOVING.** `useStack` polled while `completed_strategies < total_strategies`, and a leg that FAILED never completes — so that comparison is permanently true on a failed or partial stack. **MEASURED on the live lab: 226,036 bytes / 38 ms per poll, i.e. ~270 MB an hour.** It is keyed on the stack's own `status` now, which is computed off the same rows and is `running` only while a leg genuinely is. ⚠ **A condition that reads as "are we there yet" is only a poll guard if it can also be FALSE when the answer is never** — `done < total` is the natural way to write it and the failure mode is invisible, because a poll that never stops looks exactly like a page that is still waiting. 🔴 **`useStackContention` had TWO more ways to poll for ever, and both are ordinary rather than exotic**: `progress` lives in an in-process dict (`services/portfolio_runner._PROGRESS`), so a backend RESTART erases it while `reset_stale_runs` has already marked the legs crashed, and a CANCELLED run sets `phase: "cancelled"`, which is not `"failed"`. ⚠ **The stop condition needs `stackRunning` as well as the report**, because `_persist` marks the last leg complete BEFORE it writes `shared_summary.json` — stopping on the stack's status alone gives up exactly one poll early, and progress being present is the proof somebody is still working. ⚠ **The regime calendar is 43% of that payload (96,766 of 226,036 bytes on `st_94aeb25f0c`) and feeds an overlay that defaults OFF**, so the stack is fetched `timeline=false` and the calendar has its own hook under its OWN cache key. **A slimmed response and a full one sharing a key is the trap `GET /runs/{id}?timeline=false` already records** — whichever fetch landed last decides whether the calendar exists, so the overlay would blink out on the next poll. The page reads `has_regime_timeline`, never `regime_timeline.length`, to decide whether to offer the control. ✅ **A stack's price chart can REBUILD itself**, the sibling of `useRefreshChartSpec`, because it is the same panel with the same button and a reader who has just fixed what a layer draws must be able to act on it from either page. ⚠ **The mutation TAKES the `readyKey` rather than reconstructing one** — writing the wrong query key is invisible: the fetch succeeds, the button stops spinning, and the chart keeps the old spec. ✅ **`ChartBlock.layer` / `ChartMiss.layer` are a single strategy and `OverlayStyle.layers` is a LIST, deliberately** — a refusal belongs to one leg's rule, a gap belongs to the market and draws while ANY leg that fired near it is shown. Both are absent on a single-run spec, which is what keeps every stack affordance invisible on an ordinary backtest. ✅ 1 new browser check, typecheck and build clean. **The standing lesson is about polls rather than payloads: every one of these three stopped correctly on the happy path and ran for ever on a failure, because the stop condition was written as "is it finished" instead of "could anything still change" — and a runaway poll is the one performance bug a user cannot see.** Earlier the same day: 🔴 **A LEG YOU SWITCHED OFF CAME BACK ON WHEN ITS SIBLING FINISHED, AND THE CHECK WRITTEN FOR IT WAS VACUOUS ON ITS FIRST RUN.** Round 4 of the stacks audit. `StackDetail`'s enabled set was REBUILT from the completed legs on every change, and a running stack polls every 3s with its legs landing one at a time — so unticking a leg and waiting was the page undoing your click while you watched. It RECONCILES now (an answer already given is kept, a newly-finished leg defaults on), the rule the price chart's `groupsOn` already carries, met two pages over. ⚠ **The seen-set is what separates *switched off* from *only just finished*** — without it the two are one absence and either default is wrong for the other. 🔴 **THE BROWSER CHECK FOR IT PASSED AGAINST ITS OWN MUTATION AND HAD TO BE REBUILT, WHICH IS THE MORE USEFUL HALF.** It used TWO legs and toggled after the last one landed — the effect is keyed on the completed set, so with nothing left to finish it never re-ran and the re-seed had no moment to happen in. **Two legs cannot express this at all**: with one complete you cannot switch it off (the toggle refuses to remove the last leg on), and once both are complete nothing changes again. It needs THREE, and a leg landing AFTER the reader has answered; rewritten that way the mutation reads **`3 of 3 on`** where the reader had chosen one off. **Fifth instance of that trap recorded in this folder, and again it was caught only because the mutation was actually run.** ✅ **A stack says what it was REPLAYED with** — a per-leg Settings disclosure, because a single backtest has carried its params in a side panel since the day it existed and a stack had them nowhere; the value that exists in no other place on the page is the one the STACK pinned (`exec_secondary: false` on every shared leg, against the strategy's own `true`). ⚠ **Per LEG, never one merged list** — two strategies can hold one key at different values and a merge would have to pick. ⚠ **A boolean is rendered as written**: `false` dropped as falsy, or printed as an em-dash, hides exactly the pinned value the section exists to show, and it hides it as a tidy empty cell rather than as a missing fact. ✅ **The Stacks list has a Net P&L and Trades column** (an em-dash means NOTHING HAS FINISHED, never a flat result), and ✅ **a strategy page lists the stacks it is IN** — the Strategies list could always START a stack and nothing could answer the reverse. 🔴 **Driving that against the live lab is what made it usable: all three of B-LEG's stacks are the same two legs on XAUUSD, so the first version was three IDENTICAL buttons**; the chips carry the window and the mode now. ⚠ Matched on `strategy_ids`, never on the joined display names. ✅ 2 new browser checks (**29 green in the file, 40 with `strategies.spec.ts`**), each proven by MUTATION; typechecks, builds, and all three surfaces driven in a real browser against live data with **0 console errors**. ⚠ **`keepMounted` for the stack's Price tab was MEASURED AND DECLINED, not forgotten: the merged spec is 27.9 MB and 3.36 s** against the run page's 3.5 MB, so making every visit fetch and build it would swamp the 88 KB the payload pass had just saved. The shape that would work is *keep mounted once opened*, which is a change to `ChartTabPanel` and needs its own measurement. Earlier the same day: 2026-08-10 (latest) — 🔴 **THE STACK PAGE'S PRICE CHART HAD NO REBUILD BUTTON, AND THE REASON WRITTEN BESIDE THE PROP WAS ABOUT THE WRONG OBJECT.** Aaron: *"the rebuild chart button is missing on stacks still."* `PriceChartView` has taken `onRebuild` since the control moved onto the tool strip, and `StackDetail` passed nothing — the note said a stack *"has no single run to rebuild"*, true of the RUN and false of the CHART, which is built from the legs' cached specs and goes stale exactly as a run's does. ⚠ **`useRefreshStackChartSpec` takes the `readyKey` it must write back rather than reconstructing one.** The stack spec's query key carries the completed-leg ids, so a mutation that rebuilt that key itself would silently miss by a leg — and the miss is invisible: the fetch succeeds, the button stops spinning, and the chart keeps the old spec. ⚠ **`onRebuild` is `undefined` until `stackId` exists**, so a page still resolving its id renders no button rather than one that cannot act. ✅ **`tests/chart-rebuild-stack.spec.ts`, proven by MUTATION** — removing the two props turns it red on the first assertion — and it asserts a page-wide count of exactly ONE, which is the only assertion saying the control lives on the chart rather than being duplicated beside it. ⚠ **Its locators are scoped to `[data-applied-lo]`, the fifth time this folder has recorded that trap.** Earlier the same day: 2026-08-10 (latest) — 🔴 **ISOLATING ONE STRATEGY ON A STACK LEFT THE PRICE CHART WITH WINNERS, LOSERS AND FIBS WHILE THAT STRATEGY'S OWN BACKTEST PAGE CARRIED TEN MORE ROWS.** Aaron: *"I don't have all my typical analysis tools… fair value gaps are gone, missed trades, blocked trades, all that stuff is gone."* The merged spec had dropped them; the backend half is in `../backend/CLAUDE.md`. ⚠ **The panel's half is ONE predicate and its placement: `overlayLayerVisible` is ANY, not every** — an anchored overlay on a stack carries the layers of every leg that fired near it, so isolating one leg must not delete a zone the other leg also traded into. **A block or a miss is the opposite** and is gated on its single `layer`, inside `blockVisible` / `missVisible` rather than at the call sites, so the Step navigator, the menu counts and the drawing effect cannot disagree — the navigator must never park on a marker belonging to a strategy the reader has isolated away. ⚠ **The analysis-group ROSTER still comes from every overlay and only the COUNT is filtered**, the same rule the Missed chips follow: a row that vanished when a leg was switched off is a control the reader cannot use to get back. ⚠ **`hiddenLayers` moved ABOVE every predicate that reads it** — three dependency arrays now name it, and a `const` referenced above its declaration is a TDZ crash `tsc` cannot see inside a closure. **Third instance of that trap in this folder.** ⚠ **`StackDetail` filters blocks and misses on the leg toggles and overlays on ANY of their layers**, matching how it already filtered trades. ⚠ **The marker hover card names the STRATEGY on a stack** — a refusal attributed to the wrong bot is worse than none, and that was the original argument for dropping the layer. ✅ Typecheck clean, 17 stack browser checks green, driven in a real browser on `st_94aeb25f0c` with no console errors. Earlier the same day: 2026-08-10 (latest) — 🔴 **SWITCHING A STRATEGY OFF ON A SHARED STACK SHOWED WHAT THAT LEG CONTRIBUTED TO AN ACCOUNT THE OTHERS BUILT, AND READ AS WHAT IT WOULD HAVE MADE ALONE.** Aaron: *"if I take off the MPC SOS fade, it says the b leg made forty seven million… but then I run the MPC B leg by itself and it made twenty one thousand. How is that?"* ✅ **MEASURED, and both numbers are right: 99 trades either way, 99/99 identical entry timestamps, 99/99 identical entry AND stop prices, +17.8674R either way.** In a shared account the B-LEG sizes off a balance A+ grew to ~$169M, so its last trade risks **$16,925,791** instead of **$3,102** — 5,456x — for the identical +0.014R. ⚠ **Profit factor is distorted too and quietly: same trades, same R, same win rate, PF 1.505 → 3.111**, because dollar PF weights by how big the balance was at the time. ✅ **`composeCombined` now names the BOOK it is reading** — `screen` / `shared` / `solo` / `unmeasured` — and one leg left on swaps in that leg's **SOLO CONTROL** replay, so Made reads **+$21,064**, his standalone figure to the dollar. ⚠ **A subset nobody replayed REFUSES rather than composing one**: a shared account has exactly two books on disk (every leg together, and each leg alone) and anything between them is a different experiment. ⚠ **The refusal still renders the Verdict card** — the leg toggles live in it, so hiding the panel would strand the reader in a state they cannot click out of — with an `UnmeasuredCard` in place of the three KPI cards and a way back. ⚠ **The per-leg row leads with R now**, because it is the one figure a change of position size cannot move; the row previously showed a trade count under a tooltip reading *"It made <net_pnl> on its own account"*, which on a shared stack was **false by 2,266x**. ⚠ **Both leg maps cover every COMPLETE leg, not only the switched-on ones** — a leg's R is a fact about that leg, and falling back to a trade count made one row read `+17.87R` and the other `160` on the same card. ⚠ **A `basis-chip` beside the Performance heading states which book is on screen**, because the numbers move by orders of magnitude between them and a silent swap is its own defect even when every figure is right. ✅ 4 new browser checks (21 green), **each proven by MUTATION**; typechecks, builds, driven in a real browser at 1670×940 with no console errors. **The standing lesson is one this folder has not recorded before: two numbers can both be correct and still be a lie about each other, and a CONTROL is what tells them apart — the shared runner had computed the honest answer on every run since the day it was built and reduced it to two scalars, so the page had nothing to show and composed one instead.** Earlier the same day: 🔴 **FOUR THINGS OFF ONE SCREENSHOT, AND TWO OF THEM WERE DEFAULTS NOBODY HAD CHOSEN.** (1) *"I would never ever ever wanna do a screen. I would always wanna do a shared account, because that's what a stack IS — we're sharing the same resource. I wanna know how two strategies affect each other and where some trades are dropped because others have taken up all the capacity."* **The mode picker is gone and every NEW stack is a shared account** — offering a screen as an equal choice made the one mode he wants a coin flip, and `?? 'screen'` is what a caller got by saying nothing. ⚠ **NOT a removal**: three stored stacks are screens, they still render with their `Screen · upper bound` chip, and a RERUN carries its own mode forward. (2) *"Does this section need to be so verbose? Like I'm reading a storybook."* **The shared-account panel is three lines now** — peak risk + meter, a refused/not-refused chip, cap/concurrency caption, and `together · apart · delta` sharing a line with the disclosure. ⚠ **Every RULE those paragraphs stated is kept and is one hover away**, and both browser checks HOVER rather than reading the chip: an empty contention log still reads as a MEASUREMENT, and the together-vs-apart gap still names COMPOUNDING (read as risk it is alarming; `docs/SHARED_RISK_STACK.md` got the SIGN wrong from exactly that misreading). (3) 🔴 **Regimes default OFF now, and there were THREE definitions of that preference with only one persisting anything** — `BacktestDetail`'s stored `getOverlayPref` (defaulting ON) plus a bare `useState(true)` on the stack page and another in the tuning workbench, so switching it off on two of three surfaces did not survive a navigation. `useRegimeOverlay()` lives with its CONTROL now. ⚠ **The default is the POLARITY of the stored check** (`=== 'true'`, so unset reads OFF) — never a second key, or a reader who already answered gets asked again under a different name. (4) 🔴 **`LEG_COLORS` was `C.series.filter(c => c !== C.pos && c !== C.neg)`, and exact string inequality cannot express "not confusable with"**: `series[1]` is `#00ff7f` against `C.pos`'s `#00ff82` — the same green to any eye — so the second leg of every two-strategy stack drew in the PORTFOLIO's own colour and the legend showed two green swatches. Explicit list now, and **keep it explicit** — a filter over a shared palette silently re-breaks the day somebody adds a colour. ✅ 13 stack checks green, the two new ones proven by MUTATION; tuning suite green; typechecks and builds. ⚠ Three `backtests.spec.ts` failures are PRE-EXISTING at HEAD. Earlier the same day: 🟢 **A STACK AND A SINGLE BACKTEST ARE THE SAME PAGE NOW, AND THE STRATEGY TOGGLES MOVED INTO THE VERDICT CARD.** Aaron: *"it should look exactly like a single run backtest page… I don't want stack stuff here and a regular backtest details page to look two different"*, and *"the strategies on your stack could be toggled from within the verdict section… the KPIs and everything should change as you toggle them."* `StackDetail` already reused `PerformancePanel`, so the numbers were computed identically — **everything AROUND them differed**: a full-width strategy ribbon where a run has a Verdict card, three cards where a run has four, a bare `<h2>` where a run has a collapse control, and the leg chips in a section two scrolls further down. It is now Verdict / Made / Risked / Trusted behind the same `PerfCollapseToggle`, reading the same `usePerfCollapsed` key, with each leg a clickable `PanelRow` inside the Verdict card. ⚠ **`PanelRow` gained `onClick`/`lead`/`muted` rather than the card forking its own row markup** — a second copy of that anatomy is how the fourth card drifts out of line with the three beside it. ⚠ **The Verdict card passes `collapsed: false` always**: the collapse means *hero numbers only*, and the leg rows are a CONTROL, not a supporting metric. 🔴 **The shared-account panel was three big figures nobody could read** — *"one account, the screen promised, peak open risk — I don't know the significance of these things"* — over a seven-column table that is five columns of em-dashes on every run measured so far. One card now: the peak risk stated WITH its cap, a refused/not-refused chip, the empty-log measurement in words, and the screen-vs-shared gap as a SENTENCE naming compounding as its cause (two bare headline figures are what invited the misreading `docs/SHARED_RISK_STACK.md` already got wrong). The table is behind a disclosure that **opens itself when there is contention**. ✅ 3 new browser checks (13 green), each proven by MUTATION. 🔴 **One was VACUOUS and passed against its own mutation** — `locator('table')).toHaveCount(0)` straight after `goto` is satisfied while the PANEL is absent too; **fourth instance of that trap in this folder**, and it was caught only because the mutation was actually run. Full detail: *A stack renders a RUN's panel*. Earlier: 2026-08-09 — 🔴 **THREE COLOUR CLASSES ON THE ACCOUNTS TAB WERE NOT THEME TOKENS, SO TWO CHIPS HAD BEEN RENDERING WITH NO COLOUR SINCE THE DAY THEY SHIPPED.** Aaron: *"it's starting to deviate away from our style guide."* `bg-negative-muted`, `text-negative` and `bg-positive-muted` exist in NO theme — the real names are `neg-muted`/`neg-text` and `pos-muted`/`pos-text` (`tailwind.config.js`) — and they appeared nowhere else in `src/`, which is what a token invented at the keyboard looks like. **Tailwind emits nothing for a class it does not know and no build step complains**, so a misspelled token is not a wrong colour, it is silently no colour, on the cap chip and the cap-disagreement banner. Every pill on that tab is now the Monitor row's exact shape (`text-[10px] font-semibold px-2 py-[3px] rounded-pill uppercase tracking-[0.4px]`). ✅ **`VersionPill` is ONE component on the Monitor tab, the Accounts tab and the Configure banner** — Aaron: *"there should be a column even on the monitor page showing the bot version that's running… Everything should be aligned. All the versions. Create a pill that looks clean across all."* It reads through `useBotVersions`, which shares `useBotVersion`'s cache entries, so three surfaces are ONE fetch per bot and cannot disagree. ⚠ **It reports the DEPLOYED version, never the local one** — `v121` in the backtester is running nowhere — and its third state may never be a number: no promote, an unfetched commit or no git renders **No version**, because `v0` is the reassuring answer to a question nobody could answer and this pill is what you check before deciding anything. ✅ **`BotStatusPill` moved into its own file rather than being exported from `index.tsx`** — `index.tsx` imports `AccountsTab`, so importing back closes a CYCLE that TypeScript accepts and the bundler resolves right up until one side grows a top-level initialiser and the other reads it as `undefined`. ✅ **The raw Magic column is DELETED** (*"I don't know what the column magic even means"*): the number told the reader nothing while the fact it encodes — two bots on one account each reading the OTHER's orders as their own — is exactly what this page is about, so the backend reports `magic_clash` and the page draws a banner naming the two bots when it is true and nothing when it is not. State is the green Running pill from the shared component. ✅ **Add and remove**: an **Add bot** control per account listing every registered bot not already there (a RUNNING one is listed and DISABLED rather than hidden — *not here* and *cannot be moved now* are different answers), a per-row **Remove** that sends `account: null`, and a **Not on an account** card for the bench, which is a card of its own and NOT the unreadable-configs one. ⚠ **Nothing to add renders an explanation, not an empty list** — an empty dropdown with no wording reads as a broken control, which is this repo's own "feature nobody has driven end to end" shape. ⚠ **Every action says a RESTART is needed and never that it applied**: neither `account` nor the cap is runtime-reloadable. ✅ 11 new browser checks (19 in the file, all green), **each naming the MUTATION that turns it red** — the version pill showing the local number, falling back to `v0`, the remove button ignoring `running`, and the bench sharing the unreadable heading were all watched red. Earlier the same day: 🟢 **THE BOTS PAGE HAS AN ACCOUNTS TAB, AND THE GROUPING ON IT IS READ RATHER THAN CONFIGURED.** Aaron's ask, in his words: manage stacking from the command center — *"I shouldn't have to ask you, Claude, to help me manage any of this"* — group the bots on an account, set one shared max risk, backtest them first, and see a **Stacked** indicator on the page that shows what is running. `pages/Bots/AccountsTab.tsx` + a `StackedChip` on the Monitor row. 🔴 **There is no drag-and-drop and that is the design, not a shortcut: two bots naming the same `account` in their instance configs ARE sharing a balance whether or not anybody grouped them**, so a stored grouping would be a second answer able to disagree with what the bots actually do. ⚠ **A written cap is not a RUNNING cap** — `account_risk_cap_pct` is read by the order bridge at startup and is not runtime-reloadable — so every save says a restart is needed and the tab never claims it applied. ⚠ **A cap DISAGREEMENT quotes no ceiling at all**, because the max or the min would render as a configured number; the banner explains that the uncapped bot fills the balance while the capped one is refused. ⚠ **The save is DISABLED while any config is unreadable** — a partial write leaves exactly the disagreement the guard exists to prevent. ⚠ **Clearing the cap sends `null`, never `0`** — `null` means uncapped and `0` refuses every order, and there is deliberately no separate clear action so the absent value keeps meaning one thing. ⚠ **Running state comes from the SNAPSHOT and is joined on `key`**, because the accounts endpoint deliberately does not touch the VPS; `undefined` renders as `—`, never as *Stopped*. ⚠ **The `StackedChip` is absent while the accounts query has not answered** — unknown is not *not stacked*, and this is the one chip whose false absence understates how much risk is on. ⚠ **Its tooltip says *no account risk cap is set* when the bots disagree**, rather than quoting one of their numbers. ✅ **Backtest this stack opens the existing `StackConfigModal` in SHARED mode with the account's legs preselected and its cap prefilled** — mapping each bot's strategy PACKAGE to the lab's own strategy id — so there is no second stack form to drift. ✅ 8 browser checks, **non-vacuity by MUTATION** (a fail-watch is vacuous for a tab that did not exist), each naming its own; two were driven — chipping every account regardless of `stacked`, and allowing the save with an unreadable config — and each turned its own named check red. Earlier: 🟢 **A STACK NOW SAYS WHICH OF TWO EXPERIMENTS IT IS, AND THREE OF THE TEN CHECKS FOR IT WERE VACUOUS ON THEIR FIRST RUN.** Shared-risk stacking reached the UI (`docs/SHARED_RISK_STACK.md` item 5): `StackConfigModal` gained a mode toggle and the three account fields, `StackDetail` gained a mode chip and a `SharedAccountPanel`, and the Stacks list gained a **Mode** column. **A screen and a shared run over the same legs report different numbers, so the mode belongs in the header BEFORE any of them** — and a screen's chip says *upper bound* out loud, which is the whole reason it carries one at all. ⚠ **The panel's headline is the SCREEN-VS-SHARED delta, computed off the SOLO CONTROLS** — each leg on its own full account, i.e. a replay that really happened rather than an estimate. ⚠ **An empty contention log is rendered as a MEASUREMENT, in words**, because it is the expected state: open risk is measured to each trade's CURRENT stop, so a stop moved to breakeven releases its room before the other strategy asks, and a panel that goes blank there is indistinguishable from one that failed to load. 🔴 **THE TEST FILE IS THE MORE USEFUL RECORD.** (1) **Every route mock keyed on `http://localhost:8000` and matched NOTHING** — the app fetches through the Vite proxy at `/api` (`api/client.ts`) — so three checks silently read the LIVE lab and asserted on whichever stacks happened to be in the database. (2) **"A screen renders no shared panel" PASSED against its own mutation**: the hook is disabled on a screen, so the report is `undefined` and the panel cannot render whichever way the render guard is written — the render guard and the fetch guard are not separable from the DOM, so it counts the FETCH now (a poll enabled on a screen would leave a finished screen showing a permanent "replaying…" spinner). (3) **The counter that fixed it was registered BEFORE the mock and was shadowed** — Playwright matches the most recently registered route first — so the assertion was trivially true a third time. ✅ 10 checks, all three failures found by MUTATION rather than by review, and each one is commented in place. **The standing lesson is this folder's fail-watch rule turned one notch further: it is not enough to watch a test go red — you have to know the OBSERVATION it makes is one the mutation can change.** Full detail: *Shared-account stacks* below. Earlier: 🟢 **REBUILD CHART MOVED ONTO THE PRICE CHART'S OWN TOOL STRIP, ABOVE THE SETTINGS COG.** Aaron: *"allow me to rebuild chart on full screenview also it only allows me to do on minimized view"*, then *"put the build chart on the left nav pane closer to the setting button"*. The button lived in `ChartTabPanel`'s tab strip, and the price chart goes fullscreen by `position: fixed` over the whole app — so it took the control off screen at exactly the moment a reader is looking closely enough to notice the marks are stale (every candlestick-layer fix this week has ended *"existing runs need Reload charts"*). ⚠ **A written-down rule said actions stay inline because they are not view state, and that rule is RIGHT for the equity/breakdown `ChartModal`** — a portal over a page whose own chrome is still there — **and wrong for the price chart**, which takes its host's chrome with it. The comment now names which fullscreen it is talking about. ⚠ **MOVED, not copied.** The tool strip is INSIDE the panel, so one button covers both views, and a second copy in the tab strip would be two controls firing one action — the argument that already removed the fib tool's gear from that strip. ⚠ **Icon-only**: the strip is 40px. ⚠ **`PriceChartView` takes `onRebuild` as an optional prop and the PAGE owns the mutation** — `StackDetail` has no single run to rebuild, so it passes neither and no button appears. ✅ **`tests/chart-rebuild-fullscreen.spec.ts`, WATCHED RED against HEAD.** 🔴 **Its locators are scoped to the chart panel's own root and HAVE to be — anything the host renders is outside that root, and in fullscreen the host's chrome is still in the DOM behind the overlay, covered rather than hidden, so a page-wide `getByRole` matches it and passes against the broken page. Fourth instance of that trap recorded in this folder.** ⚠ **It also asserts a count of exactly ONE, which is the only assertion that says the button was moved rather than duplicated.** Earlier: 🟢 **A DRILL-DOWN PAGE CARRIES ITS OWN MARKET STRUCTURE, AND `?? []` IS THE HONEST DEFAULT.** `useRunCandles` now returns `overlays` beside the candles, because structure computed on the run's base timeframe was being drawn over the finer bars a drill-down fetched — the full defect is in `components/ChartPanel/CLAUDE.md`. ⚠ **The empty-array default is the part worth carrying:** a backend that predates the field means *this window has no structure to draw*, and the panel then draws none — which is right. **Defaulting to the spec's overlays instead would have been the defect wearing a fallback**, i.e. answering a question about M5 bars with the M15 answer, confidently. A missing field is not permission to substitute a different one.

**Earlier the same day:** 🔴 **"RESET CHART VIEW" ONLY RESET THE TIME AXIS.** Aaron: *"if I mess with the zoom on the chart I expect it to reset to default. Right now I think it puts me back to the default date but not zoom."* `resetView` called `setBarSpace` + `setOffsetRightDistance` + `scrollToRealTime` — **three names for the horizontal axis** — while dragging the PRICE axis takes klinecharts' y axis off auto-scale permanently. The screenshot shows the consequence: a price window of **5,100–5,460 with the market at 4,252**, i.e. a chart with no candles in it, which reads as the reset having broken the chart rather than half-finished it. ⚠ **There is no `resetYAxis` in klinecharts** — the one public route back is `setStyles({yAxis:{type}})`, passed the type already in force, so the call changes nothing except the reset. ✅ **`tests/chart-reset-view.spec.ts` (1 check), WATCHED RED against HEAD.** It hashes SCREENSHOT PIXELS, and has to: `data-applied-lo/-hi` expose the applied window but the y RANGE is drawn into the canvas and has no DOM presence at all. ⚠ **The mouse is parked off the plot before every measurement** — the crosshair paints into the same canvases, so a signature taken with the pointer over the chart differs for a reason unrelated to the view. 🔴 **Same pass, the outcome chip: `deepestNames` is read with NO fallback to `label` when the field is present** — an object missing this anchor's key means the backend WITHHELD the name because the span held only opposing candles, and the old `?? ov.label` put that candle straight back. The chip is three-state now: `no candle`, **`no matching candle`**, or the name. Earlier: 🔴 **A WINNING LONG'S OUTCOME CHIP READ `Won · Bearish Engulfing`.** Aaron, off two screenshots — a long that won named with a bearish candle, a short that won named with a bullish one: *"I should see BULLISH candle for long trades or BEARISH candle for short trades."* The candlestick layer's direction preference only ever ordered the patterns ON one bar; **which bar the chip named was still whichever sat nearest the turn.** ✅ **A winner and a 3/3 miss now take the setup-ALIGNED candle, a LOSER takes the OPPOSING one** — *"if I lost it should default to the candle that signaled why I lost"* — **69 → 98 named as asked, 44 → 10 named against, over 194 anchors.** ⚠ **Nothing is painted differently and the direction filter is still the only thing that hides a mark.** ⚠ **The chip reads a PER-ANCHOR name** (`deepestNames`), because one bar can be the deepest of a losing trade and a miss on the same leg. ✅ **8 backend tests; the fail-watch was VACUOUS (five died on the signature change) and is recorded as such, so non-vacuity is by MUTATION** — five mutations, five distinct tests red. 9 browser checks green. ⚠ **Existing runs need Reload charts.** Earlier: 🟢 **THE PRICE CHART'S ANALYSIS MENU WAS NINETEEN ROWS OF ONE FLAT LIST, AND TIDYING IT SURFACED A COUNT THAT DESCRIBED A SET NOBODY COULD SEE.** Aaron, off a screenshot: *"this looks very cluttered and confusing… should it all be together or no?"* ✅ **One menu still** — Step walks whatever it shows, so splitting it splits Step's set across two controls. 🔴 **The confusion was not styling: under Missed, the SCORE chips and the REASON chips are EACH a complete partition of the same setups** (measured: 35 + 417 = 452, and 179 + 238 + 21 + 10 + 4 = 452), stacked as one indented column of seven — so they read as seven sub-filters of one thing and the counts read as double-counting. A filter's values are wrapped CHIPS under their own caption now; layers took `Signals` / `Context` captions. ✅ **862px → 741px, and the header count `18/25` → `4/10`, MEASURED** — the old one counted filter VALUES as layers, so it described nothing; the menu is also bounded at `min(70vh, 620px)` and scrolls, because at 741px in a 940px viewport its bottom rows were unreachable. 🔴 **THEN THE NEW LAYOUT EXPOSED THE REAL BUG AND AARON CAUGHT IT: with `3 of 3` alone the layer drew 35 markers while the reason chips still read the whole 452.** Each axis now counts what the OTHER is showing — with 3/3 alone `No FVG in zone` reads **0**, which is the answer, since a 3/3 met all three confluences. ⚠ **The roster stays separate from the count**, or a chip would vanish the instant it reached zero and the reader could not get it back. ✅ **9 browser checks; the new one WATCHED RED against HEAD.** **The standing lesson is about cross-filtering, not layout: once two filters compose, every count is a statement about what the other filter is showing — and the honest layout is what made the dishonest number visible.** Earlier: 🟢 **THE PRICE CHART PAINTS THE CANDLE THAT TURNED PRICE, AND THE FIRST VERSION LOOKED RANDOM BECAUSE OF WHERE IT WAS ALLOWED TO PAINT.** Aaron's ask: highlight the reversal candle at his trades and at the setups he missed, *"so I could do analysis and say, can I add candle patterns as an additional confluence to get better entries or avoid bad trades"*. The canonical `engines/candlesticks/` engine is replayed server-side into one navy candle per setup. 🔴 **The anchor set is the whole feature, and it was wrong: BLOCKED setups were anchors, 324 of them against 159 trades and 35 three-of-three misses — two thirds of every mark — and since the Blocked layer defaults OFF they painted navy candles where the reader could see no setup at all.** Reported off the screen in exactly those words. ✅ **518 anchors / 424 marks → 194 / 153, measured.** 🔴 **The second defect was a `Bearish Engulfing` marking a LONG that WON**: ranking started at nearest-the-extreme, so whichever pattern sat closest won regardless of which way it pointed. Direction is now a three-tier PREFERENCE — aligned, neutral, opposing — with nearness the tie-break WITHIN a tier. ⚠ **The opposing tier is KEPT and is half the point** (*"if not, it will show me why I was wrong"*), so this is a preference and never a filter: **89 aligned / 49 neutral / 15 against / 41 with no pattern at the turn**, measured over the 194 anchors. ⚠ **The NEUTRAL tier does most of the work, and the reason is in the source Pine** — ten of its fifteen rules gate on a trend lookback, so in an uptrend the bullish rules cannot fire at all, which is exactly why the reference mark was bearish. ⚠ **A WINNER is searched over its whole HOLD**, because *"the ultimate reversal point before the trade went into my favour"* is the deepest adverse price of the hold — MEASURED at a median 2 bars past entry but **p90 27 and worst 112**, so the obvious fixed window finds the real turn on barely half of them; a ±2-bar tolerance stops a long hold reporting a distant pattern as its turn. ✅ **Aaron's third question — a 3/3 miss with no mark — was MEASURED rather than reasoned about: all nine unmarked 3/3 misses have ZERO patterns anywhere in their window.** No pattern at the turn is a real answer. ✅ **The pattern NAME is a Chart setting now, default OFF** — the tags carry no cross-overlay de-collision, unlike the batched `LABEL` template, so they write across neighbouring candles; the colour is the marker. 30 backend tests + 4 browser checks, **all proven by MUTATION** (a fail-watch is vacuous for a new module). 🔴 **Two existing browser checks broke and broke CORRECTLY — they read the opening viewport, which held a mark only while the run carried 424 of them, and an empty viewport is pixel-identical to a layer that never draws.** 810 backend tests green, 1 pre-existing failure. **The standing lesson is about scope rather than correctness: every individual mark this layer drew was the right candle by its own rule, and the feature still read as broken, because the rule was allowed to answer a question in places the reader could not see one being asked.** Before trusting a layer that annotates, check that everything it annotates is VISIBLE on the same screen. Earlier: 2026-08-07 — 🔴 **THE BOTS PAGE'S VERSION NUMBER WAS `v0`, IT ALWAYS WOULD HAVE BEEN, AND THE PAGE COULD NOT ANSWER THE ONE QUESTION IT EXISTS FOR.** Aaron: *"If you make me look at commit IDs or parameters from code from a configuration, like exec_time_stop_mode, I don't know what any of that means. I just wanna know what is the version that I have compiled in my backtester versus the version that is deployed... and if I'm behind, there should be a big nice button."* `strategy_version` defaults to 0 in `algos/live/live_config.py` and **nothing writes it**, so the card read `v0` before a promote and `v0` after — while the only other signal on the card was a commit hash and a raw `71 ahead`, which reads as a git log. `VersionBanner` is first and full width now: **"MPC SOS Fade is 21 versions behind · Deployed v100 · 2026-08-05 · Backtester v121"**, the settings that would move written in the STRATEGY's own words (`Time stop → Before TP1 only`), and `Deploy v100 → v121`. ⚠ **It is the ONLY promote entry point on the page** — `DeployCard` carried its own, and two controls firing one destructive action is two places for the confirmation copy and the preview gate to drift apart. ⚠ **`comparable` false renders the reason and NO BUTTON**: never promoted, commit not fetched, no git — each has its own fix and none is "press deploy", and a `0` there would read *up to date*. ⚠ **A PINNED setting is listed separately rather than filtered out** (it is also the one the promote preview does not report), and **a new setting reads `not in v100`, never `Off → On`**. ⚠ **Uncommitted edits are called out** — the backtester runs the working tree while a version describes a commit, and it caught a real one on the first render. 🔴 **AND THE FIRST REAL DEPLOY EXPOSED A SECOND DEFECT IN THE SAME PANEL: a FINISHED promote rendered under the PREVIEW's caption** (*"nothing deployed yet"*) **with Deploy & restart still sitting there.** Aaron pressed it, it worked, and the page could not say so — *"confused what to do I click deploy and restart."* The output was a bare string, and `promote.py`'s own text reads much the same either way, so **a panel that shows a result has to say which ACTION produced it**: `result.kind` now drives a green *"Deployed — restarted and is running v121"*, withdraws the button, and turns Cancel into Close. ⚠ **`restarted` and `ok` are rendered rather than assumed** — `ok && !restarted` means the new code is on disk and the OLD is still trading, and a failed deploy says the bot is untouched and still on v100. ✅ **10 browser checks, no backend and no VPS** (the real routes SSH to the live box and deploy onto it), **non-vacuity by MUTATION** since a fail-watch on a new component is vacuous. 🔴 **Two were vacuous on the first run to this file's own trap — the Risk-per-trade card has its OWN `Deploy` button**, the third recorded instance; everything is scoped to `data-testid="version-banner"`. Full detail: *The version banner* below; the derivation is in `../backend/CLAUDE.md`. Earlier: 🟢 **ANY TIMEFRAME TO ANY TIMEFRAME ON THE PRICE CHART, WITHOUT LOSING YOUR PLACE, AND THE DRILL-DOWN SAYS WHAT IT IS DOING.** Aaron's follow-ups, both off the screen. 🔴 A display timeframe switch parked the view on the newest loaded bar, which after a jump is mid-history — **MEASURED via a new `data-view-centre` seam: `M15 → H1` landed 42.2 days from the date being read, and it compounded to 105.7 by the next drill.** The cause was a SECOND `applyNewData`, found by logging the applies rather than by reading the effect: `displayCandles` depended on `fetched`, and the drill effect handed it a fresh `[]` on every switch, so the resample re-applied after the scroll had already run. ⚠ **The two fixes for it are REDUNDANT and mutation testing is what established that** — either alone leaves the check green — so neither is described as load-bearing. Every transition now lands within 0.4 days. 🔴 A drill-down's only sign of life was an 11px grey header line while the chart kept showing the previous timeframe's candles, on a fetch **measured at ~4.5s**; there is a badge over the plot now, naming BOTH timeframes. 🔴 And the *"no data here (feed offline, or none this far back?)"* message hedged because the backend's `available` was `bool(candles)` — see `../backend/CLAUDE.md`; it now prints the feed's own sentence, which names the date. ✅ **`tests/chart-drilldown.spec.ts` — 6 checks, 5 proven by MUTATION.** 🔴 **Writing them found a real defect in the fix itself: a timeframe switch could STRAND the chart.** `pickTimeframe`'s pending-jump is consumed by an effect keyed on `displayCandles`, and coming back from a drill-down to the run's own timeframe hands back the very same array — so the effect never fired, the guard was never dropped, and every later jump and page was refused for the rest of the session, silently, with a healthy-looking chart. ⚠ **The first fallback for it broke the working drill-down** by comparing the pending timestamp, which cannot tell *nobody handled it* from *the drill owns it now* — caught by a green check going red. ⚠ **One path is verified BY HAND and has no check, deliberately** (`drillNewer`: 36 requests at `tf=M5`, window 2021-06-29 → 2024-11-26); three drafts were written and none bit, and the first would have gone green on the defect. ⚠ **The 6th check is labelled as pinning a shape rather than the guard beside it**, also by mutation. **The standing lesson: a check that cannot be made to bite is worth deleting and RECORDING — but only after you have measured that the thing itself works, or you have deleted the evidence too.** Earlier: 🔴 **PRESSING M5 WHILE READING 2020 THREW THE PRICE CHART SIX YEARS FORWARD, AND THE OHLC READOUT SAID `Volume: n/a` FOR A NUMBER THE PIPELINE ALREADY HELD.** Two defects Aaron reported off the screen. The drill-down anchored its fetch on the RUN'S LAST BAR, so M1/M5 always pulled the newest weeks whatever was on screen, while M30/H1 stayed put because they are resamples of loaded bars — *"5 or 1 min does nothing, 30 and 1hr work"*, exactly as described. ✅ **MEASURED, not read off the diff: at 2020-08-05 the applied window went `2020-03-19 .. 2020-09-20` → `2025-11-09 .. 2026-08-06`, readout on `2026-08-06 13:15`** — and the data was there the whole time (853 M5 bars for 2020-08-02→06). The window follows the VIEWPORT now and the rest pages in; verified back at `2020-08-05 00:35` with volume 436 at M5 and `00:31` / 81 at M1, against M15's 1,499 on the same bar — **the bar size read off the RENDERED readout, because the timeframe button agreeing with itself proves nothing.** **H4 and D1 were simply absent from the ladder** and are now offered. ⚠ **One fix was written, MEASURED, and REVERTED**: a Playwright click on M30 after a jump timed out past 90s, which looks exactly like a frozen main thread and had a clean mechanism — but an in-page 50 ms timer logged **2,407 samples over 120 s** without missing a tick, and it reproduces at HEAD. The harness was blocked, not the page; the guard came back out rather than ship a comment claiming a measurement nobody took. Full detail: `src/components/ChartPanel/CLAUDE.md`. Earlier the same day: 🔴 **ONE NUL BYTE MADE `BacktestDetail.tsx` INVISIBLE TO `grep`, AND A GOOD TEST WAS DELETED ON THE STRENGTH OF THAT SILENCE.** The file carried a single NUL inside a string literal (`runId ?? '\0none'`, from `a314381`), and `grep` here is **ugrep**, which treats a NUL-bearing file as binary and skips it with **exit 1 and no output** — indistinguishable from a genuine no-match. A search for `useRefreshChartSpec` therefore reported zero callers against a file holding two, and that false negative was written into this file and `ChartPanel/CLAUDE.md` as the justification for deleting a browser check. **The Rebuild chart button has existed all along** (`BacktestDetail.tsx:5050`, Price tab, non-NT8). NUL removed, sentinel now `'__no_run__'`, and `grep` can see the page again. ⚠ **Any grep in this repo used to prove ABSENCE must be `grep -a` or ripgrep** — the default cannot report its own blindness, and this is the largest page in the app. ✅ **The check is restored and bites**, and restoring it exposed the mechanism the first attempt missed: **TanStack's structural sharing hands back the OLD object when a refetch returns identical content**, so a rebuild that changes nothing never recomputes the roster and the re-seed defect stays hidden. The check now has the rebuild come back carrying an extra layer — the realistic case, since that is exactly what a spec cached before the VWAP does. **4 checks, every one proven red by mutation.** **The standing lesson is about evidence rather than the bug: a negative asserted from a tool that fails silently is not a finding, and it was recorded in two docs as a reason to remove a test. Make the tool prove it can see the file before writing down what is not in it.** Earlier the same day: ✅ **THE SESSION VWAP DRAWS ON REAL BARS, AND THE PASS THAT PROVED ITS TESTS BITE DELETED ONE OF THEM.** Aaron logged the MT5_Lab terminal back in, so the layer was driven end to end for the first time: 186,274 XAUUSD M15 bars re-pulled with volume, a rebuilt spec carrying 155,805 VWAP points, and the arithmetic hand-checked against the raw cache on four sessions to the 5dp it rounds to. `tests/vwap.spec.ts` — **3 checks, and the fourth was deleted.** ⚠ **A fail-watch against HEAD is VACUOUS when HEAD already carries the feature**, so non-vacuity was established by MUTATION: dropping `defaultOn` from the emitter turns all three red, and flipping the panel's `defaultOn !== false` to `=== true` turns exactly the one naming that rule red. 🔴 **The deleted one PASSED with the guard it named removed.** It claimed the reader's toggle survives a roster rebuild and it survived `reconcileToggles` being replaced by a plain re-seed — because the condition is unreachable from the UI: the roster is memoized on `spec.indicators`, a timeframe switch is a display-only resample, and **`useRefreshChartSpec` has no callers**, so nothing in this app swaps the spec object during a session. The reconcile stays (right shape, costs nothing) and the spec file records that it is defensive rather than exercised. **The standing lesson sharpens this folder's own fail-watch rule one more turn: watching the SUITE go red is not the claim — watching THIS test go red for THIS reason is, and when it cannot, deleting it beats keeping a green test that asserts nothing.** Earlier the same day: 🟢 **THE PRICE CHART HOLDS THE WHOLE RUN AND JUMPING TO A DATE IS SEAMLESS: 90.3s → 2.0s, MEASURED.** Aaron's ask was that the page load everything it can in the background so a date jump does not mean waiting on bars. The backend half is in `../backend/CLAUDE.md`; the half that lives here is the correction that made it possible. 🔴 **Shipping the full run and handing it straight to klinecharts FREEZES THE MAIN THREAD FOR 30.8 SECONDS** — `applyNewData` lays every candle out synchronously, and 155,798 of them is 30,828 ms against 2,199 ms for the old 33k-bar spec. **It was found by a Playwright click timing out while the element was reported visible, enabled and stable**, which is what a frozen main thread looks like from outside. ⚠ **I had claimed browser candles were nearly free, and that measurement was of PREPENDING 12k-bar chunks into a live chart (11 prepends, 682 ms total) — not of one `applyNewData`.** The two are not the same operation and the claim is withdrawn. ✅ **The fix splits the two: `ChartPanel` keeps the whole spec in memory and APPLIES a window of `APPLIED_BARS` (12,000), extending or re-centring it from memory.** Worst main-thread block **30,828 ms → 508 ms**, and paging older history is now a binary search and an array slice with no network at all. 🔴 **A jump must RE-CENTRE that window, not grow it from the right edge** — growing gives the right answer in **47.6s**, because covering target→present IS the whole run and pays the full layout again; slicing around the target makes the cost independent of how far back you asked to go. ✅ **Overlay creation follows the VIEWPORT rather than the loaded history** (`drawRange`, recomputed off `OnVisibleRangeChange` behind a rAF, widened one screen either side so ordinary scrolling does not rebuild). That is what makes carrying 19,538 overlays affordable, and it took a layer toggle from 133 ms to 67 ms while scrolled back. ✅ **The server-side pager is deleted with its client half**: `useRunCandles` lost its `analysis` argument and `ChartPage` lost `overlays`/`blocks`/`misses`/`missNoise`; that fetcher is the M1/M5 DRILL-DOWN only now. ⚠ **`jumpRange` had gated the reachable half on `onRequestCandles` — correct while paging was a network call, wrong the moment it became a memory slice.** ✅ **`tests/chart-paging.spec.ts` was REWRITTEN**: its two checks pinned a jump-progress readout, which was the right fix for a ninety-second wait and is meaningless for a two-second one, so the readout and the tests went together. The replacements assert the jump is fast AND lands where it was asked, one on the clock and one on the window's span — **both watched RED by mutation.** 🔴 **A third check was written, passed, and was DELETED for failing to bite**: it claimed the jump does not walk forward off its target, and survived removing the guard it named — probing the applied window for 12s with that guard gone gives a dead-stable result, because the target is centred with ~3,000 bars to its right so klinecharts never asks for a newer page. **The comment in the source claiming that guard was load-bearing is withdrawn there too.** ⚠ **`data-applied-lo`/`-hi` on the panel root are a declared TEST SEAM** — klinecharts draws its time axis into the canvas, so which window is applied is otherwise unreadable from the DOM, and "the jump was fast" is satisfied by a jump that lands nowhere near the date asked for. **The standing lesson is the one this pass had to learn twice about its own numbers: a measurement is a claim about a specific operation, and reusing it for a different one is how "candles are cheap" became a thirty-second freeze.** Both times the fix came from driving the thing, not from reading the diff.

**Earlier the same day:** 🔴 **THE RUNS LIST COULD RERUN A RESULT AWAY ON ONE UNCONFIRMED CLICK, AND THE ONLY WARNING ABOUT WHAT A DELETE TAKES WITH IT SAT ON A CODE PATH NOBODY COULD REACH.** Aaron asked for an in-depth audit of the Backtests list and the Backtest detail page, then for the fixes — 27 findings, nine real defects, three destructive, and **not one of them rendered an error.** 🔴 **The row Rerun fired on the click**, at 13px, inside a row whose own click navigates, beside the chevron — and a rerun RESETS the run in place and discards its result, charts and evaluations; the detail page had asked first since the day it existed. 🔴 **There was no per-row DELETE at all**, so `deleteRunId` was never set and `cascadeMessage` — **the only place that says a delete also removes attached optimizations, sweeps and tuning iterations** — was unreachable, leaving the bulk checkbox path, which deletes the most, showing the generic message. Both controls now RAISE to the page, which owns the modals and is the only place the cascade can be computed. 🔴 **"Bank holidays · on by default" sat over a rule that had defaulted OFF for five days** — this folder's signature defect, on the exact control rebuilt to stop it. 🔴 **The news filter's two rules did not compose**: one `if / else if` did the counting AND the removing, so a trade that was both a holiday and a news window took the holiday branch and **stayed in the result when you ticked News with Holidays off** — silently exempt from the rule you had just switched on. 🔴 **A run with no evaluated ruleset could never show a drawdown percentage, and the page told you to do something it gave you no way to do** — "Set an account balance", while the slider renders only once a ruleset default exists, with `max_drawdown_pct` stored on that row the whole time. It falls back to the run's OWN opening balance now, which is also the more correct denominator for a self-sizing run. 🔴 **`rebaseEquity` did not anchor on the opening balance, so the detail page and the Runs list used different definitions of one number** — ✅ **MEASURED on the shape that decides it: a run down 40% from $10k that never regains the start reads 25% without the anchor and 40% with it.** 🔴 **A partly-priced re-price rendered as "Charging nothing"** with the boxes still ticked (the `isError` fix's own failure, one branch over) and 🔴 **`FailureBanner` declared an `onRetry` the page never passed**, so the banner a reader is looking at BECAUSE something failed had no Retry button. Also: excursion halos are re-priced and clamped (the solid core used to stick out past its own halo), Worst streak is computed rather than nulled on a sized view, `fmtMoney` gained an `M` step (`+$14387.5k`), five hooks stopped toasting their errors a second time on top of `api.request`, and the two `/log` polls during a run became one. ✅ **The run page stopped fetching the whole lab for one badge — MEASURED 20.6 KB → 0.002 KB** via a new `source_run_id` filter. ✅ **11 browser checks (`tests/backtests.spec.ts`), 10 WATCHED RED against HEAD**; the 11th needs a `data-testid` that is part of the fix, so it was proven by MUTATION instead. 🔴 **And that test was VACUOUS on its first attempt and PASSED against the broken page — the page HEADER carries its own Retry, so a page-wide locator was matching a control that was never in question.** **The standing lesson is the sharper form of the fail-watch rule: it is not enough to have watched the suite go red — you have to have watched THIS test go red for THIS reason.** Full detail: *The Backtests list and the Backtest detail page — audited 2026-08-06*. Earlier: 2026-08-05 — 🔴 **THE STRESS TESTS PAGE SHOWED A DRAWDOWN IN DOLLARS UNDER A GRADE THAT HAD BEEN DECIDED IN PERCENT.** Aaron asked for a full audit of the page and then for the fixes. **The frame is one query: the `stress_tests` table held ONE row, written 2026-07-27 — three days BEFORE the accuracy pass that rewrote this engine.** So the page had been driven end to end exactly once, against code that no longer exists, and nothing had re-scored the stored row since; re-grading it live moved it from a confident **D** to **ungraded**, with both probabilities NULL and sensitivity 0.858 → 0.205. **Every defect shares the Overview's shape — not one of them rendered an error.** 🔴 **The engine picks its drawdown basis per run** (`dd_basis` = `percent` on a compounding run, the 2026-07-30 fix) **and the page read neither `dd_basis` nor the percent columns** — it printed dollars against a dollar limit and coloured them over/under, while the letter beside them had been decided on percentages, **so a red "over limit" could sit next to an A**. One derivation now, showing ONE unit, never both. 🔴 **`degradation` is a MAGNITUDE and the page drew `-degradation * 100`, so a parameter shift that IMPROVED the result rendered as a long red bar** and "Median Change" was negative by construction; the engine records a signed `pf_delta_pct` now and the radar paints neutral when the direction is genuinely unknown rather than inventing one. 🔴 **`st.prob_pass_eval ?? 0` printed "0%"** — *this strategy never passes* — for a measurement never taken, and **a sensitivity shift whose backtest FAILED was drawn as a flat zero bar**, i.e. "tested, no effect", the most reassuring answer available. 🔴 **`is_trades`/`oos_trades` were written by the engine and undeclared on the API model**, so Pydantic stripped them and the page could never show that **every window on the stored test closed 6 out-of-sample trades** against the engine's own 20 floor — **the `entry_ms` / `exit_ms` / `favorable` trap for the fourth time.** 🔴 **The native walk-forward path writes `is_sharpe: null` deliberately and the chart did `?? 0`, drawing five pairs of zero bars asserting "Sharpe 0.00 in and out".** 🔴 **The equity fan drew a drawdown-limit line at `y = -max_loss_eod` on a CUMULATIVE-P&L axis** — a drawdown is peak-to-trough, so a path can breach many times without crossing a line below zero, and a fan sitting entirely above it read "no simulation breaches" while Prob. Breach said otherwise. 🔴 **`grade_reasons` had been computed and stored since the accuracy pass and NOTHING displayed it** — the one thing that says why a test has no letter. ✅ **11 browser checks (`tests/stress.spec.ts`), ALL 11 watched to fail against the page at HEAD**, mocked by mutating the real detail response. ⚠ **Two locator traps, and both produce a VACUOUS PASS rather than a failure: `page.locator('svg').first()` is the SIDEBAR LOGO**, so a page-wide search for an absent element passes on the broken page too — the fan's own check proved nothing until it was scoped to the fan's container — and a chart label appears three times (KPI card, chart `<tspan>`, Recharts' hidden measurement span), so a bare `getByText` is a strict-mode violation rather than a miss. Full detail in *The Stress Tests page — audited 2026-08-05*. Earlier the same day: 🟢 **TWO SCHEDULED JOBS LEFT THE BOTS PAGE BECAUSE THEY NEVER DID ANYTHING, AND TWO THAT DO WERE NEVER LISTED.** Aaron asked what `SYS_PNLTRACKER` and `SYS_REPORTER` were for and whether he still needed them; the answer was that **neither could have done anything if switched on** — both carried an empty bot registry inherited from the four bots deleted 2026-06-22 (`algos/CLAUDE.md`). This page had been rendering them as **DISABLED** with a deliberate comment saying a disabled task is not a failed one, which was true and beside the point: **a job that is switched off and a job that does nothing look identical from here, and only one of them is a feature.** Both deleted, and `SYS_DEADMAN` + `SYS_LOGBACKUP` — real, enabled, and never in `_SCHEDULED_JOBS` — take their place, so the three jobs listed are now the three that run. ⚠ **`_SYS_TASK_BY_JOB` is DERIVED from `_SYS_DISPLAY_NAMES`**, because the loop restated the name→task map and a job listed under a name that map missed resolved to a permanent `UNKNOWN` — a job the page cannot see, rendered as one it never asked about. 🔴 **Seven fields came off `BotStatus` with them, and leaving them would have been the real defect**: `daily_pnl`, `daily_pnl_pct`, `weekly_pnl`, `weekly_pnl_pct`, `peak_balance`, `trades_today` and the three cap fields (`daily_goal_pct` / `daily_cap_pct` / `weekly_cap_pct`) had **no writer left**, so the Monitor tab's four stat tiles and three cap chips were drawing an em-dash or nothing — which reads as *quiet*, not as *nothing measures this*. **This repo's own rule, and the third time it has been applied on this page: a fabricated value and a measured one must never look alike** (`mt5_link`, `grid_sensitivity_score`, now this). `balance`, `total_pnl_pct` and `uptime` survive because `algos/live/runner.py` writes them, and they are in the row above. ⚠ **The caps were the dangerous half: they rendered as PROTECTION.** They were the P&L tracker's Telegram ALERT levels — nothing in them could refuse a trade — so the page showed a daily cap the bot would have traded straight through. `_BOT_THRESHOLDS`, `_load_thresholds_json` and `_get_thresholds` are gone, along with `algos/shared/thresholds.json`. **A real cap belongs in the bot's own loop.** ✅ 650 backend tests green, frontend typechecks and builds. **The standing lesson is the Optimizations one from a different direction: there, a feature nobody had RUN was 24 defects waiting; here, a feature nobody had run was wearing the word DISABLED, which is what kept anyone from checking.** Before switching a dormant job on, read what it would do against today's registries — not what its name says it does. Earlier the same day: 🔴 **THE TUNING WORKBENCH WAS RUNNING EVERY ITERATION ON A FREE BOOK AND PUTTING THE RESULT IN A TABLE BESIDE A CHARGED BASELINE.** Aaron asked for an audit of the page, then for the fixes. **`runIteration` sent `commission_per_side` and `slippage_ticks` and nothing else** — no `cost_layers`, no `broker_profile`, no `sizing_mode` — so tuning a run that charged spread and swap produced a child measured under different physics from its own parent, and the Δ column attributed the whole gap to the param you moved. Retry has carried those fields deliberately since the day they existed; this was the one run-launching path in the app that did not. ✅ **PROVEN BY RUNNING IT, not by reading the diff — three real python backtests against the live backend on cached XAUUSD M15 (2024).** A charged baseline (spread + swap, `vantage_demo`) was built, then the SAME iteration (`exec_tp1_pct` 0→40) was fired twice: once with the body this page now sends and once with the body it used to. **PF 1.499 vs 1.581, P&L $3,157.33 vs $3,646.75 — and trade counts IDENTICAL at 17**, which is the check that says the charge is real and correctly placed, since spread and swap change what a trade MAKES and never whether it happens. The old page would have shown that $489 as the param's doing. All three probe runs deleted afterwards; the lab is back at the 10 rows it started with. 🔴 **"Ranked by profit factor" was a caption over a table sorted newest-first** — a label claiming something no code did, this repo's most-repeated defect, sitting on the one page whose entire job is ranking. It ranks now. 🔴 **The ★ could be won on any trade count at all**, so a fluke with three trades outranked the real winner; it needs 10 trades and the caption SAYS so, because a threshold nobody can see is indistinguishable from a bug when the obvious winner has no star. 🔴 **Max DD was listed in dollars only** — the exact defect the Runs list had fixed on 2026-08-01, in a second table, where $1.7M beside $14M of profit reads as ~12% against a true 55.9%. Percent leads now, dollars beneath. 🔴 **Tuning an iteration hid it**: the page listed DIRECT children only, so a grandchild vanished from the only page that compares it — and the same filter was letting SWEEP and OPTIMIZER children in as "tweaks", since `source_run_id` is stamped by all three. Descendants are walked transitively, sweeps and optimizations excluded by their own ids. ⚠ **Two fixes are about a page that changes under the reader rather than about a wrong number.** The palette was handed out in TABLE order, so every line on the chart swapped colour whenever a run finished and re-sorted the leaderboard — it is CREATION order now, and a colour once given is that run's for good. And edits are held in `sessionStorage` per baseline, so clicking a row to inspect it no longer throws away what you typed: **persistence rather than a navigation-guard dialog, because nothing is lost means there is nothing to warn about.** ⚠ **The changed-count on the Run button and the params in the request are filtered through ONE set of known keys**, so the button can never promise a change the request then drops. ⚠ **`cost_layers: null` on a baseline is NOT forwarded as `null`** — it means "made before layered costs existed", which is not a contract a NEW run can be created under; `[]` is its honest equivalent and charges the same nothing. **Payload: 137 KB → 49 KB per iteration** (`?timeline=false`, measured), because `regime_timeline` is 96 KB of that and is the same calendar for every run in the window — the chart bands off exactly one copy. ⚠ **It is only slimmed when the BASELINE actually carries a timeline, and it is cached under its own key**, never `['lab','run',id]`, which belongs to the run page. ⚠ **One audit finding was WRONG and is recorded as wrong: `copyChartAsPng` already toasts on every failure path**, so there was nothing to add — the call site ignoring its boolean is not a silent failure. Also: a loading chart no longer says "no completed runs", the fullscreen chart's height is measured with a `ResizeObserver` instead of a one-shot `window.innerHeight` read, the inline chart unmounts while fullscreen is open, the baseline's dot renderers are memoised (a keystroke in the param editor was repainting 165 markers), Reset is enabled whenever an edit is HELD rather than only when one differs, a Stop button sits on the running banner, iterations are named by what they CHANGED instead of `Tweak 15f0122a`, Sharpe / worthiness / Win% and Trades deltas are shown, and the regime bands finally have a key. ✅ **8 new browser checks (`tests/tuning.spec.ts`), and they were WATCHED TO FAIL first — all 8 red against the page at `HEAD`, all 8 green against the fix.** A suite written after a fix and never run against the defect is a description of the fix, not a test of it. 4 new backend tests, 506 backend green, frontend typechecks and builds. **The standing lesson is the Optimizations one arriving from one page over: this feature had been BUILT and never DRIVEN END TO END — and the giveaway is that the two worst defects were both invisible unless you compared a child to its own parent, which is the only thing this page is for.** Earlier the same day: 🔴 **THE OVERVIEW — THE PAGE YOU OPEN TO ASK "IS ANYTHING WRONG" — HAD ELEVEN DEFECTS AND NOT ONE OF THEM SHOWED AN ERROR.** Every single one rendered a confident, healthy-looking answer, which on this page is the whole failure. **Two of the three scheduled jobs on the live box are DISABLED and all three wore the gold "waiting for next trigger" pill** — the Bots page had handled that for months, with a comment saying a gold dot on a dead task is worse than no dot, and this page did exactly what that comment forbids. **A bot RUNNING with a dead terminal link read `1 / 1 · all bots live` in green** (the 2026-08-04 blind-bot incident, on the page a reader checks first). **`balance ?? 0` counted "could not tell me" as a real zero** in the fleet total. **A failed refetch drew the error banner over the last good rows, undated.** **The calendar window was `useMemo(…, [])`, so a dashboard left open past Sunday midnight asked for LAST week for ever** — proved with a faked clock, and the same test against the old code stays pinned, which is what makes it a test. **`server_now_ms` was read straight from the response, so "now" froze between polls.** Plus: a calendar error rendering as "Loading…" for ever, `0 / 0` bots reading "all bots live", a best-PF ranking with no sample floor, a running backtest announcing itself nowhere, an empty event grid, a DST-wrong week end, and rows keyed by name instead of key. ⚠ **The correction worth keeping is one I had to make to my own audit: the Overview does NOT pay for the runs/optimizations/stress-test polling — `Sidebar.tsx` is always mounted and already holds those cache entries.** Guessing at a cost is the same error as guessing at a number. **25 browser checks green** against mocked blind/empty/dead-VPS/dead-feed fleets. Full detail in *The Overview was audited 2026-08-05*. Earlier the same day: 🔴 **THREE HOOKS NOBODY RENDERED, AND TWO OF THEM RESTARTED A LIVE TRADING BOT.** The Bots audit found `useBotConfig` / `useSaveBotConfig` / `useSaveBotCaps` with **zero consumers** — and the endpoints behind them were not inert: `PATCH /{bot}/config` wrote arbitrary config sections including strategy params, going around the backend's runtime-editable allowlist, then committed, pushed, pulled and **restarted the bot**; `PATCH /{bot}/caps` did the same restart to write a threshold file for a disabled job. All three are deleted here and in the backend (`git show 407d716^`), along with the `BotConfigSections` / `BotConfigUpdate` types. `useBotParams` reads and `useSaveBotRuntime` writes the one lever that may move — **and that one does not restart the bot.** ⚠ **The lesson is about where dead frontend code is dangerous: a hook with no consumer is normally just clutter, but a hook is a claim that an endpoint is a supported thing to call, and here the endpoint's blast radius was a live position.** Deleting the hook without deleting the endpoint would have left the hazard and removed the only trace of it. Same pass, the **Users tab** now says WHICH row is saving. ⚠ **The global disable stays, and that was the finding** — it looked like an ergonomics bug (removing one user greys out every role select) and it is actually load-bearing: every one of those endpoints rewrites the WHOLE `users.json`, so two in flight means the second one's read can predate the first one's write and silently undo it. The backend now holds a lock across its own read-modify-write; the fix HERE is legibility, not concurrency — the acting row tints and says `Saving…`, so a table of greyed-out controls reads as *waiting for that one* instead of *everything broke*. **Do not "fix" the remaining rows to stay enabled.** ✅ **Third change, and it is the one that matters for bot #2: nothing on this page addresses a bot by its DISPLAY NAME any more.** `BotStatus.key` is the stable identifier — the same string on the VPS process commandline — and the `?bot=` URL param, the rail selection, `expandedBot`, `logBot`, `confirmStopBot`, the pending-row marker and every API path now key off it. ⚠ **A name is a label chosen for a human, so it is the field that eventually changes, and `?bot=MPC%20SOS%20Fade` is a bookmark that dies the day somebody renames the bot — silently, because the fallback is `bots[0]`, i.e. a DIFFERENT bot's promote button with the URL still naming the one you wanted.** ⚠ **Key and label are separate PROPS wherever both are needed** (`DeployCard`, `RuntimeEditor`, `LogModal`), and user-facing copy goes through a `labelOf(key)` lookup — a control that ACTS on a name acts on nothing the day it changes, and a dialog that shows a key is unreadable. ✅ **Verified in a real browser on a mocked 4-bot fleet including a LIVE one**: `?bot=orb_live` in the URL, **1 promote button with 4 bots registered**, the panel reads *ORB* and never leaks `orb_live`, a key deep-link survives a reload and a Monitor round trip, and stopping the live row shows **"Stop ORB?"** while calling **`orb_live/stop`**. No console errors. Frontend typechecks and builds. Earlier the same day: **SMART MONEY IS BEHIND A FEATURE FLAG AND OFF.** Aaron is auditing the command center down to what he uses, and this is not on the list for a while. `src/lib/features.ts` is the switch; **the rule is that a flag hides an AREA, not a component** — its nav row, its route and every card that summarises it move together, because hiding only the nav leaves a page reachable by URL (which is not "removed") and hiding only the route leaves a row that goes nowhere. ⚠ **Hidden means NOT FETCHED**: `useRunProgress` polls every 30s forever, so a card that is merely unrendered goes on costing a request twice a minute for something nobody can see — both hooks took an `enabled` param and the Overview now issues **0** `/smart-money` requests, measured in a real browser. ⚠ **A grid's column count has to follow what is actually rendered** — two cards left in a `grid-cols-4` row is half a row of blank space, which reads as data that failed to load, so both Overview grids pick their columns off the flag. ⚠ **`FEATURES` is typed `Record<…, boolean>` and deliberately not `as const`** — literal types narrow every `FEATURES.x && <Card/>` to `false` and TypeScript then reports the switched-off branch as dead code to delete, which is precisely what a flag exists to prevent. Nothing is deleted: pages, hooks, types, the backend router and the `smart-money/` pipeline are untouched, so one boolean restores it. **Rendering it found the one real defect: an unmatched route drew NOTHING** — a blank main area beside a working sidebar — so a stale `/smart-money` bookmark looked like the app breaking; `App.tsx` gained a `path="*"` redirect to Overview. Verified at 1670×940. Full rules in *Feature flags*. Earlier the same day: **THE BOTS PAGE'S CONFIGURE TAB IS A BOT SELECTOR NOW, AND THE POINT IS NOT THAT IT LOOKS TIDIER.** Aaron's ask: it was a flat vertical stack — a full screen per bot, risk editor + Account + Deployed version + a 53-row parameter accordion, with no selector — which is correct for one bot and does not scale to configuring and promoting several. It is a **left rail + detail panel** with a **fleet version strip** above it (closes 2 of G11's 3 bullets; Monitor's global-vs-per-bot controls are the third and are still open). ✅ **The property that earns it is measured, not argued: only the SELECTED bot's controls exist in the DOM.** Driven in a real browser against a mocked 4-bot fleet, `getByRole('button', {name: /promote/i})` counts **1** where the flat stack would render 4 — a Promote button for a bot you did not pick is not there to be hit, which no amount of spacing, ordering or confirmation copy can buy. ⚠ **The fleet strip and the per-bot card must never be two readings of one fact** — they share ONE derivation (`versionFlags`) and ONE TanStack cache entry per bot (`useBotVersions` reuses `useBotVersion`'s key), because a strip saying *all deployments clean* over a card warning *restart pending* is worse than no strip; it also therefore costs no extra fetch, since the flat stack was already reading every bot's version to render every card. ⚠ **An unreadable deployment record counts as `unreadable`, never as clean** — the same *no data ≠ cannot ask* rule the `No MT5 link` chip below exists for, and the strip has to state it or absence reads as health. ⚠ **The detail header is deliberately NOT sticky and the RAIL is:** "which bot am I editing" has to survive scrolling past 53 params, and the rail IS the selector, so its highlighted row cannot disagree with itself — a second sticky header is a second answer to one question. It also landed straight in **the 22px trap this file already records**: `<main>` is a padded scroller, so `top-0` pins 22px LOW and the params card header scrolled up through the transparent strip it left. **That is the standing lesson and it is a small one: this file had the trap written down, the code still hit it, and a screenshot found it in one look where reading the diff had not.** Render the thing. Selection lives in `?bot=` (so a refresh cannot move you to another bot's promote button) and the page's `setTab` MERGES the query string instead of rebuilding it, or leaving the tab would drop the selection. Both confirm dialogs now NAME the bot — with a selector above them the bot is a choice made a scroll ago and no longer on screen. Frontend typechecks and builds. Earlier the same day: 🔴 **AN EM-DASH IN THE BOTS PAGE'S BALANCE COLUMN WAS THE ONLY THING IN THE WHOLE SYSTEM REPORTING A BOT THAT HAD BEEN BLIND FOR 50 MINUTES.** MetaTrader auto-updated itself on the VPS and restarted, taking the running bot's terminal connection with it. The bot stayed alive and kept stamping its heartbeat, so the watchdog saw a healthy bot, the process list still had it, and this row said **RUNNING** — while it received no bars at all across an open session. `BotStatus.mt5_link` now carries the answer and Monitor renders a **`No MT5 link`** chip. ⚠ **The chip sits BESIDE the Running pill rather than replacing it**, because both are true simultaneously and they are different questions: the process is ALIVE (so a restart is the fix, and the watchdog was right not to fire) and it is BLIND (so it is trading nothing) — collapsing them into one word loses whichever half the reader came for. ⚠ **`=== false`, never falsy** — the same rule the sidebar's `mt5_connected` follows, in this same file: `null` means the bot has not stamped a link state, and painting a healthy bot as disconnected is the identical mistake in reverse. ⚠ **The balance cell says `no link` in `warn` rather than an em-dash**, so the two causes of a missing number cannot look the same again, and the tooltip names the next action because the runner self-heals. Full detail in *A blank cell is not a diagnosis*. Same pass, this file's Bots row was corrected — it claimed "no bots registered yet" while one had been live since 2026-07-31 — and now names `DeployCard` (deployed hash / commit / params, with a previewing Promote button) and the G11 caveat that Configure is a flat per-bot stack with no selector. **The standing lesson is NOT this folder's usual label-vs-code refrain: every layer beneath that cell behaved defensibly on its own** — an empty bar frame is a fine thing for a data call to return, a null balance a fine thing to write when you have none — **and the defect was that "no data" and "cannot ask" were the SAME VALUE at every hop**, so by the time it reached the browser the distinction did not exist to render. When a cell can be empty for two reasons, the API has to say which. Earlier: 2026-08-03 — 🔴 **the Costs pill and the Net hero were reporting different quantities under names that let a reader treat them as one.** Turning spread + swap on for run `75ccc776d10c` drops Net by **$18,200,741** while the pill read `Charging $332,371`; both are correct — the first is the balance impact, the second the fees actually paid — and the page showed neither the relationship nor the reason (**98.2% of that gap is lost compounding**, demonstrated by re-walking the same trades and fees at 1% risk instead of 10%, where the gap falls from 64.5% to 11.0%). Worse, the footer captioned the fees `after compounding`, the one description that does not fit them. **The headline is now R** (`Charging 12.08R`): a pill holds one number, and R is the only one that cannot contradict the page, is what the `CostRule` rows above sum to, and is comparable between runs. The popover names all three through a new `Figure` row — **Charged / Fees charged / Net before → after** — plus the ratio in words. `useCostFilter` returns `netBefore`/`netAfter`/`balanceImpact` **summed off the same rows `buildFilteredRun` sums**, so the pill and the card cannot drift, and the `Costs charged` KPI row became **`Fees charged`** because the old name invited exactly the subtraction that makes the two look contradictory. Full detail in *Costs are switchable in TWO places*. **The standing lesson is a new one and it is not this folder's usual label-vs-code refrain: every number here was correct and every label defensible on its own, and the page still misled — because two true figures of different KINDS shared a footer and the reader was left to subtract. A control that changes a headline has to state what it CHARGED and what it MOVED, under names that cannot be swapped.** Earlier the same day: **the Price tab opens instantly, because the run page now builds it in the background on arrival.** Aaron's ask: land on a backtest, and by the time you click Price it should already be there. **MEASURED on run `432aff31f374` (32,978 M15 candles): 2,453 ms from click to a painted chart → 167 ms.** ✅ **The finding worth carrying is that the wait was THREE costs, and the obvious two were the small ones.** A ~3.5 MB `ChartSpec` fetch (0.39 s served from the run dir's cache, **7.6 s on a cold build**, plus the browser's parse), the lazy klinecharts chunk, and — the bulk of it — **klinecharts laying 33k candles and their overlays out on MOUNT**. Prefetching the spec and preloading the chunk together bought only 2,453 → 1,964 ms; the remaining ~1.8 s could not be prefetched at all, because **a build cost is only payable by mounting the thing.** So `ChartTabPanel` gained `keepMounted`, and the Price tab now stays mounted while another tab shows. ⚠ **The hidden tab is `visibility: hidden` + `position: absolute`, deliberately NOT `display: none`** — a display-none container measures 0 wide, so klinecharts would size its canvas to nothing and need a resize on reveal, which is the swap this change exists to remove. `visibility` keeps the real WIDTH (verified: canvas 1033 px before and after the reveal, one instance throughout — 2 canvases, never 4) while contributing no height, and it also takes the panel out of the tab order and the accessibility tree. ⚠ **The warm mount is DEFERRED to an idle beat (floor 1.2 s), not done on mount** — klinecharts blocks the main thread while it lays out, and the equity curve is what the reader is actually waiting for on arrival. ⚠ **The prefetch is `prefetchQuery`, never a page-level `useChartSpec`** — same cache entry, but without subscribing the whole page to a multi-megabyte object it never renders; it also swallows its own errors, so a failed prefetch stays invisible and the panel reports the real error on mount. ⚠ **The cost is real and is the trade Aaron asked for: a run page now always fetches the spec and builds the chart, even if the reader never opens the Price tab.** `StackDetail` and `StressTestDetail` pass no `keepMounted` and are untouched. Verified in a real browser: hide → reveal cycle 76 ms, fullscreen resizes 1033 → 1555 and back, no console errors, and the equity tab's layout is byte-for-byte what it was. Earlier the same day: **the price chart draws ORDER BLOCKS** (Analysis → Order Blocks, default OFF), the supply/demand zones that were live when a trade fired, refused or died. **The frontend interest is entirely in what it did NOT need:** no new overlay template, no new render effect, no new concept — one string appended to `ANALYSIS_GROUPS` in `overlays.ts`, a colour beside it, and the generic `box` pipeline drew, clipped, counted and toggled the layer. That is the second entry in a list built for exactly this when the gap layer landed, and it is the shape to reach for before writing a template. ⚠ **It is deliberately NOT in Deep debug** (Aaron's call) — `DEBUG_ON_GROUPS` reads `ANALYSIS_GROUPS[0]`, so a new analysis layer goes on the END of that list and joins the preset only when someone decides it belongs in the every-trade reading. ⚠ **The one change to shared code is `BoxOverlay.label` / `labelAlign`**: an order block carries mpc's `OB` tag right-aligned, because a zone's LEFT edge is its anchor candle and a tag there sits on price. **And exercising that path for the first time found a live bug in it.** The generic `BOX` label figure had never been used by any emitter and shipped carrying klinecharts' default overlay-text style — a solid BLUE chip — so the first `OB` tag rendered as a blue pill; the `HLINE` label path carried the identical bug, still dormant. This folder's own CLAUDE.md had recorded the trap for the `LABEL` template and it applied to both, unnoticed, for months. All three now spread one `FLAT_TEXT` constant. **The standing lesson: a generic mechanism nobody has exercised is not a working mechanism — it is untested code wearing a general-purpose name, and the first real user is its first test.** Verified in a real browser on run `432aff31f374`, Order Blocks and Fair Value Gaps drawn together. Earlier: 2026-08-02 — **the price chart's Analysis menu grew a Deep debug toggle** — one row that switches the context you want behind any trade you are interrogating (Fibs, External Structure, Fair Value Gaps), so reading a run one trade at a time stops being three switches across two dropdowns set and unset constantly. **The transferable part is how many shapes it took in one day before it was right:** a segmented `Winners | Losers` pill beside the menus, then a four-way `Winners / Losers / Both / Off` radio inside Analysis, then one additive on/off row. Both earlier shapes OWNED THE OUTCOME FILTER, and that is exactly what was wrong with them — they asked "winners, losers or both" in a second place that could disagree with the rows below, and they forced the unanswerable "what does OFF restore?" (the first build shipped with no way out at all). **Additive has neither problem: it never decides which trades are drawn, so the filter keeps one home, off means off, and Step re-scopes off the same rows it always did.** On/off is DERIVED from the layers themselves, so unticking one by hand unticks the row — a remembered flag is how a label starts claiming something the chart is not doing. ⚠ **The write is unconditional but the READ is not:** setting a layer the run never emitted is inert, while a read over layers that cannot exist is vacuously TRUE and would pin the row ON for ever, so `debugAvailable` hides it on a run with nothing to deepen. It reuses the shared `ToggleMenu` rather than becoming a fourth hand-rolled dropdown, which cost two new `MenuItem` fields — `section` (caption + rule) and `action` (a shortcut row, kept OUT of the header's `on/total`, which must go on describing what is drawn). Verified in-browser on run `211384ddbea4`, including that Winners/Losers and Step are untouched by it. Detail in `ChartPanel/CLAUDE.md` → *Debug*. Earlier: 🔴 **the price chart emptied itself when you scrolled back past the shipped candles, and every toggle went on claiming otherwise.** Structure, Fair Value Gaps, Blocked and Missed are all emitted PER-WINDOW by the backend, while the panel pages bars back to the run's start — so past that boundary the layers drew nothing with their switches still ON, which reads as the panel forgetting the settings. A page now fetches its own analysis and the panel merges it (`ChartPanel/CLAUDE.md` → *Paging older history*). **The frontend half generalises past this chart: a roster DERIVED from data must be RECONCILED, never re-seeded.** `groupsOn` was rebuilt from `overlayGroups` on every change, which was safe only because that list never changed; the moment a page could rebuild it, `setGroupsOn(defaults)` would have switched the reader's layers off mid-scroll — the same bug from the other side. `reconcileToggles` keeps an answer already given and defaults only genuinely new keys; the miss-noise seed follows the same rule, seeding each label once so a reason ticked back on is never re-hidden. Verified in-browser on run `211384ddbea4` at 2024-05→06, nine months before the shipped window: BOS/SOS lines, HH/HL/LH/LL tags, gap boxes and pink Blocked markers all draw with Winners still filtered off — bare candles before. Earlier: 🔴 **two sidebar dots were rendering their field faithfully and the field was measuring the wrong thing.** The **SSH** dot's `ssh_tunnel` came from a fresh `ssh forexvps "echo ok"`, which has nothing to do with the port forwards, so after a laptop sleep it sat green beside two red agent dots and pointed at the VPS instead of at the dead tunnel on this laptop; the **MT5 Agent** dot read the Flask agent's `/health`, which answers `ok` whether or not the terminal is logged in, so a disconnected MT5_Lab showed green while every python run needing uncached bars failed at fetch time. Both are three-state now — SSH: green forwards up / **yellow tunnel down but VPS reachable** (the backend supervisor rebuilds it, so yellow means *wait*) / red VPS unreachable; MT5: green agent+terminal / **yellow agent up, terminal not connected** (needs RDP) / red agent down. ⚠ **`mt5_connected` is `boolean | null` and every check is `=== false`, never falsy** — `null` means the agent could not be asked, and rendering an unanswered question as a failure invents a measurement (the same rule `DrawdownMeter` follows for an unmeasured tail). Detail in *Two dots that were not measuring what they said*. Earlier: 2026-08-01 — **the News & Holiday filter now defaults to OFF on both rules, so a run page opens on the run exactly as traded.** Holidays had defaulted ON and news followed the strategy's `avoid_news`, which meant the headline figure on screen was not the backtest's own result — and, worse, the default DIFFERED BETWEEN STRATEGIES, so two runs over the same window could open on different trade counts with nothing on screen explaining why. Turning a rule on is a deliberate what-if now. `useNewsFilter` no longer takes `avoidNews`. Same day: **`FitMoney` never abbreviates again** — the *Made* hero shows `+$14,387,475`, not `$14.4M`. That was not a style choice but a measurement bug with a plausible-looking output: it compared the text against ITS OWN span, which is a content-sized flex item whose width IS the text's width, so "does it fit" was true by exactly `FIT_SLACK` on every value forever. It measures the hero ROW now (`data-fit-box`) and shrinks the TYPE rather than abbreviating; `dollarShort` is deleted. **The repo had already recorded this trap for `PanelRow` values and it applied to the hero too** — reason enough to check every remaining content-sized measurement. Same day: **the *Made* hero is the DOLLARS, with `from $10,000` under it** — the multiple (`1439.7x`) was the hero and the starting balance appeared NOWHERE on the page, so it was a multiple of a number the reader could not see; it is the first row now. A **`Costs charged`** row landed with it (shown only on a priced run — `$0` everywhere would read as "trading is free"), plus a **`Won / scratched / lost`** row beside the win rate and a **`Top 5 trades`** row beside profit concentration, both because the numbers above them were TRUE and were being misread. Detail in `## Backtest detail — chart and KPI conventions`. Earlier: **the price chart got a Step navigator: `◀ Loss 12/60 ▶` walks the markers, previous / next, centred.** Reading a run's losers back to back was a scroll hunt across years of bars; it is now two clicks or ← / → with the pointer over the panel, and a step into unloaded history pages the bars in through the SAME `goToDate` the date pill drives. **The design decision to keep: it has no set of its own — it walks whatever the Analysis dropdown is SHOWING.** Untick Winners and ◀ walks the losers; turn Trades off with Blocked on and it walks the refusals; leave both on and it interleaves them by time. Giving it its own filters would give the navigator and the chart a way to disagree, and would let it step to something that isn't drawn. Verified on run `0e3983a0c3c7`: 164 markers → 104 / 60, then 138 with Blocked added, stepping Loss → Blocked → Loss with the park surviving the filter change. Detail in `ChartPanel/CLAUDE.md` → *Step*. Earlier the same day: **the price chart draws fair value gaps, and the panel grew one new concept to hold them: `ANALYSIS_GROUPS`.** The backend emits a gap only where it was live at a trade / blocked / missed setup, tagged into a `Fair Value Gaps` overlay group. The panel needed **no new overlay template and no new render effect** — it is a plain `box` group, so the generic pipeline already draws, clips and toggles it. The only addition is the list in `overlays.ts` naming which overlay groups belong in the **Analysis** dropdown rather than Structure: one roster still backs `groupsOn`, only the MENU each row appears in differs (`structureGroups` / `analysisGroups`). Default OFF with its box count, like Blocked and Missed, and last in the menu because it is the CONTEXT around those three rows, not a fourth kind of signal. Adding a second analysis layer is one string. Detail — including why the drawn gaps are the INDICATOR's and not the bot's — in `ChartPanel/CLAUDE.md`. Earlier: 2026-07-31 — **the frontend had two private copies of Sharpe and neither zero-filled flat days, so every number they produced was inflated ~3x.** The backend has always zero-filled every weekday in the span (`metrics.zero_filled_daily_values`); `computeFallbacks` and `StackDetail.composeCombined` each scored only the days that CLOSED a trade, then annualized by √252 — 142 active days in a 1,447-weekday span read **2.96 against a true 0.91**, and a stack read **13.06**. It surfaced as a news-filter delta of +2.07 from removing 3 of 142 trades, which is the tell: the filtered side fell back to the frontend formula while the unfiltered side used the stored backend one, so the "delta" was two formulas, not a change. `dailySharpe()` is now the single frontend definition and reproduces the stored value to 15 significant figures. `FallbackMetrics` also lost `worstStreak` — a trades-labelled row has no honest answer from a day list, and `worstLosingStreakOf(pnls)` off trades replaces it in both synthesizers. See `## Backtest detail — chart and KPI conventions` → *Metrics that were saying the wrong thing*. Same day: **the verdict left its full-width bar and became the FIRST card**, leftmost, which takes the section to **180px collapsed / 305px expanded** at 1670 (from 234 / 345, and 318 / 496 on this panel's first build). The bar was charging a whole row — 44px plus its gap, in both states — on a panel whose point is fitting on one screen with the equity curve. **The move was only safe because the content changed shape with it, and that is the transferable lesson:** the rules were inline pills laid out by wrap, fine at full width and five lines at a quarter of it, and since the grid is `items-stretch` a tall fourth card drags the other three with it — as rows each rule is 24px whatever it says. The card anatomy (`panelCardCls` / `CardHead` / `CardHero` / `PanelRows`) moved to module scope so the fourth card cannot drift from the three beside it; `verdict` and `ribbon` are separate props because two callers still want a bar (a stack's strategy legend is genuinely horizontal, an optimizer combo has no verdict at all); and breakpoints are set by the longest real rule label, measured at 118px. See `## Backtest detail — chart and KPI conventions` → *The verdict is a card, not a bar*. Earlier the same day: **the Performance panel's rows became label + ⓘ + number, and validating its numbers turned up three that were saying the wrong thing.** Every explanation moved onto the label's tooltip, which deleted the ragged right column and the re-explaining suffixes in one move (`4 days · consecutive losing`); the panel now collapses to its three heroes + the drawdown meter, default ON, so the equity curve shares the fold. The three metric bugs: `worst_losing_streak` counts **trades**, not days (the real worst run of losing calendar days was 2, not 4); time underwater is weighted by the **calendar**, not by row count (`daily_pnl` omits flat days, so 67% "of days" was 67% of ACTIVE days — 71% by the clock); and profit concentration was measured in **dollars**, which on a compounding account reports the compounding — 89% ("edge clustered — overfit risk", the page's only warning colour) against an honest 40%. Plus `fmtDate` parsed dates as UTC midnight and printed a day early in five separate copies. See `## Backtest detail — chart and KPI conventions`. Earlier the same day: **the Evaluation + Performance panel was rebuilt as `PerformancePanel`: a verdict ribbon over three question cards** (Made / Risked / Trusted), replacing the 6+6 `KpiGrid` and its evaluation card. That layout caused all three standing complaints at once — cropped values (a fixed `KPI_ROW_H` on variable content), visibly uneven cards (`KPI_COLS` widened for one long money value), and an empty evaluation box on `unconstrained` — and none was fixable by resizing. Metrics group by the question they answer, so every one fits at once: **the expand toggle and both fixed heights are deleted**, `StackDetail` included. Two rules landed with it: **colour marks the exception, not the sign** (a wall of green ranks nothing, Worst Day can only be negative, and Sharpe 0.91 is positive AND weak — soft numbers say so in words), and the new **`DrawdownMeter` may never invent its references** — the gold limit tick only when the ruleset states a peak-% limit, the hatched tail only from a `dd_basis === 'percent'` stress test, otherwise it says the tail is *unknown, not zero*. The trade count became the ribbon's anchor with its `≈2/month` cadence. See `## Backtest detail — chart and KPI conventions`. Earlier: 2026-07-30 — **Max Drawdown and Calmar were measuring against a static account balance and both were wrong on any compounding run** (1096.7% and a red 0.11 on a run whose true figures are 54.9% and 2.25). Both now divide by the running PEAK — see `## Backtest detail — chart and KPI conventions` → *Drawdown is peak-relative*. Same day: the price chart's **scroll-left paging shows itself** (the blank strip you scroll into is shaded from the oldest loaded bar back, with a `Loading earlier bars…` chip — see `ChartPanel/CLAUDE.md` → *Paging older history*), and the News & Holiday filter **stopped duplicating the KPIs and now reshapes the real ones**. It has no section of its own: it is a pill on the empty half of the **Performance** header, driving the actual `PerformancePanel` (via a synthesized filtered `Run`) plus the Equity chart, with each card's caption swapped for its delta vs unfiltered. Bank holidays became a real checkbox instead of a hidden always-on rule (and both rules default OFF as of 2026-08-01), every label became a COUNT rather than a state word, and `exit_ms` on `EquityPoint` made **Avg Trade** computable over a subset. See `## The News & Holiday filter` below — especially the four things that deliberately do NOT follow the filter. Earlier: 2026-07-29 — the price chart's **fib levels are configurable** (add / remove / retune / recolour / hide, per drawing or as the tool's persisted default), and the News & Holiday filter became a collapsed-by-default accordion whose state lives in a page-level `useNewsFilter` hook, so the MAIN Equity chart redraws on the kept trades (its own duplicate mini-curve is gone); 2026-07-28 — price chart: a **Go to date** pill that jumps the view to a typed date (paging history in on the way); earlier, the Analysis dropdown (Trades + Winners/Losers, Blocked and **Missed** + per-reason filters), and it now ships/opens on the run's own timeframe with older history paged in on scroll-left
