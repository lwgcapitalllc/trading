# ChartPanel Build Notes

**Status:** ARCHIVE - relocated history, deliberately. Implementation-level history and
war-story detail moved OUT of `command-center/frontend/src/components/ChartPanel/CLAUDE.md` on 2026-08-12 so that file could stay standing rules
and current status. **Nothing here was deleted from the record** - same pattern as
`command-center/docs/BACKEND_BUILD_NOTES.md`.

**Why it moved.** `command-center/frontend/src/components/ChartPanel/CLAUDE.md` had grown to 150,692 bytes, most of it dated narrative,
and every byte loaded into context whenever anyone worked in this area. Worse than the size:
the diary sat in 7 paragraph(s), the largest **9,974 bytes on a
SINGLE LINE** - not readable by a person at all. A rule nobody can find is not a rule.

**What is here.** The entries below, verbatim and unedited, newest first. Any RULE they taught
stays behind in `command-center/frontend/src/components/ChartPanel/CLAUDE.md`; this is the evidence, not the instruction.

---

**Last reviewed:** 2026-08-11 (latest) — 🟢 **THE FIBS LAYER SERVES TWO BOTS NOW, AND THIS PANEL CHANGED NOTHING TO GAIN THE SECOND.** A B-LEG run offered no Fibs row at all — `tradeFibCount` was 0, because that bot recorded no ladder — while being the more fib-native of the two (entry 0.5, stop 1.0, TP1 0.0 of the frozen SOS leg). The fix is entirely in the strategy package; `TRADE_FIB` reads `(ratio, price)` pairs and knows nothing about which bot produced them, which is the whole payoff on having built it strategy-agnostic. ⚠ **BOTH bots' ladders are measured from the leg EXTREME, deliberately** — `b_leg`'s own code names its band from the leg ORIGIN, the opposite end, and recording it that way would have shifted every rung onto a different retracement while still looking like an ordinary fib. **The visible consequence: on a B-LEG trade the band's far edge draws as 0.618, not 0.382.** ⚠ **Every recorded ratio is one of the eight in `DEFAULT_FIB_LEVELS`, so none falls through to `UNNAMED_LEVEL_COLOR`** — held by a test on the STRATEGY side, and a future bot recording a rung outside that set draws grey, which is both the honest fallback and the tell. ⚠ **No chart rebuild can add this layer to an existing run** — the ladder is written into the equity curve at REPLAY time, so a run predating a bot's recording needs a RERUN. 🔴 **The browser check for it hit the EMPTY-VIEWPORT trap on its first run: this bot takes ~2 trades a month, so the opening viewport usually holds none, and a pixel count on an empty viewport is pixel-identical to a layer that does not draw. Third instance recorded in this file** — the Candlestick Reversals suite already carries the warning, which is the argument for reading a sibling spec before writing a new pixel check. Earlier: 2026-08-10 (latest) — 🟢 **THE ANALYSIS LAYERS WORK ON A PORTFOLIO STACK, AND THE TWO FILTER RULES ARE OPPOSITE ON PURPOSE.** A stack's merged spec now carries every leg's blocked setups, missed setups and anchored overlays (backend: `../../../backend/CLAUDE.md`), so this panel had to learn which strategy each belongs to. ⚠ **`overlayLayerVisible(ov)` is ANY of `ov.layers`, and an overlay with none always draws.** A gap, an order block or a liquidity level is one market fact several strategies can have fired into, so isolating a leg must not remove a zone the other leg traded — and a structure overlay belongs to no leg at all, which is why the absent case is *draw*, not *hide*. ⚠ **A block and a miss carry a single `layer` and are gated INSIDE `blockVisible` / `missVisible`**, not at the draw site: those two predicates also back the menu counts and the Step navigator, and a navigator that can park on a marker the chart is not drawing is the failure Step's whole no-filters-of-its-own design exists to prevent. ⚠ **`hiddenLayers` is declared above `blockVisible`, further up than it belongs by subject**, because three dependency arrays name it — the TDZ trap this file already records twice. ⚠ **`MarkerTip.strategy` names the leg on the hover card.** On a merged chart *whose rule refused this* is the question, and it is the reason these layers were withheld from a stack in the first place. ⚠ **Candlestick Reversals are NOT carried onto a stack**, and it is a real refusal rather than an oversight: `spans` / `deepestOf` / `deepestNames` are indices into one run's own anchor list, which a stack re-sorts and filters. Earlier: 2026-08-08 (latest) — 🟢 **`toolActions` — A HOST ACTION ON THE TOOL STRIP, SO IT SURVIVES FULLSCREEN.** Aaron: *"allow me to rebuild chart on full screenview also it only allows me to do on minimized view"*, then *"put the build chart on the left nav pane closer to the setting button"*. **Rebuild chart** lived in the HOST's tab strip, and this panel goes fullscreen by `position: fixed` over the whole app — so expanding the chart, which is exactly when a reader is looking closely enough to notice the marks are stale, took the only way to rebuild them off screen. It is now one icon button in the strip's bottom cluster, directly above the Chart settings cog. ⚠ **It was MOVED, not copied** — a second copy in the tab strip is two controls firing one action, the argument that already removed the fib tool's own gear from this strip when the ladder went into Chart settings. ⚠ **It is NOT a header slot, and `headerTrailing` could not have held it either**: that one is absolutely positioned over the price-axis COLUMN (`axisW`, ~28-70px) so the minimize X lands above the price scale, and anything wider than an icon overflows it. The strip is the honest home — it is INSIDE the panel, so one button covers both views, and it is where a reader already looks for controls about the chart rather than about the run. ⚠ **Icon-only by necessity: the strip is 40px**, so a labelled button would either overflow it or widen the strip on every chart in the app. ✅ **`tests/chart-rebuild-fullscreen.spec.ts`, WATCHED RED against HEAD.** ⚠ **Its locators are scoped to the panel root (`[data-applied-lo]`) and HAVE to be** — anything the host renders is outside that root, and in fullscreen the host's chrome is still in the DOM behind the overlay, covered rather than hidden, so a page-wide `getByRole` matches it and passes against the broken page. **Fourth instance of that trap in this app** (the sidebar logo as `svg.first()`, the page header's own Retry, the risk card's own Deploy). ⚠ **It also asserts a count of exactly ONE**, which is the only assertion that says the button was moved rather than duplicated. Earlier: 🔴 **A DRILL-DOWN KEPT THE RUN'S OWN STRUCTURE OVERLAYS AND DREW THEM ON THE FINER CANDLES.** `allOverlays` was hardwired to `spec.overlays` with no timeframe gate, so drilling a 15m run to M5 replaced the bars and left the M15 replay's swings sitting on top of them — every label at a price that is not a swing on anything on screen (`SOS @4242.99` over M5 candles whose own answer is `4224.73`, which is what TradingView draws). Aaron reported it as the chart disagreeing with `mpc_jarvis.pine`. The drill-down page now carries its own `overlays` (see `../../../../backend/CLAUDE.md`) and the panel swaps them in for the spec's. ⚠ **The swap is gated on `isFetchMode = selectedMin < baseMin`, so a COARSER resample (M30/H1/H4/D1) still draws base-timeframe structure — recorded, not fixed.** Same defect, opposite direction. ⚠ **`overlayKey` had to be rewritten and the reason generalises past this chart:** it enumerated the point-like `type`s by name and read `.t0`/`.t1` off everything else, which broke the moment `CandleOverlay` joined the `ChartOverlay` union. **In TypeScript that is a compile error; the identical shape reaches for a missing field in silence in any language that lets it.** It narrows on `'t' in ov` now — by what a variant HAS, not by a list of the ones that existed when somebody wrote it — so a future variant cannot land in the wrong branch. ⚠ **A drill-down with no `overlays` from the backend draws NO structure, deliberately, rather than falling back to the spec's** — a missing field is *this backend predates it*, and answering with the base timeframe's swings is precisely the defect. **The standing lesson: structure is a property of the BARS, so anything derived from them has to be recomputed whenever they change — a layer that survives a candle swap is a layer describing candles that are no longer there.**

🔴 **RESETTING THE CHART VIEW LEFT THE PRICE AXIS WHERE YOU DRAGGED IT, AND THE NAMED CANDLE WAS PICKED BY TIME RATHER THAN BY PRICE.** Aaron, two bugs off two screens. **(1)** *"if I mess with the zoom on the chart I expect it to reset to default. Right now I think it puts me back to the default date but not zoom"* — `resetView` set bar space, right-offset and scroll-to-realtime, **all three of which are the TIME axis**. Dragging the price axis (or panning vertically once it is manual) switches klinecharts' y axis off auto-scale and it keeps that range for ever, so the reset returned you to the right date on a chart showing **5,100–5,460 with the market at 4,252 — i.e. an EMPTY chart**, which reads as the reset breaking it. ⚠ **klinecharts exposes no `resetYAxis`**: the only public route is `setStyles` with a valid `yAxis.type`, which flips the candle pane's `autoCalcTickFlag` back on — passed the type the chart already uses, so it is purely the reset. ✅ **`tests/chart-reset-view.spec.ts`, WATCHED RED against HEAD**; it reads PIXELS because the applied window has a DOM seam and the y RANGE has none. **(2)** The chip's candle now reads `deepestNames[anchor]` with **no fallback to `label` when the field is present** — an object missing this anchor's key is the backend saying the span held nothing but OPPOSING candles, and falling back would put that candle's name straight back on the chip. The chip has three states now: `no candle` (nothing in the span), **`no matching candle`** (marks drawn, none pointing this outcome's way), or the name. Derivation and the measurements: `../../../backend/CLAUDE.md` → *"Deepest" is a PRICE*. ⚠ **Existing runs need Reload charts.** Earlier: 🔴 **A WINNING LONG WAS NAMED `Won · Bearish Engulfing`, BECAUSE THE CHIP'S CANDLE WAS PICKED BY NEARNESS RATHER THAN BY DIRECTION.** Aaron, off two screenshots. The three-tier direction preference was real and only ever ordered the patterns ON one bar — **which BAR became the span's `deepestOf` was still whichever pattern candle sat closest to the turn**, and that is the one the chip names and the one *"Only the deepest"* leaves on screen. ✅ **A winner and a 3/3 miss now want the setup-ALIGNED candle and a LOSER wants the OPPOSING one** (*"if I lost it should default to the candle that signaled why I lost"*), measured at **69 → 98 named as asked and 44 → 10 named against**, over 194 anchors. ⚠ **The panel's half is one line: read `deepestNames[anchorIndex]` before `label`** — one bar can be the deepest of a losing trade and a miss on the same leg, which want opposite directions, so the bar's single `label` is whichever anchor reached it first. ⚠ **Nothing is painted differently**; the direction filter is still the only thing that hides a mark. Derivation, the three tiers and the mutation proof: `../../../backend/CLAUDE.md` → *Which candle a setup is NAMED after*. ⚠ **Existing runs need Reload charts** — the spec is cached. Earlier: 🟢 **THE ANALYSIS MENU HAD GROWN INTO NINETEEN ROWS OF ONE FLAT LIST, AND FIXING THE LAYOUT SURFACED A NUMBER THAT WAS DESCRIBING A SET NOBODY COULD SEE.** Aaron, off a screenshot: *"this looks very cluttered and confusing… should it all be together or no?"* ✅ **It stays ONE menu** — Step walks whatever this menu shows, so splitting it splits Step's set across two controls, and layer state having one home is the argument that already killed the Deep debug radio. 🔴 **The real defect was under Missed, and it was not styling: the SCORE chips and the REASON chips are each a COMPLETE PARTITION of the same setups** — measured, 35 + 417 = 452 and 179 + 238 + 21 + 10 + 4 = 452 — **stacked as one indented column of seven, so they read as seven sub-filters of one thing and the counts read as double-counting.** A filter's values are now wrapped CHIPS under their own caption (`Score` / `Missing` / `Show` / `Refused by` / `Direction`), which is what says the two answer different questions; layers took `Signals` / `Context` captions and the three liquidity tiers a sub-group that drops the shared prefix from their labels. ✅ **MEASURED: 862px → 741px fully expanded, and the header count went `18/25` → `4/10`** — the old one was counting filter VALUES as layers, so "18 of 25" described nothing. The menu is now bounded at `min(70vh, 620px)` and scrolls, because at 741px against a 940px viewport, hanging off a header that is rarely at the top of the page, the bottom rows could not be reached at all. 🔴 **THEN AARON READ THE NEW LAYOUT AND FOUND THE REAL BUG IN IT: *"if I have on missed 3/3 or 2/3 shouldn't the knobs that influence that toggle accordingly also?"*** With `3 of 3` alone the layer drew **35** markers while the MISSING chips went on reading 179 / 238 / 21 / 10 / 4 — the whole 452. **A chip's number is a claim about what ticking it would change, so conditioned on nothing it is a claim about markers that are not on the chart.** Each axis now counts what the OTHER is letting through: with 3/3 alone, `No FVG in zone` and `No retrace` read **0** — which is itself the answer, since a 3/3 met all three confluences and cannot be missing either — and `Final hour 21 + Never filled 10 + Divergence 4 = 35`, the layer's own count. ⚠ **The ROSTER stays separate from the COUNT**: shrinking it to the values present in the filtered subset would delete a chip the instant its count hit zero, and a control that disappears at zero is one the reader cannot use to get back. ⚠ **The score chips were ALREADY cross-filtered by this fix and it shows a second real fact**: `2 of 3` reads 179 rather than 417, because `No retrace` starts hidden by the emitter's `missNoise`, so 238 of them are not drawable. ✅ **9 browser checks green; the new cross-filter one WATCHED RED against HEAD**, where the reason count sits at 179 and never moves. 🔴 **The TDZ trap from yesterday's entry recurred and `tsc` caught it this time** — the two count memos now read each other's hidden-set, so both `useState`s had to move above them; a direct reference is a compile error where the same mistake inside a closure was not. **The standing lesson is about cross-filtering rather than layout: the moment two filters compose, every count is a statement about what the OTHER filter is showing — and the honest layout is what made the dishonest number visible.** Earlier: 🟢 **THE PRICE CHART PAINTS THE CANDLE THAT TURNED PRICE, AND THE FIRST VERSION LOOKED RANDOM BECAUSE OF WHERE IT WAS ALLOWED TO PAINT.** Aaron's ask: highlight the reversal candle at his trades and at the setups he missed, *"so I could do analysis and say, can I add candle patterns as an additional confluence to get better entries or avoid bad trades"*. The canonical `engines/candlesticks/` engine is replayed server-side into one navy candle per setup. 🔴 **The anchor set is the whole feature, and it was wrong: BLOCKED setups were anchors, 324 of them against 159 trades and 35 three-of-three misses — two thirds of every mark — and since the Blocked layer defaults OFF they painted navy candles where the reader could see no setup at all.** Reported off the screen in exactly those words. ✅ **518 anchors / 424 marks → 194 / 153, measured.** 🔴 **The second defect was a `Bearish Engulfing` marking a LONG that WON**: ranking started at nearest-the-extreme, so whichever pattern sat closest won regardless of which way it pointed. Direction is now a three-tier PREFERENCE — aligned, neutral, opposing — with nearness the tie-break WITHIN a tier. ⚠ **The opposing tier is KEPT and is half the point** (*"if not, it will show me why I was wrong"*), so this is a preference and never a filter: **89 aligned / 49 neutral / 15 against / 41 with no pattern at the turn**, measured over the 194 anchors. ⚠ **The NEUTRAL tier does most of the work, and the reason is in the source Pine** — ten of its fifteen rules gate on a trend lookback, so in an uptrend the bullish rules cannot fire at all, which is exactly why the reference mark was bearish. ⚠ **A WINNER is searched over its whole HOLD**, because *"the ultimate reversal point before the trade went into my favour"* is the deepest adverse price of the hold — MEASURED at a median 2 bars past entry but **p90 27 and worst 112**, so the obvious fixed window finds the real turn on barely half of them; a ±2-bar tolerance stops a long hold reporting a distant pattern as its turn. ✅ **Aaron's third question — a 3/3 miss with no mark — was MEASURED rather than reasoned about: all nine unmarked 3/3 misses have ZERO patterns anywhere in their window.** No pattern at the turn is a real answer. ✅ **The pattern NAME is a Chart setting now, default OFF** — the tags carry no cross-overlay de-collision, unlike the batched `LABEL` template, so they write across neighbouring candles; the colour is the marker. 30 backend tests + 4 browser checks, **all proven by MUTATION** (a fail-watch is vacuous for a new module). 🔴 **Two existing browser checks broke and broke CORRECTLY — they read the opening viewport, which held a mark only while the run carried 424 of them, and an empty viewport is pixel-identical to a layer that never draws.** 810 backend tests green, 1 pre-existing failure. **The standing lesson is about scope rather than correctness: every individual mark this layer drew was the right candle by its own rule, and the feature still read as broken, because the rule was allowed to answer a question in places the reader could not see one being asked.** Before trusting a layer that annotates, check that everything it annotates is VISIBLE on the same screen. Earlier: 2026-08-07 — 🟢 **ANY TIMEFRAME TO ANY TIMEFRAME, WITHOUT LOSING YOUR PLACE — AND THE DRILL-DOWN SAYS WHAT IT IS DOING.** Aaron's follow-ups to the fix below, both reported off the screen: *"I should be able to switch from any time frame to any time frame … if candles need to load then put some indicator that is loading"*, and *"it says it is loading then stop and says 'no data here (feed offline, or none this far back?)'"*.

🔴 **A DISPLAY timeframe switch parked the view on the newest loaded bar, which after a jump is mid-history.** ✅ MEASURED with a new `data-view-centre` seam, jumping to 2020-08-05 first: `M15 → H1` landed **42.2 days** out, `H1 → D1` 29.0, `D1 → M30` 43.6 — and it compounds, because the next drill anchors on the drifted centre (`M30 → M5` was **105.7 days** out). 🔴 **The cause was a SECOND `applyNewData`, and it was found by logging the applies rather than by reading the effect**: `apply n=3002` → `flush 2020-08-05` → `apply n=3002` again. `displayCandles` listed `fetched` among its dependencies while the drill effect called `setFetched([])` — a FRESH empty array — on every timeframe change, so the resample recomputed and re-applied *after* `pendingJumpRef` had already scrolled and been consumed. Two fixes: the resample moved into its own `resampled` memo keyed only on what a resample depends on, and the clear became `prev.length ? [] : prev`. ⚠ **They are REDUNDANT and the mutation testing is what established that** — removing either one alone leaves the check GREEN, and only removing both turns it red. Both are kept (the memo split also deletes a wasted 12,000-bar resample per switch, and `pickTimeframe` covers entering drill-down where the memo cannot help), but neither may be described as load-bearing on its own. ✅ Every transition now lands within **0.4 days** of the target. ⚠ **`H1 → D1` still reads 29 days out and that is CORRECT, not a residue**: the applied window is 12,000 base bars ≈ 125 days, which at D1 is ~130 bars — fewer than a screen — so the whole window is visible and there is nothing to scroll. The middle of the plot really is the middle of the window.

🔴 **`available` ON THE BACKEND WAS `bool(candles)` — the same fact as "the list is empty" — WHILE ITS OWN DOCSTRING CLAIMED IT MEANT THE FEED COULD NOT SERVE THE WINDOW.** So the panel genuinely could not tell a dead MT5 agent from a broker with no 1-minute history that far back, and the message hedged: *"no data here (feed offline, or none this far back?)"* — **asking the reader the question the fetch had already answered.** The distinction was destroyed one layer lower, in `chart_spec._build_candles`, which swallowed the exception and returned `[]`. It is `_fetch_candles(...) -> (rows, error)` now; `available` means *the feed could be ASKED*, and `feed_error` names the failure. ✅ **Proven against the live backend: asking for M1 in 2010 returns `available: false` with `HistoryFloorError: XAUUSD has no real 1-minute history before 2018-09-14 on VantageMarkets-Demo (measured, not assumed)`** — which is the whole answer, and is why the badge prints the SERVER's own sentence rather than mapping it to a phrase of ours. `shortNote()` strips the exception class (our plumbing, not the reader's problem) and keeps the date; the full sentence is on the `title`. **The standing rule this is the third layer of: never let "no" and "cannot ask" be the same value — and check it survives every hop, because here it was destroyed in a `try/except` and then re-asserted, falsely, in a docstring two functions up.**

**(1) The drill-down fetched the newest bars, never the ones being read.** `runFetch` anchored its window on `spec.candles[last].time` — the RUN'S LAST BAR — and pulled a fixed lookback back from it (45 days of M1, 270 of M5). So M1 and M5 always loaded the newest weeks whatever the reader was looking at, while M30 and H1 stayed put because they are resamples of bars already in memory. That is exactly how it was reported: *"change the time frame to 5 or 1 mins from 15 mins does nothing… going up to 30mins and 1hr works though."* ✅ **REPRODUCED as a measurement rather than read off the diff: jumping to 2020-08-05 applied `2020-03-19 .. 2020-09-20`, and pressing M5 moved the applied window to `2025-11-09 .. 2026-08-06` with the readout on `2026-08-06 13:15` — a six-year teleport with nothing on screen saying so.** ⚠ **The data was there the whole time** — the same endpoint returns **853 M5 bars** for 2020-08-02→06 — so the lookback constants were resting on a stale belief about broker depth, not on a limit. The window is anchored on the VIEWPORT now, one `FETCH_CHUNK_BARS` (12,000) chunk weighted back past the anchor, and the rest is PAGED like any other history. ⚠ **The anchor cannot come from `visibleCentreTs()`, and that is the subtle half**: that reads klinecharts' visible INDEX range against `displayCandlesRef`, which is only correct while the two agree — and the one instant they do not is a timeframe switch, which is precisely when it is needed. `viewCentreRef` records the timestamp on every viewport change instead, so it survives the array swap. ⚠ **The cache had to be re-keyed with it.** It was keyed on the timeframe alone, on the reasoning that a completed run's window is fixed — true then, and false the moment the window follows the reader: a drill at 2020 would have been served the window pulled for 2026. A hit now requires the anchor to fall inside the range actually fetched. ⚠ **Anchoring ALONE would only have moved the wall, and would have made it dishonest.** A window stopping mid-history has no `hardEdge`, so the red *no earlier data* line correctly does not draw — leaving a blank strip with nothing saying why, which is the failure every paging marker in this panel exists to prevent. Hence `drillOlder` / `drillNewer`, and `pagingOffRef` — the blanket *no paging in drill-down* guard — is **deleted**, replaced by the load callback routing a page to `fetched` or `baseCandles` depending on which list `displayCandles` is reading. ⚠ **`goToDate` gained a drill-down branch too**: it used to degrade silently to a scroll inside whatever the one-shot fetch happened to hold. ✅ **DRIVEN END TO END, and the drive is what found the second half of the fix.** Anchoring alone still landed on `2020-10-22` — `applyNewData` parks on the newest bar it is handed — so `drillTo` borrows `goToDate`'s own `jumpingRef` + `pendingJumpRef` to put the view on the target. Verified: M5 applies `2020-06-19 .. 2020-08-20` with the readout on **`2020-08-05 00:35`** (a 5-minute boundary, volume 436) and M1 on **`00:31`** (volume 81) against M15's 1,499 on the same bar — the bar SIZE read off the rendered readout, because the timeframe button agreeing with itself proves nothing. Scrolling left pages more of the same timeframe (M5 +62.5 days, M1 +25 days). **H4 and D1 were simply absent from `DISPLAY_TFS`** — Aaron asked for them and there was no button; both are ≥ and divisible by M15, so the existing filter offers them, ~1.8 s each.

🔴 **CLOSING THE THREE UNDRIVEN PATHS FOUND A REAL DEFECT IN THE FIX ITSELF, AND IT WAS THE ONE PREDICTED (2026-08-07).** `pickTimeframe` sets `pendingJumpRef` and `jumpingRef`, and the flush that consumes them is keyed on `displayCandles` — **which a timeframe switch does not always change**: coming back from a drill-down to the run's own timeframe hands back the very same `baseCandles` array. So the effect never fired, the guard was never dropped, and `goToDate` returns immediately while it is set — **the chart silently refuses every later jump and every page for the rest of the session, looking perfectly healthy.** A two-frame fallback releases it. ⚠ **The first version of that fallback compared the pending TIMESTAMP and broke the working drill-down**: entering a drill, `drillTo` runs in an effect (before those frames) and sets its own pending jump to the SAME instant, since both anchor on the viewport centre — so the value test could not tell *nobody handled it* from *the drill owns it now*, and the fallback consumed the drill's landing mid-fetch. The hand-off is by SEQUENCE now. **Caught by the suite going red on a check that had been green, which is the argument for writing them.** ✅ **`drillNewer` WORKS and was verified by hand — scrolling right issued 36 further requests at `tf=M5` and carried the window 2021-06-29 → 2024-11-26** — but it has NO committed check, deliberately: three drafts were written and none could be made to bite, and the first of them **passed only while the view was wrongly parked on the right edge, i.e. it would have gone green on the defect.** The reasoning is recorded in the spec file in place of the test. ⚠ **The data-edge check pins the SHAPE, not the `edge` guard** — deleting that guard leaves it green, because the request still returns nothing strictly older and the pager still stops; the guard saves a round trip and is not what makes it terminate. ⚠ **A `drillNewer` visibility guard was written to cut the 11-12 needless round trips a drill landing issues, MEASURED at ~4.5s each — and REVERTED, because re-measuring showed no reduction.** The waste is real and is still there; it is named here rather than left behind an unproven fix. ✅ 6 checks in `tests/chart-drilldown.spec.ts`, 5 proven by mutation and the 6th labelled in its own docstring as pinning something else.

**Earlier the same day:** 🟢 **THE PANEL HOLDS THE WHOLE RUN AND APPLIES A WINDOW OF IT, WHICH IS WHAT MAKES A SIX-YEAR JUMP 2.0s INSTEAD OF 90.3s.** The spec now carries every candle and every overlay of the run (see `../../../backend/CLAUDE.md`), and the panel's job changed with it: `spec.candles` is the SOURCE and `baseCandles` is a **window** of it, `APPLIED_BARS` (12,000) wide. 🔴 **Handing klinecharts the lot is a 30.8-SECOND main-thread freeze** — `applyNewData` lays every candle out synchronously; 155,798 bars measured at 30,828 ms against 2,199 ms for the old 33k spec. With the window it is **508 ms**. ⚠ **This is why `PAGE_BARS` paging survived the spec growing**: the constraint was never the network, it is klinecharts' layout, and `loadOlder` is now a binary search and an `Array.slice` with no fetch at all. **`loadNewer` is new and had to be** — landing mid-history puts the window's right edge in the past, so scrolling toward the present needs a real answer. 🔴 **`goToDate` RE-CENTRES the window; growing it from the right edge gives the right answer in 47.6s**, because target→present is the whole run. It slices `[idx − 0.75·APPLIED_BARS, +APPLIED_BARS]` — weighted BACK, because the reader asked to see a date and what led to it is the context that explains it. ✅ **Overlay creation follows the VIEWPORT, not the loaded history** (`drawRange` off `ActionType.OnVisibleRangeChange`, rAF-coalesced, widened one screen either side so ordinary scrolling never rebuilds; `drawLoTs`/`drawHiTs` intersect it with the loaded bounds and every overlay effect clips to them). That is what lets the spec carry 19,538 overlays — a layer toggle while scrolled back went **133 ms → 67 ms** — and the backend's per-group caps were raised 1,200 → 20,000 on the strength of it, which fixed a silent loss of ~83% of a full run's swing labels, oldest first. ✅ **The per-window ANALYSIS fetch is DELETED**, and with it the `pagedAnalysis` state, `mergePageAnalysis`, `overlayKey`, `mergeById` and `seededNoiseRef` — the spec carries every window's overlays, blocks and misses, so `allOverlays` reads `spec.*` directly. The 2026-08-02 guarantee (a layer reaches exactly as far back as the bars do) is now structural rather than maintained by a merge. ✅ **`reconcileToggles` STAYS and is still load-bearing**: the roster no longer changes as you page, but it must still survive a spec swap without re-seeding the reader's answers. ⚠ **The jump-progress readout is GONE** (`jumping`, `jumpAt`, `MAX_JUMP_PAGES`, the pill's `busy`/`progress`) — it existed because the wait was ninety seconds, and reporting progress on a two-second operation is chrome that flashes. The `LOADING_EDGE` shading remains for a genuine drill-down fetch. ⚠ **`data-applied-lo`/`-hi` on the root are a TEST SEAM with no runtime reader** — the time axis is canvas, so the applied window is otherwise invisible to a browser check, and "the jump was fast" is satisfied by a jump that lands nowhere near the target. ⚠ **A comment here claimed `jumpingRef` was load-bearing against the jump walking forward off its own target, citing a landing on 2021-01-19 — that claim is WITHDRAWN and does not reproduce.** Removing the guard and probing the applied window for 12s gives a dead-stable `2020-01-10 .. 2020-07-16`: the target is centred with ~3,000 bars to its right, so the viewport never reaches the newest loaded bar and no Backward page is ever requested. The guard stays as cheap defence against a re-apply racing a page; the test that pretended to cover it was deleted for failing mutation. **The standing lesson is about reusing a measurement: "browser candles are nearly free" was measured on PREPENDING 12k chunks and applied to one 155k `applyNewData`, and the gap between those two operations is thirty seconds of frozen UI.**
