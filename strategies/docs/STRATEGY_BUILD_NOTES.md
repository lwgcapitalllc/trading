# Strategy Build Notes

**Status:** ARCHIVE - relocated history, deliberately. Implementation-level history and
war-story detail moved OUT of `strategies/CLAUDE.md` on 2026-08-12 so that file could stay standing rules
and current status. **Nothing here was deleted from the record** - same pattern as
`command-center/docs/BACKEND_BUILD_NOTES.md`.

**Why it moved.** `strategies/CLAUDE.md` had grown to 62,335 bytes, most of it dated narrative,
and every byte loaded into context whenever anyone worked in this area. Worse than the size:
the diary sat in 1 paragraph(s), the largest **42,571 bytes on a
SINGLE LINE** - not readable by a person at all. A rule nobody can find is not a rule.

**What is here.** The entries below, verbatim and unedited, newest first. Any RULE they taught
stays behind in `strategies/CLAUDE.md`; this is the evidence, not the instruction.

---

**Last reviewed:** 2026-08-11 (latest) — 🟢 **THE MOST FIB-NATIVE BOT IN THE REPO RECORDED NO FIB, SO ITS CHART DREW NONE — AND THE FIX WAS A CONVENTION DECISION, NOT A MISSING FIELD.** Aaron, off the lab: *"when I do a b leg backtest and I go to the price chart, I don't have any analysis… for fibs it's missing. Doesn't the b leg strategy use the fibbanacci?"* It does, for everything — entry at **0.5** of the frozen SOS leg, stop at that leg's **1.0**, TP1 at its **0.0** — and it recorded nothing, because the ladder is snapshotted in the parent's `_place_entries` and **`mpc_bleg` overrides exactly that method**. The Fibs row was therefore absent on every B-LEG run (`tradeFibCount` 0), which reads as the feature being broken rather than as a bot that never filled the field. ✅ **MEASURED before and after over the same 155,531 M15 bars: 99 trades / 0 with a fib → 99 / 99**, and each CHECKED against the trade it belongs to rather than counted — entry on the 0.5 rung, stop on 1.0, TP1 on 0.0, to 1e-6, on all 99. 🔴 **INHERITING `_freeze_fib` WOULD HAVE BEEN THE WRONG FIX AND WOULD HAVE LOOKED RIGHT**: it snapshots the live **Structure** fib, a different leg on a different bar, so every trade would have carried a real, plausible, fully populated ladder describing something it was never priced against. 🔴 **THE CONVENTION IS THE WHOLE OF THE DESIGN, AND THIS BOT SPEAKS THE OTHER ONE.** `bleg.py` and the Pine call the entry band *the 0.382-0.5 pocket*, measuring UP from the leg ORIGIN; a drawn fib measures DOWN from the leg EXTREME, which is what the A+ bot records and what `BLegState`'s own docstring already assumes when it calls `*_inv` **fib 1.0**. Same two prices, two namings — the record uses the DRAWING one, so a ratio means one thing on a chart showing both bots, and **the one visible consequence is that the band's far edge draws as 0.618 rather than the 0.382 the code calls it.** ⚠ **All eight rungs are emitted and four are COMPUTED**, through the canonical `engines.fibonacci.geometry.fib_level()` and never inline arithmetic, because a four-rung ladder reads as *this trade had no 0.786* when the level exists on that leg and the bot simply did not act on it. ⚠ **The ratios are byte-identical to the A+ bot's and a test asserts it** — which is also why every rung lands on a named factory colour in the browser instead of falling back to grey. ⚠ **The leg is frozen WITH the band and re-frozen on a migration** (the deepest-band rule can replace a band mid-watch, and a kept leg beside a moved band would draw one leg's fib around another leg's entry), and **`*_ext` is NOT `*_tgt`** — the target keeps tracking past the leg after the freeze. ⚠ **An undatable leg records NOTHING rather than a ladder with no start.** ✅ **THE COSTLY HALF WAS FREE: the structure engine has published `bull_bos_h_loc` and its three siblings since it was written**, so anchoring the drawing across the real swing — the option that looked like it needed a shared-engine change — needed none; `Signals` gained four reporting-only `_ms` fields and nothing else. ✅ **NOTHING MOVED, MEASURED rather than asserted: 99 trades / +17.8674R and A+ 159 / +142.1774R, both documented baselines to four decimals, with `compare_bleg.py` exit 0 at warmup 800 and `compare_strategy.py` exit 0 at 1000.** 🔴 **The new tests went RED on the FIXTURE first, and that is the transferable half**: `test_bleg.py` hand-builds its signals as a `SimpleNamespace` and did not carry the four new fields, which `Signals` — a dataclass — always has. **The fix was the FIXTURE, never a `getattr(sig, ..., None)` in the tracker**, which would permanently erase the difference between *no datable anchor* and *wrong object passed* — this repo's own no-vs-cannot-ask rule arriving through a test double. ✅ 7 new strategy tests, **each proven by MUTATION and each caught by the test that names it**, plus 2 browser checks whose first was **WATCHED RED against a run made before the change** — a genuine fail-watch, because the Fibs row and its template both already existed and worked on A+ runs. 🔴 **The second browser check failed first on the EMPTY-VIEWPORT trap** — this bot takes ~2 trades a month, so the opening viewport usually holds none, and a pixel count on an empty viewport is identical to a layer that does not draw; **third instance recorded in `ChartPanel/CLAUDE.md`.** 721 strategy + backtest green, 941 backend green (1 pre-existing failure). ⚠ **EXISTING B-LEG RUNS NEED A RERUN** — the ladder is written into the equity curve at replay time, so *Rebuild chart* cannot supply it. **The standing lesson is about what a subclass silently opts out of: this fork overrode one method to change WHERE it enters and inherited a hole in what it RECORDS, so the bot whose every price is a fib level was the one bot whose chart could not draw a fib — and nothing anywhere errored.** Before trusting that a reporting field is populated, ask which method writes it and whether this class still runs that method. Earlier: 2026-08-07 (latest) — 🟢 **THE BOS GATE RAN FOR THE FIRST TIME AND FOUND THREE DEFECTS THAT 54 GREEN UNIT TESTS COULD NOT SEE.** Aaron took the export; `compare_bos.py` now exits 0 — **6,300 bars compared, no divergence**, at warmups 900 / 1000 / 2000 / 3000, on 7,200 closed M15 bars (2026-04-21 → 2026-08-07) with the Python configured FROM the export's own `cfg_*` columns. 🔴 **It was RED on the first compared bar, and the first defect is about a `var` that never gets cleared: Pine's `bosL_on := false` touches NOTHING else, so `bosL_high` / `bosL_low` / `bosL_bar` / `bosL_n` / `bosL_half` all keep the last leg's values after a death — and `px_l_ext`, `px_ord_l` and `lFibsReady` are plotted from them.** The port rebuilt a blank `BosLeg`, so every bar after the first dead leg diverged (`l_ext: py=None pine=4584.26`). ⚠ **It is behaviour-neutral on BOTH sides — every consumer ANDs with `.on` first — which is the only thing that makes stale numbers safe and equally the reason no behavioural test could ever have caught it.** 🔴 **The second is the sharper one and it is about the HARNESS: `compare_bos.py` read `strat.execution._stage` INSIDE its compare loop, which runs after the replay — so every bar was diffed against the run's FINAL stage.** The run ends flat, so that constant was 0, and the column compared **nothing at all** until a Pine bar happened to report 1 or 2. Sampled per bar into `exit_stages` now. **A harness that reads live state after the fact is not reading the bar it names, and it fails by looking like it passes.** 🔴 **The third killed the run and blamed the innocent half: TradingView appends the still-forming live bar with every plotted series blank — `px_volume` included — so the NaN reached the VWAP engine and raised `VolumeUnavailable`, whose message points at the bar FEED.** `_drop_forming_tail` trims it, says how many, and REFUSES on a blank row in the middle, because only a trailing run is a live bar. ⚠ **One more shape worth naming: the harness carried an accommodation written to match the PORT rather than the Pine** — `ordinal_l` read `... if bos.long.on else 0`, which is precisely the zeroing defect the port had. **A comparator that agrees with the thing it is checking is not a comparator.** ⚠ **THE GREEN IS NARROW AND THE TOOL SAYS SO.** `bos_use_fvg` is OFF at the defaults, so rules 1–5, Method 3 and the Sniper Zone **never ran on either side**; block codes 1/3/4/5/6 never fired, the minimum-stop floor is ON and refused nothing, and only **6 trades** closed in the window. The arming and pricing evidence is 1,220 armed / 1,932 priced bars deep; the exit ladder's is six trades. ⚠ **Below warmup 900 it is RED on the BOS ordinal alone, and that is genuine cold start** — an SOS at bar 856 resets both counters, which is exactly where the disagreement ends, read out of the export rather than assumed. ✅ **8 new tests, ALL 8 watched RED against HEAD**; ⚠ **one of them was VACUOUS on the first attempt and passed against the defect** — it checked the death BAR, where `update()` has already computed the ladder from the still-live leg, so the divergence only starts on the next bar. 258 strategy + 348 backtest green. **The standing lesson is what a parity gate is FOR: this port had 54 green tests and three defects, and the run found all three in one command — two in the code and one in the tool that was supposed to be checking it.** Earlier the same day — 🟢 **THE BOS BOT IS PORTED, RUNS, SWEEPS, AND IS DELIBERATELY NOT VALIDATED — AND THE PORT'S HARDEST PART WAS THE FOUR DEFAULTS IT HAD TO REFUSE TO INHERIT.** Aaron asked for the third strategy's Python side so his brother could sweep it. `strategies/python/mpc_bos/` is built off `indicators/strategies/mpc_bos_strategy_export.pine` on the `mpc_bleg` pattern — a tracker (`BosTracker`: regime, arm, anchor fib, death, F10) plus a thin `Execution` subclass — and it **drives end to end: a 2024 replay makes 22 trades / +13.6R, and a real 4-combo `run_sweep` over 47,259 bars finishes in 33s across cores.** 🔴 **NOTHING IT PRODUCES IS TRUSTWORTHY YET AND THAT IS THE POINT OF THE PASS.** `tools/compare_bos.py` is written and unit-tested and **has never been run**, because no TradingView CSV of that export Pine is on disk — a five-minute human step. The previous port was DELETED on 2026-08-04 (commit `1946f8b`) for producing an 82-configuration sweep nobody could check, and `backtest/tools/bos_sweep.py` was **falsified by ONE Strategy Tester run** the day it was written (20 trades / PF 2.97 against 24 / PF 1.04, same config, entries roughly agreeing and the EXIT LADDER not). ⚠ **So the deliverable here is the gate, not the port.** 🔴 **THE FOUR CONFIG PINS ARE THE TRANSFERABLE HALF.** `BosConfig` is a `SosFadeConfig` superset, so every A+ default it does not re-declare is inherited — **including ones added to the A+ after this fork's Pine was written**, which is the shape that bites. `exec_fib_nearest` (parent True) rests a gap on a DIFFERENT fib from Method 3 and `mpc_bos_strategy.pine` has no such input, so every gap entry would sit at a level the Pine never chose — **and the export has no column for it, so the gate could not see the difference and would blame the entry rule**, exactly the way the `eq_exempt_fvg` incident put `compare_strategy.py` red for three days. `exec_secondary` (parent True since 2026-08-07) needs a second bar stream through `run_dual`, which this fork does not have and `run_sweep` cannot supply — **every BOS sweep would refuse outright**. Also `exec_deep_fib` (this Pine still ships Method 3 ON) and `exec_time_stop_mode` (the 36h plateau was measured on A+ trades; a continuation is a different hold). ⚠ **And the reverse rule: nothing in `config.py` may exist without a Pine input behind it.** The deleted port carried eight research dials with no counterpart, and **a field the export cannot carry is a field `compare_bos.py` can never check** — a run using one is unverifiable by construction. They are recoverable at `1946f8b^`; the honest order is Pine input first, then port. ✅ **Two things were ADDED rather than ported, both because the export carries them and the Python could not express them**: `bos_move_stop`/`bos_move_stop_val` (the A+ config has no moving stop at all, so a run taken with `execMoveStop` ON would have diverged with no field to name), and a THIRD TP rung — the A+ ladder is TP1/TP2/runner and this fork's TP3 defaults to **100%**, so at the shipped settings there is no runner at all. ⚠ **This is the first strategy in the repo that needs VOLUME.** F10 is the session VWAP, read from the canonical `engines/vwap/` (a private one anchored at the break would be a second implementation AND would not be the line on the chart), so `ReplayBar` gained an optional `volume` — **`None` meaning the feed carried none, never 0.0**, because a zero-volume bar is a real thing MT5 reports on a dead session. **A frame without it REFUSES at `run()`**, since both ways of guessing are silent: block everything → an empty book that reads exactly like a strategy with no signals, pass everything → a filter reported as ON and doing nothing. ⚠ **The comparator prints a COVERAGE table and warns when a branch was never taken**, because with `bos_use_fvg` off (today's default) the four gap rules never run and a green gate would be green about a branch neither side entered — the min-stop guard's 2026-08-04 trap, pre-empted. **The Sniper Zone branch is unverifiable at all today**: the export carries no `px_sz_top`/`px_sz_bot` and says so in its own header. ✅ 50 new tests (246 strategy + 348 backtest green); **two test FIXTURES were caught being wrong by the code rather than the other way round** — one asserted the per-anchor 0.5 latch stays unset on a break bar that already dips into its own band (Pine 3590 latches there, legitimately), and one filled the export's price columns with `0.0` where the Pine emits `na`, which the comparator correctly called a divergence. ⚠ **The second is the useful one: a parity fixture that writes 0 for `na` is how a harness gets "fixed" into ignoring the no-vs-cannot-ask distinction its whole family exists to catch.** **The standing lesson is about INHERITANCE as a delivery mechanism for other people's decisions: this fork subclasses a bot under active development, so every default added to the parent arrives here uninvited, and the two that would have hurt most were both added in the last five days.** Before subclassing a config, diff it against the Pine it claims to mirror — field by field, not by reading the subclass. Earlier: 2026-08-06 — 🟢 **B-LEG WAS RE-DEFAULTED AND THE CAUSE WAS A UNIT MISMATCH ACROSS A FORK, NOT A TUNING MISS — AND THE SAME PASS FOUND THREE A+ LAB PARAMS THAT COULD NEVER HAVE WORKED.** Aaron asked for the most optimised B-LEG on profit factor, winners and drawdown. **Charged over 186,312 M15 bars: 59 trades / −1.73R / PF 0.94 / maxDD −16.00R → 112 / +12.02R / PF 1.23 / maxDD −8.89R**, both halves positive where the old defaults lost 8R in the first. Two defaults did it — `exec_trail_pct` 1.0 → 0.05 and `bleg_max_days` 1.25 → 4.0 (Pine `maxval` 3 → 6) — and **the diagnosis is the deliverable rather than the numbers: across the 50 baseline trades the sum of maximum favourable excursion was 73.9R and the bot captured −0.9R.** 🔴 **`exec_trail_pct` is a percent of PRICE, inherited from a parent where that is the right size for a trade, into a fork whose whole 1R is 0.13%–1.25% of price — so one trail step was larger than the entire risk and the ratchet could never lift off the stage-2 floor.** That floor is `TP1 price`, and a B leg's TP1 is exactly 1R by construction, so the runner banked precisely +1.00R and handed the rest back — 9 of 50 trades, one after running +6.82R. **`mpc_sos_fade` KEEPS 1.0 and must not be reconciled: its own sweep gives the opposite answer (0.25% → 43.6R vs 109.3R).** ⚠ **The other default was a `maxval`, not a number** — 4–5 days measures best and the Pine input capped at 3, so 1.25 was a cap nobody had checked. **A `minval`/`maxval` is as unmeasured as any other constant until somebody sweeps past it.** ⚠ **Still no established edge — 95% CI on mean R −0.140 → +0.355, containing zero.** The protocol is most of the evidence at n≈60: split declared before any row ran, one axis at a time, never a grid, and the case for the result is that **PF is 1.18–1.25 across the whole 4×3 confirmation grid** — flat is what a real plateau looks like. ⚠ **Four levers measured and rejected, one lead recorded unshipped** (the Asia-session filter, PF 1.37 / maxDD −4.98R, which is new code on both sides rather than a default) — `python/mpc_bleg/CLAUDE.md` → *The exit-ladder re-default*. 🔴 **The collateral finding is the bigger one and it is not about B-LEG: `Signals.w_est_state` / `d_est_state` / `w_est_desc` / `d_est_desc` are declared, READ, and written by NOTHING in the repo**, so they are permanently `""`. That makes three `"core": true` A+ lab params unusable in three different silent ways — a bias requirement of "Must agree"/"Must oppose" blocks EVERY trade on both sides, "Must not oppose" blocks nothing, and `exec_htf_exhaust_only` refuses nothing at all while the page says the run was filtered. **All three now REFUSE at construction and in the parity harness rather than answering**, because "no" and "cannot ask" must never be the same value; the real fix is porting `f_htfBias`, and the refusals come out in that commit. **The standing lesson is the empty-registry shape arriving from a direction this repo has not seen it from: the dead registry was not a dict of bots or a list of magic strings — it was four ordinary dataclass FIELDS with sensible defaults, handled without error at every call site. Ask of every field a decision reads: what writes it, and has that writer ever run?** Earlier the same day: 🟢 **BOTH BOTS GAINED A TIME STOP, AND THE INTERESTING PART IS THE MILESTONE IT KEYS ON RATHER THAN THE CLOCK.** Aaron asked for a line to draw — *"if a trade is dancing around by this hour, cut it"* — to cut drawdown. `exec_time_stop_mode` ∈ {**"Off"**, "Before TP1 only", "Always"} + `exec_time_stop_hrs` (36.0), in `config.py`, both A+ Pine files, both B-LEG Pine files, both export mirrors, `compare_strategy.py` and both meta panels. **Defaulted ON ("Before TP1 only", 36h) on 2026-08-06 at Aaron's request, so the A+ baseline MOVED: 159 trades / +137.94R / maxDD 7.99R → 159 / +142.17R / maxDD 5.62R.** Pin `"Off"` explicitly to reproduce anything measured before that date. 🔴 **The measurement is the deliverable, and it says the obvious rule does not work: losers here die FASTER than winners** (median hold — losers 2.0h, winners 17.8h), so a plain clock cuts both at the same rate below ~16h. What makes the lever work at all is the STAGE gate: over 161 trades (2020-01-01 → 2026-08-03) **105 reached TP1 and not one of them lost; all 56 that never reached it lost** — structural, because touching TP1 stages the stop to breakeven. So the clock is only ever asked about trades still genuinely at risk. ⚠ **Breakeven was measured as the alternative milestone and is INERT** — the entry is a RESTING LIMIT, so price sits at the entry when it fills and the next bar's wick crosses back over it: **161 of 161 trades touch breakeven, median 0.25h (one bar)**, and by hour 8 the share of losers that have not is **0%**. A breakeven-gated cut fires on nothing. ✅ **MEASURED BY REAL REPLAY, not re-priced — 155,440 M15 bars (2020-01-01 → 2026-08-03), one full replay per row, at today's shipped defaults (baseline 159 trades / +137.94R):** 24h → +140.21R / maxDD 5.55R (10 cut) · 30h → +142.03R / **5.38R** (7) · **36h → +142.17R / 5.62R (6)** · 40h → +142.58R / 5.61R (6) · 48h → +140.09R / 7.35R (4) · **`Always` 36h → +97.32R (26 cut)**. ⚠ **24h–40h is a PLATEAU, not a peak**, which is the only reason 36 is defensible. 🔴 **The `Always` row is the one that proves the design: same clock, a THIRD of the edge gone** — it cuts 26 where the gated version cuts 6, and the 20 extra are winners. **The stage gate is the lever; the clock is not.** ✅ **The queue effect was CHECKED and did not materialise — 159 trades in every row, including the run that cut 26** — so the naive estimate and the replay agree to the cent. ⚠ **A fact about this window, not a licence to skip replaying**: an exit-side cut frees the slot at an arbitrary moment (~2 trades a month, so usually nothing is waiting), where an ENTRY-side filter frees it exactly when a setup exists — which is how the min-stop guard's estimate got its sign wrong (+1.84R vs **−1.84R** replayed). ⚠ **Do not read +4.23R as edge — sd is 15.06R.** The case is the **drawdown, 7.99R → 5.62R**, bought for R that is noise, resting on 6 trades in 6.5 years. ⚠ **`mpc_bleg` INHERITS it rather than pinning it Off** (unlike the min-stop guard) — the lever lives in the parent's `step()`, both bots share ONE exit ladder, and its Pine got the identical inputs in the same commit; **but the plateau was measured on A+ trades only.** ⚠ **The Pine inputs are declared beside the exit block, NOT up in the GRP_EXEC panel, and that must not be tidied** — TradingView keys saved input values off declaration order within each type, and the last `input.float/string/int` in `mpc_strategy.pine` is `execBeBandR` (~4050), so declaring them there shifts nothing while inserting them beside their siblings would silently reset every later string and float on Aaron's charts. ⚠ **NOT YET PARITY-VALIDATED** — the exports carry `cfg_time_stop` / `cfg_time_stop_hrs` and the harness decodes them (absent column ⇒ **Off**, never the Python default), but no TradingView export has been taken, **and a green run at the Off default would prove nothing about this lever** (the "green on a branch neither side entered" trap the min-stop guard hit the same week). ✅ 15 new tests, **7 watched RED against a neutered `_time_stop_due` and an 8th red against the stage gate removed**; 172 strategy + 693 backend green. Earlier: 2026-08-04 — **`python/mpc_bos/` is DELETED.** It was an unfinished port of the BOS continuation strategy: no `compare_bos.py`, no export Pine, never parity-validated, and no number in this repo ever described it. Nothing imports it — `backtest/tools/run_report.py` carried the only live reference (a roster line pointing at the package) and that line goes with it, so the tool can never name a module that is not there. Every other mention is prose in `docs/MPC_BOS_SPEC.md`, `indicators/CLAUDE.md` and a couple of study tools, and those are historical records that stay accurate: `indicators/CLAUDE.md` has always listed the Python port as **open**, which is true again. ⚠ **Recover from git, never from memory** — the files are in the history and `docs/MPC_BOS_SPEC.md` is the spec they were written to. The Pine side (`indicators/strategies/mpc_bos_strategy.pine`) is untouched and still uncompiled; deleting the port does not retire the strategy, it retires a half-built implementation of it. Earlier: 2026-08-02 — **the SPREAD and the OVERNIGHT SWAP are chargeable now, which closes the "swap is still unpriced" warning the 2026-08-01 entry below left open.** Bar mode could always have priced both and did not: swap needed **no new code at all** (the charge path has run on every bar since A2 and was dead only because the lab passed `swap=None`), and the spread arrives on `AccountProfile` as `spread` + `bid_ask_fills`, both bar-mode-only and both defaulting to zero — **omit the profile and every path stays byte-identical**, so both parity gates are untouched. **MEASURED on 155,431 M15 bars (2020 → 2026-07-31) at the shipped `mpc_sos_fade` defaults: free 161 trades / 135.94R · +spread 130.27R · +spread+swap 123.90R · bid/ask fills 159 trades / 141.93R.** ⚠ **The two spread fields are ALTERNATIVES, not layers** — running both bills one spread twice, and `_charge_spread` refuses the second. ⚠ **They deliberately disagree, and it is a fact about how these strategies ORDER, not a defect**: a flat charge is the market-order intuition, and every entry and exit here names a PRICE, so the spread changes WHEN an order fills rather than what it fills at. On a long the buy limit and the stop each fill at their own price (identical cash, the limit is just harder to reach); the burden lands almost entirely on **SHORTS**, which sell the bid to get in and BUY THE ASK to get out. **So read the flat charge as a conservative UPPER BOUND and `bid_ask_fills` as the real question** — and treat a `bid_ask_fills` result as a lab finding until it has been read on a chart. ⚠ **The spread is a fact about the SYMBOL as much as the account, and the figure this repo had recorded was the wrong broker's** — `$0.33` is PU Prime's; Vantage, which every backtest here replays, measures **$0.22** (1,494,459 cached ticks). 386 tests green. Earlier the same day: **every `mpc_sos_fade` trade now RECORDS the fib leg it was priced off**, so the lab's price chart can draw the exact ladder each entry, stop and target came from and a reader can see which retracement levels a trade went into. `Trade.fib` is snapshotted in `_place_entries` (the eight `(ratio, price)` pairs plus the bar the LEG started on), carried through `_open_position`, and copied — never re-derived — all the way to the chart. **REPORTING ONLY**, the same standing as `mfe_usd`/`tp1`/`tp2`. ⚠ **Taken at PLACEMENT and read back from the ORDER at the fill**, because a fib keeps extending while a limit rests: re-reading it at the fill would report a leg the order was never priced against, while the stop and targets beside it are frozen at placement. ⚠ **All-or-nothing** — a ladder missing a rung is dropped, since seven levels drawn where there are eight reads as "this trade had no 0.786". The 1m secondary records none by construction (it prices entries off something other than this ladder). ⚠ **`mpc_bleg` did too until 2026-08-11 and now records its OWN** — same rule, different leg: it snapshots the frozen SOS band's anchors rather than the A+ Structure fib, because inheriting the parent's snapshot would attach a real, fully populated ladder describing a leg the trade was never priced against. ✅ **Proven cosmetic by measurement:** `compare_strategy.py` **exit 0 at warmups 100 / 500 / 1000 / 2000** on the 21,715-bar 15m export, `compare_bleg.py` exit 0 at 100 and 800, and the fibonacci engine — whose `ash_loc`/`asl_loc` this needed — A/B'd at HEAD vs the working tree over 47,263 real bars with **0 field differences**, because no `fib_export.pine` CSV exists to run `compare_fib.py` against. **The standing lesson it is built on: a fib rebuilt downstream is a SECOND claim about one leg** — record what the strategy read, copy it, and derive only what the record cannot state (here: the entry's own ratio). Earlier the same day: **a parameter now has ONE name and ONE explanation, shared by its Pine input and its lab row.** The two panels had drifted into different vocabularies for the same setting, and one of the lab's names was outright wrong ("nearest fib ABOVE" — shallower is above on a long and BELOW on a short). A script diffed all 51 lab params in `python/mpc_sos_fade/mpc_sos_fade.meta.json` against their Pine titles: **6 matched, 37 differed**; now **42 of 43 shared params are byte-identical**, and every `desc` is the Pine tooltip verbatim (43 of 43). The single deviation is `exec_conf_sz`, whose lab label carries `(not supported)` — the Sniper-Zone entry is Pine-only. Same pass, at Aaron's request: **every execution and divergence tooltip was rewritten short** — what it does, what ON vs OFF means, the one fact that changes the decision. The measurement essays that had accumulated in them (the `execFvgPreZone` tooltip alone was ~1,400 characters) belong in CLAUDE.md, where they can be found again. **The rule to keep: change a label or a desc and change the Pine in the SAME commit** — it is recorded in each meta file's own `_comment`, which is the contract a future edit will actually read. ⚠ Titles only, never the ORDER of an `input.*` call: TradingView keys saved chart settings off declaration order, so a rename carries Aaron's saved values and a reorder loses them. Nothing moved — **35 of 35 execution defaults unchanged**, both export mirrors still byte-identical to their parents, 363 strategy + 337 backend tests green, and **BOTH parity gates exit 0 at warmups 100 / 500 / 1000 / 2000** on fresh 21,715-bar `VANTAGE_XAUUSD, 15m` exports taken off the renamed files (A+ `cfg_bits` 544375; B-LEG `cfg_bits` 61047). `mpc_bos_strategy.pine` was synced only where it shares a parameter; its entry inputs describe a different setup and were left alone. Earlier the same day: **the A+ ENTRY MODEL is ported to `python/mpc_sos_fade/`, and the shipped default entry PRICE changed.** Five new fields in lockstep with `mpc_strategy.pine` — `exec_fvg_pre_zone` / `exec_fib_overlap` / `exec_fib_deep_edge` / **`exec_fib_nearest` (ON)** / `exec_sl_deep` — and `exec_deep_fib` flipped **True → False** because rule 3 replaces Method 3. Method 3 only ever looked at the level ABOVE a floating gap, so a gap a hair short of 0.702 was entered back at 0.618; rule 3 measures both sides and rests on whichever is closer, which is a deeper entry and a tighter stop **bought with fill rate**. **MEASURED on 155,431 cached M15 bars (2020 → 2026-07-31): 165 trades / +126.68R → 161 / +135.94R**, and the 126.68R baseline reproduces the Pine's own stated figure to the cent — an independent cross-check on the port. Every rule scan stops at 0.786, because 0.886 is the stop and an entry resting there is a zero stop distance and a cancelled order. ⚠ **Both sibling forks had to PIN the old behaviour** — `mpc_bleg` (all six; NOT inert there, since it does not override `_entry_edges` and those edges feed its "A+ has priority" gate) and `mpc_bos` (`exec_deep_fib`) — because neither of their Pines has this model. ✅ **PARITY RE-VALIDATED GREEN the same day** — `compare_strategy.py` **exit 0 at warmups 100 / 200 / 500 / 1000 / 2000** on a fresh 21,702-bar `VANTAGE_XAUUSD, 15m` export (2025-08-31 → 2026-08-02) whose `cfg_bits` decoded **544375, bit 524288 SET**, i.e. the Pine was genuinely running rule 3 and the Python was configured to it from the export — not two sides agreeing on a model neither had enabled. 140 tests green. Earlier the same day: **`mpc_sos_fade`'s stop level escaped its five-value dropdown.** `exec_sl_level = "Custom"` reads a new `exec_sl_custom` — any fib ratio in (0, 1.0], default 0.886 — priced off the leg anchors through the canonical `engines.fibonacci.geometry.fib_level()`, so **Custom 0.886 is bit-identical to picking "0.886"** and the mode switch alone moves nothing. It opens the half of the range the ladder never covered (0.886 → 1.0 = deeper stop, smaller position) and, being a NUMBER rather than one of five strings, makes the level a real numeric optimizer axis. Out-of-range raises at construction instead of falling back to fib 1.0 — a typed number silently becoming a different stop would replay a whole backtest against a level nobody chose. ⚠ **It is the first exit lever in this folder to be PYTHON-FIRST, so it has no Pine counterpart**: `execSlLevel` is an `input.string` with five options, `compare_strategy.py` can never configure a Custom run (parity is structurally unaffected), and **a Custom result is a lab finding, not a validated one** — port the input to the Pine before trading one. ⚠ Shallower than 0.886 reproduces Run 4's hazard at any ratio, not only at three. Detail: `python/mpc_sos_fade/CLAUDE.md` → *The Custom stop level*. 129 tests green. Earlier: 2026-08-01 — 🔴 **every lab backtest of a Python strategy here was FRICTIONLESS, and said otherwise on the page.** The command center collected `commission_per_side` / `slippage_ticks` on every run and `python_runner` read neither, so every number these bots have produced was measured with no costs (the tell: 52 of one run's 54 losers each lost EXACTLY 10.00% of prior equity). A Python strategy can now take a `cost_profile` and charge it — **omit it and every path is byte-identical**, so both parity gates are untouched and every historical result stays reproducible. Two units, both pinned by tests: commission is per **LOT** per side (100 oz for gold), and slippage is charged on **MARKET exits only** — a resting limit fills at its price or better or not at all, so an entry limit and a TP rung never slip. ⚠ Swap is still unpriced outside tick mode, which matters for any strategy here that holds overnight. Detail: `python/mpc_sos_fade/CLAUDE.md` → *Bar-mode costs*. Earlier the same day: 🔴 **the phantom-exit bug is fixed in both Python bots and all five strategy Pine files.** `indicators/docs/BUG_exit_fill_price_mismatch.md`: the ENTRY bar was allowed to stage the stop, and a resting limit is reached by price coming to it from the wrong side — so the entry bar's *favourable* extreme is the approach to the order, not the trade's move. The stop went to breakeven on a trade that had gone nowhere, landed on the wrong side of the market, and every leg market-closed at the next bar's open. `execution.py` now skips `_advance_stage` on the fill bar (both the 15m primary and the 1m secondary) and seeds `_max_fav` from the fill price; `mpc_bleg` inherits all of it. On lab run `d2ab68f9e884`: **all 165 entries identical**, 30 results changed, **+101.68R → +112.43R**, win rate 63.6% → 67.3%. ✅ **Both parity gates RE-VALIDATED the same day on FULL-HISTORY post-fix exports** — `compare_strategy.py` and `compare_bleg.py`, both 21,691 bars (2025-08-31 → 2026-07-31), **exit 0 at warmups 100/200/500/1000/2000**. The bug's fingerprint — a stop already at breakeven on the entry bar — went from **4 of 26** A+ entries to **0 of 27**, with all four affected candles inside the window and readable before/after. ⚠ **12 trades that used to scratch now take a full −1R and max drawdown is still UNMEASURED** — re-run in the lab before trusting any risk number in this folder. **Standing lesson for every strategy here: a green parity run says the two implementations AGREE, never that either is right** — this bug was faithfully ported, so the gate was green for its entire life. 534 tests green. ⚠ **Recorded the same day and it applies to every strategy in this folder — a wrong-side stop fill is a BACKTEST LIMITATION, not a defect, and it will keep appearing on the chart.** Price tags TP1, the stop stages to breakeven, then price closes back through it inside the SAME bar; the stop only goes live NEXT bar, so it is behind the market and the exit fills at that bar's OPEN instead of at the stop. Being OUT is correct; only the exit PRICE is imprecise, because bar replay checks orders once per bar while a real broker watches every tick. **It errs in the safe direction (backtest looks slightly worse than reality) and is identical in Pine and Python, so parity is unaffected.** The tell that separates it from the fixed bug: THIS bug staged the stop on the **entry bar**, before the trade went anywhere; the residue only stages after a genuine TP1 touch on a later bar. Canonical write-up: `python/mpc_sos_fade/CLAUDE.md` → `### Wrong-side stop fills`. Earlier: 2026-07-31 — 🔴 **`mpc_sos_fade` was relying on an UNPINNED engine default, and the B-LEG Pine fork had missed the session windows.** Two sync gaps, both closed, neither moving a number. (1) The bot pins `fvg_max_count` and `fvg_require_close` but never pinned **`fvg_threshold_pct`** — it was inheriting `backtest/replay/stack.py`'s `0.1`, which matches `mpc_strategy.pine`'s 15m floor only by coincidence. Proven load-bearing by removing it (`compare_strategy.py` failed on the first compared bar), so anyone reconciling that "stale" shared default would have moved the A+ bot with no test failing. `engine_config()` now pins it and the test asserts all four. Same class as the 2026-07-26 `fvg_require_close` miss — **an engine input the decision stream does not export is a silent parity trap, and that applies to one a bot forgot to pin, not only to one whose default changed.** (2) `mpc_strategy.pine` has carried the DST-aware session windows since 2026-07-12 but `mpc_b_leg_strategy.pine` + its export never got them, so the two forks disagreed about when a session opens — and session H/L feed `recentBSL`/`recentSSL`, which is what `execArmSweep` arms A+ on. Both files synced (plus `mpc_m15_playbook` and `svp_export`; `mpc_jarvis_v2.pine` was DELETED instead — superseded by `mpc_strategy_export.pine`). **Both parity gates re-verified after all of it: `compare_strategy.py --warmup 100` and `compare_bleg.py --warmup 100` both exit 0, 529 tests green** — so no backtest number in this folder moves. **(2) then closed properly the same day on a FRESH B-LEG export** — the run above used the 2026-07-29 export, taken off the Pine *before* its windows changed, so it could not test the fix. `compare_bleg.py "VANTAGE_XAUUSD, 15_cabec.csv" --warmup 800` → exit 0 on 6,329 bars covering 2026-04-27 → 2026-07-31, a window sitting **entirely inside BST/EDT**, which is the half of the year where the new city-clock windows and the old fixed GMT-4 ones actually disagree. `compare_svp.py` re-run green too (12,117 bars, `--warmup 317`). That export also exposed a real harness bug: `bl_l_bar`/`bl_s_bar` hold Pine's `bar_index`, which counts from the first bar the CHART loaded, and this was the first PARTIAL export — it starts 15,362 bars in, so all 2,409 armed-bar comparisons failed at that flat constant while the logic was identical. The tool now measures the origin instead of assuming zero, majority-based so a genuine armed-bar drift still fails, with a test on each half. **Standing rule: any parity column carrying a Pine BAR INDEX is export-window-relative — a harness that diffs one raw is correct only by the accident of a full-history export.** 531 tests green. Earlier: 2026-07-30 — **`mpc_sos_fade` gained the MINIMUM-STOP GUARD, the last Pine↔Python divergence on the A+ pair.** `exec_min_stop_mode` / `exec_min_stop_val` (default `"Off"` — every historical result is unmoved), the floor applied at order placement, block reason code 7 ("Stop too tight"), a regenerated `mpc_strategy_export.pine` carrying `cfg_min_stop` / `cfg_min_stop_val`, and the decode in `compare_strategy.py`. It is step 1 of `docs/LIVE_TRADING_PIPELINE.md`, and the reason it goes first: it guards the only hazard in this bot that loses real money fast. `qty = risk / stop_distance`, so a stop that collapses onto the entry does **not** risk less — it buys an enormous position, and once the stop is narrower than an ordinary bar price runs straight through it, so the realised loss stops being the 1R that was agreed (measured at `exec_sl_level = 0.786`: a $0.20 stop, 39,033 oz, ~180% of equity in one bar). **Standing rule for every strategy here: a risk % is only the real risk if the exit actually happens at the stop price.** `mpc_bleg` PINS the mode `"Off"` — it overrides `_place_entries` so the floor never runs there, and the hazard is structurally absent (a B leg's stop is its band origin). ⚠ Unit-tested and round-tripped, but **not yet diffed ON against a real TradingView export** — re-export before trusting a run made with the filter on. 111 tests green. Earlier: 2026-07-29 — **`mpc_sos_fade` Run 12: the A+ entry rule cannot be loosened for more trades, measured four ways over 6.5 years.** Dropping the FVG requirement adds 173 trades for +13.0R net on a 110.6R book while drawdown goes 54.9% → 77.1% (and 40% of the gross is ONE 2020 trade); sizing those extras at 5% instead of 10% is NEGATIVE (their benefit scales with size, the cost of displacing a real trade does not); deeper entries give FEWER trades AND less money; loosening which gaps qualify adds 65 trades and costs 32.4R, partly by RE-PRICING trades already taken. The final-hour rule costs ~0.4R over the same window, so it stays on. **The standing lesson for every strategy in this folder: with ONE position slot, a marginal setup is a queue, not an addition — trade count is a portfolio property, and sizing up a trusted book beats adding a marginal one** (shipped A+ at `exec_risk_pct=12.5` = 832x @ 64.2% DD vs 426x @ 64.9% for the loosened book). No strategy code changed; full record in `python/mpc_sos_fade/mpc_sos_fade_optimization.md` → Run 12 / 12b. Earlier the same day: **both parity gates re-run GREEN on fresh exports.** `compare_strategy.py` (A+, `VANTAGE_XAUUSD, 15_7b2f3.csv`, 21,494 bars, 2025-08-31 → 2026-07-29, at the shipped `exec_tp1_pct = exec_tp2_pct = 0`) and `compare_bleg.py` (B-LEG, `VANTAGE_XAUUSD, 15_ab202.csv`, 21,493 bars, same window) both exit 0 at `--warmup 100` and hold green at warmup 200/500/1000/2000, so the ~100-bar skip is genuine engine cold start, not a mask. Both Pine parents compile. Every "STALE" warning below is now CLEARED — the ratchet build is validated bar-for-bar on both forks. ⚠ One gap those runs do NOT cover: `mpc_strategy_export.pine` still lacks `execMinStopMode`/`execMinStopVal`, so A+ parity is proven only at the `"Off"` default. Earlier: 2026-07-28 — the **A+ runner trail defaults to `"Structure + % ratchet"`** (`exec_runner_trail` + the new `exec_trail_pct`, 1.0), shipped through `mpc_strategy.pine` → `mpc_strategy_export.pine` (`cfg_exitmode` 2-way → 3-way, new `cfg_trail_pct`) → `mpc_sos_fade` (`config.py`, `execution.py._trail`, `compare_strategy.py`, meta, 3 tests). It anchors on the last confirmed swing like the plain structure trail, then CLIMBS one %-of-price step per step of favourable move, so the runner stops handing back the gap to a lagging swing. ⚠ **A+ parity is STALE — the 2026-07-27 GREEN run predates this, so re-run `compare_strategy.py` on a fresh export before trusting any A+ number**, and run it at the shipped `exec_tp1_pct = exec_tp2_pct = 0`: the 109.3R figure in the ratchet write-up was measured at 1%/1%, and the true 0/0 baseline is **110.65R**. Same day, **extension fibs (negative fibs past 0.0) were measured and REJECTED in every form** — as shallow take-profit rungs (109.3R → 69.1R), as a stop floor that ratchets up the extension ladder (→ 56.1R, a fib line is a fixed price and does not breathe), and as deep rungs at −1/−4/−6 (Aaron's own hand ladder = 106.3R; the only rows that beat baseline sit at −6, which ONE trade in 6.6 years ever reached). Full record + the shape data behind it in `python/mpc_sos_fade/CLAUDE.md` → `### The swing ratchet`. Earlier the same day: the **B-LEG Pine pair caught up to the A+ exit ladder**: TP rungs 30/40 → **0/0**, the `qty_percent = 0` guard (without it a 0 rung closed the whole position at TP1), and the `"Structure + % ratchet"` runner trail + `execTrailPct`, now the default there too. `mpc_bleg/config.py` dropped its `exec_runner_trail` pin as a result. Both forks are back on ONE ladder; **the B-LEG export is stale until `compare_bleg.py` is re-run** (`cfg_exitmode`'s trail digit went 2-way → 3-way). Earlier: 2026-07-27 — the TP1/TP2 scale-out rungs default to **0/0** across both A+ Pine files and `config.py` (bank nothing, ride the runner — `mpc_sos_fade_optimization.md` Run 1 adopted), with a Pine-side guard because `strategy.exit(qty_percent = 0)` closes the WHOLE position; A+ parity re-validated GREEN at those defaults + SL fib 0.886. Earlier: 2026-07-26 — both Python bots gained the Pine's new exit levers (structure runner trail, TP2 stop floor), and the B-LEG's Pine-parity harness landed and went GREEN on its first real export; the switchable TP/SL register lives in `python/mpc_sos_fade/CLAUDE.md` → `## The exit ladder`
