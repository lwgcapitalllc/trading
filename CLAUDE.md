## Communication Rules — Non-Negotiable

- Plain English only. Short sentences.
- Never use bullet points to explain a simple thing.
- No preamble. No "Great question." No "Sure, I can help with that."
- Spawn subagents for routine tasks. Work sequentially unless the task explicitly requires parallel execution.


# CLAUDE.md — LWG Capital Monorepo

**Purpose:** Standing instructions for Claude Code across all subsystems.
**Scope:** This covers repo-wide rules, VPS workflow, and branch conventions. It does NOT cover subsystem internals — each subsystem has its own CLAUDE.md.
**Status:** Active — four apps, ten canonical engines, and tooling in various stages of production.
**Last reviewed:** 2026-08-09 (latest) — 🟢 **STACKING IS FINISHED END TO END: A STACK IS NOW EITHER A SCREEN OR A SHARED ACCOUNT, AND THE LAB CAN RUN BOTH.** Aaron's requirement — *"if one bot wants to trade, but there's no risk available to trade on, it should be blocked by the system"*, and in the backtest the legs must *"share the equity and react the exact same way they would do in a demo account"* — is now reachable from the UI rather than only from a CLI. `stacks` gained `mode` + the three account knobs, `POST /stack` branches into `command-center/backend/services/portfolio_runner.py`, and `GET /stacks/{id}/contention` serves what the budget did; the page carries a mode chip, a shared-account panel and a Mode column. **The runner CALLS `backtest/portfolio/run_stack` and owns no account model of its own** — a second one would be a second answer about what a shared budget does, and the live allocator already has to be a third. ✅ **DRIVEN through the running backend, not reasoned about: XAUUSD M15 2024, two legs, $10,000, cap 10% — 33 trades, +26.35R, $36,805.85 closing, and every leg posts the SAME R shared as solo**, which is the control saying the seam moved no decision. 🔴 **THE PLAN'S OWN VERIFICATION CRITERION WAS WRONG AND THE RUN PROVED IT.** `SHARED_RISK_STACK.md` said a shared run must close LOWER than the screen and that a higher one means the gate is not enforced; it closes **$36,806 against $26,870 — HIGHER — with the cap working and nothing refused**, because a screen gives each leg a PRIVATE balance while a shared account COMPOUNDS both onto one, so the second leg sizes off a balance the first has grown. Two effects in opposite directions; the compounding one is unbounded and the refusal one is capped by how often the budget is genuinely full. **Compare R, never net dollars** — that test would have condemned a correct implementation. 🔴 **The first launch DIED IN A BACKGROUND TASK**: `exec_secondary` has defaulted ON since 2026-08-07 and a leg on a merged clock is one bar frame, so the simulator refused it. The router PINS it and **writes the pinned value onto the child run**, so the row and the replay cannot disagree; a test READS `legs.py` and fails if it grows a refusal the router misses. ⚠ **The schema change needed the columns declared in BOTH the migration list and the CREATE TABLE** — migrations run first, so on a fresh database the `ALTER TABLE` is swallowed and a brand-new clone would come up WITHOUT them while every existing one has them. Both directions proven by test. ⚠ **A shared stack REUSES NOTHING** (a finished standalone run was measured un-contended) and always takes the python lock: it is `1 + legs` full replays, the shared book plus one SOLO CONTROL each. ✅ **Cancel reaches the replay now** — `simulate` gained `progress`/`should_cancel`, and a cancelled run returns a PARTIAL book flagged as such which nothing persists, and skips the solo controls (a control over the full history beside a book that stopped a year in is two experiments in one table). 🔴 **FOUR TESTS WERE VACUOUS ON THEIR FIRST ATTEMPT AND EACH FAILED DIFFERENTLY — that is the transferable half.** The cancel test's two obvious assertions stayed true under its own mutation, because each solo replay cancels on its own first poll and bails before recording anything, so the run does the extra work and looks identical from outside (it counts the legs BUILT now). And in the browser suite: every route mock keyed on `http://localhost:8000` while the app fetches through the Vite proxy at `/api`, so three checks silently read the LIVE lab; "a screen renders no panel" passed against its mutation because the hook is disabled there so the panel cannot render either way; and the counter written to fix that was registered BEFORE the mock, which shadows it. ✅ 15 new backend/backtest tests and 10 browser checks; 880 backend green (1 pre-existing failure), 377 backtest green. ⚠ **Contention MARKERS on the price chart are DEFERRED and named rather than dropped** — every measured run refuses nothing, so a marker layer would be a generic mechanism nobody has exercised, which is this repo's own recorded trap. **The standing lesson is about criteria rather than code: a verification test written before the thing exists is a prediction, and reality is allowed to disagree with it — the reason this one was wrong (compounding, not refusals) is a fact about shared accounts nobody had stated.** Earlier: 🔴 **THE CANDLE MARKS AND THE INDICATOR THEY ARE READ AGAINST RUN ON DIFFERENT TAPES, AND 16% OF EVERY MARK DISAGREES BECAUSE OF IT.** Aaron came back with eight candles that "are obviously engulfings" and were not marked. ✅ **MEASURED against his own export's `px_*` flags over 20,053 shared bars, and the split is the finding: SIX of the eight his own TradingView indicator does not draw either** — four fail the full-body engulf by **$0.01 / $0.05 / $0.06 / $0.01 on HIS OWN prices**, one fails the 117-bar uptrend gate with price $13 below where it was 117 bars back — **and TWO are genuine misses whose cause is in none of the code.** The chart draws the run's MT5 cache bars; the indicator runs on TradingView's. Same broker, same symbol, **median close 6 cents apart, and the INTRA-BAR variation is larger than the offset** — on the 2025-12-09 case the two feeds differ by 14 cents on the open and 3 on the prior close, i.e. **17 cents on the single comparison that decides the rule**, so TradingView clears `open >= close[1]` by +0.09 and the cache misses by −0.08 with every other clause passing on both. 🔴 **A constant offset would flip NOTHING** — every clause compares two prices, so a whole-feed shift cancels — which is exactly why "the feeds are only a few cents apart" is the wrong intuition here. **600 marks his chart draws that ours misses, 515 ours draws that his does not; bearish engulfing 150 of 914**, and one of them was a trade's adverse extreme, so it also changed the CHIP. 🔴 **A TOLERANCE WAS SWEPT AND IS STRICTLY WORSE, which is the opposite of what the noise story predicts:** miss/invent goes **243 / 190 at epsilon 0**, 143 / 664 at $0.02, 95 / 822 at $0.05, 65 / 900 at $0.10 — **two cents recovers 100 marks and manufactures 474** — because on a gapless intraday feed the open sits within a cent or two of the prior close as the ORDINARY case, so slack floods the rule instead of nudging it. **Zero is the minimum-disagreement setting by a factor of two.** ✅ **ACCEPTED, NO CODE CHANGE (Aaron's call):** the only other lever is the candle SOURCE, and the chart must draw the bars the strategy actually traded or the marks stop lining up with the trades. **The standing lesson is about what a parity gate can cover: `compare_candles.py` feeds BOTH sides the export's own prices, so it proves the RULES agree and is structurally silent about the data a consumer feeds them — every layer was green and the reader still saw the wrong candle. Before claiming one system matches another, ask whether they are reading the same INPUT; a validated rule on a different tape is a different answer.** ⚠ **It is `shadow_diff.py`'s 2026-08-04 finding arriving in a second subsystem** — there four cents of broker difference moved a resting entry $10.12, here six cents moves one mark in six — **so treat "the feeds are basically the same" as a claim needing a number, every time.** ⚠ **And do not answer a report like this by asserting the engine is parity-green**; print the failing CLAUSE with both feeds' numbers beside it. Earlier the same day: 🔴 **THE CANDLE LAYER'S "DRAWDOWN ZONE" WAS A REGION IN TIME, AND THE ZONE IS A REGION IN PRICE — ONE MISTAKE, FOUR SEPARATE COMPLAINTS ACROSS TWO ROUNDS.** Aaron reported six trades off the chart. ✅ **MEASURED on run `e51d95f212e3`, 166 anchors, and the defects point in OPPOSITE directions, which is why no single bound could have fixed it.** (1) **255 of 1084 marks — 23% — sat entirely on the FAVOURABLE side of the entry**: 35 from a confirm tail measured off the end of the drawdown instead of off the TURN (the allowance exists because a pattern is reported on the bar it COMPLETES, so it is measured from the extreme and means nothing anywhere else — on his 2026-07-15 long the drawdown ends 17:15, the next bar rallies $16 clear, and the `Inverted Hammer` two bars later at **$25 above the entry** was the trade's ONLY mark, so it named the chip), and 196 from the span opening at the ENTRY BAR, so a trade that ran into profit before coming back painted the whole excursion between — **one turn is 112 bars after entry**. (2) 🔴 **A STOPPED-OUT LOSER WAS MARKED AFTER IT CLOSED**, because a loser's adverse extreme IS its final bar, so `turn + _CONFIRM_BARS` reached past the exit into the next setup — *"Trade already lost. You already hit stop loss… I don't care what the candles after the trade. It has to be within the trade."* (3) 🔴 **AND THE FIRST FIX WENT TOO FAR THE OTHER WAY**: walking one contiguous excursion and stopping at the first favourable bar meant a return to the entry an hour later fell outside the zone, while the chart's own red box plainly covered it — *"we came back up to entry. And there was at least three different candles you coulda highlighted there, and you didn't highlight any of them."* ✅ **The rule is now one price test and one clamp: a bar is drawn while ANY part of it trades on the adverse side of the entry, anywhere between the entry bar and the EXIT bar.** `_drawdown_end` is DELETED — no walk can express *and again when price comes back* without swallowing the profit in between. ✅ **Driven through the live backend: 1090 → 853 marks, ZERO past any exit, 31 outside the band and every one within two bars of the extreme; 101 of 166 trades changed.** 🔴 **A FOURTH defect fell out of the audit and nothing had ever reported it: a trade that OPENS AND CLOSES INSIDE ONE BAR read as having NO END** (`j > i`) and fell through to the 3-bar fallback — 4 marks on 3 trades, one a `Bearish Engulfing` drawn after a trade that lasted **four minutes**. ⚠ **FOUR of the six candles he named are NOT this layer's doing, and saying so needed the clause breakdown rather than an assurance** — measured against `candle_sticks.pine:32`, three miss `open >= close[1]` by **$0.05 / $0.06 / $0.01** and one fails the **117-bar uptrend gate** (price $13 BELOW where it was 117 bars back). **On a gapless intraday feed a red bar opens within pennies of the prior close, so a full-body engulf is decided at the cent.** All four are IN the zone and would paint the instant the engine saw them; his own TradingView chart does not draw them either. ⚠ **NEVER add a tolerance to close that gap** — a 0/1 flag has no "close enough", and the engine matching the indicator his brother reads is the entire reason the layer is trustworthy. ✅ **10 new tests, 6 WATCHED RED against HEAD**, the rest by MUTATION, with the fixes covered INDEPENDENTLY; 870 backend tests pass, 1 pre-existing failure. ✅ **Parity unaffected and CHECKED rather than asserted: both commits touch only `command-center/backend/`, and `compare_candles.py` is exit 0 — zero rule differences over 302,070 comparisons.** ⚠ **Existing runs need Rebuild chart.** **The standing lesson is about restating a reader's words as a rule: he kept saying "in the drawdown", which is the red box on his screen — a region in PRICE — and every version of this that reasoned in BARS was self-consistent, easy to test, and answering a question nobody asked.** Earlier the same day: 🟢 **TWO BOTS RAN ON ONE SHARED BALANCE FOR THE FIRST TIME, AND IN 6.5 YEARS THE RISK BUDGET REFUSED NOTHING.** Aaron asked to finish stacking — *"if one bot wants to trade, but there's no risk available to trade on, it should be blocked by the system"* — and added the requirement that decides the design: in the backtest the legs must *"share the equity and react the exact same way they would do in a demo account"*, i.e. ONE balance both bots size off and both bots move, not two private ledgers added up. `backtest/portfolio/` gained `legs.py` (a real `strategies/python/` bot as a simulator leg), `runner.py` (`run_stack`, which also replays every leg SOLO as the control) and `tools/stack_run.py`; both strategy constructors now thread `account` / `leg` through to `Execution`, **where the seam had existed unused since 2026-07-17 because nothing could pass it one** — every run silently got a private uncapped `SoloAccount`. ✅ **MEASURED, 155,807 M15 bars, $10,000 account, cap 10% of the LIVE balance: A+ 159 trades / +142.18R · B-LEG 99 / +17.87R · shared account $10,000 → $204,918,789**, and **every leg posts the SAME R shared as solo** — which is the control this first run exists to establish rather than the headline: R is normalised to the trade's own risk, so with nothing refused the shared account changed the DOLLARS (one balance compounding both legs) and moved no decision. A+ reproducing its documented +142.18R baseline to the cent is the cross-check that this drives the real bots and not a third thing. 🔴 **THE CONTENTION LOG IS EMPTY AND THAT IS THE FINDING, NOT A PASS.** Peak open risk touched **exactly 10.00%** with **2 of 2 legs holding at once**, and still nothing was blocked — because **open risk is measured to each trade's CURRENT stop, so a stop moved to breakeven releases its room**, and A+ touches breakeven on 161 of 161 trades at a median of ONE BAR. By the time the second leg asks, the first is reserving nothing. ⚠ **Read it as "the allocator would rarely have had anything to arbitrate", never as "a cap is unnecessary"** — it is the overlap audit's 27 shared bars arriving through the budget, and the window where two bots genuinely carry 2× risk is the bar before the stop stages. ⚠ **A cap BELOW a leg's own risk % does not arbitrate, it RE-SIZES**: at `--risk-cap 5` against two bots each risking 10%, all 258 entries are shrunk and NONE is blocked, R is unchanged and the closing balance falls $204.9M → $4.7M. 🔴 **The run found a defect in the log on its first pass — 11 contention events totalling $0.00 of refused risk, every one float noise**: a leg derives its qty by DIVIDING by the stop distance and the account re-MULTIPLIES by it, so an entry that exactly fills the cap disagrees in the last bit and reads as a shrink. **A log that reports contention where none occurred is worse than a quiet one** — downstream it marks trades as shrunk that were granted in full, and it buries the real events among the noise. ⚠ **The first test of that fix was VACUOUS and passed against the bug**, because 10,000 × 0.10 = 1,000.0 is exact in binary; it now tests the RULE at the seam instead of trying to synthesise a balance that happens to round. ✅ **`build_strategy` REFUSES a strategy that cannot accept the account**, and it is a sharper refusal than the `cost_profile` one beside it: a dropped profile under-charges a run, a dropped ACCOUNT sends the leg back to its own `SoloAccount`, which has an **infinite** budget — so the run would report a capped shared portfolio while that leg sized off the whole balance and contended with nobody. **A risk cap claimed on screen and enforced nowhere.** ✅ 13 new tests, 370 backtest + 276 strategy green; the epsilon rule watched red against a mutation in BOTH directions. 🔴 **NOTHING ABOUT THE LIVE BOT CHANGES, AND THE REASON MATTERS MORE THAN THE FACT.** Checked rather than assumed: `algos/markets/fx/instances/` holds exactly ONE instance, nothing in `algos/live/` references the portfolio package (grepped, zero hits), and the live config pins `exec_bleg: false`. The new constructor arguments default to the old behaviour and the live runner does not pass them. **The live allocator cannot reuse this object at all** — `PortfolioAccount` is an in-process Python object, live bots are separate OS processes, and every MT5 read in `mt5_ops.py` is filtered by MAGIC, which is correct for isolation and is exactly what makes an account-level allocator blind. It has to read the broker's real exposure ACROSS magics. ⚠ **Two more live-side gaps found in the same pass and left open**: nothing enforces distinct magic numbers across instance configs, and there is no fleet-wide halt — the bridge halts only its own bot. **The standing lesson is about what an empty guard log means: the cap reported no contention and was RIGHT, and the reason was not that the legs never met — they held together at the cap — but that the budget was released before either could be refused. Before reading a quiet guard as a healthy system, ask what would have had to happen for it to speak.** Earlier: 2026-08-08 — 🟢 **THE BACKTEST PRICE CHART PAINTS THE CANDLE THAT TURNED PRICE, AND THE FIRST VERSION LOOKED RANDOM BECAUSE OF WHERE IT WAS ALLOWED TO PAINT.** Aaron asked for the new `engines/candlesticks/` engine on the chart — the reversal candle at his trades and at the setups he missed — *"so I could do analysis and say, can I add candle patterns as an additional confluence to get better entries or avoid bad trades"*. 🔴 **The anchor set is the whole feature and it was wrong: BLOCKED setups were anchors, 324 of them against 159 trades and 35 three-of-three misses — two thirds of every mark — and since the Blocked layer defaults OFF they painted navy candles where the reader could see no setup at all.** ✅ **518 anchors / 424 marks → 194 / 153, measured.** 🔴 **The second defect was a `Bearish Engulfing` on a LONG that WON**: ranking started at nearest-the-extreme, so whichever pattern sat closest won whichever way it pointed. Direction is now a three-tier PREFERENCE — aligned, neutral, opposing — nearness the tie-break WITHIN a tier. ⚠ **The opposing tier is KEPT and is half the point** (*"if not, it will show me why I was wrong"*): **89 aligned / 49 neutral / 15 against / 41 with no pattern at the turn**. ⚠ **The NEUTRAL tier does most of the work and the reason is in the source Pine** — ten of its fifteen rules gate on a trend lookback, so in an uptrend the bullish rules cannot fire at all, which is exactly why the reference mark was bearish. ⚠ **A WINNER is searched over its whole HOLD** — MEASURED, the adverse extreme is a median 2 bars past entry but **p90 27 and worst 112**, so a fixed short window finds the real turn on barely half of them. ✅ **The "3/3 miss with no mark" was MEASURED, not argued: all nine unmarked ones have ZERO patterns in their window** — no pattern at the turn is a real answer. 30 backend tests + 4 browser checks, **all proven by MUTATION**, since a fail-watch is vacuous for a new module. **The standing lesson is about scope rather than correctness: every mark this layer drew was the right candle by its own rule, and the feature still read as broken, because the rule was allowed to answer a question in places the reader could not see one being asked.** Earlier: 2026-08-07 — 🟢 **THE BOS GATE RAN FOR THE FIRST TIME. IT WENT RED ON THE FIRST COMPARED BAR, AND THE THREE DEFECTS IT FOUND WERE ALL INVISIBLE TO 54 GREEN UNIT TESTS.** Aaron took the TradingView export; `compare_bos.py` now exits 0 — **6,300 bars compared, no divergence**, at warmups 900 / 1000 / 2000 / 3000, over 7,200 closed M15 bars (2026-04-21 → 2026-08-07), with the Python configured **from the export's own `cfg_*` columns** (ATR 1.3 stop · 0.786 entry · Break-leg anchor · VWAP on · gap entry OFF · 0/0/100). 🔴 **DEFECT 1 — A DEAD LEG KEEPS ITS NUMBERS AND THE PORT CLEARED THEM.** Pine's `bosL_on := false` touches nothing else: `bosL_high` / `bosL_low` / `bosL_bar` / `bosL_n` / `bosL_half` are `var`, reassigned only when a NEW break arms, and `px_l_ext` / `px_ord_l` / `lFibsReady` are plotted straight off them. The port rebuilt a blank leg, so **every bar after the first dead leg diverged** (`l_ext: py=None pine=4584.26`). ⚠ **It is behaviour-neutral on BOTH sides — every consumer ANDs with `.on` first — which is exactly what makes the stale numbers safe AND what guarantees no behavioural test could ever have caught it.** 🔴 **DEFECT 2 IS IN THE TOOL AND IS THE SHARPEST OF THE THREE: the harness compared a CONSTANT for its entire life.** `compare_bos.py` read `strat.execution._stage` inside its compare loop, which runs **after** the replay, so every bar was diffed against the run's FINAL stage. The run ends flat, so that constant was 0 — the column checked **nothing at all** until a Pine bar happened to report 1 or 2. Sampled per bar into `exit_stages` now. **A harness that reads live state after the fact is not reading the bar it names, and it fails by looking like it passes.** 🔴 **DEFECT 3 KILLED THE RUN AND BLAMED THE INNOCENT HALF.** TradingView appends the still-forming live bar to every export with all its plotted series blank — `px_volume` included — so the NaN reached the VWAP engine and raised `VolumeUnavailable`, a message that points at the **bar feed**. `_drop_forming_tail` trims it, prints how many, and REFUSES on a blank row in the MIDDLE, because only a trailing run is a live bar. ⚠ **A fourth shape, worth naming on its own: the harness carried an accommodation written to match the PORT rather than the Pine** — `ordinal_l` read `... if bos.long.on else 0`, precisely the zeroing defect the port had. **A comparator that agrees with the thing it is checking is not a comparator; ask what each parity column is compared AGAINST.** ⚠ **THE GREEN IS NARROW, AND THE TOOL SAYS SO OUT LOUD.** `bos_use_fvg` is OFF at the shipped defaults, so **rules 1–5, Method 3 and the Sniper Zone never ran on either side**; block codes 1/3/4/5/6 never fired; the minimum-stop floor is ON and refused nothing (the 2026-08-04 min-stop incident's exact shape); and **6 trades** closed in the window against 1,220 armed and 1,932 priced bars. The arming and pricing evidence is thousands of bars deep; the exit ladder's is six trades. ⚠ **Below warmup 900 it is RED on the BOS ordinal alone, and that is genuine engine cold start** — an SOS at bar 856 resets both counters, which is exactly where the disagreement ends, **read out of the export rather than assumed**. ✅ **8 new tests, all 8 watched RED against HEAD**; ⚠ **one was VACUOUS on the first attempt and PASSED against the defect** — it checked the death BAR, where `update()` has already built the ladder from the still-live leg, so the divergence only begins on the next bar. 258 strategy + 348 backtest green. **The standing lesson is what a parity gate is FOR, and it is not reassurance: this port had 54 green tests and three real defects, and one command found all three — two in the code and one in the tool that existed to check it.** Earlier the same day: 🟢 **THE THIRD BOT HAS A PYTHON SIDE, AND THE PASS'S REAL WORK WAS THE FOUR PARENT DEFAULTS IT REFUSED TO INHERIT.** Aaron asked for the BOS port so his brother could sweep it, after a doc pass found the Pine side complete and the Python side simply absent. `strategies/python/mpc_bos/` is built on the `mpc_bleg` pattern (a tracker plus a thin `Execution` subclass) and **drives end to end — a 2024 replay makes 22 trades / +13.6R and a real 4-combo `run_sweep` over 47,259 bars finishes in 33s across cores.** 🔴 **AND NOTHING IT PRODUCES IS TRUSTWORTHY YET, WHICH IS THE DELIVERABLE.** `tools/compare_bos.py` is written and unit-tested and **has never been run**, because no TradingView CSV of `mpc_bos_strategy_export.pine` exists — five minutes of human time nobody has spent. The previous port was DELETED on 2026-08-04 for producing an 82-configuration sweep nobody could check, and `backtest/tools/bos_sweep.py` was **falsified by ONE Strategy Tester run** the day it was written. 🔴 **THE LESSON IS ABOUT INHERITANCE AS A DELIVERY MECHANISM FOR OTHER PEOPLE'S DECISIONS.** `BosConfig` is a `SosFadeConfig` superset, so every A+ default it does not re-declare arrives uninvited — **and the two that would have hurt most were both added in the last five days.** `exec_fib_nearest` rests a gap on a different fib from Method 3, which this fork's Pine has no input for, **so the export has no column and the gate would have blamed the entry rule** — the `eq_exempt_fvg` incident's exact shape. `exec_secondary` needs a second bar stream `run_sweep` cannot supply, so **every BOS sweep would have refused outright.** ⚠ **The reverse rule is as load-bearing: nothing in the config may exist without a Pine input behind it** — a field the export cannot carry is a field the gate can never check, which is most of why the old port died. ⚠ **First strategy in the repo to need VOLUME** (the session-VWAP filter, read from the canonical `engines/vwap/`), so `ReplayBar` gained an optional `volume` — **`None` = the feed carried none, never 0.0** — and a frame without it REFUSES rather than guessing, since blocking everything reads exactly like a strategy with no signals and passing everything is a filter reported as on and doing nothing. ✅ **`docs/STRATEGY_WORKFLOW.md` is new and is the point of the whole thread**: the six stages from spec to a trustworthy sweep, what each produces, which one only a human can do, and a per-strategy status table. ✅ 50 new tests, 246 strategy + 348 backtest green; **two test FIXTURES were caught being wrong by the code**, one of them writing `0.0` where the Pine emits `na` — which is how a parity harness gets "fixed" into ignoring the no-vs-cannot-ask distinction its whole family exists to catch. **Before subclassing a config, diff it against the Pine it claims to mirror field by field, not by reading the subclass.** Earlier the same day: 🔴 **THE LIVE BOT SENT A 54.82-LOT ORDER ON A $2,000 ACCOUNT AND NOTHING IN THE SYSTEM OBJECTED UNTIL THE BROKER DELETED IT AT THE FILL.** Aaron asked why the notifications kept coming; the bot was HALTED. Intended size **0.25 lots, sent 54.82 — 221x** — and it rested for eight hours looking perfectly healthy. 🔴 **Two faults multiplied. (1) UNITS: `Execution` sizes in OUNCES and the bridge handed that to MT5 as LOTS** (gold's contract is 100 oz, so every order this bot ever placed was **100x**), and the only place `trade_contract_size` appeared in the whole live path was reading a fill *back*. **(2) EQUITY: the strategy replays 5,000 warm-up bars through its own emulator, whose imaginary trades compound its balance** — it was sizing against **~$4,423** against a real $2,000, proven by its own orders all risking **$442.30** across three different stop distances. ✅ **MEASURED off MT5's own order history (`deleted [no money]`), and the −$7.50 on the balance was a HAND trade, magic 0, not the bot.** ✅ **Fixed at ONE seam — `algos/shared/order_sizing.py` is the only place in the live path a lot count is produced.** Lots come from the MONEY (`(stop_distance / tick_size) x tick_value`), so gold, a JPY pair and an index are one arithmetic and nothing is gold-shaped. ⚠ **Be exact about which guard catches what:** the units cross-check does NOT catch a wrong quantity (both routes scale with it) — it catches the SPEC disagreeing with the STRATEGY, e.g. `point_value = 1.0` inherited from gold onto USDJPY, wrong by ~150x. **The MARGIN check is the causeless backstop and alone would have stopped this eight hours early.** ⚠ **Refusing is the answer and is never a rounding-down** — below the broker minimum, above its maximum, or unaffordable all mean NO TRADE, never round up / clamp / shrink to fit, **because a resized order is not the trade the emulator is holding and the two drift apart silently.** ⚠ **"Cannot ask" is never "affordable."** 🔴 **Two more defects came out of the same pull: a resting order the BROKER deletes left no trace anywhere** (now `order_vanished`, and the halt QUOTES the refusal that caused it instead of a sentence true of every cause at once), **and `log_review.py` keyed its "still halted" alert on the PULSE timestamp, written every 15 minutes** — so the hourly reviewer re-alerted all night about one incident. **That is the de-duplicating-alerter bug inside the module that warns about it, and the existing test could not see it because it asserted the two runs SHARE a key rather than naming the key that must be stable.** ✅ **46 new tests, 463 algos green; the eight bridge tests and the dedup test WATCHED RED against HEAD** — the units one failing with *"sent 24.79 lots"*, the incident reproduced in a test. ⚠ **Two test FAKES were found lying and fixed** — one kept no order book so "nothing resting" was indistinguishable from the broker having deleted the order, and the bridge fake now returns the SAME dataclass production returns so a fixture can never again be more complete than the code. **The standing lesson is about where a unit lives: the strategy and the broker each had a coherent, documented, tested idea of what a quantity meant, and NOTHING owned the conversion between them — so there was no single place a reviewer could look to find it missing, and every artefact on both sides read as correct. Before trusting a number that crosses a system boundary, ask what its UNIT is on each side and which line converts it.** Earlier the same day: 🟢 **THE 1-MINUTE RE-ENTRY SHIPS ON NOW, CAPPED AT ONE PER PRIMARY, AND TURNING IT ON MADE TWO UNRELATED CODE PATHS WRONG.** Aaron read two `SEC` chips on one 2024-12 screen and asked whether ONE primary could hand out several re-entries. **It could, and that was the design**: the latch retired the 1-MINUTE leg, so a live 15m setup kept producing fresh legs — 2024-12-02 took two off one structure break (15m SOS bar 7893, 1m legs 120399 and 120499, the second filling **two minutes** after the first closed). ⚠ **They were never concurrent and could not be** — one position slot, enforced in two places — **but the same 15m idea was risked 10% three times in fourteen hours**, which is the real shape of his concern. ✅ **`exec_sec_once_per_setup` (default ON) retires the 15m SOS BAR on a fill**, one-to-one with the primary because the arm already requires the primary reached breakeven; **per SETUP, not per lifetime** — a new break re-opens it. ✅ **MEASURED, one real replay each over 186,366 M15 + 2,745,711 M1 bars: uncapped 190 trades / +165.46R / maxDD 6.53R (50.7%) · capped 188 / +165.46R / maxDD 5.53R (45.3%), zero primaries moved either direction.** It fires on **two setups in 7.9 years**, removing 2024-01-16 18:44 (−1.000R) and 2024-12-03 01:51 (+1.000R). 🔴 **The total R matching to fourteen decimal places is a COINCIDENCE — those two are exactly ∓1R and cancel — and must not be read as "capping is free by construction"**; on another history the second re-entry could have been the +27R one. **What is not luck is the drawdown**: the −1R sat inside the worst losing stretch, so the capped book is now marginally BETTER than the primary-only baseline (5.53R vs 5.61R) where the uncapped one was clearly worse. ⚠ **`exec_secondary` DEFAULTED ON at Aaron's request AGAINST the standing measurement, and that is recorded as overridden rather than quietly reversed**: eight re-entries in 7.9 years, 2023-04-03 is still the whole result, and the book's average R/trade excluding it is **0.739 against the baseline's 0.777** — it still dilutes. **Pin `exec_secondary=False` to reproduce ANY figure in this repo measured before today**, including the 159 / +142.18R A+ baseline. 🔴 **THE DEFAULT IS WHAT TAUGHT THE LESSON, NOT THE FEATURE. `run_dual` has exactly ONE caller.** `backtest/optimizer.run_sweep` replays a single frame, so the optimizer, sweeps and the stress test's pooled sensitivity had no 1m stream — they would have replayed a **primary-only book and ranked it against a baseline that has re-entries**, this repo's most-repeated defect arriving through a default rather than through a forgotten field. They **REFUSE** now, naming the fix, in two places (before the pool is spawned, and at the seam every combo goes through). ⚠ **And `mpc_bleg` had to PIN it False**: A+ never places an order in that fork so there is no primary to follow, `MpcBLegStrategy.run_dual` raises, and an inherited `True` would have killed **every B-LEG lab run** on a `NotImplementedError`. ✅ **The live bot is untouched** — its instance config states `exec_secondary: false` explicitly and `algos/live/bridge.py` refuses the config outright. ✅ **6 new tests: the cap watched RED against HEAD naming the re-armed 1m leg, and the three that CANNOT be watched red (they pin rules older than the cap) proven by MUTATION** — a lifetime latch, a merged stop-out latch, and a stop-out rule gated on the preference each turn one red. ⚠ **One of those mutations was caught by a DIFFERENT test than the one whose docstring claimed it, and the docstring was corrected rather than the mutation re-chosen** — it is not enough to know the suite catches a shortcut, you have to know which test does. 191 strategy + 341 backtest green. ⚠ **One PRE-EXISTING failure is untouched and is not mine**: `test_python_runner.py::test_the_broker_profile_changes_the_spread` still asserts 0.33 where `fills.py` has said 0.32 since it was re-measured — already recorded as stale in `command-center/backend/CLAUDE.md`. **The standing lesson is about how far a DEFAULT reaches: flipping one inside a strategy package made a shared library in another subsystem silently wrong and a sibling fork loudly broken, because a default is read by every caller — including the ones that cannot honour it.** Before defaulting a capability ON, enumerate what reads the flag, not what implements the feature. Earlier: 2026-08-06 — 🔴 **THE 1-MINUTE RE-ENTRY HAD NEVER OPENED A POSITION ON REAL DATA, THREE WEEKS OF GREEN TESTS SAID OTHERWISE, AND MEASURING IT SAYS IT DOES NOT EARN ITS PLACE.** Aaron asked to build out secondary trades on `mpc_sos_fade`. 🔴 **`run_dual` built its 1m signal as a namedtuple WITHOUT `last_conf_high` / `last_conf_low`** — the structure runner trail's anchors, which the shared `_advance_stage` reads on **every** managed bar, primary or secondary — so **the first 1m bar after any secondary fill raised `AttributeError` and the run died.** Not a wrong number: the feature could not run. ⚠ **The reason the suite missed it is the transferable half: `tests/test_secondary.py` hand-builds its own 1m bar as a `SimpleNamespace` carrying both fields, so the FIXTURE was more complete than production and every test exercised a shape the code never produced.** The regression test now DERIVES the required set by reading `_advance_stage`'s own source for `sig.<field>` — a hand-written list would have re-frozen exactly the assumption that failed — and was watched red naming both fields. 🔴 **It had also gone unmeasured for three weeks because of a GUESS written into three files as a fact: "the broker serves ~35 days of 1m".** Never measured, and false — probed against the live terminal, real M1 XAUUSD runs back to **2018-09-14, ~2.8M bars**, the same depth as the M15. So the only windows anyone believed reachable were far too short for a setup that fires ten times in 7.9 years, and the feature looked untestable rather than untested. ⚠ **Verify intraday depth by bar DENSITY, never by the earliest timestamp** — 2,417 days at 1,136 bars/day, with the 414 short days being Sunday opens. ✅ **MEASURED over the full history, 186,274 M15 + 2,744,333 M1 bars: baseline 180 trades / +139.90R / maxDD 45.6% · secondary ON 190 / +165.46R / maxDD 50.7%.** ✅ **A CONTROL run of `run_dual` with the secondary OFF reproduced the baseline exactly (180 trades, identical entries)**, which is what makes the delta attributable — the two share one position slot, so without it a difference is a mix of *the re-entries made money* and *the 1m stream nudged the primary*, and nothing afterwards separates them; **zero primaries were displaced either direction.** 🔴 **Ten re-entries in 7.9 years and 2023-04-03 alone is +27.33R of the +25.56R total — remove that one trade and the other nine are −1.77R**, with the book's average R/trade falling **+0.777 → +0.731, i.e. below baseline. A rising total with a falling average is what dilution looks like from outside**, and it is the test Aaron asked for (*"I don't care about trade count, more trade quality"*). ⚠ **The fat-tail defence does not rescue it and is worth refuting explicitly, because this repo's own philosophy invites it**: A+ is designed tail-heavy, but the primary stays positive without any single trade and these ten do not. **Ten trades cannot tell a small edge from a small negative one — the same verdict B-LEG got, for the same reason.** **Default stays OFF.** ✅ **Aaron then asked what happens if the 38.2% retrace comes out and it just enters on the 1m SOS. `exec_sec_retrace` is a config field now** (default 0.382, byte-identical to the constant it replaced, refused outside `[0, 1.0)`, sweepable). **Four full-history replays with 0.382 as the control: 0.0 → 192 trades / +154.38R · 0.236 → 190 / +159.92R · 0.382 → 190 / +165.46R · 0.5 → 189 / +170.07R.** 🔴 **Entering on the SOS is the WORST row, monotonically, and the mechanism is arithmetic: the stop is the 1m leg ORIGIN whatever the entry, so a shallower entry is a WIDER stop, a SMALLER position for the same risk, and less room to the 15m targets — +2 trades for −11R.** ⚠ **But strip each row's best trade and ALL FOUR are negative (−2.03 / −1.90 / −1.76 / −3.84), so the sweep measures how large one winner grew as the stop tightened, not which entry is better — and the clincher is that 0.5 posts the worst hit rate in the table (1 win, 4 losses of 9) and the best total.** 🔴 **RUNNING ALL THIS DESTROYED THE BAR CACHE TWICE IN ONE DAY, BY MY OWN CONCURRENT BACKTESTS, AND BOTH TIMES IT LIED ABOUT BEING COMPLETE.** `BarCache.save` is a read-modify-write over the whole file with nothing serialising it, so two interleaving is not a race that loses rows — the second writer lands its bytes on the first's at whatever offset it reached. **Measured on the live M1 file: 2,596,551 rows claiming to be one sorted history and holding two, spliced at line 618,164 where a timestamp reads `6-17 07:47:00`.** The M15 incident hours earlier is the worse one because it raised NOTHING: the file lost ~31,000 rows out of its middle while `ranges.json` went on claiming the whole span, so those bars would never have been re-fetched. ✅ **Fixed in `backtest/data/atomic.py`, and the pairing is the lesson: atomicity and locking answer different halves and neither is sufficient.** Temp-file-then-`os.replace` means a reader never sees a partial file, which kills the splice — it does **not** stop two writers each merging into the same base and replacing it in turn, which parses perfectly with one writer's whole fetch silently gone. ⚠ **The lock covers the CSV and its `ranges.json` TOGETHER and `_load_base` holds it across the save/record pair: the invariant is not "each file is well-formed" but COVERAGE NEVER CLAIMS MORE THAN THE BARS ON DISK**, and two individually-atomic writes still leave a window where exactly that lie is true. ⚠ **Re-entrant by necessity** — `flock` is per file DESCRIPTOR, so the nested acquire inside `save` would deadlock against its own process. ✅ **7 new tests, the two that matter watched RED for the right reasons**; ⚠ **the first splice test PASSED against the broken code and was vacuous — 5 writers × 40k rows finish too fast to collide, and only at 250k does the real interleave reproduce. A concurrency test that does not lose the race proves nothing.** ✅ 338 backtest + 159 strategy tests green. **The standing lesson is about which failures a cache can even report: every other guard in that package protects against the BROKER answering a narrower question than the one asked, and this came from inside — two of our own processes, no error, and a sidecar still vouching for bars that were gone. Before trusting a shared file, ask what happens when two of you write it.** The second lesson is the one that cost three weeks: **a plausible guess written into a doc is not a cheap placeholder, it is a signpost, and a wrong one sends the next reader away from the thing they could have measured in one command.** Earlier the same day: 🔴 **THE COMMAND CENTER'S BACKTESTS PAGES WERE AUDITED: STOP CANCELLED WHATEVER JOB A SHARED FILE HAPPENED TO NAME, AND A RERUN SERVED THE PREVIOUS ATTEMPT'S CHART.** Aaron asked for an in-depth audit of the Backtests list and the Backtest detail page, then for the fixes — **27 findings, nine real defects, three destructive, and not one of them produced an error.** 🔴 **`stop_backtest_run` read its job id out of `lab_progress.json`** — ONE file shared by every runner — while `job_id == run_id` for every backtest this app starts, so the lookup could only ever be wrong; **measured on the live file it held a stale `"j2"`**, so a Stop cancelled another platform's job or nothing at all, swallowed the failure, marked THIS run cancelled, released its platform lock **and left the real job running.** 🔴 **Cancelling did not stop the poller either**, so when the agent finished, `_handle_complete` wrote `complete` back over `failed_cancelled` — a run you had stopped came back to life with results. 🔴 **A rerun deleted two of a run's artefacts and left the rest**, so the page served the PREVIOUS attempt: the cached `chart_spec.json` drew the old candles and trades, the blocked/missed layers showed the old refusals, the regime calendar described the old window, and a stale `engine_timeline.json` kept `sized` true. ⚠ **The converse is the transferable half: an optional artefact's ABSENCE is what removes its chart layer, so `if blocked:` was not a write guard — it was a way of publishing last time's data.** 🔴 **A ten-minute WALL CLOCK killed healthy heartbeating jobs and reported "No heartbeat for 0s"**, blaming the agent for something the lab did. 🔴 **On the list, the row Rerun fired on the click** while **there was no per-row DELETE at all**, so the one warning naming what a delete cascades through was unreachable and the bulk path showed the generic message. 🔴 **The news filter's two rules did not compose**, so a trade that was both a holiday and a news window **stayed in the result when you ticked News with Holidays off**. 🔴 **A run with no evaluated ruleset could never show a drawdown percentage, and the page asked you to "set an account balance" while rendering no control that could** — the run's own opening balance was on its equity curve the whole time. 🔴 **The detail page and the Runs list used two definitions of that one number**; ✅ **MEASURED on the shape that decides it, a run down 40% from $10k that never regains its start reads 25% without the opening-balance anchor and 40% with it.** ✅ **Efficiency MEASURED rather than asserted: the runs list was N+1 (one sqlite connection per row) — 12 connections / 23.72 ms → 1 / 2.28 ms with byte-identical output; the run page stopped pulling the whole lab for one badge, 20.6 KB → 0.002 KB; the python runner's `_JOBS` was never evicted (4.14 MB per finished run, retained for the process's life) and now sheds the PAYLOAD while keeping the status, because deleting the entry turns *finished an hour ago* into *failed*.** ⚠ **Two audit findings were WRONG and are recorded as wrong** — the per-GET regime breakdown and the re-price's four curve walks were flagged as costs and measure **20.1 ms and 19.3 ms for the whole endpoint**; *a cost guessed at is the same error as a number guessed at.* 🔴 **A TENTH defect was found by DRIVING the fixes rather than by reading them, and it is the quietest of the lot: `GET /backtests/running-job` named `nt8` and `mt5` and never passed `python`.** `lab_db` computed the bucket, `RunningJobStatus` DECLARED it, and the model's `running=False` default filled the hole in silence — **so a python backtest reported its own platform FREE for its entire run.** The LOCK was never affected (a second run was refused `409`, measured), so nothing could double-run; what broke is every control gated on that answer — the list's Rerun, the page's Retry and Rerun, the Run modal, Optimize — all enabled through a running job and able only to raise an error toast. ⚠ **It is the `entry_ms`/`favorable` trap from the opposite direction, and the more dangerous direction: not a field the model failed to DECLARE, but a declared field the constructor failed to ASSIGN — and a Pydantic default is indistinguishable from a measurement.** ✅ **Both destructive paths were then PRESSED against real running work rather than reasoned about: Stop cancelled a full-history replay in 287 ms, released the lock, and the run was still `failed_cancelled` 120 s later with `[failed_cancelled] cancelled` in the job's own log; a rerun drove a run directory 6 artefacts → 0 → 5 rebuilt, the cached `chart_spec.json` included.** 🔴 **The two loose ends were closed the same day and BOTH had been MISDIAGNOSED by the note recording them, which is the sharpest lesson of the pass.** **One reload could leave the app DEAD with port 8000 still bound** — written down as an unexplained one-off blamed on `--reload` killing a replay thread, and in fact nothing to do with backtests: **`touch main.py` on an idle server reproduces it**, /health going from `200` in 19 ms to a hard timeout with the same worker alive at 0% CPU. The reloader joins the worker, the worker's shutdown waits for open connections to close, and **the Vite dev server holds a keep-alive POOL to that port — 19 established sockets, measured — which will never close on their own**, so the socket stays bound with nobody accepting and every request HANGS instead of being refused. ✅ **Fixed with `--timeout-graceful-shutdown 10` and proven by reproducing the sequence twice, `200` in 1.9 ms.** And **a red Calendar test blamed on a clock-dependent fixture was really the MOCK defeating the test's clock** — it served `server_now_ms` from Node's real clock while `useServerClock` trusts the server, so `page.clock.install` did nothing in that entire file and only the one assertion that reads a rendered time could ever show it. ⚠ **The standing lesson is about the notes themselves: a plausible guess written into a doc is not a cheap placeholder for a diagnosis — it is a signpost, and a wrong one costs more than no sign, because it sends the next reader at the innocent half of the system.** Both cracked in minutes by refusing to reason about them and just running the thing. ✅ **25 backend tests (723 green) and 11 browser checks; 19 of the 25 and 10 of the 11 were WATCHED RED against HEAD**, the rest labelled in their own docstrings as pinning the half of a rule that was already right. 🔴 **One browser check was VACUOUS on its first attempt and PASSED against the broken page** — it asserted a page-wide Retry button while the page HEADER carries its own, so the locator matched a control that was never in question; its non-vacuity was then proven by MUTATION. **The standing lesson is a new one and it is about identity: `lab_progress.json` exists to drive one progress bar, and the moment something READ an identifier back out of that shared mutable file, an unrelated platform's job became the target of this run's Stop — while the run id was in hand the whole time. Never recover an identity from a channel built to carry a status.** The sharper second lesson is on the tests: it is not enough to have watched the suite go red, you have to have watched THIS test go red for THIS reason. Earlier the same day: 🟢 **BOTH BOTS HAVE A TIME STOP, AND THE MEASUREMENT SAYS THE CLOCK IS NOT THE LEVER — THE MILESTONE IS.** Aaron asked for a line to draw: *"if a trade is dancing around by this hour, cut it"*, to cut drawdown, and said he did not mind losing profit for it. `exec_time_stop_mode` ∈ {**"Off"**, "Before TP1 only", "Always"} + `exec_time_stop_hrs` (36.0), landed in `config.py`, **all four strategy Pine files**, both export mirrors, `compare_strategy.py` and both lab panels. **Defaulted ON ("Before TP1 only", 36h) on 2026-08-06 at Aaron's request — the A+ baseline MOVED, 159 trades / +137.94R / maxDD 7.99R → 159 / +142.17R / maxDD 5.62R. Pin `"Off"` to reproduce anything older.** 🔴 **The obvious version of this idea does not work, and the data says why: losers here die FASTER than winners** — median hold, losers **2.0h** against winners **17.8h** — so a plain clock cuts both at the same rate. The stop IS the fast exit; a clock can only ever catch the tail that lingers. **What makes it work is the STAGE gate: over 161 trades (2020-01-01 → 2026-08-03), 105 reached TP1 and NOT ONE of them lost, while all 56 that never reached it lost.** That is structural rather than lucky — touching TP1 stages the stop to breakeven — so the clock is only ever asked about trades still genuinely at risk. ⚠ **Breakeven was measured as the alternative milestone and is INERT, which is the counter-intuitive half**: the entry is a RESTING LIMIT, so price is sitting at the entry the moment it fills and the next bar's wick crosses back over it — **161 of 161 trades touch breakeven, median 0.25h, one bar**, and by hour 8 the share of losers that have not is **0%**. A breakeven-gated cut fires on nothing at any usable cutoff. ✅ **MEASURED BY REAL REPLAY, not by re-pricing — 155,440 M15 bars, one full replay per row, at today's shipped defaults (baseline 159 trades / +137.94R):** 24h → +140.21R / maxDD 5.55R · 30h → +142.03R / **5.38R** · **36h → +142.17R / 5.62R (6 trades cut)** · 40h → +142.58R / 5.61R · 48h → +140.09R / 7.35R. **24h–40h is a PLATEAU, not a peak, and that is the only reason 36 is defensible.** 🔴 **`"Always"` at the same 36 hours gives +97.32R — a THIRD of the edge gone** — because it cuts 26 trades where the gated version cuts 6, and the 20 extra are the winners. ✅ **The queue effect was CHECKED rather than assumed and did NOT materialise: 159 trades in every row, including the run that cut 26**, so the cheap estimate and the replay agree to the cent. ⚠ **That is a fact about THIS window, not a licence to re-price instead of replaying** — an exit-side cut frees the single position slot at an arbitrary moment and this strategy takes ~2 trades a month, whereas an ENTRY-side filter frees it exactly when a setup exists, which is how the minimum-stop guard's cheap estimate got its **SIGN** wrong (+1.84R estimated, −1.84R replayed). ⚠ **Do not read +4.23R as edge — the jitter audit put this strategy's run-to-run spread at sd 15.06R, so it is a quarter of one standard deviation. The case for the lever is the DRAWDOWN (7.99R → 5.62R, −30%), bought for R that is noise, and it rests on 6 trades in 6.5 years.** ⚠ **`mpc_bleg` INHERITS it** (the lever lives in the parent's `step()`, both bots share ONE exit ladder) unlike the min-stop guard which that fork pins Off — **but the plateau was measured on A+ trades only**, and a B leg waits for a late retrace by construction. ⚠ **The Pine inputs are declared beside the exit block, NOT in the exec panel, and must not be tidied**: TradingView keys saved input values off declaration order within each type, so inserting a string and a float beside their siblings would silently reset every later one on Aaron's charts — declaring them after the last `input.float/string/int` in the file shifts nothing, so **no "Reset settings to defaults" is needed for this paste.** ⚠ **NOT COMPILED and NOT PARITY-VALIDATED** — and a green parity run taken at the Off default would prove **nothing** about this lever, which is the "green on a branch neither side entered" trap the min-stop guard hit the same week. ✅ 15 new tests, **7 watched RED against a neutered `_time_stop_due` and an 8th against the stage gate removed**; 172 strategy + 693 backend + 297 backtest green. **The standing lesson is about where a rule's power actually sits: the request was for a TIME, and the time turned out to be the least important part of the answer — every usable cutoff sat on a 16-hour plateau, while changing the MILESTONE the clock is gated on moved the result by a third of the strategy.** Before tuning a threshold, check whether the threshold is what the rule is made of. Earlier the same day: 🟢 **EVERY TELEGRAM MESSAGE HAS ONE SHAPE NOW, AND ASKING WHICH BOT COMMANDS WERE STILL USED FOUND SIX THAT COULD NOT WORK — TWO OF WHICH REPORTED SUCCESS.** Aaron's brief on the messages: *"concise, but not so concise I can't diagnose it"*, information that is logically grouped on one line and the rest on the next. **The formats were chosen by RENDERING them into the health chat and letting him pick**, three rounds of it — which is why the shipped shape is his and not an argument from this side of the screen. `shared/alert_format.py` is now the one renderer for all ~30 senders: **`<icon> <LABEL> · <subject>`, then the facts grouped, then what to act on.** ⚠ **The LABEL names the STATE, not the event** — `WILL NOT START` rather than `CRITICAL`, because the first says what is true now, and a test caps the header at 45 characters because that is what a lock screen shows. ⚠ **A health message ends with the CONSEQUENCE, and "Nothing to do" counts as one**: the old messages stated a fact and left the reader to decide whether it mattered, which at 3am is the difference between a glance and an investigation. 🔴 **The timestamp finding is the one worth carrying: Telegram already prints the send time in EACH READER'S OWN local clock, directly above the message, and a bot cannot do better — it sends one string to a group and cannot know where anyone is reading it.** So a second clock was pure duplication, and a UTC one beside Telegram's local one actively invited the reader to reconcile two times for one event. Every "this just happened" message dropped it. **The exception is the one case nobody would think of first — a message about the PAST**, the hourly reviewer at 21:20 reporting a restart at 18:06, where the time IS the fact; `when()` renders those in the box's clock **with the zone named**, because the ledger and the logs are UTC and a bare "6:06" is an hour of arithmetic away from the record it points at. ⚠ **`log_review._ts` and `_at` had to be SPLIT** — one built the dedup key and rendered the display, so improving the wording would have re-announced every outstanding finding exactly once. ⚠ **The entry states the risk and the exit does not restate it** (Aaron: *"these messages reply to the initial message… I already know how much I risked"*), which makes the entry the ONLY place it is said and is why a test guards it. **"Risking", never "losing if stopped"** — a gap fills worse than the stop. 🔴 **THE COMMAND AUDIT IS THE LARGER FINDING, AND IT IS THE EMPTY-REGISTRY ROT FOR THE THIRD TIME.** Aaron asked whether all the bot's commands were still used. **`BOTS` and `TASK_NAMES` in `telegram_bot.py` were `{}` — and had been since the four first-attempt bots were deleted on 2026-06-22.** So `/restart` and `/stop` **asked you to confirm, acted on an empty list, and reported SUCCESS**; `/trades` read a per-bot file the live runner has never written and always answered 0; `/resume` and `/resetweek` drove fields the deleted `pnl_tracker.py` used to write; `/emergency` matched the same empty map. All six deleted, with their fifteen helper functions. ⚠ **`/confirm` went too, and it is the subtle one: it worked perfectly and could only ever reply "No pending action", because nothing can create one any more** — the same defect, one level quieter. ⚠ **Control belongs in the command center and that is not laziness**: the Bots page can see how many copies of a bot are running and a phone command cannot, which is exactly why the duplicate-bot guard had to live with the PROCESS (2026-08-04). **A confirmation step cannot make an uncounted start safe.** 🔴 **`/status` was itself broken and nobody could have noticed** — it looped over a `BOT_SCRIPTS = {}` literal declared two lines above the loop, printing a "Trading Bots" heading with nothing under it. It reads `bot_state.read_all()` now, written by the runner every poll, **so a bot appears by RUNNING — the only registry that cannot go stale** — and it reports the process, the heartbeat and the MT5 link as THREE facts, because a bot can be alive and blind. 🔴 **`telegram_bot.py` was not importable on a Mac at all** (`str | None` needs 3.10; the VPS runs 3.11), which is precisely why it had zero tests: **a module nothing can import is a module nothing can test, and nothing says so.** ✅ 419 algos + 678 backend tests green; **27 of the 30 new command tests watched RED against HEAD**, and one of the three that passed there is KEPT AND LABELLED VACUOUS — the old `/status` listed no bots, so "no MT5 link" was trivially absent from its output. **The standing lesson is the one this repo keeps re-learning from a new direction, and the sharpest version yet: a feature that resolves through a REGISTRY can go dead without anything changing, and from outside it is indistinguishable from one that works.** `pnl_tracker.py` and `reporter.py` were caught because somebody asked what they were for; these six were caught the same way, on the same day, in a file nobody had thought to check. **Ask of every command, job and button: what does it resolve through, and is that thing still populated?** Earlier the same day: 🟢 **THE TRADES CHAT CARRIES TRADES NOW, AND FINDING OUT WHY IT DID NOT IS THE POINT.** Aaron asked whether there was a config deciding which chat each notification goes to. **There was one, for exactly one sender.** A sweep of every Telegram send in the repo found **32 messages aimed at the group he reads for entries and exits, and 2 of them were trades**: the live runner's twelve lifecycle messages (link lost, link restored, re-warming, startup banner, clean stop, config refused, and the bridge's **HALTED**), the watchdog's nine, the command center's nine buttons, the Telegram bot's own startup ping, and every finished stress test. **The log reviewer shipped hours earlier with `telegram_health_chat` and nothing else had anything** — which is the shape of fix that looks complete and leaves the problem where it was. Every message now declares a KIND and the kind picks the room: `TRADE` → `telegram_chat_id`, sent by `live/bridge.py` and by NOTHING else; `HEALTH` → `telegram_health_chat`, everything else in the repo. ⚠ **`kind` is REQUIRED with no default**, because a default routes silently and *the wrong room, quietly* is the whole failure — the same reasoning that makes the ledger's stream routing a table rather than a guess. ⚠ **But the required argument is not the guard**: a forgotten one is a `TypeError` raised at 3am inside the very alert that was trying to tell you something, so `test_notification_routing.py` on **both** sides greps every call site and fails in the SUITE instead — and each sweep asserts it MATCHED something, because a grep test that finds nothing passes for ever (the vacuous-check trap the Stress Test browser suite hit the same week). ⚠ **The fallback is deliberately asymmetric**: HEALTH with no room of its own borrows the trades chat and warns once, TRADE never borrows the other way — health in the wrong room is a nuisance you can see, a fill buried in re-warm chatter is the thing being prevented. ⚠ **The bridge HALT is HEALTH, and that is the call worth defending** — it is the most consequential message here, which is exactly why it must not sit in a room only checked when a fill arrives, and why `log_review.py` raises it AGAIN as a standing chip on the Bots page. ⚠ **Command REPLIES are not routed at all** (`telegram_bot.send_to`): an answer belongs where the question was asked. ⚠ **`command-center/backend/services/notify.py` carries a SECOND copy of the table** — the subsystem boundary allows a shared FILE and forbids a shared import — held together by routing on the same credential KEYS, pinned by a backend test that READS `shared/notify.py`, and that app **refuses `TRADE` outright by test** since it cannot know a trade happened. 🔴 **The collateral defect: `log_review.py` read `credentials.json` directly, so `LWG_TELEGRAM_HEALTH_CHAT` — an env override this repo's own template documented — did nothing at all.** One resolver now; **a second reader of one credential is a second answer.** ⚠ **Getting the group's chat id took four failed attempts and both causes produce an IDENTICAL empty `getUpdates`**: BotFather privacy mode is ON, so a bot never receives a plain group message (only a slash command or an @mention), and the running Telegram bot's long poll CONFIRMS each update, which deletes it — with `SYS_MONITOR` restarting that bot inside ~60s to eat the next one. Disable `SYS_TELEGRAM` first, and **re-enable it**, because disabling it silences every fill alert and nothing reports that it is off. ✅ 371 algos + 678 backend tests green, the guard watched RED against HEAD where it named all three unrouted senders; deployed, the bot and the Telegram bot both restarted onto it, live and current. **The standing lesson is not about Telegram: a routing rule that exists in ONE of its callers is not a routing rule, it is a special case wearing the word configuration.** The reviewer's own chat setting was real, correct, and tested — and it made the other thirty messages harder to notice, because the feature now looked like it had been done. **Before believing a cross-cutting concern is handled, enumerate its callers and check them, rather than checking the one you just wrote.** Earlier the same day: 🟢 **SOMETHING READS THE BOT'S RECORD NOW, AND THE FIRST THING IT CAN SEE IS A HALTED BRIDGE.** Aaron asked whether a problem found in the logs could notify him or mark a bot as needing review. **Nothing was reading those logs at all**: `monitor.py` asks whether the PROCESS is there and stamping, `deadman.py` asks whether the BOX can still talk, and neither opens a line the bot wrote. 🔴 **So the order bridge could be HALTED with no alert in this system able to see it** — the loop runs, the heartbeat ticks, `wmic` lists the process, the Bots page says RUNNING, and the bot places nothing. Same for a crash loop overnight, a terminal link lost and regained four times, or a runtime config change the bot REFUSED, so the page shows settings it is not using. `algos/notifications/log_review.py` (`SYS_LOGREVIEW`, hourly) reads the health stream and reports in **two places because they fail differently**: Telegram gets one message per new finding, and `<instance>/review.json` is a standing **Needs review** chip on the Bots page — *an alert you scrolled past at 3am is gone; a chip is still there tomorrow.* ⚠ **The charter is what keeps it from becoming a second watchdog: `monitor.py` owns NOW, this owns THE RECORD.** It deliberately does not alert on *process gone* or *heartbeat stale* — those are the watchdog's, and two alerts for one event is how a channel gets muted — and it **restarts nothing**, because two things issuing starts for one bot is how a book gets doubled. ⚠ **One alert per OCCURRENCE, not per run**: a finding's key carries the timestamp of the thing that happened, so the same halt does not ping 24 times a day **and a NEW halt still does** — keying on the KIND of thing would alert once and then stay silent through every future incident, which is the de-duplicating-alerter bug and is silent by construction. ⚠ **Being unable to read is a FINDING, never silence** — the `mt5_link` rule for the fifth time, and here the reassuring answer is the dangerous one, because a checker with nothing to say is indistinguishable from a system with nothing wrong. **The other half matters as much: a bot you stopped ON PURPOSE has no record to write, and an alarm that fires every time you stop one is an alarm you learn to ignore.** ⚠ **Silent when clean, no exceptions** — the reason `reporter.py` was deleted rather than fixed. ⚠ **`telegram_health_chat` routes findings to a SEPARATE chat from the one carrying fills**: this traffic is routine chatter, and the day you mute it you must not also mute your trades — the dead-man's switch's use of EMAIL for the same reason. Unset falls back to the main group and SAYS which it used, the opposite call from `deadman_url`, where unset means the check cannot work at all. ✅ 29 new tests (354 algos + 657 backend green), frontend typechecks. **The standing lesson is the one the record itself taught, one layer up: writing the evidence down is only half of it — a log nothing reads is a log nobody will read on the day it matters, and the failures worth catching here are the ones that never raise anything.** Earlier the same day: 🟢 **THE BOT'S DAILY RECORD IS NOW TWO STREAMS THAT NEVER OVERLAP, AND A SILENT DEATH LEAVES A TRACE FOR THE FIRST TIME.** Aaron's spec: every day must carry enough about the bot's HEALTH that a bug or a silent death is readable from the logs, everything about TRADES down to why one was skipped or blocked, **nothing overlapping between the two**, and the lot pushed to GitHub — *"once a day is wrong, I think it should be at least twice a day."* Three per-day files per bot now: `decisions-*.jsonl` (bar, blocked, missed, trade, order events), `health-*.jsonl` (starts, stops, crashes, link outages, re-warms, config changes, halts, a `pulse` every 15 min) and a dated text log. **The dividing line is the SUBJECT, not the severity** — `order_refused` is a DECISION (why no trade on that setup), `halted` is HEALTH (why no trading at all) — and routing is ONE dict that `test_ledger_streams.py` **enforces in both directions** by grepping every `ledger.event()` call in `algos/live/`. 🔴 **The load-bearing fix is that every failure worth catching here PRODUCES NO OUTPUT.** A killed process writes nothing, a wedged loop writes nothing, and a quiet Sunday writes nothing either — so "the file is short today" was never a signal. Two mechanisms answer it. **(1) Every exit the process CHOOSES now writes a `shutdown` record with its reason and exit code**, which is what makes the converse informative: *no shutdown record ⇒ it was killed or the box died.* ⚠ **Until this pass only the clean Ctrl-C path wrote one** — a failed connect, a halted bridge and ten consecutive loop errors all returned in silence, so the absence meant *killed, crashed, OR one of three ordinary refusals*, i.e. nothing. The NEXT run's startup reads it back and warns, **because the trace of a hard kill has to be written by something still alive**; `None` = unknown, never `True`, the `mt5_link` rule for the fifth time. **(2) A `pulse` on a fixed cadence** — the one record whose ABSENCE is the signal, so a stall is a gap of known size rather than an absence somebody has to interpret. It is deliberately not `bot_state.json`'s heartbeat, which is overwritten in place and so can say the bot is blind NOW and never for how long — exactly what the 50-minute outage of 2026-08-04 left behind. ✅ **Backup is automatic and 12-hourly**: `scripts/install_ledger_sync.sh` installs a launchd agent at 00:05/12:05 that fetches the VPS's closed AND open files and commits them. ⚠ **Today's files are still being written**, so a torn trailing JSON line is truncated — and the check is *does the last line PARSE*, never *does it end in a newline*, because a record can be flushed complete a moment before its newline lands and the newline test would silently discard the newest record on every single sync. ⚠ **`closed` and `open` stay separate concepts though both are committed** — merging them is what lets a torn half-day later read exactly like a whole one. ⚠ **The VPS still does not push and must not be made to** (SYSTEM's credential manager makes `git push` BLOCK, and the fix would be a GitHub write token on a box holding a live MT5 password), so the honest cost is that **the backup runs only while the Mac is on** — launchd fires a missed job on wake, so a closed laptop delays rather than skips. ⚠ **The text log rolls by choosing a NAME, never by renaming**, because renaming a file Windows holds open is a sharing violation — the same trap `log_backup.py` already recorded. ⚠ **`log_backup.py` IMPORTS the filename pattern from the module that writes the files**: two copies drift, and the drift's symptom is a whole stream silently never committed, which looks exactly like a stream never written. ✅ **25 new tests (310 algos green), and 12 of the 13 health-stream tests were WATCHED RED against HEAD** — the 13th can only be red once the code it guards exists, and is kept and labelled as such. 🔴 **DEPLOYING IT BROKE STARTING THE BOT, TWICE OVER, AND BOTH FAILURES REPORTED SUCCESS** — found by stopping the live bot to deploy and being unable to bring it back, not by the suite. **(a)** `startup_coordinator.bot_is_running` matched the coordinator ITSELF: in single-bot mode that process IS `startup_coordinator.py --bot <key>`, and the check was a substring search over the whole `wmic` dump — **so the command center's Start button could never start a bot**, and it printed *"already running — left alone"* while failing. ⚠ **`runner.already_running()` got the identical check right by excluding its own PID**: two implementations of one rule, one wrong, this repo's signature shape. The match now needs the runner SCRIPT and the key, per line. **(b)** `wait_for_connection` watched `<bot>.log`, which the new dated handler had stopped writing an hour earlier, so a healthy start would have timed out at 180s and been marked `offline` — **worse than a silent failure, because it sends you to fix a bot that is fine** — and the baseline PATH now travels with the baseline SIZE, since the first start of a UTC day writes a *different* file from the one measured a moment before and yesterday's size as an offset would slice the front off today's. ⚠ **`schtasks /run` reported SUCCESS for the run that started nothing**, exactly as this file already warns; `wmic` is what caught it. 🔴 **And the commit-msg hook silently broke the unattended backup for the SECOND time in one day.** Its exemption had been widened that morning to `*/ledger/decisions-*.jsonl` after the sync was found refusing with a day outstanding; the split added two new file shapes and **the sync broke again on its very first run**, because an exemption enumerates the shapes that existed when somebody wrote it. Both are exempt now and — the actual fix — **the hook is TEST-DRIVEN for real against the exact paths the sync writes** (6 tests, 3 watched red), so a fourth artefact fails in the suite rather than at midnight. ⚠ **The exemptions stay narrow PATHS, never `*.jsonl` / `*.log`**, pinned by a test: a future file holding a contract under either extension must still demand its doc, the way `*.meta.json` explicitly does. **The standing lesson is the repo's own probe rule turned around one more time: it is not enough for a log to record what HAPPENED, because the failures that matter here happen by nothing happening. A record that only grows when things go right cannot distinguish a healthy quiet day from a dead one — so something must be written on a CADENCE, and every deliberate ending must be written down, precisely so that silence becomes evidence.** Earlier the same day: 🔴 **THE STRESS TEST FEATURE HAD BEEN RUN ONCE, THREE DAYS BEFORE ITS OWN ENGINE WAS REWRITTEN, AND EVERY NUMBER ON THE PAGE STILL DESCRIBED THE OLD ONE.** Aaron asked for a full in-depth audit of the stress test page and then for the fixes. **One query framed the whole thing: `SELECT count(*) FROM stress_tests` returned 1, and that row was written 2026-07-27 — three days before the accuracy pass that replaced the shuffle series, the drawdown basis, the sensitivity metric and the walk-forward floor.** Nothing had re-scored it, so a stored **D** sat on the Stress Tests list for over a week; re-grading it through the live backend after the migration gives **ungraded**, both probabilities NULL, sensitivity 0.858 → 0.205. 🔴 **The load-bearing defect is this repo's newest refrain reaching a third page: a walk-forward or sensitivity child was launched WITHOUT the baseline's `cost_layers` / `broker_profile` / `sizing_mode`.** So stress-testing a run that charged spread and swap measured every child on a FREE BOOK, and the sensitivity score attributed the cost gap to the parameter it had just shifted. ✅ **Proven on real data rather than argued — every child of a charged 161-trade baseline now carries `["spread","swap"] / vantage_demo / consistent`.** The Tuning workbench had the identical defect the same day; **the generalisation is the one recorded there and it is now a rule this app has broken three times: carry forward everything that decides what a run is MEASURED on, or the comparison becomes the thing that lies.** 🔴 **The page rendered drawdown in DOLLARS against a dollar limit while the grade beside it had been decided in PERCENT** — the engine picks its basis per run (`dd_basis`) and the page read neither that field nor the percent columns — **so a red "over limit" could sit next to an A.** 🔴 **`degradation` is a MAGNITUDE and the sensitivity radar drew `-degradation`, so a parameter shift that IMPROVED the result rendered as a long red bar** and "Median Change" was negative by construction. 🔴 **A shift whose backtest FAILED was drawn as a flat zero bar** — *tested, no effect*, which is the most reassuring answer available for a measurement that never happened — and **`prob_pass_eval ?? 0` printed "0%"** for a ruleset with nothing to breach. ⚠ **Both are the same rule the live bot's blind-terminal incident wrote: never let "no" and "cannot ask" be the same value.** 🔴 **`is_trades`/`oos_trades` were written by the engine and undeclared on the API model, so Pydantic stripped them** and nothing could show that every walk-forward window on the stored test closed **6** out-of-sample trades against the engine's own floor of 20 — **the `entry_ms` / `exit_ms` / `favorable` trap for the fourth time in this app.** 🔴 **The equity fan drew a drawdown-limit line at `y = -max_loss_eod` on a CUMULATIVE-P&L axis**: a drawdown is peak-to-trough, so a path can breach many times over without crossing a line below zero, and a fan sitting entirely above it read "no simulation breaches" while Prob. Breach said otherwise. 🔴 **`grade_reasons` had been computed and stored since the accuracy pass and NOTHING rendered it** — the one thing that explains a missing letter. 🔴 **Cancel did not stop the work; delete left the files** (`reports/lab` held **191 directories against 84 live runs**), and a real delete now takes 216 → 192, the test's own directory plus every child's. 🔴 **`asyncio.create_task` was called with no strong reference**, so a long-awaiting stress test was collectable mid-flight and would leave its row `running` for ever. ⚠ **One fix is about honesty rather than a wrong number, and it is the nastiest: a phase that RAN AND CRASHED left a NULL summary, which grading reads as "not run" — explicitly unpenalised — so a crashed walk-forward could hand out an A carrying the caveat "walk-forward not run".** `phases_requested` is now written when the row is INSERTED (the only moment that cannot be missed — proven by the backend reloading under a live test mid-audit and leaving exactly that hole) and `phase_failures` names what died. ⚠ **Sensitivity no longer perturbs a param that is UNREACHABLE in this run** — behind a `show_if` switch it has off, 3 of 17 on the measured baseline, i.e. 12 backtests that could only reproduce the baseline — and it reports its coverage rather than letting the reader assume everything was tested. ✅ **27 new backend tests (650 green) and 11 new browser checks, ALL 11 watched to fail against the page at HEAD.** ⚠ **Two of those checks were VACUOUS on the first attempt and passed against the BROKEN page** — `page.locator('svg').first()` is the sidebar logo, and a chart label exists three times over — **which is the whole argument for the fail-watch: a green new test proves nothing until you have seen it red.** **The standing lesson is the Optimizations one at one remove, and it is the more uncomfortable version. That page had never been run at all, which is at least legible. This one had been run — once — and then the engine beneath it was replaced, so it carried the evidence of a completed end-to-end drive and every bit of that evidence described code that no longer existed. Before trusting a subsystem, do not only ask how many times it has executed; ask when, and against what.** Earlier the same day: 🟢 **TWO "DISABLED" JOBS WERE DELETED, AND THE REASON THEY SURVIVED SIX WEEKS IS THAT "DISABLED" AND "DOES NOTHING" LOOK IDENTICAL FROM OUTSIDE.** Aaron asked what `SYS_PNLTRACKER` and `SYS_REPORTER` were for and whether he still needed them. **Neither could have done anything if switched on**: both carried an EMPTY bot registry (`BOT_TRADES = {}`, `BOTS = {}`) inherited from the four bots deleted 2026-06-22, and the live bot writes none of the files they read. They were listed in `algos/CLAUDE.md` as *"still deliberately disabled — waiting for a bot registry"*, and **that label is what protected them**: a job nobody has run is indistinguishable from a job nobody has switched on, and only one of those is a feature. ⚠ **The judgement call, not just the tidy-up: a daily report on a strategy taking ~2 trades a month says "no trades today" almost every day, and a channel that is noise 95% of the time is the one nobody reads on the day it matters.** The bot already pings on entry and exit. ⚠ **And the loss cap was an ALERT, not a LIMIT** — nothing in it could refuse a trade, so it rendered on the Bots page as protection while the bot traded straight through it. **A real cap belongs inside `live/runner.py` where it can stop the loop; a cap that can only send a message is a cap in name only.** ⚠ **The collateral is the more transferable half.** `shared/thresholds.json` and `BOT_THRESHOLDS` went with them (no other reader). The derived P&L fields left `bot_state.py`'s defaults — `daily_pnl`, `weekly_pnl`, `total_pnl_pct`, `peak_balance`, `trades_today` — **rather than being left at `0.0`, because with no writer they would render "+0.00% today" under a field nothing measures**, which is this repo's own rule broken in its usual direction: a fabricated zero and a measured zero must never be the same value. `balance` stays; `live/runner.py` writes it. 🔴 **Checking that claim found the defect this pass nearly shipped: `total_pnl_pct` had no writer either** — the Bots page's *Overall P&L* and Telegram's `/balance` both defaulted it to `0.0`, so a live account up 5% reported dead flat in two places. **The runner writes it now** (it already reads the balance every poll; `starting_balance` is anchored once, and the percentage is `None` when the terminal is blind, never `0.0`). ⚠ **The lesson is about the DELETION rather than the field: removing a writer leaves its READERS behind, and a reader with a numeric default goes on answering confidently.** The seven fields nothing read were the easy half. 🔴 **Telegram lost `/report`, `/demo`, `/live`, `/all` and — the one that mattered — `/force`, which executed WHATEVER action was pending.** It existed to push a report out on a weekend, so with reports gone it was an undocumented second route to `/restart`, `/stop` and `/emergency`, **and the `readonly` role held it.** `/confirm` is the one path now. 🔴 **The same pass found `scripts/bootstrap_vps.ps1` registering neither `SYS_DEADMAN` nor `SYS_LOGBACKUP`**, though both task XMLs sit in `algos/scheduler/` — so **a rebuilt box came back with no dead-man's switch**, the one alarm that fires when the box itself dies, and nothing would have said so, because a missing alarm is silent by construction. Both added. ✅ Deleted on the VPS as well as in git; 271 algos tests green. **The standing lesson is the cousin of "a feature nobody has RUN is not a feature": a feature nobody has run, wearing the word DISABLED, is one nobody will even check.** Before enabling any dormant job, read what it would do against today's registries — not what its name says it does. Earlier the same day: 🔴 **A SETTING WAS SHIPPED LIVE ON A GREEN PARITY RUN, AARON ASKED WHAT IT COST, AND THE MEASUREMENT SAID IT WAS ON THE WRONG SIDE OF THE OPTIMUM.** The minimum-stop guard went live at `"% of price"` 0.10 in the morning — parity green, docs written, hazard closed. **It costs 7 trades and 1.84R over 7.9 years.** Swept properly at Aaron's request (23 configs, 186,220 M15 bars, 2018-09-13 → 2026-08-04, **one real replay each**): baseline **183 trades / +134.75R**; **0.08 → 181 / +136.75R (+2.00)**; 0.10 → 176 / +132.92R (−1.84); 0.15 → −25.28R; 0.30 → −47.65R; `Fixed $` 1.25 → +3.00R; `x ATR(14)` 0.30/0.35 → +0.00. **Now 0.08 everywhere** — strategy default, both A+ Pine files, the lab meta, the instance template and the live bot, promoted and restarted. **The standing lesson is aimed at this repo's own gates: a parity gate proves the two implementations AGREE about a setting; it says nothing about whether the setting is a good one — and a green run reads exactly like validation.** Two more from the same afternoon. ⚠ **A green gate can also say nothing at all: the first export that day ran `"Fixed $"` 0.10 — a ten-cent floor on a $4,000 instrument — and raised block code 7 ZERO times in 21,897 bars.** Green on a branch neither side ever entered. **Before trusting a gate on a feature, check the feature was EXERCISED** — here a one-line block-code histogram (the good export raised code 7 **213 times**). ⚠ **Refusing a trade is NOT subtraction, and the cheap estimate had the SIGN wrong**: with one position slot a refused setup frees the slot and the trade list reshuffles, so deleting the refused rows from a finished list scores 0.10 at **+1.84R** where a real replay gives **−1.84R**. ⚠ **A small floor GAINS R for a mechanical reason — the three tightest stops in 7.9 years ($1.03 / $1.06 / $1.18) were all full −1.00R losers** (median stop distance is $8.88, so they are true outliers). ⚠ **Do NOT read +2R as an edge**: the jitter audit measured this strategy's run-to-run spread at **sd 15.06R**, so 0.05 → 0.08 are indistinguishable. 0.08 is the HIGHEST value that does not start costing — **the most protection for nothing, a SAFETY choice.** ⚠ 🔴 **`x ATR(14)` was measured and REJECTED, and it overturns the intuitive answer**: it is the only mode that adapts to volatility and it was the cheapest on R, but at 0.35 and 0.40 **it never refuses the tightest stop in the entire history**, because that bar was quiet so $1.03 was not tight *relative to ATR*. **The hazard is `qty = risk / stop_distance` — pure price units, volatility nowhere in it — so ATR blocks a different set of trades from the one at risk.** It bought cheapness, not safety. ⚠ **The shipped A+ baseline moved: 183 / +134.75R → 181 / +136.75R.** `exec_min_stop_mode` was `"Off"` precisely so no historical result moved; that protection is now spent, deliberately, so **pin the mode explicitly when reproducing any older run.** `mpc_bleg` pins `"Off"` and is unmoved (`compare_bleg.py` exit 0). 157 strategy + 297 backtest tests green; `compare_strategy.py` exit 0 at warmups 100 / 500 / 1000 / 2000. Earlier the same day: 🔴 **THE NEWS CALENDAR'S BEAT/MISS COLOURS HAD BEEN WRITTEN FOR THE WRONG PROVIDER, AND THE FED'S OWN INFLATION GAUGE WAS COLOURED BACKWARDS.** Aaron asked for an audit of the calendar feature, then for the fixes, applied to the Overview's preview as well. `calendar_service._LOWER_IS_BETTER` decides which way a released `actual` is painted, and it was a flat substring list **written against Forex Factory's naming while the tab reads TRADINGVIEW.** ✅ **MEASURED rather than argued — 811 distinct real titles pulled off the live feed over ~275 days: SIX of its eleven keys matched ZERO of them.** ⚠ **A dead key costs nothing visible, and that is exactly what makes it dangerous: the damage is what it leaves UNCOVERED.** The casualty was **`Core PCE Price Index MoM` — HIGH impact, USD** — which matched nothing and fell through to higher-is-better, so **a cool PCE printed RED "miss" rows beneath a cool CPI printing GREEN "beat"**: two inflation prints, one table, opposite colours, both confident. ✅ **PROVEN THROUGH THE RUNNING BACKEND on real released data: that print at 0.1% against a 0.2% forecast now reads `beat` where it read `miss`, across 18 real PCE rows**, with `Initial Jobless Claims` 229 vs 219 → `miss` and `EIA Crude Oil Stocks Change` −8.262 vs −4.6 → `beat`, neither of which the old list covered. ⚠ **The fix that matters is the GUARD, not the added keys — the suite now FAILS THE BUILD on any key matching nothing in a committed corpus of real titles**, and candidates that read well but matched nothing (`bankruptcies`, `foreclosure`, `unemployment change`) were written, measured and removed rather than left in as aspiration. 🔴 **Nine more defects on the page, and not one of them rendered an error either.** **The week was memoized on `weekOffset`, which does not change at midnight, so a tab left open across Sunday→Monday asked for LAST week for ever — the SAME defect the Overview fixed hours earlier, whose own comment asserted that the Calendar page recomputed and was right. It did not.** **Paging a week rendered the PREVIOUS week's rows under the new week's header with an all-zero day strip**, because `placeholderData` holds the old payload and the page only checked `isLoading` (false while placeholder data exists) — ⚠ **held data is only honest while the KEY is unchanged; when the key changes it is not stale, it is the answer to a different question.** **A failed background poll DELETED a good week** (a dated banner over retained rows now, the rule the bot snapshot already followed); `?day=abc` rendered as an empty week; a category the loaded week has none of rendered as a blank dropdown over an empty list; the "now" line drew on weeks that do not contain now; and **duplicate React keys that are real rather than theoretical — the live feed carries two `CAD Budget Balance` rows at one timestamp.** ⚠ **The efficiency cost was the CLOCK, not the data**: the 1s server-clock tick rebuilt ~200 rows a second, each constructing an `Intl` formatter per call. Also: `dayIndexOf` is shared because **the Overview WRITES the day index the Calendar page READS**, the fetch cache is bounded and locked, and every upstream failure is classified at one seam — **a `JSONDecodeError` IS a `ValueError`, so a third-party outage was surfacing as a 400 "your request was malformed"**. ✅ **88 new backend tests (618 green) and 11 new browser checks, 10 of them WATCHED TO FAIL against the page at HEAD**; the 11th passed there and was kept deliberately, because it pins the half of the error rule that was always right and a rule stated in one direction is the one that gets "simplified" back. ⚠ **That spec needs NO BACKEND, only the dev server** — the calendar reads one endpoint, so intercepting it whole makes the suite runnable without the SSH tunnel or the live MT5 box; prefer that shape wherever a page allows it. **The standing lesson is this repo's own arriving through a PROVIDER boundary: a list of magic strings matched against somebody else's vocabulary is a claim nobody is checking, and it fails in the quietest way available — not by matching the wrong thing, but by matching NOTHING and letting the default answer.** Earlier the same day: 🔴 **THE TUNING WORKBENCH RAN EVERY ITERATION ON A FREE BOOK AND PUT THE RESULT IN A TABLE BESIDE A CHARGED BASELINE.** Aaron asked for an audit of the page, then for the fixes; 17 items landed, and this is the one that changes what a number means. The page exists ONLY to compare a child run against its parent, and `runIteration` sent the window and the two legacy cost fields — **no `cost_layers`, no `broker_profile`, no `sizing_mode`** — so tuning a run that charged spread and swap produced a child measured under different physics from its own parent, with the Δ column attributing the whole gap to the param you moved. **The retry path has read those fields off the row deliberately since the day they existed. This was the one run-launching path in the app that did not.** ✅ **PROVEN BY RUNNING IT — three real python backtests against the live backend on cached XAUUSD M15.** A charged baseline (spread + swap, `vantage_demo`), then the SAME iteration (`exec_tp1_pct` 0→40) fired twice, once with the body the page now sends and once with the body it used to: **PF 1.499 vs 1.581, P&L $3,157.33 vs $3,646.75, and trade counts IDENTICAL at 17** — which is the check that says the charge is real and correctly placed, since spread and swap change what a trade MAKES and never whether it happens. The old page would have shown that $489 as the param's doing. All three probe runs deleted afterwards. 🔴 **"Ranked by profit factor" was a caption over a table sorted newest-first** — this repo's most-repeated defect, a label claiming something no code did, sitting on the one page whose entire job is ranking. 🔴 **The ★ had no sample floor**, so a 3-trade fluke at PF 99 outranked the real winner; it needs 10 trades and the caption SAYS so. 🔴 **Max DD was listed in dollars only** — the identical defect the Runs list had fixed on 2026-08-01, found in a second table, where $1.7M beside $14M of profit reads as ~12% against a true 55.9%. 🔴 **Tuning an iteration hid it** (direct children only, so a grandchild vanished from the only page that compares it) — **and the same filter was admitting SWEEP and OPTIMIZER children as "tweaks", because `source_run_id` is stamped by all three.** ⚠ **Two fixes are about a page that moves under the reader rather than a wrong number**: the chart palette was assigned in TABLE order, so every line changed colour whenever a run finished and re-sorted the leaderboard (creation order now), and edits are held in `sessionStorage` per baseline so clicking a row to inspect it no longer discards the form — **persistence rather than a navigation-guard dialog, because nothing lost means nothing to warn about.** ⚠ **`cost_layers: null` is forwarded as `[]`, never as `null`** — null means "written before layered costs existed", which is not a contract a NEW run can be created under, and `[]` charges the same nothing. ⚠ **One audit finding was WRONG and is recorded as wrong**: `copyChartAsPng` already toasts on every failure path, so the call site ignoring its boolean is not a silent failure. Payload 137 KB → 49 KB per iteration (`GET /backtests/runs/{id}?timeline=false`; `regime_timeline` is 96 KB of it and is the same calendar for every run in the window), slimmed only when the baseline actually carries one and cached under its own key. ✅ **8 new browser checks, WATCHED TO FAIL first — all 8 red against the page at HEAD, all 8 green against the fix**, because a suite written after a fix and never run against the defect is a description of the fix rather than a test of it. 4 new backend tests, 517 backend green. **The standing lesson is the Optimizations one from one page over, sharpened: this feature had been BUILT and never DRIVEN END TO END, and the giveaway is that the two worst defects were both invisible unless you compared a child to its own parent — which is the only thing the page is for.** The generalisation for anything that compares: **carry forward everything that decides what a run is MEASURED on — window, costs, broker, sizing — or the difference column becomes the thing that lies.** Earlier the same day: 🔴 **THE COMMAND CENTER'S OPTIMIZATIONS PAGE WAS AUDITED AND IT HAD NEVER BEEN RUN — 24 DEFECTS, AND THE WORST OF THEM TOOK THE WHOLE BACKEND DOWN.** Aaron asked for an audit of the page, then for the fixes. **The frame for every finding is one query: the `optimizations` table held ZERO rows.** Nothing here had ever been driven end to end, so every defect was latent rather than corrupting data — and equally, none of them could have been caught by use. 🔴 **A `0` typed into a step box hung the ENTIRE BACKEND, not the optimization.** The grid expander ran `while v <= hi: v += step` with no guard on step, **on the event loop inside the request handler**, so a typo froze every endpoint until the process was killed. It counts arithmetically now (`n = floor((hi-lo)/step) + 1`, which also kills the old loop's float drift) and REFUSES step ≤ 0, max < min, non-finite values and absurd axis lengths — **checked from the COUNT, before the list is built, because a guard that materialises the thing it guards against IS the event.** 🔴 **Cancel did not cancel.** It wrote `failed_cancelled` to the row and nothing else: the sweep kept every core, the per-platform job lock (which reads that status) reported the platform FREE so a second job could start on top of it, and the job finished by **overwriting its own cancelled status with `complete`**. 🔴 **Every grid was ranked on a FREE BOOK** while the run it was launched from had spread and swap charged — two numbers produced under different physics, presented as a comparison. 🔴 **Delete and re-run both 500'd on a foreign key**, so re-run crashed on *every* optimization that had a ruleset. ⚠ **The two most interesting defects were in the fix, not in the original, and both are this repo's own lessons arriving from a new direction.** The backend had been computing and STORING a grid-sensitivity score on every optimization since that pass landed and **nothing rendered it** — the one number a parameter sweep exists to produce was the one number the page did not show. Rendering it exposed that it anchored on the **highest profit factor**, which is the ★ only under `raw` mode with no trade floor; under `eval`/`funded`, or with a floor set, it described a *different combination* than the star above it. **And the first fix returned `0.0` when it could not measure — which is the PERFECT-PLATEAU score, the strongest "trust this winner" the metric can say.** It would have printed the most reassuring number on screen exactly when nothing had been checked. It returns NULL now and the card does not render: **the same rule as `mt5_link` and `mt5_connected`, met for the third time — never let "no" and "cannot ask" be the same value.** ✅ **PROVEN BY RUNNING IT, not by the tests — four real python optimizations against the live backend on cached XAUUSD M15.** `step: 0` returned `400` in milliseconds with the server still answering. The same 4-combo grid free vs `spread`+`swap` moved PF 2.631→2.551 and P&L $33,228→$31,149 **with trade counts IDENTICAL at 33 across all eight runs** — which is the check that says the charge is real and correctly placed, since spread and swap change what a trade MAKES, never whether it happens. A cancelled full-history sweep sat at `failed_cancelled` with **0 runs for 100+ seconds** and released the lock immediately. A 500-trade floor on a 4-trade window completed with a winner and said so in words. All four deleted 204 leaving zero orphaned runs or evaluations. ⚠ **`min_trades` defaults to 30 in the MODAL and 0 in the API, deliberately** — profit factor has no opinion about sample size (two lucky trades at PF 8.0 outrank two hundred at PF 2.0), but nothing is assumed of a caller that states nothing, and the modal's 30 is visible and editable, which is what keeps a floor from being a silent narrowing. Also: ~2,000 sqlite connections on a 1,000-combo grid down to a handful, the polled payload trimmed to the grid's own param keys, and two full-lab-run downloads removed. 32 new tests, 499 backend green. **The standing lesson is NOT the label-vs-code refrain this repo keeps meeting — it is one layer earlier: a feature nobody has ever RUN is not a feature that works, and 24 defects in one page is simply what unexercised code looks like when somebody finally looks.** Each one was individually plausible; what made them survive was that no human had ever clicked through the page to the end. ⚠ **Its nearest cousin is already recorded here** — *"a generic mechanism nobody has exercised is not a working mechanism"* (the BOX template's unused label path, 2026-08-03). **The generalisation: before trusting any subsystem, ask how many times it has actually executed. If the answer is zero, its test suite is a statement of intent.** Earlier: 🟢 **THE BOT IS ARMED ON THE DEMO ACCOUNT, AND THE ARMING EXPOSED THAT ITS FIRST TRADE WOULD HAVE REPORTED A P&L DISAGREEING WITH THE BALANCE.** Aaron's call. `--live` is now in `startup_coordinator.py`'s `STARTUP_SEQUENCE`. **The dry run was ended deliberately and early, for a reason worth carrying: it blocked nothing, because nothing arrived.** 274 logged bars over three days, `long_armed`/`short_armed` false on every one, stage never above 1, no stop ever set, not one order suppressed — which is exactly what a strategy taking **~2 trades a month** looks like over three days. **A waiting period only buys confidence if the thing being waited for can happen inside it**, and G5 (*PU Prime's spread, swap, commission and minimum stop distance are assumed, never measured*) cannot be answered by watching at all — every cost figure in this repo was measured on **Vantage**, the bot trades **PU Prime**, and the 2026-08-04 shadow diff already showed the feeds differ by a systematic 4-5 cents. ⚠ **The flag went in `STARTUP_SEQUENCE`'s argv because that tuple is the SINGLE SOURCE for all three start paths** — boot, the watchdog restarting a dead bot, and the command center's Start/Restart buttons. Arming one caller would mean **a watchdog restart silently returning a live bot to dry run**, with the ledger still filling and the page still saying RUNNING: the "two paths, one drifts" defect in the one place where the quiet direction is the dangerous one. **Live is therefore this bot's DEFAULT on this box now — disarming means deleting the flag, not stopping the process, which the watchdog undoes in ~60s.** 🔴 **The defect found BEFORE the first order rather than after: `mt5_ops.get_deal_result` returns MT5's `d.profit`, which is the PRICE MOVE ONLY, read off the CLOSING deal alone.** Swap was dropped and commission — normally booked on the **ENTRY** deal, which that function never looks at — was never fetched. The first live trade would have written a `pnl_usd` quietly disagreeing with the account balance, under a field name giving a reader no reason to check. **This repo's signature defect again: a number correct about a narrower question than the one asked.** `get_deal_breakdown()` now sums **every deal of the position** and reports `gross_usd`/`swap_usd`/`commission_usd`/`net_usd` **separately**, the bridge writes all of them plus fill-vs-intended entry price, and `pnl_usd` and R are NET. ⚠ **Keeping the PARTS is the measurement** — a netted number cannot be taken apart later; gross + swap + commission can always be re-netted. ⚠ **Swap keeps MT5's own sign and is never `abs()`-ed** — gold's short swap is a real CREDIT, and the command center booked exactly that credit as a charge on 2026-08-03 and overstated fees 25%; **a cost field that cannot be positive cannot measure a broker that pays you**. ⚠ **`None` = could not ASK, `0.0` = charged nothing** (`deals: 0` is what an unreachable terminal returns, and writing its zeros down would FABRICATE a measurement) — the same three-state rule as `mt5_link`. 🔴 **Same day, the minimum-stop export finally arrived and the gate it passed proves less than it looks.** `compare_strategy.py` is **exit 0 at warmups 100 / 500 / 2000** on a 21,897-bar export with the guard enabled — so switching it on does not break the port, a real risk now retired. **But the guard never FIRED**: the export's own columns read `cfg_min_stop = 2`, which is **"Fixed $"**, not "% of price" — a **10-cent** floor on a $4,000 instrument — and **block code 7 ("Stop too tight") appears zero times in 21,897 bars**, with the tightest filled stop at **$6.76**, 67× the floor in force. **The repo's own standing lesson pointed at its own gate: a green parity run says the two implementations AGREE, never that either is RIGHT — and it cannot say even that about a branch neither one entered.** Re-export at `"% of price"` 0.30 or `"Fixed $"` 20 to make it bite. 271 algos tests green (16 new in `algos/tests/test_trade_costs.py`; also repaired 8 tests left erroring by the previous day's `LOCAL_INSTANCES` → `LOCAL_ARCHIVE` rename — **a rename that passes its own module and breaks its tests is invisible until somebody runs the suite**). Earlier: 2026-08-04 — 🟢 **THE SHADOW DIFF RAN AGAINST THE LIVE BOT AND FOUND A KNIFE-EDGE NOBODY HAD MEASURED.** Step 9.2 of the live plan: the live bot and the lab replay run the SAME strategy object, so their decision streams should be identical and **any difference is data, never logic** — a narrow, checkable claim, which is what makes it a gate rather than a reassurance. `algos/tools/shadow_diff.py` joins the bot's own ledger to a lab replay **on bar TIMESTAMP** (never on index — the two count from different places, the exact trap the B-LEG harness hit) and diffs field by field. **148 live bars.** ✅ **The clock is right: 148 of 148 timestamps align, zero missing bars.** ✅ **Ten of eleven decision fields are bar-for-bar identical** — stages, arms, vetoes, stops, TP ladder. ✅ **The feeds differ by a SYSTEMATIC 4-5 cents**, Vantage above PU Prime on every single bar and never once the other way — a quote-level difference between two brokers, expected, since every backtest here was measured on Vantage and the bot trades PU Prime. 🔴 **The eleventh field is the finding: `long_edge` diverged by $10.08 on 25 consecutive bars, and the cause was ISOLATED rather than guessed — both prices are rungs on the SAME fib ladder.** Same leg, same anchors, same stage; the live bot rested at **0.618** and the lab at **0.702**. That is `exec_fib_nearest` (rule 3, ON by default since 2026-08-02), which rests on whichever bracketing level is NEARER the gap edge — **a discontinuous choice, and nothing had measured how sharp it is: four cents of feed difference moved the resting entry by $10.12.** ⚠ **It is a different TRADE, not just a different price** — with the stop at 0.886 the two entries are a $32.28 stop and a $22.16 stop, **46% apart**, so the nominal 1R is unchanged while position SIZE, fill probability and the distance price must travel all move materially. **A backtest's fill rate does not transfer across brokers at the margin, and this is the mechanism.** ✅ **Nothing was affected in this window** — no trade, stage never above 1, no stop ever set. A measured sensitivity, not an incident. ✅ **HOW OFTEN IT FLIPS WAS MEASURED THE NEXT DAY, AND THE RUNG FLIP IS THE SMALL HALF.** `backtest/tools/jitter_audit.py`, 186,220 M15 bars (2018-09-13 → 2026-08-04), baseline 183 trades / +134.75R, 12 replays at ±$0.05 per bar: **flips 1.4 per run (0.8%)** — real but rare — while **~10.7 trades are lost and ~10.4 gained per run, about 6% of the trade list**, because every entry here is a resting limit at an exact price and five cents decides whether price reaches it. **The FILL is the sensitivity, not the entry rule.** ✅ **All 12 seeds finished POSITIVE (+92.24R to +148.13R, mean +131.74R, sd 15.06R), and the baseline is NOT optimistic — median jittered +134.52R against +134.75R**, i.e. mid-distribution rather than the top of it. **So the STRATEGY transfers and the TRADE LIST does not; expect a cousin of the backtest, not a twin.** ⚠ **A flip is violent when it fires — median 31% change in the 1R stop distance, max 84.5% — and the nominal R never moves, because the trade is sized to its stop. Read it as a SIZE event.** ⚠ **The jitter model is the design: a CONSTANT offset flips nothing** (the whole ladder translates with it), so the offset is redrawn per BAR and applied to all four prices at once — independent per-price noise would build candles no feed can produce. ⚠ **Retiming is why the churn reads 6% and not 9%**: a limit that fills a bar later is the same setup, matched one-to-one nearest-first, and without that constraint one trade gets claimed as the twin of two and both counts collapse toward zero — the tool reporting perfect stability by double-counting. 🔴 **Running the diff found a defect one layer down, and it is the bigger one: the bar cache recorded the window it REQUESTED rather than the data it RECEIVED.** Ask for bars through a date the broker does not have yet — every `--end today` — and that date is marked fetched forever, so every later run reads a cache HIT and gets a frame that silently stops early, with a clean DataFrame and no way to notice. **Measured: the sidecar claimed history through 2026-08-06 while the file held nothing past 2026-08-03 03:45**, and the agent was serving the missing 170 bars on request the whole time. Fixed with two clamps (never past the last bar returned, never into today — a day still filling is indistinguishable from a complete one). **The standing lesson is this repo's own by a third route: the system quietly answered a NARROWER question than the one asked.** The hardcoded history floor did it at the START of the window, `run_report.py`'s default `--start` did it by defaulting, and this did it at the END — and every one of them returns a result that looks perfect. **Never record what you requested as though it were what you received.** 241 algos tests green (11 new on the diff's join, 5 new on the cache clamp). Earlier the same day: 🟢 **THE SUITE HAS AN ALERT THAT SURVIVES THE BOX NOW.** Every alert here originated ON the VPS — the bot's own Telegram messages, the watchdog, the P&L tracker, the daily report — so the one failure nothing could report was the box or its network dying: that produces **silence**, and silence is exactly what a healthy Sunday produces. `algos/notifications/deadman.py` + `SYS_DEADMAN` (every 5 min, SYSTEM) checks each bot's **process, heartbeat freshness and `mt5_link`** and pings an **off-box** service only when all are good; that service alerts when the pings stop. ⚠ **The ping is CONDITIONAL on health, and that is the entire design — it is this repo's own probe rule read from the other side.** An unconditional ping proves only that Task Scheduler is alive, so a healthy box and a bot dead an hour would send the identical green tick. The standing rule gains its mirror: **never trust a POSITIVE result a broken system can produce, just as you must not trust a NEGATIVE one a healthy system can.** ⚠ **Two signals, because otherwise a dead bot and a dead box are the same silence**: a plain ping (missing ⇒ timeout alert, meaning *nothing on that box can talk to me* — and the far end genuinely does not know why, so it says nothing more) and **`<url>/fail` carrying the reasons** when the script runs and FINDS a fault, which alerts at once and by name. ⚠ **Absence is never scored as health** — an unreadable `wmic`, an unreadable `bot_state.json` and a missing heartbeat field are each a FAILURE, and `mt5_link` is read `is False`, never falsy (`None` = UNASKED). ⚠ **It restarts nothing** — the watchdog owns recovery, and two things issuing starts for one bot is how the book gets doubled — and it is a **separate task** from the watchdog on purpose, since a switch sharing a process with the bigger, likelier-to-break program is not an independent check. ✅ **ARMED 2026-08-05 — the switch is real now, and it was proven by making it FAIL.** `deadman_url` is set on the VPS (healthchecks.io, 5 min period / 15 min grace, notifying by **EMAIL** — a different inbox from every other alert here, which matters on the night it fires). Four checks, not one: `--status` reads the URL back, a healthy run reported `sent`, **a deliberate `/fail` ping was sent and the alert arrived**, and a healthy ping cleared it; `SYS_DEADMAN` Enabled, last result 0, every 5 min. ⚠ **The fail ping is the only one of the four that proves anything a human sees** — the other three prove the box can reach the service, and the entire value of this switch is what happens when the box can reach nothing. **An alarm nobody has heard ring is a configuration, not an alarm**; ring it again after any change to the URL, the provider or its notification channel. Unset remains a deliberate, honest no-op (a task that fails every five minutes is one everybody learns to ignore), so a cleared or rotated URL puts this straight back to closed-in-CODE and open-in-FACT — do not read the task existing as the switch existing. ⚠ **The URL is a SECRET**: whoever holds it can send your pings for you and hold the alert green forever, which is worse than no switch because you would believe it. 21 tests (`algos/tests/test_deadman.py`), weighted toward the ways a check can wrongly say "fine" — **a bug in this module is silent by construction; every other watchdog here fails loudly and gets reported, this one fails by staying quiet, and quiet is the normal state.** Earlier the same day: 🟢 **THE OVERLAP AUDIT FINALLY RAN, THE DESIGN INTENT HELD, AND THE SAME RUN FOUND THAT B-LEG HAS NO EDGE.** "The legs don't overlap" has been this repo's load-bearing portfolio assumption and a standing unmeasured item for a year. `backtest/tools/overlap_audit.py` measures it, and on **155,453 M15 bars (2020-01-01 → 2026-08-03)** it passes comfortably: **A+ and B-LEG held a position at the same time on 27 bars** — 0.3% of A+'s own hold time, 0.8% of B-LEG's, 18 of them same-side. Six of A+'s 161 trades share a bar with a B-LEG trade; **two pairs are same-direction**. ✅ **The specific worry was the two firing on ONE structure break, and it is answered directly: across 6.5 years exactly ONE A+ trade has a same-direction B-LEG entry within 16 bars** (2023-07-27, one bar apart, both lost). Monthly R correlation **+0.155** over 75 traded months, **+0.208** over the 33 both traded, both negative together in 3 of those 33. ✅ **The tool is trustworthy because A+ reproduced its documented 161 trades / +135.94R baseline to the cent** on the same run — the cross-check that it drives the real strategies and not a third thing. 🔴 **The finding that actually changes the plan is the other one: B-LEG over the same 6.5 years is 50 trades and −0.94R.** Not a loss — a *nothing*. 34% win rate, +1.65R average win, −1.01R average loss, expectancy −0.02R. ⚠ **And 50 trades cannot tell a small edge from a small negative one**: its 95% CI on mean R is **−0.40 to +0.37**, so the 6.5-year total lands anywhere between −20R and +18R, against A+'s **+0.29 to +1.40** which is entirely positive. ⚠ **The SHAPE is worse than the total** — B-LEG's peak-to-trough drawdown in R is **−15.62R** on the way to finishing at −0.94R, nearly DOUBLE A+'s −7.99R for none of the return. ⚠ **This is a statement about the SHIPPED DEFAULTS, not about the setup**: B-LEG runs 0/0 TP rungs and stop fib "1.0" pinned to its own Pine and has never been optimized over a long window. ⚠ **AND IT IS NOT NEWS — which is the more uncomfortable half.** `backtest/archive/2026-07-29_.../README.md` already said "B-LEG is roughly flat over 7.9 years and is the one that most needs the analysis", at **58 trades / +3.5R**. That snapshot predates the 2026-08-01 phantom-exit fix, so it was on the re-baseline list and today's number is the current one — but the verdict did not change and nobody had acted on it. **What today adds is the ERROR BARS and the SHAPE**, which is what turns "roughly flat" from a shrug into a decision: flat with a ±19R interval and a −15.62R drawdown is not a strategy waiting for its moment, it is a measurement that has not started. **Consequence: bot #2 is no longer blocked on the allocator or the overlap audit. It is blocked on B-LEG having something to deploy.** G10 (allocator) and G11 (multi-bot page) are still unbuilt and still prerequisites, but they stopped being the binding constraint today. **The standing lesson is about the class of claim, not about this bug: an assumption that everything downstream is built on, and that nobody has ever measured, is the most expensive kind of unknown in a repo — and this one was CHEAP to measure.** It took one tool, one afternoon and no new engine, after a year of being restated as intent in three CLAUDE.md files. The audit passing is the small news; the fact that the run also overturned the bot-#2 plan in a completely unrelated direction is why unmeasured assumptions get measured rather than argued. Earlier the same day: 🔴 **"WHY DOES THE TELEGRAM BOT KEEP RESTARTING?" WAS NOT A CRASH, AND ANSWERING IT FOUND A DUPLICATE TRADING BOT.** Aaron had watched it stop and come back for weeks. ✅ **Measured, not guessed: 4,764 Windows Application events over 14 days, none mentioning python, and no crash event (1000/1001/1026) since 26 July** — a `taskkill /f` leaves no event and a real fault does, so every "stop" was an external kill. Four kill paths; three had been fixed the day before without anyone realising they answered this. **The fourth was BY DESIGN**: `startup_coordinator.py` ended by launching `start_telegram.py` unconditionally, whose first act is to force-kill any running Telegram bot — so every Start/Restart killed the alert channel, and SYS_MONITOR then sent 🟢 *Telegram Bot Restarted*. **That message is what made a routine event read as a crash.** 🔴 **Then verifying the fix by actually FIRING `SYS_STARTUP` left the box running TWO copies of the trading bot**, four minutes apart, with nothing anywhere reporting it — the coordinator launched every bot in `STARTUP_SEQUENCE` unconditionally, exactly as it had been doing to Telegram, one level up. ⚠ **Two copies of one bot is the worst duplicate in this system**: same account, same magic number, same strategy, so both see the same setup on the same bar and each sizes a FULL position — double the intended risk from a state neither can see. The bridge filters `get_open_positions()` by MAGIC, so each finds the other's position and reads it as its own; only `adopt_broker_state`'s HALT-on-unknown-position makes it survivable. Dry run, so it cost nothing. **Fixed with three guards:** the coordinator skips a running bot on **BOTH** launch paths (full startup and `--bot` single-bot mode — the second was missed on the first pass and is the one the command center's Start button drives, i.e. the likeliest way anyone makes a duplicate), and `runner.already_running()` refuses to be a second copy at all, covering the paths the coordinator does not own. ⚠ **All match on `--bot <key>`, never the script name** — every live bot IS `runner.py`, so the script identifies the FLEET and only the key identifies the bot. ⚠ **The coordinator and the runner default in OPPOSITE directions when the process list is unreadable, deliberately: neither default is "safe" in the abstract, each is safe against the failure ITS path causes** — a duplicate bot is two positions, an unstarted bot is silence. ✅ **Verified live twice: Telegram kept its PID across a startup run, and re-firing the task with all guards in place left the box unchanged — one bot, one Telegram, nothing new started.** 209 algos tests green. **The standing lesson: a start command that is not idempotent is a duplicate generator, and every automatic recovery here works by re-issuing a start** — the boot task, the watchdog, the agent supervisor. If "start" is not safe to call on something already running, each of those is a route to doubling the book. Read alongside G10/G11 before bot #2. Earlier the same day: 🔴 **METATRADER RESTARTED ITSELF UNDER THE LIVE BOT, THE BOT WENT BLIND FOR 50 MINUTES, AND A BLANK BALANCE CELL WAS THE ONLY SYMPTOM ANYWHERE IN THE SYSTEM.** `C:\MT5_FFT\terminal64.exe` was rewritten by an auto-update at 02:57:53 UTC and the replacement process started two seconds later, taking the running bot's IPC handle with it. From the 02:30 bar onward it saw no market at all, across an open session in which it would have taken no entry and managed no exit. ✅ **Proven rather than inferred: a separate read-only probe attached to the SAME terminal and got balance $2,000, live ticks at 4056.77 and fresh M15 bars, while the bot's own process got `None` from every call** — the terminal was healthy, only the bot's link was dead. 🔴 **The reason nothing caught it is the transferable part, and it is deliberately NOT this repo's label-vs-code refrain: every failure on that path returns an ABSENCE rather than raising.** `copy_rates_from_pos` → None → `get_candles` returns an empty frame (documented "never None", which is right for its callers and fatal here) → `new_bars` reads *no bar has closed* and `gap_bars` reads *no gap*; `account_info` → None → the heartbeat wrote a null balance. So the loop kept stamping, **SYS_MONITOR saw a healthy bot**, `wmic` still listed the process, the **Bots page said RUNNING**, and the log carried not one warning — and Aaron found it by asking why the balance was blank. **Fixed:** `runner.probe_link()` asks `account_info()` FIRST every poll, and `_recover_link()` logs, alerts ONCE per outage, retries on a 30s floor, and on reconnect **RE-WARMS** — an outage is a hole in the bar stream, i.e. the `gap_bars() > 4` condition arriving by another route, and resuming on the next bar would leave the streaming engines carrying a market history that never happened. ⚠ **A bar-based probe cannot do this job and would have looked perfectly reasonable**: an empty frame is also exactly what a QUIET MARKET produces, so such a check either cries wolf out of hours or treats a dead link as a quiet market forever — which is how it shipped. `account_info()` answers whenever the link is alive, 3am Sunday or mid-session, so `None` means one thing. ⚠ **It deliberately does NOT reason about an open position** — if the broker holds one the rebuilt emulator does not, `OrderBridge._agrees` HALTS on the next bar, which is correct and already built; a second, less-tested answer there is how a restart doubles a book. ⚠ **`bot_state.json` gained `mt5_link` because a null balance is not a diagnosis**, and the Bots page draws it as a `No MT5 link` chip BESIDE the Running pill rather than replacing it: the process being ALIVE and being BLIND are both true and are different facts (alive ⇒ a restart is the fix, and the watchdog was right not to fire). ⚠ **The heartbeat is still stamped while blind, on purpose** — dropping it would raise the stall alert, which describes a different failure and would restart a process whose problem is not the process. ⚠ **`mt5_link` is `Optional[bool]` and `None` means UNASKED**, read `=== false` and never falsy — the same rule `mt5_connected` follows on the health strip. 12 new tests (`algos/tests/test_mt5_link.py`), 198 algos tests green, frontend typechecks; deployed and verified live (`mt5_link: true`, balance $2,000, bars current). **The standing lesson is a NEW one: before trusting a probe, ask whether a HEALTHY system can produce its negative result — if it can, it is not a probe.** Every layer here was individually defensible; the defect was that **"no data" and "cannot ask" were the SAME VALUE at every hop**, so the distinction was destroyed at the bottom and unrecoverable above it. ✅ **CLOSED 2026-08-04 by `SYS_DEADMAN`** — see the header entry above; this incident was the argument for it. Same day, recorded rather than built: **the Bots page is CORRECT for many bots and unreadable with them** — `ConfigureTab` maps over every registered bot with per-bot endpoints, so promoting one cannot reach another, but both tabs are flat vertical stacks with no selector and Configure runs about a screen per bot, so the promote button you want sits between two identical ones you do not. Logged as **G11** in `docs/LIVE_TRADING_PIPELINE.md` and paired with G10 there: G10 stops two bots overdrawing one account, G11 stops a human doing the same by clicking the wrong row, and both gate bot #2. Earlier: **Last reviewed:** 2026-08-03 — 🔴 **THE COST FEATURE WAS AUDITED END TO END AND FOUR MORE DEFECTS CAME OUT, ONE OF THEM A LIVE 25% OVERSTATEMENT.** Aaron's instruction was that this feature has to be perfect, so every layer was re-priced against a REAL charged replay and every UI path walked. **The arithmetic is sound and now proven per layer: spread `0.0000R`, commission `0.0000R`, swap `−0.0376R` over 161 trades**, the swap gap isolated to a SINGLE trade held 2022-12-28 → 2023-01-03 (the documented New Year supersede — 0.03% of R, 0.32% of the balance). **All 81 stored python runs re-price through the live endpoint without error.** The defects were all around it: (1) 🔴 **`-Math.abs(cost_usd)` booked every swap CREDIT as a charge** — a short's gold swap is a real credit and **39 of 161 trades are net credits** — so the `Fees charged` row read **$415,990 against the pill's true $332,371 (25% high), and $514,315 vs $252,998 on swap alone (103% high)**: two numbers, one label, six inches apart. (2) 🔴 **A layer the RUN already charged could be charged AGAIN** — the endpoint never read `cost_layers`, so a priced run would be double-billed into a plausible wrong number; now refused per-layer, reported in `already_charged`, priced at 0.0, and rendered ticked-and-LOCKED. (3) 🔴 **The pill stayed live under a firm's SIZED numbers**, reading `Charging 12.08R` over a page that (correctly) ignored it — a sized curve is path-dependent, so it now disables exactly as the news pill does. (4) 🔴 **A server REFUSAL rendered as "Charging nothing"** with the reader's boxes still ticked — `reprice.py` refuses rather than guesses on purpose, and the UI was turning that discipline into a silent lie. Also: **swap, the BIGGEST layer, had no real-replay test at all** (the suite covered only the two exact ones), the broker whose spread was charged was never named on screen, and the "~0.3%" caption never said 0.3% *of what*. All fixed, all verified in a real browser. **The standing lesson is about where to look: every one of these sat in the GAP between a correct backend and a correct page — a sign convention crossing a boundary, a guard applied to one control and not its neighbour, an error path nobody rendered. The arithmetic had tests and was right; the seams had none and were wrong four times.** Earlier the same day: 🔴 **THE COSTS PILL SHOWED THE FEES AND THE PAGE SHOWED THE BALANCE IMPACT, AND NOTHING ON SCREEN SAID THEY WERE DIFFERENT NUMBERS.** Aaron read run `75ccc776d10c` — free $28,248,768, spread+swap on $10,048,027 — and said the obvious thing: *"there's no way my cost could be more than fifty percent of the trades."* **He is right, and the page was inviting the mistake.** The actual fees are **$332,371 — 0.9% of gross winnings**; the $18,200,741 he subtracted is the BALANCE IMPACT, of which **98.2% is lost compounding and 1.8% is fees**. ✅ **Proven by re-walking the identical trades and identical fees at different risk sizes: the gap is 64.5% of the result at 10% risk per trade and 11.0% at 1%.** Same R, same charges — the dollar gap is a leverage artefact, not a cost. The numbers themselves were never wrong: a real charged replay lands 161 trades / 123.90R / $10,090,716 against the re-price's 123.86R / $10,058,027, i.e. inside the documented ~0.3% swap approximation. ⚠ **The defect was the LABELS, and the worst of them was on the one figure it did not describe** — the footer printed `$332,371 after compounding`, which is precisely the number that is NOT the compounding effect. The pill headline is now **R** (`Charging 12.08R`) because a pill has room for one number and R is the only one that cannot contradict the page, and the popover names all three: **Charged −12.08R · Fees charged $332,371 · Net $28,248,768 → $10,048,027**, with "the balance moves −$18,200,741, 55x the fees" spelled out. The `Costs charged` KPI row became **`Fees charged`**. Same pass, the question underneath it: **CLOSING BEFORE THE DAILY CLOSE KILLS THE SWAP AND COSTS 12x WHAT THE SWAP DOES.** `flat_by_close` already existed (default off), so this needed no code — four real replays over 155,453 M15 bars: **hold 161 trades / 135.94R / $28.3M, flat-by-close 161 / 59.82R / $411k.** The swap charge really does go to zero (12.04R → 5.64R, the rest pure spread), so **you save 6.4R and give up 76.1R**. Entries are identical; **73 trades are cut short**, worth 140.39R held and 64.28R cut, and the worst ran 274 hours for +23.96R before becoming a −0.46R scratch. ⚠ **It INVERTS the long side — longs +70.96R → −12.10R** — while shorts survive, because gold's short swap is a CREDIT (+26.98 points/night): over the run shorts were PAID 2.14R while longs paid 8.55R. So "swap is expensive" is a fact about LONGS, and a rule that hits both sides cannot be the fix. **The standing lesson is the repo's own with the emphasis moved: every previous instance was a label claiming something no code did — here every number was correct and every label was defensible in isolation, and the page still misled, because two true figures of different KINDS were shown as if they were the same one and the reader was left to subtract.** When a control changes a headline, show what it changed AND what it charged, under names that cannot be swapped. Earlier the same day: **THE BACKTEST PAGE'S PRICE TAB IS INSTANT — IT IS BUILT IN THE BACKGROUND THE MOMENT YOU LAND ON THE RUN.** Aaron's ask. **MEASURED on run `432aff31f374` (32,978 M15 candles), verified in a real browser: 2,453 ms from clicking Price to a painted chart → 167 ms.** ✅ **The finding is the BREAKDOWN, and it is the transferable part: the two obvious costs were the small ones.** The wait was a ~3.5 MB `ChartSpec` fetch (0.39 s cached, **7.6 s on a cold build**) plus its parse, the lazy klinecharts chunk, and klinecharts laying 33k candles and their overlays out ON MOUNT. Prefetching the first and preloading the second — the two things a page can obviously do ahead of time — bought only 2,453 → 1,964 ms. **The remaining ~1.8 s is a BUILD, and a build cost is only payable by mounting the thing**, so `ChartTabPanel` gained `keepMounted` and the Price tab now stays mounted, hidden, behind whatever tab is showing. ⚠ **Hidden means `visibility: hidden` + `position: absolute`, and `display: none` is the trap that would have looked like it worked** — a display-none container measures 0 wide, so klinecharts sizes its canvas to nothing and has to resize on reveal, which is the visible swap the change exists to remove; `visibility` keeps the real width (canvas 1033 px either side of the reveal, one instance throughout) and contributes no height. ⚠ **The warm mount is DEFERRED to an idle beat (floor 1.2 s), never done on mount** — klinecharts blocks the main thread while it lays out, and the equity curve is what the reader is actually waiting for on arrival. ⚠ **The prefetch is `prefetchQuery`, not a page-level `useChartSpec`**: same cache entry, without subscribing the page to a multi-megabyte object it never renders. ⚠ **The cost is real and is the trade that was asked for — a run page now always fetches the spec and builds the chart, even if the Price tab is never opened.** `StackDetail`/`StressTestDetail` pass no `keepMounted` and are untouched; frontend typechecks and builds, and the build's chunk shape is identical to before, so the lazy klinecharts split is intact. Earlier the same day: **THE BACKTEST PRICE CHART DRAWS ORDER BLOCKS.** Aaron's brother asked to see them, so the canonical `engines/order_blocks/` engine is replayed server-side (`command-center/backend/services/ob_overlays.py`) and each block becomes a `box` overlay behind **Analysis → Order Blocks** (default OFF, and **deliberately NOT in Deep debug** — Aaron's call). It uses the SAME anchor rule as the fair-value gaps: a block is drawn only if it was LIVE on the bar of a trade ENTRY, a blocked setup or a missed setup. **MEASURED on run `432aff31f374` (32,978 M15 candles, 217 anchor bars): 2,567 blocks created, 579 drawn**, beside the gap layer's 661 — the two together ~1,240 boxes instead of ~3,200. ✅ **The result worth carrying is the COST: no new overlay template, no new render effect, no new panel concept — one string in `ANALYSIS_GROUPS`, a colour, and a backend module that is the FVG one with a different engine imported.** That is the payoff on a decision made when the gap layer shipped, and it is the shape to reach for before writing anything bespoke. ⚠ **The one real difference is the BOX GEOMETRY, which is exactly the kind of thing that looks plausible while being wrong**: mpc gives an order block a fixed **30-bar stub** from its anchor candle, stretched to the live bar only while price has come back within one block-height, where a gap box tracks the live bar. Two consequences read as bugs and are correct — a block's box ends long BEFORE the block dies (the zone stays live and keeps answering anchors), and it can end AFTER the bar it died on (the stub runs past the live bar into empty space). ⚠ **There is no settings fork to warn about here, unlike the gaps** — the strategy files dropped order blocks entirely on 2026-07-24/25, so `mpc_assistant.pine` is the only source and the engine defaults ARE its constants. The flip side is worth saying out loud: **a drawn block never explains an entry, because `mpc_sos_fade` reads none.** ⚠ **`obDirOnly` (trend-aligned hide, default off) is NOT modelled** — it is a drawing filter and `engines/order_blocks/CLAUDE.md` names it as something a consumer must not bake in. ⚠ **The emitter is unit-tested (18 tests) and the box-vs-Pine-array cross-check the gap layer carries could NOT be run**: all three OB exports on disk predate the 2026-07-31 re-port (six slots, no `cfg_ob_*` columns) and `compare_ob.py` refuses them outright, so the missing half is NAMED in the test file rather than quietly skipped — the ENGINE's parity is proven separately on a 21,691-bar 15m export and a 13,186-bar 5m one. Re-run `compare_ob.py` on the next real export and add that half. Same pass, a real hole closed: **`_page_analysis` now builds its ANCHORS over the warm-up as well as the paged window** and slices for content, because a zone can outlive the bars its box covers — anchoring off the page alone would drop one that a trade just off the left edge is the entire reason for. ✅ Verified in a real browser on run `432aff31f374` with both layers on together; 388 backend tests green (18 new), frontend typechecks and builds. **The standing lesson is a NEW one, and it is about generic code rather than about labels on a screen: the generic `BOX` template's `label` path had never once been used, and it shipped carrying klinecharts' solid-blue-chip default — so the first `OB` tag rendered as a blue pill.** The `HLINE` label path carried the identical bug, still dormant; `ChartPanel/CLAUDE.md` had recorded that exact trap for a THIRD template and nobody had connected it, because nothing was drawing one. **A generic mechanism nobody has exercised is not a working mechanism — it is untested code wearing a general-purpose name, and its first real user is its first test.** All three now share one `FLAT_TEXT` style. Earlier: **COSTS ARE NOW SWITCHABLE FROM THE RUN PAGE, AND THE ARGUMENT THAT THEY COULD NOT BE WAS WRONG.** Aaron asked for the cost toggles on BacktestDetail as well as in the Run modal. The day before, this repo's own docs argued that was impossible: a cost changes what the trades would have been, so a page-level toggle would flip a number while the trade list under it stayed put. **The premise is right and the conclusion does not follow.** Every re-priceable cost is, in R, **INDEPENDENT of position size** — a trade risking `dist * qty` and charged `spread * qty` loses `spread / dist` of R whatever the size — so the R is knowable even though a charged run compounds into different sizes, and the dollars follow from re-walking the balance. `backtest/reprice.py` does it; `GET /backtests/runs/{id}/repriced` serves it; a Costs pill beside the News & Holiday one reshapes the real KPIs and the Equity chart. ✅ **PROVEN AGAINST REAL REPLAYS, NOT ARGUED:** the test replays `mpc_sos_fade` free and charged, discards the charged replay, rebuilds it from the free run's stored curve and demands equality — and on the live 161-trade lab run `75ccc776d10c` the endpoint reproduces a real charged replay to **37¢ on $16,266,933**. ⚠ **The real split is not "before vs after the run", it is whether a cost changes WHICH TRADES EXIST.** `bid_ask_fills` moved that run 161 → 159 with four setups that never existed on the free path, and `slippage` depends on which exits were market orders; no arithmetic over a stored trade list can produce either, so both are **REFUSED AND NAMED** rather than silently dropped — dropping one would show a spread-only number under a bid/ask label, which is this repo's signature defect. ⚠ **Two accuracies are CAPTIONED rather than hidden:** swap is ~0.3% (the replay charges it on the first BAR after a rollover and latches once, so any rollover in a barless stretch is superseded — Friday and Saturday always, since gold shuts AT Friday's rollover and the weekend rides on the triple-swap Wednesday, plus holidays unpredictably), and a run predating the new stored per-trade `r`/`risk_usd` is ~0.02%. ⚠ **Mirroring the replay's swap code line-for-line would have been the WRONG answer** — it skips Saturday only, and charging Friday books 8 nights a week instead of 7, measured at 0.74R too expensive. ⚠ **`build_equity_curve` gained `r` and `risk_usd` per trade, COPIED from the strategy** — recovering them from a 2dp profit and a 5dp stop price is correct but lossy, and it compounds. ⚠ **Costs compose BEFORE the news filter** and rebuild through the SAME `buildFilteredRun` it uses; both are refused under a firm's sizing, where the curve is path-dependent and the size-independence evaporates. 407 strategy/backtest + 367 backend tests green, `compare_strategy.py` exit 0, frontend typechecks and builds. **The standing lesson is aimed at this repo's own habit, and it is the inverse of the last four: those were a page CLAIMING something no code checked — this was a page claiming something could NOT BE DONE, which had never been measured either. "That cannot be derived" is a claim, and it earns the same evidence as any other.** Earlier: 2026-08-02 — 🔴 **THE LAB WAS NOT PRICING THE TWO COSTS IT ALREADY KNEW: THE SPREAD AND THE OVERNIGHT SWAP.** Aaron's framing is the right one — *"you know what the spread is based upon your broker… the only thing we don't know is slippage"* — and bar mode charged neither, so every Python backtest here has been frictionless in a way that goes beyond the 2026-08-01 commission fix. Both are chargeable now, from a **broker profile** rather than a typed number, behind **independent layers that are ALL OFF by default** (Aaron's call: the baseline run stays free so it stays comparable to the TradingView Strategy Tester, and each cost is switched on deliberately). Contract: `command-center/backend/services/python_runner.py::COST_LAYERS`, stored per run as `cost_layers` + `broker_profile`. **Swap needed almost no code** — the charge path has run on every bar in bar mode since A2 and was dead only because the lab passed `swap=None`; it matters here because the A+ runner is designed to hold overnight. 🔴 **The spread figure this repo had recorded was the WRONG BROKER'S.** `$0.33` is PU Prime's (688k ticks). Vantage — the broker every backtest here actually replays — **measures $0.22** (median over **1,494,459** cached ticks, 2025-08 → 2026-07; p90 0.27, p99 0.31), so quoting 0.33 overstates every backtest cost by 50%. It was MEASURED before being hardcoded, and `GET /backtests/broker-profiles` now SERVES the profiles to the Run modal so no number is ever retyped into a form. **MEASURED on 155,431 M15 bars (2020 → 2026-07-31) at the shipped defaults: free 161 trades / 135.94R / $28.26M · +spread 130.27R / $16.27M · +spread+swap 123.90R / $10.09M · bid/ask fills 159 trades / 141.93R / $29.48M.** ⚠ **A small charge is not a small effect: 12.04R of cost turns $28.3M into $10.1M — 64% of the balance for 9% of the R** — because at fixed % risk a dollar not earned early never compounds. Read a cost against the R, never against the net dollars. ⚠ **The bid/ask row is HIGHER than the free baseline, and that is the finding rather than a bug.** A flat spread charge is the MARKET-ORDER intuition — buy the ask, sell the bid, lose the spread — and nothing in this strategy is a market order: every entry and exit names a PRICE, so the spread changes WHEN an order fills, not what it fills at. A long's buy limit fills at its own price and its stop sells at its own price (identical cash result; the limit is merely harder to reach), so the burden lands almost entirely on SHORTS, which sell the bid to get in and BUY THE ASK to get out — their stops arrive a spread early and their targets a spread late. Six marginal entries stopped filling and, with one position slot, four different setups took their place: the queue effect Run 12 already measured, arriving from a new direction. **So the flat charge is a deliberately conservative UPPER BOUND, and `bid_ask_fills` is the question of what the spread actually does** — it is also the newest path here, so treat its result as a lab finding until it has been read on a chart. ⚠ **`cost_layers = NULL` is not `[]`** — NULL means a row written before layers existed and keeps the OLD contract, `[]` means charge nothing; collapsing them would silently re-price all 80 stored runs on their next retry. ⚠ **Slippage keeps its own switch and its own typed number**, deliberately apart from the measured costs: it is the one thing history cannot measure, and Aaron's standing read is that a live account will differ. ⚠ **Sweeps and stacks do not write the columns yet**, so they stay on the legacy (frictionless) branch — correct today, wire them before anyone expects a priced stack. ✅ **Proven rather than argued: the free path reproduces the documented 161 / +135.94R baseline to the cent, `compare_strategy.py` is exit 0, and 386 strategy + 356 backend tests are green.** **The standing lesson is the repo's own from the other side: the previous four instances were a page CLAIMING something the code did not do — here the code quietly did LESS than the page implied, for months, and there was no wrong label on screen to notice.** A cost never charged leaves no false claim behind, so nothing can catch it except asking, of every number a system could price, whether it is actually pricing it. Earlier the same day: **EVERY TRADE ON THE PRICE CHART CAN NOW DRAW THE FIB LEG IT WAS ACTUALLY PRICED OFF.** Aaron's brother asked to see, on each plotted trade, the fib run on the points that trade used — so a reader can tell which retracement levels it went into. The strategy RECORDS the ladder on the bar it places the order (`mpc_sos_fade/execution.py` → `TradeFib`: the eight `(ratio, price)` pairs plus the bar the LEG started on), `backtest/output.py` puts it on the equity-curve point, `chart_spec._trade_fib` maps it to `trades[].fib`, and the panel draws it as a locked `TRADE_FIB` overlay behind **Analysis → Fibs** (default OFF; renamed from "Trade fibs" and un-nested from under Trades on 2026-08-03, Aaron's call — it is its own reading of the chart, it draws with Trades off, and it sits directly before Fair value gaps because both are the CONTEXT a setup was priced in rather than a kind of signal). **The design decision worth carrying is where the numbers come from: every hop COPIES, none derives.** Those are the same `fiboP*` values that chose the entry edge, the stop anchor and both targets, so the chart cannot land on a price the bot never used — and there is no fib maths in the backend or the browser at all. **A fib rebuilt downstream from anchors and a direction would be a SECOND claim about one leg, and that is this repo's most-repeated failure** (the Run modal collected costs the runner never read; the Optimize modal displayed params the grid builder never read; the SSH dot named a tunnel it never probed; the lab named a rule the Pine implemented differently). What the chart DOES derive is only what a price ladder cannot state on its own: `entryRatio`, where the fill landed on that leg, and `deepestRatio`, how far the retracement really ran afterwards — both pure geometry off two levels the ladder already carries, so no anchor, no direction and no branch for a short. ⚠ **Since 2026-08-03 nothing DRAWS those two** — they are still computed and still ride on the spec, but the chips that printed them were removed with the layer's rename to **Fibs**: the trade underneath annotates the same two price rows itself (`Entry` / `Deepest`, now with prices), and one number told twice by two layers is what made the chart read as doubled up. The fib level labels moved to the RIGHT, ratio only, the way a hand-drawn fib reads. ⚠ **The ladder is snapshotted at PLACEMENT and read back from the ORDER at the fill**, because a fib keeps extending while a limit rests: re-reading it at the fill would report a leg the order was never priced against, while the stop and targets beside it are frozen at placement. ⚠ **Recording is ALL-OR-NOTHING** — a ladder missing a rung is dropped, because seven levels drawn where there are eight reads as "this trade had no 0.786" rather than "this record is incomplete". ⚠ **`deepestRatio` is NOT clamped at 1.0**: a stop-out genuinely traded past the leg origin, and clamping would report every one as having stopped exactly there. ⚠ **NO BACKFILL** (it would mean replaying the strategy), so the toggle correctly does not appear on older runs, on NT8/MT5, or on `mpc_bleg` (which keeps its own `BLegExecution` and never calls `_freeze_fib`); **existing runs need a RERUN — "Reload charts" cannot supply it, and neither can a `?refresh=true` spec rebuild**, because the ladder is written into the run's own equity-curve JSON at replay time and a spec can only copy what is already there. It needed one small addition to the canonical fib engine — `StructureFibEvents.ash_loc`/`.asl_loc`, the BARS the anchors sit on, beside the `ash`/`asl` prices — because **a fib is a LEG, not just a ladder**: two prices say what the levels are, never where the leg is, and a drawing that starts at the fill hides the retracement that produced it. The strategy converts those bars to TIMES (`Signals.fibo_ash_ms`/`fibo_asl_ms`) rather than shipping indices, since **an index is relative to the window that produced it** and the chart trims its candles — the same trap `strategies/CLAUDE.md` records from the B-LEG harness bug. ✅ **PROVEN COSMETIC BY MEASUREMENT, NOT ARGUED, THREE WAYS:** `compare_strategy.py` **exit 0 at warmups 100 / 500 / 1000 / 2000** on the 21,715-bar `VANTAGE_XAUUSD, 15m` export; `compare_bleg.py` exit 0 at 100 and 800; and — because **no `fib_export.pine` CSV is on disk, so `compare_fib.py` could not run** — the fib engine was replayed at HEAD and at the working tree over **47,263 real cached M15 bars** and every field the parity tool compares was diffed bar by bar: **0 differences** across 47,214 active bars, 377,712 level values and 1,824 touch events. ⚠ That A/B is the same evidence by a different route, **not a substitute for the gate** — re-run `compare_fib.py` on the next real export. Validated FORWARD as well as for absence of drift: on a 23,716-bar 2024 replay all 17 trades carry a ladder, and their entry ratios independently reproduce the documented entry model — **0.618 ×5 / 0.702 / 0.786 ×3 exactly on a level (the `_fib_snap` rules), the rest between levels (gap-edge entries)**, with deepest ratios spanning 0.62–0.98, i.e. never past the 0.886 stop. 349 backend + 79 strategy + 45 fib-engine tests green; frontend typechecks and builds. Earlier the same day: **A PARAMETER NOW HAS ONE NAME AND ONE EXPLANATION, SHARED BY ITS PINE INPUT AND ITS LAB ROW.** The TradingView settings panel and the command-center lab had drifted into separate vocabularies for the same settings, and one lab name was simply **wrong**: `exec_deep_fib` read "Floating gap → nearest fib **ABOVE**" while its own Pine tooltip said "shallower than it" — shallower is above on a long and **below on a short**, so the label was false half the time. A script diffed all 51 lab params against their Pine input titles: **6 matched, 37 differed.** Now **42 of 43 shared params are byte-identical** and **43 of 43 lab `desc` fields are the Pine tooltip verbatim** — one parameter, one name, one explanation, two UIs. The single deviation is deliberate: `exec_conf_sz` carries a `(not supported)` suffix in the lab only, because the Sniper-Zone entry is Pine-only and turning it on changes nothing on a lab run. Same pass, at Aaron's request: **every execution and divergence tooltip was rewritten short.** They had grown into forensic essays — the `execFvgPreZone` tooltip alone ran ~1,400 characters of measurement narrative — and a tooltip you have to study is not a tooltip. The rule now is **what it does, what ON vs OFF means, and the one fact that changes the decision**; the long write-ups live in the CLAUDE.md files, which is where someone can find them again. 10 settings that had no tooltip at all (Trade longs/shorts, Risk % per trade, the stop buffer) got one, and both A+ Pine files shrank ~6,000 characters with the B-LEG pair ~3,600, so this RELIEVES the CE10117 compile-token pressure rather than adding to it. Touched: `indicators/mpc_strategy.pine`, `mpc_b_leg_strategy.pine`, `mpc_bos_strategy.pine`, both `_export` mirrors, both `*.meta.json`, both `config.py` comment blocks. ⚠ **The contract is recorded in each meta file's own `_comment`, not only in a CLAUDE.md** — that is the text an editor of that file will actually read. ⚠ **Titles only — never a plot title, never a default, and never the ORDER of an `input.*` call.** TradingView keys a chart's saved input values off declaration order: a rename carries Aaron's settings forward, a reorder silently resets every chart he runs the script on. Block-reason sentences were safe to reword because `f_blkWhy` maps an int CODE to display text — the parity stream carries the code, never the sentence. ⚠ **`mpc_bos_strategy.pine` was only partly synced, on purpose**: its divergence group and Trade longs/shorts were aligned, but its entry inputs ("…and require one (no gap = no trade)") describe a genuinely different setup, and forcing the A+ vocabulary onto them would have made two different rules look like one. ✅ **Proven cosmetic rather than argued cosmetic**: 35 of 35 execution defaults unchanged, both export mirrors still byte-identical to their parents except the line-32 `strategy()` title, 363 strategy + 337 backend tests green, and **BOTH parity gates exit 0 at warmups 100 / 500 / 1000 / 2000** on fresh **21,715-bar** `VANTAGE_XAUUSD, 15m` exports (2025-08-31 → 2026-08-02) taken off the renamed Pine files — `compare_strategy.py` at `cfg_bits` **544375** (rule 3 live, Method 3 and the pre-zone gate off, i.e. the panel Aaron trades) and `compare_bleg.py` at `cfg_bits` **61047** (`execBLeg` ON, A+ priority ON, `execDeepFib` ON, matching that fork's pins). Both files therefore also COMPILE after the rename, which was the only real risk in it — a title is a string literal, and a mangled one is a compile error, never a silent behaviour change. **The standing lesson, and it is the third face of one this repo keeps meeting: a label is a CLAIM, and a claim in two places is two claims that can disagree.** The Run modal collected costs the runner never read; the Optimize modal displayed params the grid builder never read; here the lab named a rule the Pine implemented differently. The fix each time is to make one side the source and derive the other, then check the derivation mechanically. Earlier the same day: 🔴 **THE PRICE CHART EMPTIED ITSELF WHEN YOU SCROLLED BACK FAR ENOUGH, AND EVERY TOGGLE WENT ON CLAIMING OTHERWISE.** The spec ships the newest ~17 months of a 6.5-year run (`chart_spec._capped_start`) and the panel pages the rest in on scroll-left — but everything drawn on those bars EXCEPT the trades (structure overlays, fair value gaps, blocked and missed setups) was only ever built over the shipped window. Past that boundary each layer the reader had switched on drew **nothing**, with its switch still ON and nothing on screen saying why; Aaron read it as the chart forgetting his settings, which is exactly what it looks like. A page now carries its own analysis — `GET /runs/{id}/candles?analysis=true` → `command-center/backend/services/chart_spec.py::_page_analysis`, built with the SAME functions the spec uses — and the panel MERGES it (`allOverlays` / `blocks` / `misses`, deduped by identity), so a layer reaches back exactly as far as the bars do. ⚠ **The engines are streaming state machines, so a page is replayed with `_PAGE_WARMUP_BARS` (2,000) of older bars in front of it** and only overlays reaching into the window are returned: a cold replay opens every page with no swings and no live gaps, which is the same failure one page further back. ⚠ **A page's internal structure is demoted to HISTORIC** — the engine calls the newest leg in whatever it replayed "current", and only the shipped window holds the leg the run actually ended in, so several pages each claiming a current leg would make that toggle describe something that does not exist. **The frontend half is the part that generalises past this chart: a roster DERIVED from data must be RECONCILED, never re-seeded.** `groupsOn` was rebuilt from `overlayGroups` on every change — harmless only because that list never changed, and the moment a page could rebuild it, re-seeding with the defaults would have switched the reader's layers off mid-scroll (the same bug, reintroduced from the other side); `reconcileToggles` keeps an answer already given and defaults only genuinely new keys, and the miss-noise seed now seeds each label ONCE so a reason ticked back on is never re-hidden. **Validated in a real browser, not only by tests:** on run `211384ddbea4` at 2024-05→06 — nine months before the shipped window — BOS/SOS lines, HH/HL/LH/LL tags, gap boxes and pink Blocked markers all draw with the Winners filter still off, in a region that was previously bare candles. ⚠ Costs **~+2s and ~+230 KB per page**, paid per page by a multi-page `goToDate` jump. 337 backend tests green (7 new in `test_chart_page_analysis.py`); frontend typechecks and builds. **The standing lesson is a NEW one, and deliberately not the repo's label-on-a-screen refrain: a per-window computation sitting behind a view the reader can EXTEND — by paging, zooming or filtering — is a silent hole. Whatever the view can reach, the data has to reach too.** Earlier the same day: **THE A+ ENTRY MODEL IS PORTED, AND THE DEFAULT ENTRY PRICE CHANGED.** `mpc_strategy.pine` gained a four-rule entry model on 2026-08-02 and only the Pine had it; `strategies/python/mpc_sos_fade/` now carries all of it — `exec_fvg_pre_zone`, `exec_fib_overlap`, `exec_fib_deep_edge`, **`exec_fib_nearest` (ON)** and `exec_sl_deep` — with `exec_deep_fib` flipped **True → False** because rule 3 replaces Method 3. ⚠ **THAT PAIR MOVES TRADES.** Method 3 only ever looked at the fib ABOVE a floating gap, so a gap sitting a hair short of 0.702 was still entered way back at 0.618 (Aaron caught it on the 30 Jul 2026 trade); rule 3 measures BOTH sides and rests on whichever is closer — a deeper entry and a tighter stop, **bought with fill rate**, because when the deeper level wins the limit sits PAST the gap and a setup that only tags it never fills. **MEASURED on 155,431 cached M15 bars (2020 → 2026-07-31): 165 trades / +126.68R → 161 / +135.94R**, and the old baseline reproduced the Pine's own stated figure to the cent — an independent cross-check on the port. Every rule scan stops at 0.786: 0.886 is the stop, so an entry resting there is a zero stop distance and a cancelled order, which is also why no rule here can REMOVE a trade. The gate needed two pieces of state Python never had — `Signals.fvgs` is now a 4-tuple carrying each gap's **born bar**, and `fibo_half_bar` latches the bar price first tagged 0.5 — and it is applied to **BOTH** gap consumers, because a gap the entry may not use must never be reported as the confluence that armed the setup. ✅ **VALIDATED END TO END, TWICE**: both Pine files compile (Aaron exported from them), and `compare_strategy.py` is **exit 0 at warmups 100 / 200 / 500 / 1000 / 2000** on a fresh 21,710-bar export whose `cfg_bits` decoded **544375 with bit 524288 SET** — so the Pine really was running rule 3 and the Python was configured to it FROM the export, not two sides agreeing on a model neither had enabled. ⚠ **Two sibling forks had to PIN the old behaviour** — `mpc_bleg` (all six; NOT inert there, since it does not override `_entry_edges` and those edges feed its "A+ has priority" gate) and `mpc_bos` (`exec_deep_fib`) — because neither of their Pines has this model. Same day: **the JARVIS confirmation table is restored in `mpc_strategy.pine`**, dead since 2026-07-24 (the drawing was cut for compile tokens and the three inputs survived, so ticking the box drew nothing). **Recovered from `b25789d~1`, not rewritten from memory**, and **proven cosmetic by measurement**: two exports taken at an identical config were diffed cell-by-cell over 21,702 shared bars and **21,701 are byte-identical**, the only differences being the first export's still-forming last bar. Not one fill, exit, stage, block or R moved. **The standing lesson this session adds: a green parity run says the two implementations AGREE, never that either is right — so when a change is meant to be cosmetic, diff the decision stream and prove it, rather than arguing it from the code.** 140 strategy tests green. Earlier the same day: 🔴 **THE COMMAND CENTER'S "SSH" DOT NEVER CHECKED THE TUNNEL, AND ITS MT5 DOT NEVER CHECKED MT5.** `_check_ssh` ran `ssh forexvps "echo ok"` — a **brand-new connection with nothing to do with the port forwards** — so after a laptop sleep the dot sat GREEN beside two red agent dots, sending you to the VPS when the problem was the dead tunnel on this laptop; and `/health` on the MT5 agent answers `ok` whether or not the terminal is running or logged in, so an MT5_Lab that had dropped its broker connection showed green while **every python backtest needing uncached bars failed at fetch time** (the local runner pulls its bars through port 8766). Both measure what they name now — `ssh_tunnel` = the two LocalForwards are BOUND, a new `vps_reachable` carries the old question (which is what separates a dead tunnel from a dead network), and `mt5_connected`/`mt5_server`/`mt5_account` come off the agent's own `/status`; both dots gained a yellow state. ⚠ **`mt5_connected` is `Optional[bool]` and `None` means the agent could not be ASKED — never render an unanswered question as a disconnected terminal.** Alongside them, `main.py`'s one-shot auto-start thread (8s after boot, never again — the reason the MT5 agent had to be started by hand after every sleep) became **`command-center/backend/services/agent_supervisor.py`**, a 60s loop whose first pass is identical to every later one, so "it works on launch" and "it recovers from sleep" cannot diverge. It probes the TUNNEL by port binding and the AGENTS by HTTP, because **`ssh -L` binds the local port itself** — a bound port with both agents silent is a stale tunnel forwarding into nothing, an unbound one is a dead tunnel, and only the pair tells them apart. ⚠ **THE GUARD IS THE FEATURE, NOT THE LOOP:** no action while the scope it would disturb holds a running job, with a **python run counted as MT5 traffic**, and an unreadable job table treated as busy (doing nothing is always safe; the wrong guess kills a live run). One deliberate asymmetry — an **unbound** port is rebuilt even under a running job (nothing can connect, so that job is already failing and this is its only route back) while a merely **stale** tunnel is not, because an agent driving a heavy backtest legitimately stops answering `/health` (the NT8 agent does exactly this under pywinauto). Every `schtasks /run` is **re-probed**: it reports SUCCESS for a task Windows refuses to launch, the same trap `algos/CLAUDE.md` records for the `SYS_*` tasks. ⚠ **The supervisor is OFF under pytest** (`CC_DISABLE_SUPERVISOR=1`, set at module scope in `conftest.py` — a fixture is too late): every endpoint test fires the startup hook, so without it a plain `pytest tests/` would rebuild the tunnel and fire two scheduled tasks on the live VPS, the same class of hazard as `test_integration.py`. **Validated against the live system, not only mocks** — the tunnel was killed and rebuilt for real, and that run caught a genuine incident worth keeping: the NT8 agent died on a backtest submission while its run row stayed `running`, which is exactly the case the supervisor **will not** repair (from here, "dead" and "too loaded to answer" are indistinguishable and the wrong guess kills a live run), so it now NAMES the deadlock in the log instead of retrying silently. Also landed: `services/readiness.py`, a boot-time report of the dependencies that fail SILENTLY — an un-backfilled news calendar makes the News & Holiday filter **inert** (it tags nothing, which is indistinguishable from a broken filter) and missing `algos/credentials.json` makes every Telegram send a no-op — served at `GET /system/readiness`; and `start.sh` reinstalls `node_modules` when `package.json` is **NEWER**, not only when the folder is missing, so a dependency added by someone else's commit installs after a `git pull` instead of failing at import like a code bug. 330 backend tests green (31 new across `test_agent_supervisor.py` / `test_system_health.py`, most of them about what the supervisor REFUSES to do); frontend typechecks and builds. **The standing lesson is the repo's own, now for the third time in three days: a label on a screen is a CLAIM about code somewhere else, and nothing was checking it.** The Run modal collected costs the runner never read; the Optimize modal displayed params the grid builder never read; the SSH dot named a tunnel it never probed. Earlier the same day: **the A+ stop level escaped its dropdown.** `mpc_sos_fade`'s `exec_sl_level` offered five fixed fibs and Aaron wanted 0.90, so it gained a **"Custom"** choice reading a new `exec_sl_custom` — any ratio in (0, 1.0], default 0.886 — with a numeric field appearing under the dropdown in the lab (`show_if`, the same pattern `exec_min_stop_mode` → `exec_min_stop_val` uses). The price comes from the canonical `engines.fibonacci.geometry.fib_level()` fed the leg anchors `Signals` has carried since `e2140c3`, **so Custom 0.886 is bit-identical to picking "0.886"** and switching the mode alone moves nothing — pinned by a float-equality test on a bull leg and a bear one. Two things it buys: the half of the range the ladder never covered (**0.886 → 1.0** = deeper stop, smaller position, more room before the setup is wrong), and, because it is a NUMBER rather than one of five strings, a real **numeric optimizer axis** — sweep 0.88 → 1.00 step 0.01. An out-of-range ratio **raises at construction rather than falling back to fib 1.0** (the shape `_sl_anchor` already had for an unrecognised level): a number a human typed that silently becomes a different stop would replay a whole backtest against a level nobody chose and report it as theirs. Validated only when the mode reads it, so the optimizer may sweep it behind a fixed level — a wasted grid, not an error. ⚠ **NO PINE COUNTERPART, and it is the first exit lever here to be PYTHON-FIRST**: `execSlLevel` is an `input.string` with five options, so `compare_strategy.py` can never configure a Custom run — which is exactly why parity is structurally unaffected, and equally why **a Custom result is a LAB finding, not a validated one**. ⚠ Shallower than 0.886 reproduces Run 4's detonation at any ratio, not only at the three unsupported ones; `dist <= 0` cancels the order outright, and a merely TINY distance is the dangerous case because `qty = risk / dist` balloons the position — turn `exec_min_stop_mode` on first. Also landed: the lab's `min`/`max` schema keys finally reach the number input (carried by the scanner since it was written, read by nothing), as a CUE only — a native number input still accepts a pasted out-of-range value, so the strategy's own check is the real refusal. 129 strategy tests + 299 backend tests green; frontend typechecks and builds. Earlier the same day: 🔴 **THE OPTIMIZER'S "INHERITED" LABEL WAS NOT TRUE.** The optimize modal launched from a backtest's detail page shows every un-swept parameter at the **source run's** value and tags it `inherited · not swept`; `command-center/backend/services/optimization_runner.py` built the grid from the strategy's scanned `default_params` and **never read the run at all**. So optimizing from a TUNED run silently measured a different configuration from the one on screen, and nothing on the page could say so. Measured on the live lab DB: run `096432c2ad20` (MPC B-LEG) carries `exec_tp1_pct = 30` / `exec_tp2_pct = 40` while `config.py` defaults are 0/0, so **every combo in a grid launched off it ran the 0/0 book**. Fixed at ONE seam — `base_params_for(opt, strategy)`, used by both the native and the brute path — and it may change a **VALUE**, never introduce a **KEY**: a run can carry leftovers from an older schema, and for MT5 a `fixed_params` dict holding an input the EA does not declare makes the tester treat the set file as mismatched and silently run a single backtest instead of the optimization. Re-measured on the same run after the fix: **0 keys added, TP1 0→30, TP2 0→40.** Same day, and the reason half that panel was read-only in the first place: **a dropdown or an on/off can now be SWEPT as a value list** — "test every stop level" is one click instead of five runs. `_expand_axis` always accepted `[val, ...]` beside `{min,max,step}`, but **only the Python runner expands a grid locally**; NT8 and MT5 hand a Start/Step/Increment RANGE to their own tester, so a list of strings has nowhere to land there and the job would report success having optimized nothing. Refused on BOTH sides — the UI offers the sweep button only on python runs, and `POST /optimizations/run` returns a 400 naming the params. The chips start with every option ticked and the LAST one cannot be unticked (an empty set expands to zero combinations). 299 backend tests green (9 new in `tests/test_optimizer_grid.py`); frontend typechecks and builds. ⚠ **Every optimization run in this repo launched from a tuned source run was ranked on the strategy's defaults, not on what the page said** — re-run any whose winner still matters. **The standing lesson: a label on a screen is a CLAIM about code somewhere else, and nothing was checking it.** This is the same failure as the frictionless-lab bug below, in different clothes — the Run modal collected costs the runner never read; the Optimize modal displayed params the grid builder never read. When a page states a value, find the line that consumes it. Earlier: 2026-08-01 — 🔴 **THE LAB WAS RUNNING EVERY PYTHON BACKTEST FRICTIONLESS.** `commission_per_side` and `slippage_ticks` were collected in the Run modal, stored on the run row and shown on the page, and `command-center/backend/services/python_runner.py` **read neither** — so every Python result in this repo was measured with no costs while reporting a cost profile it had not applied. The tell was in the data and is worth remembering as a shape: **52 of run `f866873aa862`'s 54 losing trades each lost EXACTLY 10.00% of prior equity**, which no cost model can produce. Worse, the values it was reporting (2.25/1) came from a **FUTURES prop-firm ruleset** and mean nothing for spot gold. Fixed at the seam (`_cost_profile` → `backtest.fills.AccountProfile` → `Execution`), on the single-run AND sweep paths, so the optimizer cannot rank combos on a frictionless book and hand the winner to a run that is not. **Defaults are now 0/0 everywhere** (Aaron's call: a cost you did not state must never be charged), which also exposed the reason the futures figure reached a forex run at all — `RunBacktestModal` looked its primary ruleset up in the futures list ONLY, so a forex selection resolved to null and the form shipped its initial state instead of the ruleset's 0/0. **Costs are byte-identical to before when 0/0** — no profile is built, so no charge path is entered, and every historical result stays reproducible; both parity gates are untouched because `compare_strategy.py` never passes one. Two units that would look plausible if wrong, both pinned by tests: commission is per **LOT** per side (100 oz — reading it per-unit overcharges gold 100x), and slippage is charged on **market exits only** and only in bar mode (a resting limit fills at its price or better or not at all; tick mode measures the real thing off the tape). **Never construct a `LAB_STRATEGY` class directly — go through `backtest.replay.build_strategy`**, which REFUSES to run a strategy that cannot accept a cost profile when the caller stated costs, rather than silently dropping them. 831 tests green (541 + 290). ⚠ **Swap is still not charged from the lab's fields** — the lab does not collect it and this strategy holds overnight, so that cost is real and unpriced outside tick mode. Same day: **three numbers on the lab's pages were TRUE and were being misread, and each needed a companion that had never been computed.** An audit of the same run reproduced every stored KPI exactly from the raw trades, Sharpe included — the arithmetic was never lying. What was: **max drawdown** was stored and LISTED in dollars only ($1.73M beside $14.4M of profit reads as ~12%; against the running peak it is **55.9%** — the detail page had the percentage, the Runs list, where runs are compared, did not); the **win rate** counts a trade that closed a cent up as a win (45 of 111 "winners" made under a sixth of a typical loss, all exiting at the breakeven-stop buffer — honest split **40% won / 27% scratched / 33% lost**); and **profit concentration** measures the largest QUARTER, a question about time, while the reader hears the question about trades (34.5% by quarter on a run where **5 of 165 trades made 47%** of everything won). `max_drawdown_pct`, `scratch_count` and `trade_concentration_pct` are now computed, stored, backfilled onto all 79 historical runs, and shown. **The standing lesson, and it is a different one from the phantom-exit bug's: every number can reproduce and the page can still mislead.** Verifying arithmetic is not verifying meaning — ask what a reader will CONCLUDE from a number, not just whether it is correct. ⚠ A high trade concentration is **not** a verdict: a runner-based strategy is meant to be fat-tailed and this repo's design intent is few high-quality setups, so read it as "the edge lives in the tail, size for that". Earlier the same day: 🔴 **THE PHANTOM-EXIT BUG IS FIXED, AND EVERY BACKTEST NUMBER IN THIS REPO WAS MEASURED THROUGH IT.** `indicators/BUG_exit_fill_price_mismatch.md` — open since 2026-07-14, the "all three legs at one price one bar after entry" shape Aaron's brother spotted by eye — was never a TradingView fill artifact. **The FILL BAR was allowed to stage the stop.** A resting limit is reached by price coming to it from the wrong side (a buy limit fills on the way DOWN, a sell limit on the way UP), so the entry bar's *favourable* extreme is the approach to the order, not a move the trade made. The staging block read it anyway: stage 1 fired on the fill bar, the stop went to entry ∓ the breakeven buffer — **already through the market** — and TradingView market-closed every leg at the next bar's open, at the price that matched no stop and no target. Confirmed to the cent on real Vantage bars (the reference trade's entry bar low was **3637.80**, ten dollars below the 3647.91 fill and every cent of it pre-fill), reproduced against the real Python `Execution`, and fixed in **all five strategy Pine files plus `strategies/python/mpc_sos_fade/execution.py`** (which `mpc_bleg` reuses). **Measured on lab run `d2ab68f9e884`, 165 trades over 6.5 years: all 165 ENTRIES are identical** — not one setup added or removed, which is the evidence the fix is confined to the stop — while 30 results changed, 18 better / 12 worse, **+101.68R → +112.43R**, win rate 63.6% → 67.3%. Four trades the bug had killed at breakeven were really +3.90R / +2.98R / +2.86R / +1.87R. ⚠ **It is not free: 12 trades that used to scratch now take a full −1R, and max drawdown was NOT measured.** ✅ **BOTH PARITY GATES RE-VALIDATED THE SAME DAY** on FULL-HISTORY post-fix exports — `compare_strategy.py` and `compare_bleg.py`, both 21,691 bars (2025-08-31 → 2026-07-31), **exit 0 at warmups 100/200/500/1000/2000** with no truncation warning — **and the fingerprint is measurably gone from the bars**: a stop already staged to breakeven on the entry bar went from **4 of 26** A+ entries to **0 of 27** (B-LEG 0 of 5), with ALL FOUR affected candles inside the window and readable before/after on the same bar. ⚠ **Re-baseline everything measured before today** — 110.65R, the run-capture figures, all twelve runs in `mpc_sos_fade_optimization.md`, both `backtest/archive/` snapshots. 534 tests green. **The standing lesson, and it generalises past this bug: a green Pine↔Python parity run says the two implementations AGREE, never that either is RIGHT.** This defect was faithfully ported, so the gate was green for its entire life; it took a human looking at a price chart to find it. ⚠ **Recorded the same day and repeatedly mistaken for this bug — a wrong-side stop fill is a BACKTEST LIMITATION, not a defect.** With the staging fixed, price can still tag TP1, stage the stop to breakeven, and close back through it inside the SAME bar; the stop only goes live NEXT bar, so it is behind the market and the exit fills at that bar's OPEN instead of at the stop. Being OUT is correct — price genuinely went through the stop — only the exit PRICE is imprecise, because a bar-replay tester checks orders once per bar while a real broker watches every tick. **It errs in the safe direction (the backtest looks slightly worse than reality), and it behaves identically in Pine and Python, so parity is unaffected.** Do not "fix" it to make numbers look better; a "never place a stop through the market" clamp would change real behaviour in all five Pine files and is its own change with its own measurement. Canonical write-up: `strategies/python/mpc_sos_fade/CLAUDE.md` → `### Wrong-side stop fills`. Earlier the same day: **the command center's price chart got a Step navigator, so reading a run one setup at a time no longer means hunting for them.** `◀ Loss 12/60 ▶` beside Go to date (or ← / → with the pointer over the panel) jumps to the previous / next marker and centres it, marking the one you landed on, and a step into unloaded history pages the bars in through the same jump the date pill drives. **The design decision worth carrying to anything similar: it has NO set and NO filters of its own — it walks whatever the chart's Analysis dropdown is already showing.** Untick Winners and ◀ walks the losers; turn Trades off with Blocked on and it walks the refused setups; leave both on and it interleaves them by time. Its own filters would be a second place for the navigator and the chart to disagree, and would let it step to something that isn't drawn. Verified in-browser on run `0e3983a0c3c7`: 164 markers → 104 / 60 by outcome, 138 with Blocked added, stepping Loss → Blocked → Loss and keeping its place across the filter change. Detail in `command-center/frontend/src/components/ChartPanel/CLAUDE.md` → *Step*. Earlier the same day: **the price chart now draws fair value gaps, and only where something happened.** The canonical `engines/fair_value_gaps/` engine is replayed server-side (`command-center/backend/services/fvg_overlays.py`) and a gap is drawn ONLY if it was live on the bar of a trade ENTRY, a blocked setup or a missed setup — all of them when several overlap, nothing anywhere else. That filter is the feature: on the shipped 142-trade run it is **215 anchor bars → 655 boxes** out of a 33,060-bar chart that would otherwise carry thousands. One toggle in the chart's **Analysis** dropdown, default OFF; it needed no new overlay template on the frontend (it is a plain `box` group). **⚠ The gaps are `mpc_assistant.pine`'s, which is deliberately NOT the set the bot traded** — the indicator runs cap 8 / no middle-bar close check / the timeframe-split 0.0–0.04 floor, while `mpc_sos_fade` pins 7 / close-check ON / 0.1, because `mpc_strategy.pine` hardcodes them. So a drawn gap is one the INDICATOR shows and not always one the entry rule counted (the bot sees strictly fewer), and a visible gap beside a "no FVG" block is not a bug. Aaron's call: the chart was asked to match what TradingView draws. **Validated against the Pine itself, not just unit-tested** — a real TradingView export is replayed and every box is diffed against the Pine's own live gap arrays (`px_fvg_top_k` / `px_fvg_bot_k` / `px_fvg_count`): on each sampled anchor bar the boxes covering it must be exactly the gaps mpc had open, price for price. 16 tests green. Two details that would silently draw the WRONG gaps if broken, both pinned: the floor is timeframe-SPLIT (so the same run at M5 and M15 legitimately differs, and an unknown timeframe takes the stricter branch — under-filtering invents gaps the indicator never drew), and the box span mirrors the Pine box, which is DELETED on the bar the gap dies, so `t1` is the bar before. Earlier: 2026-07-31 — **`/audit-engines` against the new `mpc_assistant.pine` paste found three stale engines; all three are fixed and PINE-PARITY VALIDATED on two real grand exports.** (1) **`engines/order_blocks/` was RE-PORTED, not re-synced** — an order block is a different object now: structure breaks no longer create them, so the engine is **STANDALONE** (plain OHLC in, no upstream engine), every block belongs to a `ta.pivot(2,2)` turn read two ways, mitigation is three rules plus a separately-reported age expiry, and `max_active` went 2 → 10. (2) **`engines/sessions/`** — all three windows re-stated in their own city's clock and DST-aware, which really moves London and New York an hour earlier in UTC for ~7 months a year (Tokyo is unchanged); `engines/liquidity/` inherited it and was re-validated, `engines/session_volume_profile/` is unaffected and a test now pins why. (3) **`engines/fair_value_gaps/`** — cap synced to 8, and the harness's flat gap floor replaced by mpc's real timeframe split. **Validation: two grand exports, four harnesses on one chart each, all four comparators exit 0 on both** — `VANTAGE_XAUUSD, 15m` (21,691 bars, spanning four DST changeovers, which is what actually tests the window change) and `5m` (13,186 bars, which is the only way to cover the NY opening range). **Deliberately NOT synced: the Cycle/Macro fib** — mpc reworked its anchor and now sources it from a 1-minute `request.security`, but `mpc_strategy.pine` still has the OLD version, so porting it would MANUFACTURE drift in the A+ bot rather than remove it (Aaron's call: leave it, document the fork). **Three real bugs found on the way, each of which would have produced a misleading GREEN:** `fvg_export.pine` plotted 6 array slots against a cap of 8 (two live gaps were never compared); `ob_export.pine` could not compile at all (`CE10088` — a Pine function may READ a global but never WRITE one); and **`mpc_sos_fade` was reading an engine default it had never pinned** (`fvg_threshold_pct`), so a routine tidy-up of a "stale" shared default would have silently moved the A+ bot's trades with no test failing. Also: `mpc_b_leg_strategy.pine` had never received the session windows its A+ parent has carried since 2026-07-12 — a genuine fork, now closed; both bots re-verified green. `indicators/mpc_jarvis_v2.pine` deleted (superseded by `mpc_strategy_export.pine`; last at `825592a`). **Closed out the same day on two more real exports:** `compare_bleg.py` exit 0 on a fresh 6,329-bar B-LEG export whose window sits entirely inside BST/EDT — the only period where the new city-clock session windows and the old fixed GMT-4 ones actually differ, so it is the run that tests the fork fix rather than the Python side agreeing with itself — and `compare_svp.py` exit 0 on 12,117 bars. That B-LEG export was also the first PARTIAL chart export this repo has seen, and it found a real harness bug: **`bl_l_bar`/`bl_s_bar` carry Pine's `bar_index`, which counts from the first bar the CHART loaded, not from the export's first row.** All 2,409 armed-bar comparisons failed at one flat offset of 15,362 while the logic was bar-for-bar identical. The tool now MEASURES that origin (modal difference, so a genuine armed-bar drift stays a minority and still fails) with a test on each half. **Standing rule for every parity harness here: a column holding a Pine BAR INDEX is export-window-relative, and diffing one raw is correct only by the accident of a full-history export.** 531 tests green. Earlier: 2026-07-30 — **the live-trading pipeline started.** `docs/LIVE_TRADING_PIPELINE.md` is the plan that takes a validated `strategies/python/` bot to real orders on a chosen MT5 terminal, driven from the command-center Bots page: what exists (the strategy is already a pure function of the bar stream, so `strategy.step()` IS the live seam; `algos/shared/mt5_ops.py`, the Task Scheduler boot chain and the entire per-bot start/stop/restart API survive from the deleted 2026-06 suite with only their registry dicts emptied), what does not (a live bar loop, an order bridge, pending-limit orders, the broker-clock fix on the live feed, version pinning, trade logging, log backup), and the order to build them. **Steps 1-4 are DONE.** (1) the minimum-stop guard is ported into `mpc_sos_fade` — it was the one known Pine↔Python divergence on the A+ pair and it guards the only fast money-loser in the bot (`qty = risk / stop_distance`, so a stop collapsing onto the entry balloons SIZE; measured once at ~180% of equity in a single bar). Default `"Off"`, byte-identical to the previous build, so nothing historical moves. `mpc_strategy_export.pine` was regenerated off its parent in the same pass and now differs from it by exactly one line (the title). (2) **every hardcoded Telegram token is out of git** — it was committed in six files, is revoked, and is now resolved at runtime from env or the git-ignored `algos/credentials.json` (`algos/shared/credentials.py`; the command center reads the same FILE without importing across the app boundary). (3) `shared/mt5_ops.py` gained **pending/resting LIMIT orders** — it could only send market orders, and the MPC strategies enter on a limit — plus the **broker-clock fix on `get_candles`**, which was labelling broker-server seconds as UTC, putting every bar 2-3 hours out behind a perfectly valid-looking timestamp and silently moving every session boundary the strategy trades off. (4) **`algos/live/` is built** — runner, order bridge, feed, ledger, instance config, version pin — dry-run by default, 51 offline tests. **Two design decisions recorded there and binding on the rest of the build:** the strategy stays AUTHORITATIVE and the bridge only mirrors its intent onto MT5 (that is what preserves the parity that was expensive to earn), and version isolation is TWO mechanisms — params frozen in the bot's own instance config so lab edits cannot reach it, plus a source-hash pin the bot refuses to start against on mismatch. Earlier: 2026-07-29 — **"trade count is a PORTFOLIO property" is no longer just design intent — it is measured** (`strategies/python/mpc_sos_fade/mpc_sos_fade_optimization.md` → Run 12 / 12b). Four independent attempts to make A+ trade more all failed over 6.5 years: dropping the FVG requirement (+173 trades, +13.0R net on 110.6R, drawdown 54.9% → 77.1%, and 40% of the gross is ONE 2020 trade), sizing those extras at half risk (NEGATIVE — the benefit scales with size, the cost of displacing a real trade does not), deeper entries (fewer trades AND less money), and loosening which gaps qualify (+65 trades, −32.4R). **The mechanism is the one-position-slot rule: a marginal setup is a QUEUE, not an addition.** It also gives the account-level allocator a price tag — the displacement cost (17 / 36 / 2 trades in the three tests) exists ONLY because concurrency is 1, so "would A+ improve with two slots?" is now a real open question and it needs the unbuilt allocator below. No strategy code changed; the only code change was a Pine UI cap (`aplusWindow` maxval 4320 → 20160 on the A+ pair, default unchanged). Earlier the same day: **BOTH parity gates re-run GREEN on fresh exports, so the ratchet build is validated end to end.** `compare_strategy.py` (A+, 21,494 bars, 2025-08-31 → 2026-07-29) and `compare_bleg.py` (B-LEG, 21,493 bars, same window) both exit 0 at `--warmup 100` and stay green at every warmup up to 2000; the A+ run used the shipped `exec_tp1_pct = exec_tp2_pct = 0`, and both Pine parents compile in TradingView. ⚠ Still open and NOT covered by those runs: `mpc_strategy_export.pine` lacks `execMinStopMode`/`execMinStopVal`, so parity is only proven at the `"Off"` default — see `indicators/CLAUDE.md`. Earlier: 2026-07-28 — the shared exit ladder's runner trail defaults to **"Structure + % ratchet"** across all four strategy Pine files and both Python bots: anchor on the last confirmed swing, then climb one %-of-price step per step of favourable move, so a runner stops handing back the gap to a lagging swing. The headline 109.3R was measured at 1%/1% rungs; the true shipped 0/0 baseline is **110.65R**. Extension-fib take-profits were measured and rejected the same day — see `strategies/python/mpc_sos_fade/CLAUDE.md`. Earlier: 2026-07-27 (`## Trading Philosophy` added — few high-quality setups by design, sample size arrives at the portfolio level, risk budgeted per account; TP1/TP2 scale-out rungs defaulted to 0/0 across both A+ Pine files and `config.py`; A+ parity re-validated green at SL fib 0.886 + 0/0)

---

## Trading Philosophy — read this before judging any strategy

**Recorded 2026-07-27, Aaron's standing design intent. This governs how every strategy in this repo is evaluated.**

**Few high-quality setups, not many mediocre ones.** A low trade count is the design target, not a defect. The whole point of a selective entry is that when it fires you can put real risk behind it. Chasing quantity means loosening the filter, and a loose filter is what blows an account in bad conditions or a black swan. A strategy that trades a couple of times a month and is right is worth more than one that trades weekly and is marginal.

**Sample size arrives at the PORTFOLIO level, not the strategy level.** Multiple strategies stack on one account. Each may trade 2–4 times a month; together they produce the trade frequency a single strategy is never asked to produce alone. So do NOT reject a strategy, or hedge every conclusion about it, on the grounds that "164 trades is a small sample." That objection has been raised and answered — stop re-raising it as a blocker.

**MEASURED 2026-07-29 (Run 12), and it cuts both ways: you may not buy trade count by loosening a strategy either.** Four ways of relaxing the A+ entry rule to add trades were replayed over 6.5 years and every one lost money or was noise. The reason is structural, not specific to A+: **with one position slot, an extra setup does not ADD to the book, it QUEUES in front of it** — the loosened runs displaced 17, 36 and 2 real trades respectively, and one displaced winner was worth +16.5R on its own. Corollary worth remembering before any future "let's take more setups" idea: **spending drawdown on SIZE beat spending it on FREQUENCY** — the shipped 164 trades at `exec_risk_pct = 12.5` made 832x at 64.2% max drawdown, while 337 loosened trades at half risk made 426x at 64.9%. So the honest routes to frequency stay the ones this section already names (another LEG, another instrument, another timeframe), not a looser filter.

Two things this does NOT excuse, and which must still be said plainly when they're true:

1. **A small sample still means wide error bars on that strategy's own edge.** Stacking five strategies does not make strategy A's edge more certain; it just spreads A's outcomes over more calendar time. Confidence in each edge is still earned per strategy. Say so when it matters — as a caveat on a number, never as a reason to refuse the work.
2. **Stacking only reduces drawdown if the strategies are actually independent.** Everything here reads the same `engines/market_structure/` on the same instrument. Two "different" strategies off one structure stream can fire together, lose together, and behave as one position at 2x the size. Correlation between strategies is a real open question in this repo, not a solved one.

**The suite is carved up by LEG, not by signal.** Aaron's answer to the correlation problem (2026-07-27): the strategies share a confluence source on purpose, but each takes a different part of the move — think Elliott waves. A+ SOS Fade catches the REVERSAL. The breakout-structure bot (not built yet) catches the LEGS IN BETWEEN. B-LEG catches SOS setups that take a long time to play out. By construction they should not be in the market on the same swing at the same time.

**Risk is budgeted per ACCOUNT, and never layered.** The intended rule: there is one risk pool (call it 10%, whatever the number lands on) available at any moment. Concurrent setups either SHARE that pool or the later one is BLOCKED outright. Risk is never stacked on top of risk. `exec_risk_pct = 10` is today a per-trade figure with no allocator above it — the account-level cap is UNBUILT and is a prerequisite for running more than one bot live. The Pine input's own ceiling was raised 10 → **100** on 2026-07-27 at Aaron's request (defaults unchanged at 10, and the Python side never had a cap), so nothing in the code now refuses a per-trade risk the account rule would.

**The overlap audit — RE-MEASURED 2026-08-09, and the design intent HELD MORE CLEANLY than at the first run.** "The legs don't overlap" was intent rather than fact for a year; `backtest/tools/overlap_audit.py` measures it, and on 155,531 M15 bars (2020 → 2026-08-03) **A+ and B-LEG held a position at the same time on 49 bars** — 0.5% of A+'s hold time — of which **exactly ONE was same-side** and 48 were opposite, i.e. partially hedged. The specific worry was the two firing on ONE structure break, and across 6.5 years there is now **NO** A+ trade with a same-direction B-LEG entry within 16 bars. Monthly R correlation +0.172 over 78 traded months. 🔴 **THE RE-RUN IS THE FINDING, NOT THE NUMBERS.** The 2026-08-04 audit was measured on a B-LEG that no longer exists: three of its defaults moved on 2026-08-06 (`bleg_max_days` 1.25 → 4.0, `exec_trail_pct` 1.0 → 0.05, `exec_time_stop_hrs` 36 → 8), which **doubled its trade count and tripled how long a frozen band lives**. The instruction to re-run was written down and nobody ran it, because the change landed in the B-LEG package and the verdict lived in `docs/LIVE_TRADING_PIPELINE.md`. **A cross-cutting measurement has to be re-run by whoever moves the inputs, not by whoever wrote the conclusion.** ⚠ **The absolute overlap went UP (27 → 49 bars) and the same-side overlap went DOWN (18 → 1), from the one change** — B-LEG holds longer so it shares more bars, and the bars it gained are opposite-side. Do not read the bigger number as a regression without reading the direction split under it. ⚠ **Re-run it after any entry-logic change on either bot** — this is a fact about today's config, not about the setups, which is why it is a tool and not a paragraph. ⚠ **It does not make the two independent**: both read one structure stream on one instrument, and being flat at different moments is not losing for different reasons. ⚠ **It does not retire the allocator** — the peak was still 2 concurrent positions, each sized off its own equity, so one account would have carried 2× `exec_risk_pct` on those bars. It says the allocator would rarely have had anything to arbitrate. 🟢 **And the thing that was recorded as blocking bot #2 is no longer true**: B-LEG at today's defaults is **99 trades / +17.87R** over the same 6.5 years, not the 50 / −0.94R the old entry quoted, with the drawdown down from −15.62R to −5.15R (charged, over the full history). It is a candidate again — see `docs/LIVE_TRADING_PIPELINE.md` → G15. ✅ **And the error bar it was missing now exists: `jitter_audit.py` ran on B-LEG for the first time (186,128 bars, ±$0.05 per bar, 12 seeds) and ALL TWELVE SEEDS FINISHED POSITIVE — min +21.63R, mean +26.33R, sd 2.61R, with the baseline sitting BELOW the jittered median.** In proportion that is the same feed sensitivity as A+ (11.2% of total against 10.6%), so the sign of this edge is not a feed artefact. ⚠ **A jitter audit is not an out-of-sample test** — it says the edge survives another broker's quotes, not that one tuning pass generalises. Still open: re-export and re-run `compare_bleg.py` at the new defaults, since every export on disk decodes the old ones.

**The BACKTEST side of the allocator was built 2026-08-09 and it re-states the same result through a second mechanism.** `backtest/portfolio/run_stack` replays both bots on ONE balance with ONE risk budget, and over 155,807 M15 bars at a 10% cap **it refused nothing** — peak open risk touched exactly 10.00% with both legs holding, and the contention log is empty, because risk is measured to each trade's CURRENT stop and A+ reaches breakeven in a median of one bar. ⚠ **The LIVE allocator is still unbuilt and cannot reuse that object** (separate OS processes; every MT5 read is magic-filtered), so "risk is budgeted per ACCOUNT" remains intent on the live side. See `docs/LIVE_TRADING_PIPELINE.md` → G10.

**It is drivable from the LAB as of the same day, and doing so refuted the plan's own success test.** A stack in `command-center/` is now either a **screen** (N standalone runs added together — every leg on its own full account, so nothing can block anything and it is an UPPER BOUND) or a **shared account**, and the page says which in the header before any number below it. 🔴 **`command-center/docs/SHARED_RISK_STACK.md` had stated that a shared run must close LOWER than the screen and that a higher one means the risk gate is not being enforced. The first real run closes $36,806 against the screen's $26,870 — HIGHER — with the cap working and nothing refused.** The prediction assumed refusals are the only difference between the two views; they are not. **A screen gives each leg a private balance and a shared account COMPOUNDS both onto one**, so the second leg sizes off a balance the first has grown — more money on the same trades, rather than less risk. Two effects in opposite directions, and the compounding one is unbounded while the refusal one is capped by how often the budget is genuinely full. ⚠ **So compare R, never net dollars**: the checks that say the gate is enforced are `peak_open_risk_pct <= risk_cap_pct` and every R difference tracing to a row in the contention log. **The standing lesson is about the criterion rather than the code: a verification test written before the thing exists is a prediction, and this one would have condemned a correct implementation.**

---

## Repo Structure

See `README.md` for the full repo map and subsystem list.

`algos/`, `smart-money/`, and `command-center/` are fully independent from each other. Engines under `engines/` are canonical shared libraries, and their dependency map is: `market_structure/` is the base and `fibonacci/` is its one downstream consumer (public `StructureSnapshot` only, never its internals). **`order_blocks/` was downstream too until 2026-07-31 and is now STANDALONE** — the mpc rework stopped creating blocks on structure breaks, so it takes plain OHLC and consumes no engine at all; `sessions/` is standalone and time-driven; `liquidity/` and `session_volume_profile/` compose `sessions/`; `vwap/` and `news/` are standalone and time-driven; `vwap/` and `session_volume_profile/` are the two engines that need the bar's **volume**; `fair_value_gaps/` is standalone and OHLC-driven (no upstream engine, no volume, no timestamp — pure price-pattern detection); `rsi_divergence/` is likewise standalone (needs close for Wilder's RSI + the bar's high/low for the price anchor — no upstream engine, no volume, no timestamp); `equal_highs_lows/` is likewise standalone (needs high/low/close for ATR(50) + strict price pivots — no upstream engine, no volume, no timestamp); `candlesticks/` is likewise standalone (needs OHLC only — no upstream engine, no volume, no timestamp — and is the only engine here ported from a THIRD-PARTY indicator rather than from `mpc_assistant.pine`). `engines/regime/` and `engines/market_structure/` are imported by `algos/` via thin shims in `algos/shared/`. **`command-center/` imports six directly** (bare-name, public API only, never a second implementation): `regime/` and `news/` for tagging and the news filter, and `market_structure/` + `fair_value_gaps/` + `equal_highs_lows/` + `order_blocks/` for the backtest PRICE CHART's overlay layers (`backend/services/structure_overlays.py`, `fvg_overlays.py`, `ob_overlays.py`). Those four are **display** consumers — no strategy reads them, so a change there moves what a chart shows and never a trade — but they are consumers, and an engine's own CLAUDE.md must say so rather than claiming nothing imports it. Every other engine gets its `algos/shared/` shim when a bot first uses it. `strategies/` is consumed by `command-center/` (scanner + deploy) and deployed to the VPS strategy folders. Per-engine detail lives in each engine's CLAUDE.md — do not restate it here.

---

## System Summaries

### algos/
Automated trading on PU Prime demo accounts (Windows VPS via Task Scheduler). No live bots currently — the four first-attempt bots (SMC Trend, Scalper, FFT, Mean Reversion) were deleted 2026-06-22 to rebuild the suite backtest-first per `docs/BOT_DEVELOPMENT_METHOD.md`. Deployment learnings (MT5 connection, configs, scheduler, liveness layer) are preserved in `algos/docs/BOT_DEPLOYMENT_INFRA.md`. **CLEAN SLATE 2026-07-31 (Aaron's call): the suite starts from scratch — nothing before that date carries forward.** The VPS and the repo were leaned out: the four dead `shared_*` modules deleted (commit `e92304a`), and on the VPS a 36 MB pre-migration copy of the whole old suite, its backup, every stale task XML/log, the old bots' state files and all scratch. **There is no historical trade data anywhere.** A missing file is deliberate, not a bug — to recover anything use the commit hash via `algos/docs/DELETED_CODE.md`, never a rewrite from memory. Only the Telegram bot survives from the original suite. **The last two relics went 2026-08-05: `pnl_tracker.py` and `reporter.py` are DELETED** — they had been listed here as "disabled until a live bot is registered", but both carried an empty bot registry from the June deletion, so switching either on would have run a script that found no bots and exited. **Telegram is TWO rooms since 2026-08-05**: `telegram_chat_id` carries TRADES and nothing else, `telegram_health_chat` carries every message about the machinery — every sender in the repo states a `TRADE`/`HEALTH` kind and a test greps them all. Full rules in `algos/CLAUDE.md` → *Two rooms — every message declares its KIND* **Every message is also rendered by ONE formatter** (`shared/alert_format.py`) — icon, LABEL, subject, grouped facts, then what to act on, and no timestamp, because Telegram already prints the send time in the reader's own clock. **The Telegram bot answers four commands** (`/status`, `/balance`, `/help`, `/users`); the six control commands were deleted 2026-08-05 because their bot registry had been empty since June and two of them reported success on an empty list. **There is an ACCOUNT-level risk cap since 2026-08-09** — `shared/account_risk.py` + `mt5_ops.account_exposure()` + `bridge._account_cap_check`, the layer above `exec_risk_pct` that had never existed, reading the BROKER's real exposure across every magic rather than trusting any bot's own record. **Off by default (`account_risk_cap_pct: null`) and the runner says so at every start**; the live bot is unchanged. Magic numbers are enforced unique per account in the same pass. **There is also a FLEET KILL SWITCH since 2026-08-09** — `shared/fleet_halt.py` + `tools/fleet_halt.py`, a flag file every bot re-reads each poll that stops NEW ORDERS across the whole box without closing anything or killing a process. **Cannot-read HALTS**, deliberately the opposite default from `stop.request`, and it LATCHES: clearing the flag does not resume trading, the bots must be restarted.

### smart-money/
Scans and profiles consistent crypto/forex traders for copy trading candidate pool construction. Runs locally on Mac. Stages 1–2 and 5 live; Stages 3–4 need API keys. Full rules in `smart-money/CLAUDE.md`.

### command-center/
React + FastAPI local operations platform. Monitors bots via SSH, runs and evaluates NinjaTrader, MT5, and local Python backtests. **The Smart Money UI is built and FLAGGED OFF since 2026-08-04** (`command-center/frontend/src/lib/features.ts`) — Aaron is auditing the app down to what he uses; nothing was deleted, the backend endpoints are still served, and one boolean restores the nav row, the routes and the Overview cards together. **Self-healing since 2026-08-02** — `backend/services/agent_supervisor.py` keeps the SSH tunnel and both VPS agents up on a 60s loop, guarded on the per-platform job lock, so starting it fresh and reopening a slept laptop are the same code path. **The Bots page was audited end to end 2026-08-05 and 14 defects fixed** — three of them destructive (a per-bot Stop that could kill the wrong process, a dead SSH rendering as "all bots stopped", an add-user that could delete every other user), plus two unused endpoints that **restarted a live trading bot**, now deleted. Two facts from it bind anything that touches this subsystem: **a bot is registered ONCE** (`routers/bots.py::BotReg` — there were nine parallel maps, and a missing entry answered confidently and wrongly, always in the safe-looking direction), and **a bot is addressed by its KEY, never its display name** (`BotStatus.key`; a name is a label and a rename would have broken every URL). **The Tuning workbench was audited 2026-08-05 and 17 items fixed**, the load-bearing one being that it launched every iteration WITHOUT the baseline's `cost_layers`/`broker_profile`/`sizing_mode` — so tuning a charged run produced a child measured on a free book and put it in a table beside its own parent (proven with three real python runs: PF 1.499 charged vs 1.581 free on identical params and an identical 17 trades). **The rule it leaves behind binds every page in this app: a page whose whole job is COMPARING two runs must carry forward everything that decides what a run is measured on — the window, the costs, the broker, the sizing — or its own difference column becomes the thing that lies.** **The News Calendar was audited 2026-08-05 and 10 defects fixed**, the load-bearing one being that the beat/miss polarity list had been written against **Forex Factory's** naming while the tab reads **TradingView** — six of eleven keys matched none of 811 real titles, and `Core PCE Price Index MoM` (HIGH, USD) was therefore coloured the opposite way to CPI on the same screen. **The guard it leaves behind is the transferable part: a key that matches nothing now fails the build**, because a list of magic strings matched against another system's vocabulary fails silently by matching nothing at all. **The Stress Test feature was audited 2026-08-05 and the frame was one query: the `stress_tests` table held ONE row, written three days BEFORE the accuracy pass that rewrote the engine** — so nothing had been exercised against the code it runs on, and re-grading that row live moved it from a confident **D** to **ungraded**. Its load-bearing defect is the Tuning workbench's, in a second page on the same day: **a walk-forward or sensitivity child was launched without the baseline's `cost_layers` / `broker_profile` / `sizing_mode`, so stress-testing a charged run measured every child on a free book** and sensitivity blamed the cost gap on the parameter. **The rule this app now states three times over: any page that COMPARES two runs must carry forward everything that decides what a run is measured on — window, costs, broker, sizing — and a page that has been run once, before its own engine was replaced, has not been run.** The same pass made sensitivity run its shifts **across the cores** — it replayed 60 full-history backtests one at a time on a 12-core box (69s each, measured) while `backtest/optimizer.run_sweep` had been fanning optimizer grids across every core the whole time; **46 shifts went 53 min → 14 min (3.8x), proven identical to the cent against the single-run path.** ⚠ **The obvious fix would have looked like it worked and bought almost nothing** — those backtests run on threads and an engine replay is pure-Python bar-by-bar, so it is GIL-bound and needs PROCESSES; the cache was never the bottleneck. Full rules in `command-center/CLAUDE.md`.

### backtest/
Top-level Python backtest runner — strategy- and instrument-agnostic shared infrastructure (same character as `engines/`): broker-data layer with disk cache, bar-replay loop over the canonical engines, tick-level fill & cost model, lab output adapter, a local multi-core optimizer, and **`reprice.py`** (2026-08-03) — charging costs onto a COMPLETED run from its stored trades, which works because each re-priceable cost is size-independent in R; it refuses `bid_ask_fills`/`slippage` outright rather than approximating them. **`portfolio/` gained its runner 2026-08-09** — `run_stack` replays several `strategies/python/` bots on ONE shared balance and ONE risk budget they compete for, plus a SOLO control replay per leg (without which a difference is a mixture of *the cap bit* and *the shared balance re-sized everything*); driven by `tools/stack_run.py`. Consumed by `strategies/python/` bots and by the command-center lab as `runner="python"`. **Deliverable A complete 2026-07-16.** 🔴 **A COST NOBODY MEASURED ON THAT ACCOUNT NOW REFUSES RATHER THAN BORROWING A SIBLING'S (2026-08-06).** All four PU Prime tiers in `fills.py::PROFILES` carried ONE spread and ONE swap, both measured on a **Standard** demo — the single tier priced by a marked-up spread — so `puprime_ecn` charged ECN's commission on top of Standard's costs, a model no real account offers, and **nothing errored**. The raw tiers now carry `SPREAD_UNMEASURED` / `UNMEASURED_SWAP` and raise, naming `algos/tools/broker_facts.py`; **commission still charges, because it is the one of the three a broker states unambiguously per lot.** ⚠ **The swap half was MEASURED, not reasoned:** the assumption *"swap is a fact about the symbol, so it is the same across a broker's tiers"* was written down as a named caveat in the morning and disproved the same day by `broker_facts.py --symbols` — on ONE PU Prime account `XAUUSD.s` and `XAUUSD.crp` are the SAME market (median M15 close difference **$0.08** over 200 shared bars) carrying **swaps 8.5x apart (−79.60 vs −9.35) with the short CREDIT gone entirely (+30.25 vs +0.04)**. This strategy trades both sides and its swap arithmetic rests on that credit. ⚠ **`XAUUSD.crp` is `trade_mode: DISABLED`, so it is EVIDENCE and not a cheaper way to trade** — the tool prints the trade mode beside the tempting numbers for exactly that reason. **The standing lesson is about how an assumption survives: it was checkable in one command the whole time, and it lasted because no command existed. Naming an assumption is not testing it — when you write one down, ask what it would cost to check.** **Broker history floors enforced 2026-07-26** (`data/history.py`): MT5 silently returns COARSER bars when a symbol lacks history at the requested timeframe, so `BarSource.load` refuses a window before the broker's real start — the floor is MEASURED by probing the live terminal (bar density, binary search) and cached per broker, never hardcoded, so swapping brokers re-measures. `tools/run_report.py` is the "why did it make/lose money" multi-year run (per-trade regime/session/excursion + a row per A+ leg that never traded); its `--start` defaults to that measured floor as of 2026-07-29 (it was hardcoded to 2022-01-01, silently reporting 4.6 of 7.9 available years). `backtest/archive/` holds committed, frozen snapshots of that output — `backtest/reports/` is git-ignored scratch, so the archive is how real multi-year trade data reaches a machine with no VPS, no MT5 and no cache. Full rules in `backtest/CLAUDE.md`.

### engines/regime/
Shared market regime classifier. Imported by the live bots (via `algos/shared/shared_regime.py` thin shim) and by the command-center backtest lab. Single output set: 5 labels (TRENDING, TRANSITIONING, RANGING, HIGH_VOLATILITY, LOW_VOLATILITY). Each bot owns its own `REGIME_RISK_TABLE` mapping labels to trade decisions. Full rules in `engines/regime/CLAUDE.md`. Algorithm documented in `engines/regime/REGIME_CLASSIFIER.md`.

### engines/market_structure/
Canonical market-structure detection engine (BOS/CHoCH, swing highs/lows, HH/HL/LH/LL, internal structure). A stateful streaming state machine ported line-by-line from `indicators/structure_engine.pine`. **Re-synced 2026-07-12 to the `choch_lock` removal in `mpc_assistant.pine`** (a CHoCH no longer needs the anti-whipsaw latch; on an SOS the promoted extreme prints ASH/ASL and is NOT written to the confirmed-swing map — this is the fix for the missing higher high) and re-validated at 100% Pine parity on a real `VANTAGE_XAUUSD, 5m` export. Note the public label domain widened: `broken_high_label`/`broken_low_label` now also carry `"ASH"`/`"ASL"` = *not yet classified*. Imported by the live bots via `algos/shared/structure_engine.py` (thin shim); the command-center backtest lab is a future consumer. This is the single implementation — do not build another anywhere. Full rules in `engines/market_structure/CLAUDE.md`; algorithm in `engines/market_structure/MARKET_STRUCTURE_ENGINE.md`.

### engines/fibonacci/
Canonical fib engine. Turns `engines/market_structure/` output (public `StructureSnapshot` only) into fib LEVEL EVENTS — the first-touch of each level (E1–E4 entries, TP1–TP3 targets, 1.0) — via four fib state machines (Structure "FFT", Sniper, Macro, Internal) ported from `indicators/mpc_assistant.pine`. Unit-tested (40 tests), 100% Pine parity re-confirmed after the 2026-07-12 structure re-sync (the fibs were STALE-BY-INPUT — own code untouched, but the structure stream feeding them changed; fresh 5m export, exit 0). Full rules in `engines/fibonacci/CLAUDE.md`.

### engines/order_blocks/
Canonical order-block engine — **RE-PORTED 2026-07-31 and it is a DIFFERENT OBJECT now**. Structure breaks no longer create blocks at all, so this engine is **STANDALONE**: it consumes no other engine and `update()` takes plain OHLC (it used to take a `StructureSnapshot`). Every block now belongs to a `ta.pivot(2,2)` **TURN**, read two ways at most once each — a PUSH (the engulf reading) and the TURN itself — behind six creation gates (min-back, dead, 1.0x ATR displacement, tap-after-departure, 0.5 overlap dedupe, 2.0x ATR height ceiling). Mitigation is three rules (wick-tap / close-inside-then-out / close clean through) plus a 500-bar age cap reported SEPARATELY as `expired`, because a block price never returned to was not consumed. `max_active` 2 → **10**. In shape it is now a sibling of `fair_value_gaps/` and `equal_highs_lows/`, not of `fibonacci/`. Ported from `mpc_assistant.pine`, 19 hand-traced unit tests, **100% Pine parity 2026-07-31** on two real exports (21,691-bar 15m at `--warmup 798`; 13,186-bar 5m at `--warmup 326`). ⚠ **Budget ~300 bars of warm-up**, and expect a cold engine to be MISSING a block rather than inventing one. **First consumer landed 2026-08-03: `command-center`'s backtest price chart** (`backend/services/ob_overlays.py` → Analysis → Order Blocks), which reads the public events only and is a DISPLAY consumer — no strategy reads a block, so a change here moves what a chart shows and never a trade. Full rules in `engines/order_blocks/CLAUDE.md`.

### engines/sessions/
Canonical sessions engine — the first **time-driven** engine (input = the bar's UTC timestamp + high/low). Emits session EVENTS — Tokyo/London/NY windows + running session high/low, the three DST-aware NY kill zones, the NY opening range, new-day/weekday flags — **each window now stated in its own city's clock and DST-aware** (re-synced 2026-07-31 from a fixed GMT-4: Tokyo `0900-1800` Asia/Tokyo, London `0800-1700` Europe/London, New York `0800-1700` America/New_York — Tokyo is UTC-identical year-round, London and NY shift an hour earlier under BST/EDT). **Validated on two timeframes 2026-07-31** — a 15m export spanning four DST changeovers, and a 5m export for the NY opening range (a ≤5m feature the coarser export cannot test). Standalone, and the base the liquidity and SVP engines compose. Ported from `mpc_assistant.pine`, unit-tested (17 hand-traced tests), 100% Pine parity on a real `VANTAGE_XAUUSD, 5m` export. Full rules in `engines/sessions/CLAUDE.md`.

### engines/liquidity/
Canonical liquidity-levels engine. Turns the bar stream into liquidity LEVEL EVENTS — prev day/week high & low, prev week close, the H4 sweep high/low, and Asia/London/NY session high & low, each with sweep/break mitigation; composes `engines/sessions/` for the session levels. **Non-repainting by Aaron's explicit decision (2026-07-05): every HTF level uses the PREVIOUS completed period only — never the current period's forecast.** Unit-tested (14 hand-traced tests), 100% Pine parity re-confirmed after the 2026-07-09 monthly-level (PMH/PML) removal (fresh 5m export, exit 0). XAUUSD trading day opens 18:00 NY (baked-in default). Full rules in `engines/liquidity/CLAUDE.md`.

### engines/vwap/
Canonical session-VWAP engine — a volume-weighted running mean of `hlc3` re-anchored each trading day (the same 18:00-NY boundary as the liquidity daily level), plus a derived close-vs-line cross. The first engine to need a **volume** column in the feed. Unit-tested (13 hand-traced tests), 100% Pine parity on a real `VANTAGE_XAUUSD, 5m` export (relative 1e-6 tolerance — the cumulative sum drifts at float-rounding level). Full rules in `engines/vwap/CLAUDE.md`.

### engines/session_volume_profile/
Canonical Session Volume Profile engine — the Asia point-of-control ("MV" line): on each Asia session close it builds a **50**-row volume profile over the session range (`svpRows`, cut 100 → 50 on 2026-07-08; this line said 100 until 2026-07-31 — the code and `svp_export.pine` were always right), reports the highest-volume row's mid-price as the POC, and marks it confirmed when price first straddles it. Composes `engines/sessions/`; needs the **volume** feed. Unit-tested (12 hand-traced tests), 100% Pine parity on a real `VANTAGE_XAUUSD, 5m` export. The **last of the roadmap's eight planned SMC-port engines — the core extraction roadmap is complete** (the fair-value-gap engine below was pulled later, for the A+ setup). Full rules in `engines/session_volume_profile/CLAUDE.md`.

### engines/fair_value_gaps/
Canonical fair-value-gap engine — turns the bar stream into FVG EVENTS: a price void left by a 3-candle imbalance (the LuxAlgo definition — the two outer candles don't overlap, the middle bar's close cleared the gap, and the gap clears a **timeframe-split** size floor — 0.0% below 15m, 0.04% at 15m and above), plus its later mitigation (a candle CLOSING fully past the far edge — a wick no longer counts) and FIFO eviction past `fvgMaxCount` (default 6). Standalone and OHLC-driven — no upstream engine, no volume (the Pine's directional-visibility filter is drawing-only and is deliberately not reproduced; every gap is emitted with its `is_bullish` flag and a consumer decides alignment). Ported line-by-line from `mpc_assistant.pine`'s FVG block, unit-tested (15 hand-traced tests). **Re-synced 2026-07-18** to the mpc default drift: the middle-bar close-cleared check is now the OPTIONAL `require_close` flag (Pine `fvgRequireClose`, default False — the classic FVG that the mpc default produces; the engine had silently required it since the gate landed in mpc on 2026-07-17), and the defaults were reconciled to the Pine (`max_count` 6→10, `threshold_pct` 0.1→0.0). `fvg_export.pine` now carries `cfg_fvg_*` columns and `compare_fvg.py` configures the engine from them, so parity survives any input tweak. **Pine-parity RE-VALIDATED 2026-07-19 (exit 0)** on a fresh 16,639-bar `VANTAGE_XAUUSD, 5m` grand export at the reconciled defaults. Pulled off the indicator later than the eight planned engines, to feed the A+ setup. Full rules in `engines/fair_value_gaps/CLAUDE.md`.

### engines/rsi_divergence/
Canonical RSI-divergence engine — turns the bar stream into RSI-DIVERGENCE EVENTS: a confirmed regular divergence at the extremes (price lower-low while Wilder's RSI higher-low from oversold = bullish; the overbought mirror = bearish), plus the live confluence flags (`bull_active`/`bear_active`) a consumer reads. Standalone and price-driven (close for RSI + the bar's high/low for the anchor — no upstream engine, no volume, no timestamp); a sibling of `fair_value_gaps/` in shape. Pivots confirm `pivot_len` bars late (non-repainting by design). Pulled off the indicator later than the eight planned engines (like FVG), to feed the A+ setup. Ported line-by-line from `mpc_assistant.pine`'s RSI DIVERGENCE block, unit-tested (9 hand-traced + reference-cross-check tests), **100% Pine parity** on a real `VANTAGE_XAUUSD, 5m` export (`compare_rsi_div.py --warmup 1630`, exit 0). Full rules in `engines/rsi_divergence/CLAUDE.md`.

### engines/equal_highs_lows/
Canonical Equal Highs/Lows (EQH/EQL) engine — turns the bar stream into EQH/EQL LEVEL EVENTS: when two consecutive same-side strict price pivots land within an ATR(50)×mult band of each other, a horizontal liquidity level prints (EQH = buy-side resting above, EQL = sell-side below) and lives until a candle CLOSES through it; FIFO cap per side (default 6). Standalone and price-driven (high/low/close — no upstream engine, no volume, no timestamp); a sibling of `fair_value_gaps/` and `rsi_divergence/` in shape. Ported line-by-line from `mpc_assistant.pine`'s EQ block, unit-tested (7 tests). **Pine-parity VALIDATED 2026-07-19 (exit 0)** on a fresh 16,639-bar `VANTAGE_XAUUSD, 5m` grand export. The real-export run exposed and FIXED a genuine pivot bug: Pine's `ta.pivothigh`/`pivotlow` allow a tie on the LEFT of the centre but require a STRICT extreme on the RIGHT (the last bar of an equal run is the pivot); the engine had used strict-both-sides, which silently dropped the frequent raw-price ties on gold. The identical latent bug was fixed in `rsi_divergence/` too (there ties on RSI values are rare, so it only surfaced as a couple of diagnostic-column misses). The Pine's `eqExemptFvg` coupling (a gap behind an EQ level survives the FVG cap) is now MODELLED (2026-07-18, Aaron's exact-match call): the FVG engine's `update()` takes `eq_levels`/`eq_tol` and the consumer runs EQ→FVG (`backtest/replay/EngineStack` wiring is a follow-up). Full rules in `engines/equal_highs_lows/CLAUDE.md`.

### engines/candlesticks/
Canonical candlestick-pattern engine — **built 2026-08-08 and the first engine here ported from a THIRD-PARTY indicator** (`indicators/candle_sticks.pine`, © repo32, v6) rather than from `mpc_assistant.pine`. Turns the bar stream into CANDLESTICK PATTERN EVENTS: fifteen classic single-, two- and three-bar patterns, each carrying the direction its source Pine draws it in. Standalone and OHLC-driven (no upstream engine, no volume, no timestamp) — a sibling of `fair_value_gaps/`, `rsi_divergence/` and `equal_highs_lows/` in shape. It exists to be a CONFLUENCE source: something a strategy ANDs into a setup it already has. A pattern is a property of ONE bar, so there is no live list, nothing is mitigated and nothing expires; a window ("within the last 3 bars") is asked of the engine (`bars_since`), never of a bar's events. 42 hand-traced tests green; replayed clean over 186,366 real XAUUSD M15 bars with all fifteen patterns firing at least once. ✅ **PINE-PARITY VALIDATED 2026-08-08 (exit 0)** on a real 20,138-bar `VANTAGE_XAUUSD, 15m` export, green at warmups 0 / 100 / 500 / 2000, configured from the export's own `cfg_*` columns — and **NOT VACUOUS: 14 of the 15 patterns fired**, 302,070 flag comparisons, **zero rule differences**. ⚠ **Three BOUNDARY TIES, and how they are handled is the transferable part**: `doji`, `invHammer` and `shootingStar` each compare two quantities that come out **exactly equal in decimal** on real prices (0.26 vs 0.26 · 3.96 vs 3.96 · 5.43 vs 5.43, confirmed with `Decimal`), neither side is representable in binary float, so the two implementations land on opposite sides of a rule they both compute correctly. **A tolerance was deliberately NOT used** — a 0/1 flag has no "close enough" and a tolerance would swallow real bugs with the ties — so the harness CLASSIFIES instead: it re-runs each mismatch with every price in the bar's window nudged ±1e-6 and asks whether the answer flips. Proven non-vacuous by injecting two fabricated flips, which came back REAL with exit 1. ⚠ **An unclassifiable mismatch counts as REAL, never as a tie.** ⚠ **The measured frequencies split the set in two and that decides how it can be used:** five patterns fire on **5–9% of every bar** (both haramis, both engulfings, hammer 8.9%, inverted hammer 8.3%) — arriving every ~12 to 20 candles, so on their own they are not filters — while ten fire between **19 and 661 times in eight years**, with `bullish_belt` at 19 and `hanging_man` at 25 too rare to measure at all, which is the B-LEG sample-size problem in miniature. ⚠ **Hammer, Inverted Hammer and Doji are emitted direction-NEUTRAL, deliberately: the source draws them as a neutral diamond and gives them NO trend filter**, where ten of the fifteen rules gate on `open[trend]`. A hammer with no trend context is a candle shape, not a reversal — a consumer that wants it bullish states so in its own config, because the moment the engine decided it, the engine and the chart would disagree about the same candle. ⚠ **7.4% of bars carry more than one pattern and that is correct** — every Hanging Man is also a Hammer by construction and the Pine plots both. Full rules in `engines/candlesticks/CLAUDE.md`.

### engines/news/
Canonical economic-calendar (news) engine — **off the extraction roadmap and NOT a Pine port**. Standalone and time-driven: turns each bar's UTC timestamp into trade-BLACKOUT events around scheduled macro releases plus bank-holiday reporting; the engine reports, the bot decides via its own `NewsPolicy`. **Honest-coverage by Aaron's decision (2026-07-05): the filter is inert before the cache's earliest fetched date; `coverage_start_ms` marks the boundary.** Validated by 29 unit tests + live checks (no Pine source to diff). Full rules in `engines/news/CLAUDE.md`.

### strategies/
Generic trading strategy source files, organized by runner platform. `strategies/ninjatrader/` holds the only live NinjaScript strategy, `ORB.cs` (VWAP_MR and Momentum were deleted 2026-06-21 — they baked risk management into the strategy, against the gated-layer rules). The command center scanner reads from here to register strategies in the database; the Deploy button uploads files to the VPS (NT8 or MT5 folder by extension). `strategies/mt5/` holds one MQL5 strategy: `LondonBreakout.mq5` (instrument-agnostic Asian-range → London breakout). `strategies/python/` holds Python strategy packages run locally by the lab's python runner (no deploy) — currently `mpc_sos_fade/` (MPC SOS Fade, **Pine-logic-parity green, re-validated 2026-07-27** on 21,320 bars at the settings Aaron trades — SL fib 0.886, TP rungs 0/0), `mpc_bleg/` (MPC B-LEG, the late-retrace setup split out of `mpc_strategy.pine` to run parallel to A+ — unit-tested, **Pine-parity GREEN 2026-07-26** on a real 21,231-bar export) and `mpc_bos/` (MPC BOS, the break-of-structure continuation setup, built 2026-08-07 — **Pine-parity GREEN 2026-08-07**, `compare_bos.py` exit 0 on 6,300 of a 7,200-bar M15 export at warmups 900 / 1000 / 2000 / 3000; ⚠ **green about the SHIPPED defaults only** — the gap entry is OFF there, so the whole FVG ladder, the Sniper Zone and block codes 1/3/4/5/6 were never exercised, and 6 trades closed in the window). **The end-to-end process — spec → Pine → export twin → a real CSV → the Python port → the parity gate, and which of those only a human can do — is `docs/STRATEGY_WORKFLOW.md`.** Both share ONE exit ladder, and its switchable TP/SL levers are registered in `strategies/python/mpc_sos_fade/CLAUDE.md` → `## The exit ladder` — add a new one there, in `config.py`, in `mpc_strategy_export.pine` and in `compare_strategy.py` in the same commit. Full rules in `strategies/CLAUDE.md`.

### scripts/
Cross-subsystem VPS bootstrap and full-recovery scripts (`bootstrap_vps.ps1` for the MT5/algos side, `bootstrap_ninjatrader.ps1` for the NT8 side). Idempotent, run on a wiped or new VPS. Full run order in `scripts/README.md`.

### indicators/
From-scratch Pine Script rewrite of the "Structure OS / SMC Engine" market-structure indicator (`indicators/smc_engine_v2.pine`), replicating a private TradingView indicator using a pullback-only (no pivot lookback) swing detection method. Mid-rebuild: swing detection and break-gated BOS/CHoCH (Stage 2b) are ~95% validated against the original; internal structure (Stage 3) and full multi-symbol comparison (Stage 4) are not started. Full rules in `indicators/CLAUDE.md`.

---

## VPS Deploy Workflow

**Pulling does NOT change what a bot trades — promoting does.** Since 2026-08-03 a live bot
imports from a frozen snapshot in `algos/markets/fx/instances/<bot>/deployed/`, not from the
repo, so a pull is safe at any time and a restart still comes back on the SAME version. See
`algos/live/version.py`.

```bash
# Push changes
git add . && git commit -m "..." && git push

# Pull on the VPS — safe while a bot is running; it will not move the deployment
ssh forexvps "cd C:\trading && git pull origin main"

# Deploy the code to a bot (the ONLY thing that changes what it trades).
# Stages, verifies it imports, then swaps; a failure leaves the running bot untouched.
ssh forexvps "C:\Users\Administrator\AppData\Local\Programs\Python\Python311\python.exe C:\trading\algos\tools\promote.py --bot mpc_sos_fade_demo"

# Restart onto the new version. SYS_MONITOR also restarts a dead bot on its own within
# ~60s, so killing it is enough — but this is the deliberate path.
ssh forexvps "schtasks /run /tn SYS_STARTUP"
ssh forexvps "wmic process where \"name='python.exe'\" get commandline"
```

⚠ **NEVER `taskkill /f /im python.exe`.** It kills every Python process on the box — the
trading bot, the Telegram bot, the MT5 backtest agent and the NT8 agent — and it is what
killed the live bot on 2026-07-31 (dead for three days; nothing restarted it then). This
workflow told you to run it, which is how it kept happening. Kill ONE bot by its commandline:

```bash
ssh forexvps "wmic process where \"name='python.exe' and commandline like '%--bot mpc_sos_fade_demo%'\" call terminate"
```

VPS path: `C:\trading\algos\` (main)

---

## Committing — the docs land in the same commit as the code

**Enforced since 2026-08-04 by a git hook, not by memory.** Two people work this repo from
two machines, and these CLAUDE.md files are the only way each learns what the other did. A
code change whose doc never landed is invisible on the other machine until it bites.

`.githooks/commit-msg` refuses any commit where a changed file's **owning CLAUDE.md** is not
in the same commit. The owner is the nearest CLAUDE.md walking UP from the file's folder —
so `engines/vwap/engine.py` needs `engines/vwap/CLAUDE.md`, a ChartPanel component needs
`ChartPanel/CLAUDE.md`, and a root-level file needs this file. Updating a PARENT does not
satisfy a child: the nearest one is the one somebody reads next.

Exempt: markdown, lock files, images, data (`.csv`, `.db`, `.pkl`), `.gitignore`,
`.claude/settings.local.json`, and **`*/ledger/decisions-*.jsonl`**. Merges, rebases, reverts and
`fixup!` commits pass through — they carry somebody else's change and would ask for the same
paragraph twice. ⚠ `*.meta.json` is deliberately NOT exempt: it is a contract the Pine and the lab
both read, not data.

🔴 **The ledger exemption was added 2026-08-05 because the hook had QUIETLY DISABLED THE ONE BACKUP
THAT MATTERS.** `algos/tools/ledger_sync.py` commits the live bot's decision record unattended —
that record is the only copy of what the bot decided, including every setup it refused, and no
broker statement contains it. The hook classified the `.jsonl` as code and demanded a paragraph in
`algos/CLAUDE.md` for a data file, so **every automated sync failed and a closed day sat on the VPS
alone.** Measured: the sync refused with 2026-08-04 outstanding, and it went through the moment the
exemption landed. ⚠ **It is a PATH, not `*.jsonl`** — the extension is generic, and a future
`.jsonl` carrying a contract would be waved through the way `*.meta.json` explicitly is not, while
`*/ledger/decisions-*.jsonl` can only ever be this. **The standing lesson is about guardrails, not
about this file: a rule that fires on a robot's commit has no human to read its message, so it does
not nag — it silently stops the job.** When you add a check, ask what it does to the things that
commit without a person watching.

When a change genuinely needs no doc update, say so **in the message** — the reason is
required and is recorded where the other person can read it:

```
fix(chart): correct a comment typo

DOCS: none - comment only, no behaviour change
```

### Getting it switched on — four tripwires, because nothing runs on clone

⚠ **The hook lives in `.githooks/` and is switched on by `core.hooksPath`, which is per-clone
LOCAL config that `git clone` does not carry.** A fresh clone is unprotected and looks
IDENTICAL to a protected one — measured, not assumed: a clone of this repo committed a code
change with no doc and no complaint.

**Git will not execute repo code on clone or fetch**, and that is a security property, not an
oversight — so no hook of ours can fire first. The answer is to check at every entry point
somebody plausibly uses first. All four call the one installer, `scripts/install_hooks.sh`,
which is **silent when nothing needs doing** and speaks up only when it just installed:

| Tripwire | Fires when | Catches |
|---|---|---|
| `.githooks/post-merge` | every `git pull` / merge | drift, an unset config, a hook arriving without its executable bit |
| `.claude/settings.json` → `SessionStart` | every Claude Code session in this repo | **a fresh clone** — neither of us works here without Claude |
| `conftest.py` | any `pytest` run | a fresh clone, before the suite runs |
| `./go` | every launch of the command center | a fresh clone |

`post-merge` also **says so when the pull changed the rules themselves** (`.githooks/` or the
installer), because a rule that changes under you without a word makes the next refusal read
as a bug.

⚠ **`post-merge` cannot cover the clone case and must not be read as if it does** — it is the
one tripwire that requires the hooks to already be installed. The clone is covered by the
other three, and they are three rather than one because each only fires if you happen to do
that thing first.

⚠ **The pytest notice is suppressed by `pytest -q`** (which hides the header). The install
still happens; only the message is hidden. Run plain `pytest` to see it.

⚠ **A hook that is not executable is skipped by git in silence** — same "looks installed, does
nothing" shape one level down. The installer chmods every hook every run.

## Branches

- `main` — active development, all code changes go here

---

## Never Do

- Commit `credentials.json`, `users.json`, `.env`, any `.pkl` model files, or API tokens/keys
- Commit with `--no-verify` to get past the CLAUDE.md hook. It leaves NO trace, so the next person cannot tell a deliberate skip from a forgotten one — which is the whole problem the hook exists to fix. The honest skip is a `DOCS: none - <reason>` line in the message, and it costs one sentence. See `## Committing`
- Touch `algos/` when working on `smart-money/` or `command-center/` and vice versa
- Build a second regime classifier in `command-center/` or anywhere else — `engines/regime/classifier.py` is the canonical implementation; all consumers import from there
- Build a second structure engine, fib engine, order-block engine, sessions engine, liquidity engine, VWAP engine, SVP engine, fair-value-gap engine, RSI-divergence engine, equal-highs-lows engine, candlestick-pattern engine, or news/economic-calendar engine anywhere — `engines/market_structure/engine.py`, `engines/fibonacci/`, `engines/order_blocks/`, `engines/sessions/`, `engines/liquidity/`, `engines/vwap/`, `engines/session_volume_profile/`, `engines/fair_value_gaps/`, `engines/rsi_divergence/`, `engines/equal_highs_lows/`, `engines/candlesticks/` and `engines/news/` are the canonical implementations; all consumers import from them
- Commit `engines/news/data/events.json` (or anything under `engines/news/data/`) — it is fetched calendar data, git-ignored, not source
- Commit a new or changed engine before its Pine↔Python parity check has actually run and passed (exit 0) on a real TradingView CSV export — unit tests pin the logic but do not prove parity. Build engine + tests + harness, then wait for the real export and the `compare_*.py` pass; only then commit (Aaron's standing rule, 2026-07-05)
- Hardcode a broker's history depth — including as a "sensible" DEFAULT start date on a tool. A default is a hardcode with better manners: it fails quietly in the direction nobody checks, silently narrowing every run that didn't pass the flag (`run_report.py` did exactly this until 2026-07-29). Measure the floor, or refuse to run and ask.
- Construct a `LAB_STRATEGY` class directly when a run may carry costs — go through `backtest.replay.build_strategy`. `LAB_STRATEGY` is an open contract, so a strategy may predate the `cost_profile` kwarg; passing it unconditionally crashes, and passing it conditionally reintroduces the exact bug that let the lab collect commission and slippage for months and charge neither. The helper refuses to run instead
- Report a metric as verified because its arithmetic reproduces. Every stored KPI on run `f866873aa862` recomputed to the cent and the page still misled three ways — a drawdown in dollars only, a win rate counting breakeven scratches, a concentration measured over quarters answering a different question from the one its name asks. **Ask what a reader will CONCLUDE from a number, not just whether it is correct**
- "Fix" a wrong-side stop filling at the next bar's open. It is the one-bar order delay every fill model here is built on, it is identical in Pine and Python (so parity is unaffected), and it makes the backtest look slightly WORSE than reality — the safe direction. Removing it is a real behaviour change across all five Pine files and needs its own measurement, not a tidy-up. See `strategies/python/mpc_sos_fade/CLAUDE.md` → `### Wrong-side stop fills`
- Read a `/health` response as a statement about the thing BEHIND the agent, or a `schtasks /run` exit code as evidence the task started. The MT5 agent answers `ok` while its terminal is disconnected; `schtasks` answers SUCCESS for a task Windows refuses to launch. Probe the thing you are actually claiming, and re-probe after any action you take
- Trust a probe whose NEGATIVE result a healthy system can also produce — that is not a probe, it is a coin flip you have decided to believe. The live bot asked its bars whether the terminal was alive, and an empty bar frame is equally what a quiet market returns: when MetaTrader auto-updated and restarted itself on 2026-08-04 the bot read the dead link as a quiet market and sat blind for 50 minutes with its heartbeat ticking, the watchdog green and the Bots page saying RUNNING. `account_info()` is the probe now, because it answers whenever the link is alive whatever the market is doing. **The generalisation is about VALUES, not labels: never let "no data" and "cannot ask" be the same value.** Every layer in that path was individually defensible — an empty DataFrame is a reasonable return for a bar fetcher, a null balance a reasonable write when you have no balance — and the distinction was destroyed at the bottom and unrecoverable at every level above it, leaving a blank cell as the only symptom in the entire system
- Record what you REQUESTED as though it were what you RECEIVED. The bar cache did exactly that until 2026-08-04: it fetched a window, saved whatever came back, and then marked the whole **requested** range as covered — so asking for bars through a date the broker did not have yet (every `--end today`) marked that date fetched forever, and every later run read a cache HIT and got a frame that silently stopped early. Measured: the sidecar claimed history through 2026-08-06 while the file held nothing past 2026-08-03 03:45, with the agent serving the missing bars on request the whole time. **The returned frame is clean and gives you no way to notice.** This is the same defect as the hardcoded history floor arriving from the other end of the window — the system answering a narrower question than the one asked. Clamp coverage to the data, and never into today, because a day still filling looks exactly like a complete one
- Hardcode a broker's history depth, or trust MT5's `/data_availability` for it. MT5 answers a request for a timeframe it has no history at with the nearest COARSER bars, still labelled as what you asked for — a backtest fed those runs clean and lies. Depth is MEASURED by bar density (`backtest/data/history.py`, probed per broker and cached); `/data_availability` samples one bar per end and is fooled the same way (it reported M1 back to 2007, false by ~11 years). Verify by bars-per-day, never by the earliest timestamp
