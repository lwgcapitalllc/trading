[pytest]
# importlib mode lets same-named test files (e.g. several engines' tests/test_engine.py)
# be collected together without __init__.py packages or basename collisions, so
# `pytest engines/` runs the whole engine suite in one go.
addopts = --import-mode=importlib
